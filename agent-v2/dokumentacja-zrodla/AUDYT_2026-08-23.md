# Audyt dokumentacji odtworzeniowej wobec kodu — 23 sierpnia 2026

Jedenastu agentow przeszlo `JAK_ZBUDOWANY_JEST_BOT.md` odcinek po odcinku
obok kodu `agent-v2`. Ponizej WSZYSTKIE 74 znalezisk, najpowazniejsze pierwsze.

Pozycje oznaczone **[ZAMKNIETE]** naprawilem tego samego dnia — zostaja
na liscie, zeby bylo widac, czego dotyczyly. Reszta czeka.

| # | powaga | stan | znalezisko | plik |
|---|---|---|---|---|
| 1 | blokujaca | **ZAMKNIETE** | Wada 5 mowi, ze WORST_NOTE_HOURS jest nieuzywane — a ta stala codziennie zamyka agentowi usta na dwie godziny | `dokumentacja-zrodla/wady.md:17` |
| 2 | blokujaca | **ZAMKNIETE** | Dokument mowi, ze okladka powstaje tylko przy --wyslij; kod generuje ja zawsze — i to jest odwrocenie naprawy, ktora kod opisuje jako swiadoma | `dokumentacja-zrodla/rozdzial_artykul.md:1180` |
| 3 | blokujaca | **ZAMKNIETE** | Par. 2.2 pokazuje skasowana wersje zostal_czas — i dokument zaprzecza sam sobie | `dokumentacja-zrodla\rozdzial_dzien.md:138` |
| 4 | blokujaca | **ZAMKNIETE** | Piec blokow kodu w rozdziale IV cytuje stages.odczekaj PO dzialaniu — test zabrania tego w run.py | `dokumentacja-zrodla\rozdzial_dzien.md:661` |
| 5 | blokujaca | otwarte | Par. 2.4 opisuje nieistniejaca architekture przerw ("po dzialaniu") i wskazuje zla linie stages.py | `dokumentacja-zrodla\rozdzial_dzien.md:200` |
| 6 | blokujaca | **ZAMKNIETE** | Rozdzial IV pokazuje STARE cialo `zostal_czas` — sekcja VII pokazuje nowe, w tym samym dokumencie | `dokumentacja-zrodla/rozdzial_dzien.md:138` |
| 7 | blokujaca | **ZAMKNIETE** | `run.rytm` nie istnieje w calym rozdziale IV — petle dnia pokazane sa ze starym `odczekaj` PO dzialaniu | `dokumentacja-zrodla/rozdzial_dzien.md:649` |
| 8 | blokujaca | otwarte | Odsiew ocenia `depth` na slepo — `threads` nigdy do niego nie dociera, a caly akapit promptu na nich stoi | `stages.py:1949` |
| 9 | blokujaca | otwarte | Wykrywacz martwych sygnalow celuje w zly slownik — trzy stale falszywe alarmy w KAZDYM przebiegu, a prawdziwe samooceny nie sa sprawdzane nigdy | `stages.py:2259` |
| 10 | blokujaca | otwarte | --stop-after review i --stop-after forma sa przyjmowane, ale NIC nie zatrzymuja — z --wyslij publikuja na zywo | `run.py:1007` |
| 11 | powazna | otwarte | Sekcja I zaklada, ze stages.py nie dotyka przegladarki — a dotyka, w glownej sciezce artykulu | `dokumentacja-zrodla\wstep.md:92` |
| 12 | powazna | **ZAMKNIETE** | Tabela mandatu podaje 10 171 wierszy — dokument w sekcji II sam sobie przeczy liczba 10 475 | `dokumentacja-zrodla\wstep.md:52` |
| 13 | powazna | **ZAMKNIETE** | "35 zestawow testow, 950 asercji, zaden nie wola platnego modelu" — trzy liczby, trzy razy nie ta liczba | `dokumentacja-zrodla\wstep.md:98` |
| 14 | powazna | **ZAMKNIETE** | Diagram rozkladu odpowiedzialnosci pokazuje 8 modulow, spis w sekcji II — 11; brakuje config.py, od ktorego zalezy kazdy modul | `dokumentacja-zrodla\wstep.md:82` |
| 15 | powazna | otwarte | Sam config.py twierdzi, ze cztery stale sa martwe, a test pilnuje, zeby to nieprawdziwe zdanie tam zostalo | `config.py:1060` |
| 16 | powazna | otwarte | Wada 7 (kolejnosc bloku komentarzy) jest sprzeczna z §7.3 tego samego dokumentu i byla nieprawdziwa juz w dniu spisania listy | `dokumentacja-zrodla/wady.md:19` |
| 17 | powazna | **ZAMKNIETE** | Dokument opisuje jako WADE, ze THIN dostaje dlugosc RICH — a config ma juz osobny wpis THIN i domysl SINGLE | `dokumentacja-zrodla/rozdzial_artykul.md:919` |
| 18 | powazna | **ZAMKNIETE** | Dokument ostrzega przed sekcja [Install] w nia-agent.service, ktorej w pliku juz nie ma | `dokumentacja-zrodla/rozdzial_dane.md:919` |
| 19 | powazna | otwarte | Dokument cytuje z jednostki systemd komentarz, ktory w pliku zostal skreslony jako nieprawdziwy, i opisuje jako WADE brak strazy czasu, ktora istnieje | `dokumentacja-zrodla/rozdzial_dane.md:890` |
| 20 | powazna | otwarte | Straz budzetu miesiecznego przed artykulem nie istnieje w dokumencie — a to krok, ktory potrafi zakonczyc caly przebieg przed skautem | `dokumentacja-zrodla/rozdzial_artykul.md:3` |
| 21 | powazna | **ZAMKNIETE** | WADA „regula roznorodnosci domen jest liczona i wyrzucana" jest nieaktualna — `recent_domains` trafia do promptu jako `ostatnie_domeny` | `dokumentacja-zrodla/rozdzial_artykul.md:453` |
| 22 | powazna | **ZAMKNIETE** | Blok „Wejscie" dyskoverii nie przekazuje pola `ostatnie_domeny`, ktorego prompt wymaga — odtworzenie z dokumentu konczy sie KeyError na etapie 3 | `dokumentacja-zrodla/rozdzial_artykul.md:380` |
| 23 | powazna | **ZAMKNIETE** | WADA „THIN dostaje dlugosc RICH" jest nieaktualna — THIN ma wlasny wpis, a galaz domyslna to teraz SINGLE | `dokumentacja-zrodla/rozdzial_artykul.md:919` |
| 24 | powazna | otwarte | Numery linii dla run.py i stages.py sa systematycznie rozjechane w calej mapie etapow i we wszystkich naglowkach etapow | `dokumentacja-zrodla/rozdzial_artykul.md:20` |
| 25 | powazna | otwarte | Par. 9 i 3.2 cytuja budzet["follow"] zamiast na_teraz["follow"], a par. 17 poz. 3 opisuje wade jako aktualna mimo etykiety NAPRAWIONE | `dokumentacja-zrodla\rozdzial_dzien.md:1107` |
| 26 | powazna | otwarte | Zwloka przed pierwsza notka nadal spi do 40 minut bez pytania o zegar — wbrew docstringowi rytm | `run.py:425` |
| 27 | powazna | otwarte | rytm pyta o zegar PO oplaceniu wywolania modelu — gotowy tekst jest wyrzucany | `run.py:487` |
| 28 | powazna | otwarte | Wszystkie numery linii run.py w rozdziale IV sa przesuniete o 35-58 wierszy — naglowki blokow wskazuja cudze bloki | `dokumentacja-zrodla\rozdzial_dzien.md:1224` |
| 29 | powazna | **ZAMKNIETE** | Dokument opisuje sekcje [Install] w nia-agent.service, ktorej w pliku nie ma — a odtworzenie jej z dokumentu wlacza platny, publikujacy przebieg przy  | `dokumentacja-zrodla\rozdzial_dane.md:919` |
| 30 | powazna | otwarte | Tabela kontroli zdrowia w alarm.py wymienia szesc pozycji, kod ma siedem — brakuje tej, ktora dokument sam nazywa NAJWAZNIEJSZA | `dokumentacja-zrodla\rozdzial_dane.md:999` |
| 31 | powazna | otwarte | Uzasadnienie TimeoutStartSec=9000 zacytowane z komentarza, ktory sam plik uslugi oznacza jako NIEPRAWDE — wraz z odstepami 10-25 min zamiast 45-90 min | `dokumentacja-zrodla\rozdzial_dane.md:877` |
| 32 | powazna | otwarte | Prog kontroli koszt() podany tylko dzienny — kod ma tez galaz miesieczna przy 75% MONTHLY_LIMIT_USD | `dokumentacja-zrodla\rozdzial_dane.md:998` |
| 33 | powazna | **ZAMKNIETE** | Cytowany `DLUGOSC_WG_GLEBOKOSCI` nie ma klucza THIN, a oparta na tym WADA jest juz nieprawdziwa | `dokumentacja-zrodla/rozdzial_artykul.md:828` |
| 34 | powazna | **ZAMKNIETE** | WADA "regula roznorodnosci domen liczona i wyrzucana" zaprzecza sekcji VII i zalacznikowi A | `dokumentacja-zrodla/rozdzial_artykul.md:453` |
| 35 | powazna | **ZAMKNIETE** | WADA o sekcji [Install] w nia-agent.service opisuje ryzyko, ktorego juz nie ma | `dokumentacja-zrodla/rozdzial_dane.md:919` |
| 36 | powazna | **ZAMKNIETE** | `stages.odczekaj` opisany pod zla linia, ze stara sygnatura i starym cialem | `dokumentacja-zrodla/rozdzial_dzien.md:199` |
| 37 | powazna | otwarte | Odsylacze `plik.py:linia` w rozdzialach analitycznych rozjechaly sie masowo — 32 z 50 wskazuja zle miejsce | `dokumentacja-zrodla/rozdzial_artykul.md:20` |
| 38 | powazna | otwarte | Naglowek gates.py mowi "Cztery bramki, ktore blokuja", a zadna nie blokuje — dokument cytuje tylko drugi akapit tego samego docstringa | `gates.py:1` |
| 39 | powazna | otwarte | "Siedem plikow w tests/platne/" — jest ich dziewiec, a poza tabela kosztow zostal najdrozszy test korpusu Federal Register | `dokumentacja-zrodla/rozdzial_bramki.md:1034` |
| 40 | powazna | otwarte | Tabela "Testy — wszystkie zestawy" pomija piec plikow testowych, a suma 945/5 jest juz nieprawdziwa | `dokumentacja-zrodla/rozdzial_bramki.md:1011` |
| 41 | powazna | otwarte | Zdanie otwierajace 0025 przypisane "bramce statystyk", ktora nie jest w stanie go zlapac — kod wskazuje FAKT_BEZ_POKRYCIA | `dokumentacja-zrodla/rozdzial_bramki.md:496` |
| 42 | powazna | otwarte | `confidence: 0.0-1.0` to dokladnie zakazana samoocena — trzy inne prompty jej zabraniaja, a odsiew nadal ja zamawia i kod nadal nia sortuje | `prompts/wykonalnosc.md:88` |
| 43 | powazna | otwarte | Odwrotnosc tej samej wady: odsiew ma uporzadkowac oceny best-first (ranking odporny na wyrownanie), a `pick_topic` tej kolejnosci nie czyta | `stages.py:2038` |
| 44 | powazna | otwarte | `or "RICH"` w run.py omija zabezpieczenie, dla ktorego galaz domyslna w `dlugosc_dla` zmieniono na SINGLE | `run.py:976` |
| 45 | powazna | otwarte | Blok komentarzy placi za wybor celow, gdy przydzial komentarzy wynosi zero (jego dwaj sasiedzi maja te zapore) | `run.py:468` |
| 46 | powazna | otwarte | Zwloka 0-40 min przed pierwsza notka jest odsypiana bez pytania, czy sie zmiesci — dokladnie ta wada, ktora naprawil `rytm` | `run.py:425` |
| 47 | powazna | otwarte | `notki_dnia` pisze i weryfikuje CALA paczke notek przed pierwszym sprawdzeniem zegara; niewystawiona notka przepada, choc istnieje bank na takie notki | `run.py:426` |
| 48 | powazna | otwarte | Kazda petla bloku wchodzi w platne dzialanie, majac na zegarze „jakakolwiek sekunde" — `potrzeba_s` jest podawane dopiero po zaplacie | `run.py:382` |
| 49 | powazna | otwarte | W sciezce artykulu sesja jest sprawdzana dopiero przy publikacji — po calym oplaconym przebiegu — a jej brak zostawia wiersz przebiegu jako RUNNING | `run.py:1136` |
| 50 | powazna | otwarte | Nieudane wywolanie ksieguje sie jako koszt ZERO, choc tokeny byly zuzyte i policzone przez dostawce — straz budzetu tego nie widzi | `llm.py:482` |
| 51 | powazna | otwarte | Licznik dzienny nie zna kluczy 'follow' i 'subskrypcje' — naprawa z 20 sierpnia opisana w run.py nigdy nie zadzialala | `browser.py:107` |
| 52 | powazna | otwarte | Gdy Substack nie odda JSON-a, licznik dzisiejszych notek po cichu zwraca zero — nie do odroznienia od prawdziwego zera | `browser.py:626` |
| 53 | powazna | otwarte | W sciezce artykulu SIGTERM i wygasla sesja Substacka omijaja `except Exception` — przebieg zostaje na zawsze w RUNNING, wbrew wlasnej dokumentacji | `run.py:1146` |
| 54 | powazna | otwarte | Baza NIE WIE, czy artykul poszedl w swiat — publikacja jest odnotowana wylacznie w dwoch plikach, obu zapisywanych PO klikniecu Publish | `stages.py:198` |
| 55 | powazna | otwarte | Straz budzetu przed artykulem pyta TYLKO o sufit miesieczny — limit dzienny i sufit przebiegu nadal zabijaja artykul po oplaconym researchu | `run.py:760` |
| 56 | powazna | otwarte | Artykul tygodniowy i rutyna dnia dziela jeden zamek — kolizja kasuje caly tygodniowy artykul po cichu, z kodem wyjscia 0 i bez sladu w bazie | `run.py:709` |
| 57 | drobna | otwarte | Diagram wiesza gates.py pod stages.py, a bramki orkiestruje run.py | `dokumentacja-zrodla\wstep.md:83` |
| 58 | drobna | otwarte | kopia_subskrybentow.py: 125 wierszy w sekcji I, 135 w sekcji II tego samego dokumentu | `dokumentacja-zrodla\wstep.md:61` |
| 59 | drobna | otwarte | Wada 9 (skaut nie trafia w kryteria artykulowe) nie odnotowuje, ze prompt przepisano wlasnie pod to — 103 wiersze zmian po dacie listy | `dokumentacja-zrodla/wady.md:21` |
| 60 | drobna | otwarte | Wada 8 wymienia trzy kopie `.przed-*` w data/, ktorych tam nie ma | `dokumentacja-zrodla/wady.md:20` |
| 61 | drobna | otwarte | Numeracja etapow w dokumencie (1-14) klóci sie z numeracja w docstringach stages.py (1-9) i nikt tego nie zaznacza | `dokumentacja-zrodla/rozdzial_artykul.md:811` |
| 62 | drobna | otwarte | Cytat z `llm._call_claude` w rozdziale o EFFORT nie odpowiada juz kodowi — brakuje galezi, ktora ostrzega o martwym wpisie | `dokumentacja-zrodla/rozdzial_artykul.md:40` |
| 63 | drobna | otwarte | zmiesci_sie zaklada o jedna przerwe mniej niz dzialan, ale rytm_stanu jest wspolny dla calego przebiegu | `run.py:219` |
| 64 | drobna | otwarte | Sekcja VI cytuje _cost w wersji SPRZED poprawki, sekcja VII te sama funkcje po poprawce — dokument zawiera dwie sprzeczne wersje jednej funkcji finans | `dokumentacja-zrodla\rozdzial_dane.md:447` |
| 65 | drobna | otwarte | Odsylacz do config.py:588 przy MAX_TOKENS wskazuje na inna stala | `dokumentacja-zrodla\rozdzial_dane.md:558` |
| 66 | drobna | otwarte | Sciezka artykulowa nie ma wlasnego zegara: nia-artykul.service tnie po 5400 s, a agent na tej galezi nie ustawia konca czasu w ogole | `dokumentacja-zrodla\rozdzial_dane.md:903` |
| 67 | drobna | otwarte | ZALACZNIK A: cztery pliki referencyjne stoja pod naglowkiem "A.1. Prompty robocze", a A.2 juz nie ma | `dokumentacja-zrodla/sklej.py:255` |
| 68 | drobna | otwarte | "Szesc plikow pilnuje produkcji" — wyliczenie tuz obok podaje siedem nazw, a w kodzie jest ich osiem | `dokumentacja-zrodla/rozdzial_bramki.md:1017` |
| 69 | drobna | otwarte | "Piec plikow nie ma kontrdowodu" — po dwukropku sa cztery i cztery sa w tabeli | `dokumentacja-zrodla/rozdzial_bramki.md:1165` |
| 70 | drobna | otwarte | Odnosnik "stages.scout (linia 2068)" wskazuje komentarz o pytaniach czytelnikow; prog 5 stoi w wierszu 2096 | `dokumentacja-zrodla/rozdzial_bramki.md:152` |
| 71 | drobna | otwarte | Sekcja 6 opisuje leniwa weryfikacje notek jako zywa oszczednosc, a kandydat na notke jest dzis jeden | `dokumentacja-zrodla/rozdzial_bramki.md:831` |
| 72 | drobna | otwarte | Prompt skauta podaje modelowi dokladny prog, ktorego kod uzywa — `ile_precedensow` jest wypelnianiem limitu, nie pomiarem | `prompts/skaut.md:290` |
| 73 | drobna | otwarte | Jedyny licznik komentarzy, lajkow i restackow moze paść bez jednej linii w logu — dwa puste `except` pod rzad | `browser.py:82` |
| 74 | drobna | otwarte | Polubienie zapisuje sie jako udane od razu po klikniecu — jedyne dzialanie w pliku bez potwierdzenia | `browser.py:1040` |

## Szczegoly

### 1. Wada 5 mowi, ze WORST_NOTE_HOURS jest nieuzywane — a ta stala codziennie zamyka agentowi usta na dwie godziny

**Powaga:** blokujaca   **Plik:** `dokumentacja-zrodla/wady.md:17`

**Dowod:** wady.md:17 (= JAK_ZBUDOWANY_JEST_BOT.md:7213): „| 5 | `BEST_NOTE_HOURS`, `WORST_NOTE_HOURS`, `BEST/WORST_NOTE_DAYS` nieuzywane | ... | notki wystawiane rownomiernie w oknie 6-22 ET, bez wazenia |". Kod mowi co innego — config.py:341-342 wewnatrz `pora_na_publikacje`: „ if g in WORST_NOTE_HOURS:\n return False, (f\"{g:02d}:00 u czytelnikow — najgorsze okno wg researchu\")", a `WORST_NOTE_HOURS = (12, 13)` (config.py:1071). Funkcja jest realnie wolana raz na przebieg dnia: run.py:338 „wolno, powod = config.pora_na_publikacje()" i run.py:341-343 „na_teraz[\"notki\"] = 0 / na_teraz[\"komentarze\"] = 0".

**Dlaczego to wada:** Trzy rzeczy naraz. (1) Zdanie o „rownomiernym wystawianiu w oknie 6-22 ET" jest nieprawdziwe: miedzy 12:00 a 13:59 czasu nowojorskiego agent nie wystawia ANI notek, ANI komentarzy — to dwie z szesnastu godzin okna, czyli 12,5 procent doby roboczej, o ktorych czytelnik dokumentu nie ma prawa wiedziec. (2) Ktos odtwarzajacy bota z tego dokumentu pominie ten warunek i dostanie agenta o innym rozkladzie dnia niz opisany. (3) Najgorsze: VIII.2 prosi wlasciciela o decyzje („Zanim cokolwiek zacznie wazyc godziny, trzeba rozstrzygnac, ktorym z dwoch wlasnych zrodel wierzymy") na falszywej przeslance, ze NIC jeszcze nie wazy godzin. Jesli po tej decyzji ktos „wlaczy" stale, dolozy drugie wyciszenie do juz dzialajacego.

**Plan naprawy:** Rozbic pozycje 5 na dwie. Zywa: `WORST_NOTE_HOURS = (12, 13)` jest EGZEKWOWANE przez `config.pora_na_publikacje` (config.py:341) i zeruje notki oraz komentarze w run.py:341-343 — to nie jest martwa stala, tylko nieudokumentowana cisza 12:00-13:59 ET. Martwe: `BEST_NOTE_HOURS`, `BEST_NOTE_DAYS`, `WORST_NOTE_DAYS` — tu opis jest poprawny. W VIII.2 przeformulowac pytanie do wlasciciela: nie „czy zaczac wazyc godziny", tylko „czy zostawic istniejace wyciszenie 12-13 ET, skoro research z 18 sierpnia wskazuje inne okno".

---

### 2. Dokument mowi, ze okladka powstaje tylko przy --wyslij; kod generuje ja zawsze — i to jest odwrocenie naprawy, ktora kod opisuje jako swiadoma

**Powaga:** blokujaca   **Plik:** `dokumentacja-zrodla/rozdzial_artykul.md:1180`

**Dowod:** Dokument (rozdzial_artykul.md:1180, w sklejce JAK_ZBUDOWANY_JEST_BOT.md:1609): "**Funkcja:** `stages.grafika` (`stages.py:457`), wolana z `run.py:1030` **tylko przy `--wyslij`**." oraz WADA (rozdzial_artykul.md:1236): "**WADA — grafika nie powstaje bez `--wyslij`.** Wywolanie `stages.grafika(...)` siedzi wewnatrz `if args.wyslij:`." Kod robi cos dokladnie odwrotnego, run.py:1122-1136: "# OKLADKA POWSTAJE Z ARTYKULEM, NIE Z PUBLIKACJA. Stala wczesniej / # wewnatrz galezi `--wyslij`, wiec kazdy przebieg bez publikacji / # zapisywal na dysk artykul BEZ okladki" a potem, przed galezia: linia 1134 " stages.grafika(conn, run_id, draft, sciezka_artykulu=path)" i dopiero linia 1136 " if args.wyslij:". Ten sam blad powtarza sie w czterech miejscach: mapa etapow (rozdzial_artykul.md:1180), blok kodu w "Etap 14 — publikacja" (rozdzial_artykul.md:1246, gdzie `stages.grafika(...)` jest wcieta w `if args.wyslij:`), tabela "Co trafia do bazy i na dysk" (rozdzial_artykul.md:1307: "| `data/articles/NNNN-slug.png` | naglowek | etap 13, tylko przy `--wyslij` |") i zbiorcza lista wad (rozdzial_artykul.md:1332: "16. **Grafika nie powstaje bez `--wyslij`**").

**Dlaczego to wada:** Dziala w dwie strony i obie sa kosztowne. (1) Kto odtwarza bota z tego dokumentu, wklei `stages.grafika(...)` do srodka `if args.wyslij:` — i odtworzy dokladnie te usterke, ktora komentarz w run.py:1122-1132 opisuje jako naprawiona i uzasadnia dwoma akapitami ("nie bylo ani jednego przebiegu, w ktorym mogla sie zepsuc bezpiecznie"). (2) Kto czyta zbiorcza liste wad jako liste zadan, pojdzie naprawiac punkt 16, ktory juz nie istnieje — i przy okazji zobaczy w kodzie cos innego niz w dokumencie, wiec straci zaufanie do calej listy. Dodatkowo tabela dyskowa klamie o tym, kiedy powstaje plik .png, czyli o jedynej pozycji, ktora dokument gdzie indziej (JAK_ZBUDOWANY_JEST_BOT.md:5057) wskazuje jako rosnaca bez limitu.

**Plan naprawy:** W rozdzial_artykul.md: (a) linia 1180 — zamienic "wolana z `run.py:1030` **tylko przy `--wyslij`**" na "wolana z `run.py:1134`, ZAWSZE — bezposrednio po `stages.save`, PRZED galezia `--wyslij`"; (b) linia 1246 — przepisac blok kodu "Etap 14" tak, by `stages.grafika(...)` stalo przed `if args.wyslij:`, zgodnie z run.py:1134-1136; (c) linia 1307 — "etap 13, tylko przy `--wyslij`" -> "etap 13, kazdy przebieg"; (d) usunac WADE z linii 1236 i punkt 16 z linii 1332, zastepujac je jednym zdaniem o tym, ze naprawiono to 2026-08-20 wraz z uzasadnieniem z komentarza run.py:1122-1132 (przebieg bez publikacji musi umiec zepsuc grafike bezpiecznie). Przenumerowac liste wad. Przesklejac przez dokumentacja-zrodla/sklej.py.

---

### 3. Par. 2.2 pokazuje skasowana wersje zostal_czas — i dokument zaprzecza sam sobie

**Powaga:** blokujaca   **Plik:** `dokumentacja-zrodla\rozdzial_dzien.md:138`

**Dowod:** Rozdzial IV (rozdzial_dzien.md:134-142, w sklejonym dokumencie 1908-1916) podaje jako cialo funkcji: if _KONIEC_CZASU is None: return True zostalo = _KONIEC_CZASU - time.time() if zostalo > 0: return True Kod (run.py:141-166) ma inna sygnature i inny warunek: def zostal_czas(na_co: str = "", potrzeba_s: float = 0.0) -> bool: ... zostalo = _KONIEC_CZASU - time.time() if zostalo > potrzeba_s: return True Ta sama funkcja w sekcji VII tego samego dokumentu (JAK_ZBUDOWANY_JEST_BOT.md:7146, generowana przez sklej.py ze znacznika <!--KOD:run.zostal_czas-->) jest juz nowa. Dokument podaje dwie sprzeczne wersje jednej funkcji.

**Dlaczego to wada:** Kto odtwarza bota z rozdzialu IV, napisze zostal_czas bez parametru potrzeba_s. Wtedy `rytm` (run.py:168) nie ma jak zapytac, czy PRZERWA sie zmiesci — pyta tylko, czy zostala jakakolwiek sekunda. To jest doslownie awaria opisana w docstringu stages.losuj_odstep (stages.py:590-593): "Przebieg 28 zginal dokladnie na tym: odczekaj losowalo 86 minut i od razu zasypialo, a na zegarze przebiegu zostalo dwadziescia". Kto zamiast tego czyta kod, znajdzie w dokumencie dwie wersje i nie bedzie wiedzial, ktora obowiazuje.

**Plan naprawy:** W rozdzial_dzien.md:134-142 wstawic aktualne cialo run.zostal_czas (z potrzeba_s i obiema galeziami print), a zdanie z linii 145 przepisac: to nie jest juz pytanie zerojedynkowe, tylko pytanie o konkretna liczbe sekund. Najlepiej zamienic recznie wklejony blok na odsylacz do sekcji VII, ktora sklej.py generuje z kodu — inaczej ten rozjazd wroci przy nastepnej zmianie.

---

### 4. Piec blokow kodu w rozdziale IV cytuje stages.odczekaj PO dzialaniu — test zabrania tego w run.py

**Powaga:** blokujaca   **Plik:** `dokumentacja-zrodla\rozdzial_dzien.md:661`

**Dowod:** Wydruki blokow w par. 5.1, 6, 7, 8 i 9 koncza sie wywolaniem odczekaj po akcji: rozdzial_dzien.md:482 `stages.odczekaj("odpowiedz")`, :661 `stages.odczekaj("notka")`, :854 `stages.odczekaj("komentarz")`, :1068 `stages.odczekaj("komentarz")`, :1116 `stages.odczekaj("komentarz")`. W kodzie nie ma ani jednego takiego wywolania. Zamiast tego przerwa stoi PRZED akcja: run.py:401 `if not rytm("odpowiedz", "odpowiedzi", rytm_stanu): return`, run.py:435 `if not rytm("notka", "notki", rytm_stanu): return`, run.py:487, :539, :579, :614 analogicznie. Pilnuje tego test: tests/test_rytm_zegar.py:148-149 sprawdz("run.py nie wola juz odczekaj bezposrednio", "stages.odczekaj(" not in zrodlo)

**Dlaczego to wada:** To sa jedyne w rozdziale wydruki "przepisz to do pliku". Odtworzenie ich literalnie daje kod, ktory (a) po OSTATNIEJ notce w bloku spi jeszcze 45-90 minut nie majac czego robic, (b) zasypia bez pytania, czy sen sie zmiesci — czyli obie wady wymienione w docstringu run.rytm (run.py:172-182) jako powod jego istnienia. Dodatkowo odtworzony kod natychmiast obleje tests/test_rytm_zegar.py, ktory jest w repozytorium.

**Plan naprawy:** W kazdym z pieciu wydrukow zamienic koncowe `stages.odczekaj(...)` na `if not rytm(<rodzaj>, <nazwa_bloku>, rytm_stanu): return` przed akcja plus `rytm_stanu[<rodzaj>] = True` po niej, dokladnie tak jak w run.py:401-409, 435-448, 487-496, 539-544, 579-584, 614-617. Dopisac tez inicjalizacje `rytm_stanu: dict[str, bool] = {}` (run.py:329), bo bez niej wydruki nie sa samowystarczalne.

---

### 5. Par. 2.4 opisuje nieistniejaca architekture przerw ("po dzialaniu") i wskazuje zla linie stages.py

**Powaga:** blokujaca   **Plik:** `dokumentacja-zrodla\rozdzial_dzien.md:200`

**Dowod:** Dokument: "Zuzywaja go dwie drogi:\n- `stages.odczekaj(co)` (`stages.py:586`) — po odpowiedzi, notce, komentarzu, obserwacji i subskrypcji" (rozdzial_dzien.md:199-200), z wydrukiem ciala bez parametru `ile` (linie 202-206). W kodzie stages.py:586 to `def losuj_odstep(co: str = "") -> float:` — "Losuje przerwe, ale jej NIE odsypia". `odczekaj` przeniosla sie na stages.py:602 i ma teraz drugi parametr: `def odczekaj(co: str = "", ile: float | None = None) -> None:` z `ile = losuj_odstep(co) if ile is None else float(ile)`. Droga jest jedna, nie dwie, i biegnie inaczej: run.rytm (run.py:190-195) robi `przerwa = _s.losuj_odstep(co)` -> `zostal_czas(na_co, przerwa)` -> `_s.odczekaj(co, przerwa)`.

**Dlaczego to wada:** Rozdzial IV jest jedynym miejscem, ktore TLUMACZY mechanizm odmierzania przerw (sekcja VII pokazuje sam kod, bez narracji). Czytelnik dostaje tu opis architektury, ktorej juz nie ma: nie dowie sie, ze losowanie i spanie sa rozdzielone wlasnie po to, zeby dalo sie odmowic przerwy, ani ze `rytm_stanu` (run.py:329) jest WSPOLNY dla wszystkich blokow, wiec pierwsze dzialanie danego rodzaju w przebiegu nie czeka wcale, a komentarz tuz po obserwacji juz czeka. Odsylacz stages.py:586 prowadzi do innej funkcji, niz nazywa.

**Plan naprawy:** Przepisac akapit 199-206: jedna droga (run.rytm), kolejnosc losuj_odstep -> zostal_czas(potrzeba_s) -> odczekaj(co, ile), poprawic odsylacze na stages.py:586 losuj_odstep i stages.py:602 odczekaj, dopisac akapit o rytm_stanu (wspolny slownik, pierwsze dzialanie rodzaju bez przerwy, obserwowanie/subskrypcje/dyskusje dziela klucz "komentarz" z blokiem komentarzy).

---

### 6. Rozdzial IV pokazuje STARE cialo `zostal_czas` — sekcja VII pokazuje nowe, w tym samym dokumencie

**Powaga:** blokujaca   **Plik:** `dokumentacja-zrodla/rozdzial_dzien.md:138`

**Dowod:** rozdzial_dzien.md:132-143 (JAK_ZBUDOWANY_JEST_BOT.md ~1904): naglowek "#### 2.2 `zostal_czas` (`run.py:141`)" i pod nim blok: zostalo = _KONIEC_CZASU - time.time() if zostalo > 0: return True run.py:141 i 157 mowi co innego: def zostal_czas(na_co: str = "", potrzeba_s: float = 0.0) -> bool: ... if zostalo > potrzeba_s: Sekcja VII tego samego dokumentu (kod.md:1247, znacznik <!--KOD:run.zostal_czas-->) wypisuje wersje z `potrzeba_s` — bo jest generowana z `ast`. Rozdzial IV nie jest.

**Dlaczego to wada:** Dokument podaje dwa rozne ciala tej samej funkcji i nie mowi, ktore obowiazuje. Kto odtwarza bota czytajac po kolei, przepisze wersje z rozdzialu IV (bo tam jest opis i uzasadnienie) i dostanie `zostal_czas`, ktore odpowiada tylko na pytanie "czy zostala jakakolwiek sekunda". Dokladnie ta wersja przepuszczala 90-minutowa przerwe przy 20 minutach na zegarze i doprowadzila do smierci przebiegu od SIGTERM — powod jest opisany w docstringu `run.rytm` (run.py:179-181).

**Plan naprawy:** Podmienic blok w rozdzial_dzien.md:134-143 na obecne cialo z run.py:141-165 (z galezia `if potrzeba_s:` w komunikacie) albo — lepiej — usunac blok i zastapic go odsylaczem do znacznika `<!--KOD:run.zostal_czas-->` w sekcji VII, zeby fragment mial jedno zrodlo prawdy. Przy okazji dopisac akapit o nowym drugim parametrze.

---

### 7. `run.rytm` nie istnieje w calym rozdziale IV — petle dnia pokazane sa ze starym `odczekaj` PO dzialaniu

**Powaga:** blokujaca   **Plik:** `dokumentacja-zrodla/rozdzial_dzien.md:649`

**Dowod:** rozdzial_dzien.md:649 i 661, blok petli notek: for n in stages.notki_dnia(...): if not zostal_czas("notki"): return ... if wyslij: wynik = browser.wystaw_notke(...) ... stages.odczekaj("notka") run.py:428-448 robi to inaczej: if not zostal_czas("notki"): return ... if wyslij: if not rytm("notka", "notki", rytm_stanu): return wynik = browser.wystaw_notke(...) ... rytm_stanu["notka"] = True `grep -n rytm dokumentacja-zrodla/rozdzial_*.md` nie zwraca ani jednego trafienia na `run.rytm` — slowo pada tylko w "arytmetyka" i w nazwie `test_rytm`. To samo dotyczy pozostalych piecu blokow: rozdzial_dzien.md:464, 835, 1057, 1108 pokazuja `zostal_czas(...)` tam, gdzie run.py:401, 487, 539, 579, 614 wola `rytm(...)`.

**Dlaczego to wada:** Rozdzial IV jest jedynym miejscem, gdzie dokument opisuje petle dnia w calosci, a sekcja VII pokazuje `run.rytm` bez ani jednego wywolania. Odtwarzajacy zbuduje petle z rozdzialu IV i odtworzy OBIE wady, ktore `rytm` zamkal, a ktorych docstring (run.py:174-181) nazywa wprost: (1) po ostatniej notce agent spi jeszcze 45-90 minut nie majac co robic, (2) sen zaczyna sie bez sprawdzenia, czy sie zmiesci. Rozdzial VI to jeszcze wzmacnia: rozdzial_dane.md:890 konczy opis smierci przebiegu zdaniem "Kod nie wie, ile ma czasu" — dzis wie.

**Plan naprawy:** Dopisac do rozdzialu IV podsekcje o `run.rytm` (przerwa MIEDZY dzialaniami, `rytm_stanu`, kolejnosc: losuj -> sprawdz zegar -> spij) i poprawic szesc wycinkow petli (rozdzial_dzien.md:464, 649/661, 835, 1057, 1108) tak, by pokazywaly wywolanie `rytm(...)` w miejscu, w ktorym stoi w run.py. Usunac lub przedatowac zdanie "Kod nie wie, ile ma czasu" w rozdzial_dane.md:890.

---

### 8. Odsiew ocenia `depth` na slepo — `threads` nigdy do niego nie dociera, a caly akapit promptu na nich stoi

**Powaga:** blokujaca   **Plik:** `stages.py:1949`

**Dowod:** prompts/wykonalnosc.md:59-61 kaze modelowi ocenic RICH m.in. tak: "or the topic's own `threads` list carries three or more separate questions, each answerable from its own documents and each leaving the others open." Linie 77-79 zamykaja to warunkiem: "Either name the parallels in `parallels` — two of them, or it is not RICH by that route — or point at the three-plus threads the topic already carries. One of the two must hold." Tymczasem stages.py:1948-1951 wysyla do tego promptu WYLACZNIE trzy pola: `compact = [ {"index": i, "title": t.get("title"), "question": t.get("question")} for i, t in enumerate(topics) ]`. Ani `threads`, ani `precedents`, ani `scale`, ani `kind` nie sa przekazywane.

**Dlaczego to wada:** Model dostaje sam tytul i pytanie, wiec jedna z dwoch UDOKUMENTOWANYCH drog do RICH jest fizycznie niedostepna — moze albo zmyslic liczbe watkow, albo zawsze isc droga `parallels`. Prompt na dodatek poswieca calą sekcje (linie 63-70, z przykladem konklawe) na tlumaczenie, ze ta droga "matters and is easy to miss", czyli namawia model do korzystania z danych, ktorych nie widzi. `depth` nie jest ozdobnikiem: stages.py:2034 sortuje nim tematy (`waga.get(str(a.get("depth", "RICH")).upper(), 1)`), a run.py:976 przekazuje go do pisarza i przez config.dlugosc_dla ustala dlugosc artykulu (420 vs 650 vs 1075 slow). Czyli decyzja o dlugosci opłaconego artykulu zapada na podstawie oceny, ktorej model nie ma z czego wystawic — i dokladnie dlatego to pole musi sie wyrownac. Ktos odtwarzajacy bota z promptu zbuduje etap, ktory przekazuje `threads`, i dostanie inne zachowanie niz produkcja.

**Plan naprawy:** Dolozyc `threads` (a najlepiej takze `precedents` i `scale`) do slownika `compact` w stages.py:1948-1951 — sa juz policzone w scout() jako `ile_watkow`/`ile_precedensow`/`zasieg`. Albo, jesli oszczednosc tokenow byla celowa, usunac z wykonalnosc.md:59-61 i 77-79 cala droge "three-plus threads" i zostawic wylacznie `parallels`, zeby prompt nie obiecywal drogi, ktorej nie ma. Nie zostawiac obu stanow naraz.

---

### 9. Wykrywacz martwych sygnalow celuje w zly slownik — trzy stale falszywe alarmy w KAZDYM przebiegu, a prawdziwe samooceny nie sa sprawdzane nigdy

**Powaga:** blokujaca   **Plik:** `stages.py:2259`

**Dowod:** stages.py:2259-2262: `martwe_pola = _stale_sygnaly(topics, ("nosny", "na_artykul", "nasycony", "ile_watkow", "ile_precedensow", "zasieg", "depth", "confidence", "expected_primary_sources"))`. Wywolanie jest w srodku `scout()`, a `depth`, `confidence` i `expected_primary_sources` NIE sa polami tematu — powstaja dopiero w `feasibility()` (prompts/wykonalnosc.md:88) i zyja w `assessments`, czyli w innej liscie i o etap pozniej. Odtworzylem to na zywym ksztalcie danych: `_stale_sygnaly` na szesciu roznicujacych sie tematach zwraca `['nosny=True', 'nasycony=False', 'depth=None', 'confidence=None', 'expected_primary_sources=None']`.

**Dlaczego to wada:** Log "MARTWE W TYM PRZEBIEGU" bedzie ZAWSZE zawieral `depth=None, confidence=None, expected_primary_sources=None`, niezaleznie od tego, co model oddal. To lamie warunek postawiony wprost we wlasnym tescie: tests/test_martwe_sygnaly.py:225-228 wymaga, zeby pole rozroznajace NIE trafialo na te liste, "inaczej wykrywacz jest tylko halasem i przestaniemy go czytac". Gorsze jest drugie dno: TRZY pola tej wlasnie klasy — `confidence` (0.0-1.0), `expected_primary_sources` (policz ile) i `depth` — realnie istnieja w `assessments` i realnie sortuja (stages.py:2034-2036), a wykrywacz nie oglada ich NIGDY, bo `assessments` nie sa mu podane. Czyli jedyna siec na te klase wad jest zawieszona nad pusta podloga i jednoczesnie halasuje.

**Plan naprawy:** Usunac `"depth", "confidence", "expected_primary_sources"` z krotki w stages.py:2261-2262 (w scout() nie maja prawa istniec) i dodac drugie wywolanie `_stale_sygnaly(assessments, ("feasible", "depth", "confidence", "expected_primary_sources"))` w `pick_topic` albo na koncu `feasibility()`, gdzie te pola faktycznie sa. Dopisac do tests/test_martwe_sygnaly.py asercje, ze wykrywacz jest wolany takze na ocenach odsiewu.

---

### 10. --stop-after review i --stop-after forma sa przyjmowane, ale NIC nie zatrzymuja — z --wyslij publikuja na zywo

**Powaga:** blokujaca   **Plik:** `run.py:1007`

**Dowod:** Argparse przyjmuje wszystkie dziesiec nazw: `parser.add_argument("--stop-after", choices=STAGES, help="zatrzymaj się po tym etapie")` (run.py:712), a `STAGES = ("scout", "feasibility", "discovery", "fetch", "classify", "synthesis", "warto_pisac", "write", "review", "forma")` (run.py:25-28). Osiem pierwszych etapow ma po sobie wartownika `if args.stop_after == stage: return _done(conn, run_id, stage)` — ostatni raz w linii 1004, tuz po `write`. Po `stage = "review"` (run.py:1007) i po `stage = "forma"` (run.py:1059) takiego wartownika NIE MA ANI JEDNEGO — grep po calym pliku daje tylko osiem wystapien `stop_after` (linie 777, 796, 823, 868, 886, 918, 970, 1004). Kod leci prosto do `path = stages.save(...)` (run.py:1119), `stages.grafika(...)` (run.py:1134) i `wynik = browser.wystaw_artykul(path, wyslij=True)` (run.py:1140). Dokumentacja utwierdza w bledzie: JAK_ZBUDOWANY_JEST_BOT.md:441 mowi „Cztery kroki końcowe — bramki, zapis, grafika, publikacja — nie mają nazwy etapu i nie da się na nich zatrzymać przez `--stop-after`", czyli a contrario: na dziesieciu nazwanych da sie; a linia 507 mowi wprost „Czyli `--stop-after warto_pisac` działa".

**Dlaczego to wada:** To jest wylacznik bezpieczenstwa, ktory nie wylacza. Ktokolwiek chce sprawdzic recenzje albo bramke formy na zywym materiale — a to jedyne dwa etapy, ktorych nie da sie sensownie przetestowac bez pelnego artykulu — wpisze `python agent-v2/run.py --stop-after review --wyslij` (albo odpali to na serwerze, gdzie `--wyslij` siedzi na stale w nia-artykul.service:12) i dostanie OPUBLIKOWANY ARTYKUL na zywym koncie zamiast zatrzymania. Publikacji nie da sie cofnac w oczach tych, ktorzy ja zobaczyli — to ta sama klasa szkody, przed ktora broni `odmow_publikacji_z_kopii` (run.py:68-80). Nie ma zadnego komunikatu ostrzegawczego: argparse przyjmuje nazwe bez mrugniecia, wiec uzytkownik ma dowod, ze opcja jest obslugiwana. Odtwarzajacy bota z dokumentacji zbuduje to samo, bo dokumentacja mowi, ze te dziesiec etapow to punkty zatrzymania.

**Plan naprawy:** Dolozyc brakujacych dwoch wartownikow: po bloku recenzji (przed `stage = "forma"`, run.py:1058) oraz po bloku formy (przed `findings = gates.deterministic_floors(...)`, run.py:1082) wstawic `if args.stop_after == stage: return _done(conn, run_id, stage)`. Zeby ta klasa nie wrocila, dolozyc do tests/ test strukturalny, ktory dla KAZDEJ nazwy z `STAGES` sprawdza, ze w zrodle run.py wystepuje para (`stage = "<nazwa>"`, a po niej `if args.stop_after == stage`) — dokladnie tak jak test_czas.py:69-71 sprawdza, ze kazda petla pyta o czas. Alternatywnie (mocniej): zamiast osmiu recznych wartownikow zrobic jeden `def moze_dalej(stage)` wolany w jednym miejscu na koncu kazdego etapu.

---

### 11. Sekcja I zaklada, ze stages.py nie dotyka przegladarki — a dotyka, w glownej sciezce artykulu

**Powaga:** powazna   **Plik:** `dokumentacja-zrodla\wstep.md:92`

**Dowod:** Dokument (wstep.md:91-95): "Regula rozdzialu, przestrzegana bez wyjatku poza jednym udokumentowanym: `stages.py` nigdy nie dotyka przegladarki, `browser.py` nigdy nie wola modelu. Wyjatek: `browser.restackuj_w_kanale(ile, decyzja, wyslij)` przyjmuje funkcje decyzyjna jako argument". Diagram (wstep.md:82-88) nie ma krawedzi stages -> browser. Kod, stages.py:1672-1678: " import browser" ... " strony = {s[\"url\"]: s for s in browser.read_pages([x[\"url\"] for x in brakujace])}" — i nie jest to kod martwy, `fetch` wola to na zywo: stages.py:1785 " fetched.extend(_dobierz_przegladarka(conn, run_id, do_przegladarki, fetched))". Sam dokument wymienia te funkcje w sekcji II (linia 195) z opisem "Drugie podejscie do stron, ktore zwyklemu pobieraniu daly pusty szkielet", wiec kontrprzyklad stoi 100 wierszy pod regula.

**Dlaczego to wada:** To jedyne twierdzenie strukturalne, ktore dokument oznacza jako "bez wyjatku poza jednym udokumentowanym" — czyli wprost mowi czytelnikowi, zeby przestal szukac dalszych wyjatkow. Odtwarzajacy bota zbuduje `fetch` jako czysty HTTP i straci drugie podejscie w przegladarce dla stron rysowanych JavaScriptem (dokladnie te, ktore zwracaja pusty szkielet). Podkopuje tez uzasadnienie podane dwa akapity nizej ("cala warstwa myslowa da sie testowac bez przegladarki") — `stages.fetch` wymaga Playwrighta. Ta sama nieprawda jest wpisana jako rola modulu w generatorze (sklej.py:49 "wszystkie etapy myslowe; nie dotyka przegladarki"), wiec trafia takze do naglowka sekcji II (linia 146).

**Plan naprawy:** W wstep.md:91-95 wymienic DWA wyjatki: (a) `browser.restackuj_w_kanale` bierze funkcje decyzyjna, (b) `stages._dobierz_przegladarka` (stages.py:1656) importuje `browser` i wola `browser.read_pages` jako drugie podejscie w `fetch` — z zaznaczeniem, ze import jest odroczony wewnatrz funkcji i dotyczy wylacznie stron o pustej tresci, nie odmow i nie 404. Do diagramu (wstep.md:82-88) dorysowac krawedz `stages.py --> browser.py (tylko read_pages)`. W sklej.py:49 zmienic role stages.py na np. "wszystkie etapy myslowe; przegladarki uzywa tylko do drugiego podejscia w `fetch`".

---

### 12. Tabela mandatu podaje 10 171 wierszy — dokument w sekcji II sam sobie przeczy liczba 10 475

**Powaga:** powazna   **Plik:** `dokumentacja-zrodla\wstep.md:52`

**Dowod:** Dokument (wstep.md:52): "| maksimum 10 plikow `.py` | **11 plikow**, 10 171 wierszy | **PRZEKROCZONE** |". Zmierzone `wc -l *.py` w agent-v2: 559+2305+1586+203+514+295+135+558+1171+3043+106 = 10 475. Suma liczb z WLASNEJ sekcji II dokumentu (linie 127, 148, 228, 286, 304, 327, 342, 359, 384, 396, 406: 1171+3043+2305+558+514+203+295+559+106+135+1586) daje dokladnie 10 475. Roznica 304 wiersze.

**Dlaczego to wada:** Sekcja II jest od dzis generowana z kodu, sekcja I nie — wiec dokument w dwoch miejscach podaje dwie rozne wielkosci tego samego bota, a czytelnik nie ma jak zgadnac, ktora jest swieza. To jest naglowkowa liczba calego mandatu: ktos porownujacy swoja odtworzona wersje z dokumentem zobaczy 300 wierszy nadmiaru i zacznie szukac kodu, ktorego nie napisal.

**Plan naprawy:** Albo wpisac 10 475, albo — lepiej, bo to sie znowu rozjedzie za trzy dni — przeniesc te komorke do czesci generowanej: niech `sklej.py` liczy sume wierszy modulow z `MODULY` i wstawia ja przez placeholder w wstep.md, tak jak juz robi to z liczbami per-modul w `gen_moduly()`.

---

### 13. "35 zestawow testow, 950 asercji, zaden nie wola platnego modelu" — trzy liczby, trzy razy nie ta liczba

**Powaga:** powazna   **Plik:** `dokumentacja-zrodla\wstep.md:98`

**Dowod:** Dokument (wstep.md:97-99): "cala warstwa myslowa da sie testowac bez przegladarki i bez pieniedzy**. 35 zestawow testow, 950 asercji, zaden nie otwiera Chrome i zaden nie wola platnego modelu." Zmierzone: `agent-v2/tests/test_*.py` = 39 plikow (nie 35). Testy nie uzywaja `assert` (AST: 0 wystapien) — licza przez helper `sprawdz(...)`, ktorych jest 892 (nie 950). Obok lezy `agent-v2/tests/platne/` z 10 plikami `.py`, ktorych `PRZECZYTAJ.md` opisuje slowami "Wszystko w tym katalogu robi prawdziwe wywolania API", a `test_integracja.py` importuje `run` i `browser` (linie 14-17) i jest opisany jako "PELNY PLATNY PRZEBIEG DNIA z przerwami 45-90 min | godziny pracy, kilka USD".

**Dlaczego to wada:** Odtwarzajacy bota uzywa tych liczb jako listy kontrolnej pokrycia: przy 35 plikach uzna zestaw za kompletny, majac cztery mniej. Zdanie "zaden nie wola platnego modelu" nie jest tu w zaden sposob zawezone do darmowego katalogu — zawezenie pojawia sie dopiero 4500 wierszy dalej (linia 4644) i tez z bledna liczba ("Siedem plikow w `tests/platne/`" wobec 10). Ktos, kto z sekcji I wyniesie "testy sa darmowe" i puszczy `pytest agent-v2/tests/` (rekurencyjnie, w odroznieniu od udokumentowanej petli `test_*.py`), wejdzie w `platne/test_integracja.py` i zaplaci.

**Plan naprawy:** Poprawic na 39 zestawow i 892 sprawdzenia (albo, skoro testy nie uzywaja `assert`, nazwac je "sprawdzen" zamiast "asercji"), a zdanie o pieniadzach zawezic: "...zaden z 39 testow w `tests/` nie otwiera Chrome i nie wola platnego modelu; platne leza osobno w `tests/platne/` i nie lapie ich petla `test_*.py`". Liczby najlepiej generowac w `sklej.py` (policzyc pliki i wywolania `sprawdz` AST-em), tak jak reszte czesci mechanicznych. Przy okazji poprawic "Siedem plikow" w linii 4644 na 10.

---

### 14. Diagram rozkladu odpowiedzialnosci pokazuje 8 modulow, spis w sekcji II — 11; brakuje config.py, od ktorego zalezy kazdy modul

**Powaga:** powazna   **Plik:** `dokumentacja-zrodla\wstep.md:82`

**Dowod:** Dokument (wstep.md:81-89): "run.py --+-> stages.py --+-> llm.py --> DeepSeek | Anthropic | OpenAI / | +-> gates.py / | +-> style.py / | +-> db.py / +-> browser.py --> Playwright --> Chrome --> Substack / +-> kanal.py / +-> alarm.py" — osiem modulow, bez `config.py` i bez `kopia_subskrybentow.py`. Sekcja II tego samego dokumentu ma juz 11 naglowkow modulowych, w tym linia 404: "### `config.py` — wszystkie liczby i decyzje w jednym miejscu (patrz ZALACZNIK B)" oraz linia 406: "1586 wierszy, 16 funkcji na poziomie modulu, 0 klas". Sprawdzone: `import config` wystepuje we WSZYSTKICH dziesieciu pozostalych modulach (run.py:20, stages.py:17, browser.py, llm.py, gates.py, db.py, kanal.py, alarm.py, style.py, kopia_subskrybentow.py).

**Dlaczego to wada:** Diagram jest pierwszym modelem mentalnym czytelnika i jedynym miejscem, gdzie widac ksztalt systemu na raz. Pominiety jest w nim drugi co do wielkosci plik projektu (1586 wierszy, 151 stalych, 16 funkcji losujacych i progowych), od ktorego zalezy dosłownie kazdy modul — czyli odtwarzajacy zobaczy osiem skrzynek, a w sekcji II znajdzie jedenascie i nie bedzie wiedzial, gdzie te trzy dopiac. Rozjazd powstal wlasnie teraz: sekcja II jest generowana i wciagnela config.py oraz kopia_subskrybentow.py, diagram jest reczny i o nich nie wie.

**Plan naprawy:** Dorysowac w wstep.md:81-89 `config.py` jako warstwe pod wszystkimi modulami (np. osobna linia "wszystkie moduly --> config.py (stale i losowania, ZALACZNIK B)") oraz `kopia_subskrybentow.py` jako narzedzie poza przebiegiem, wolane recznie i pilnowane przez `alarm.kopia_subskrybentow()` (alarm.py:300, wpiete w `sprawdz_wszystko` alarm.py:306-312). Diagram i lista `MODULY` w sklej.py powinny miec te sama liczbe pozycji — warto dopisac w generatorze ostrzezenie, gdy sie rozjada.

---

### 15. Sam config.py twierdzi, ze cztery stale sa martwe, a test pilnuje, zeby to nieprawdziwe zdanie tam zostalo

**Powaga:** powazna   **Plik:** `config.py:1060`

**Dowod:** config.py:1060: „# UWAGA: CZTERY PONIZSZE STALE NIE SA UZYWANE PRZEZ ZADNA LINIE KODU.", a 19 wierszy nizej config.py:1071: „WORST_NOTE_HOURS = (12, 13) # ET, zwlaszcza w piatek" — uzywane w tym samym pliku, w config.py:341. Do tego tests/test_martwe_sygnaly.py:96 wpisuje `\"WORST_NOTE_HOURS\": \"j.w.\"` na liste dopuszczonych martwych stalych, a tests/test_martwe_sygnaly.py:178-179 asercja utrwala zdanie: „sprawdz(\"config OSTRZEGA, ze sa nieuzywane\",\n \"NIE SA UZYWANE PRZEZ ZADNA LINIE KODU\" in cfg)".

**Dlaczego to wada:** To dokladnie ta klasa bledu, ktora dokument sciga w VIII.3 („stale udajace zabezpieczenia"), tylko odwrocona: komentarz zaklada, ze stala nic nie robi, a ona rozstrzyga o milczeniu agenta. Ktos czytajacy kod (nie dokument) usunie `WORST_NOTE_HOURS` jako smiec albo — gorzej — „ozywi" ja przez dopisanie drugiego warunku. Test, ktory mial chronic przed martwymi stalymi, przybil tu falszywe twierdzenie: `test_martwe_sygnaly` oblewa sie, jesli ktos poprawi komentarz na prawdziwy.

**Plan naprawy:** Wyprowadzic `WORST_NOTE_HOURS` poza blok „CZTERY PONIZSZE STALE" i postawic ja przy `OKNO_PUBLIKACJI_ET` z komentarzem, ze jest egzekwowana w `pora_na_publikacje`. Naglowek zmienic na „TRZY PONIZSZE STALE". W tests/test_martwe_sygnaly.py skreslic wpis z wiersza 96 i dopisac asercje odwrotna — ze `WORST_NOTE_HOURS` MA byc uzywane w kodzie, tak jak juz zrobiono dla `OKNO_PUBLIKACJI_ET` w wierszach 186-191.

---

### 16. Wada 7 (kolejnosc bloku komentarzy) jest sprzeczna z §7.3 tego samego dokumentu i byla nieprawdziwa juz w dniu spisania listy

**Powaga:** powazna   **Plik:** `dokumentacja-zrodla/wady.md:19`

**Dowod:** wady.md:19 (= JAK_ZBUDOWANY_JEST_BOT.md:7215): „| 7 | kolejnosc bloku komentarzy | trzy warianty + factcheck placone **zanim** sprawdzimy, czy pod postem jest pole komentarza | ~3 centy na kazdy post bez pola |". Kod: run.py:476-479 „ if not browser.mozna_komentowac(cel[\"url\"]):\n continue\n strony = browser.read_pages([cel[\"url\"]])" — a `stages.comment_on` (trzy warianty + factcheck) stoi dopiero w run.py:481. Sprawdzenie idzie PIERWSZE. `git show 47f18f8:agent-v2/run.py` pokazuje to samo wywolanie w wierszu 433, czyli straz istniala juz w commicie, ktorym spisano te liste. Ten sam dokument opisuje ja poprawnie: JAK_ZBUDOWANY_JEST_BOT.md:2691 „#### 7.3 Prawo do komentowania PRZED pisaniem — `mozna_komentowac` (browser.py:1787)".

**Dlaczego to wada:** Dokument w dwoch miejscach mowi rzeczy wykluczajace sie: §7.3 chwali straz przed pisaniem, VIII.1 #7 kaze ja dopiero zbudowac. Czytelnik listy wad pojdzie przestawiac blok, ktory jest juz przestawiony, i albo zdubluje wywolanie `mozna_komentowac` (kazde otwiera osobna przegladarke — browser.py:1798-1799 `podlacz_sie()`), albo uzna, ze zle czyta kod. Prawdziwa jest tylko WEZSZA wersja tej wady, ktorej wpis nie nazywa: `mozna_komentowac` pyta o `write_comment_permissions` i przy braku odpowiedzi zwraca True (browser.py:1806 i 1813, „nie wiem, wiec probuje") — wiec dla postow, o ktorych API milczy, trzy centy nadal ida przed odkryciem, ze pola komentarza nie ma w DOM.

**Plan naprawy:** Przepisac pozycje 7 na to, co zostalo: „posty, dla ktorych API nie oddaje `write_comment_permissions` — `mozna_komentowac` zwraca wtedy True (browser.py:1806, 1813), wiec `stages.comment_on` placi zanim okaze sie, ze pola komentarza nie ma w drzewie". Dopisac odsylacz do §7.3, zeby nie czytalo sie to jak zaprzeczenie tamtego akapitu.

---

### 17. Dokument opisuje jako WADE, ze THIN dostaje dlugosc RICH — a config ma juz osobny wpis THIN i domysl SINGLE

**Powaga:** powazna   **Plik:** `dokumentacja-zrodla/rozdzial_artykul.md:919`

**Dowod:** rozdzial_artykul.md:919 (= JAK_ZBUDOWANY_JEST_BOT.md:1348): „**WADA — `THIN` dostaje dlugosc `RICH`.** `DLUGOSC_WG_GLEBOKOSCI` nie ma klucza `\"THIN\"`, a `dlugosc_dla` robi `.get(..., DLUGOSC_WG_GLEBOKOSCI[\"RICH\"])`". Powtorzone w podsumowaniu rozdzialu, rozdzial_artykul.md:1323 (= dokument:1752): „7. **`THIN` dostaje dlugosc `RICH`** — brak klucza w `DLUGOSC_WG_GLEBOKOSCI`". Kod: config.py:470 „ \"THIN\": {\"cel\": 420, \"min\": 300, \"max\": 560}," oraz config.py:482-483 „ return DLUGOSC_WG_GLEBOKOSCI.get(\n (glebokosc or \"\").upper(), DLUGOSC_WG_GLEBOKOSCI[\"SINGLE\"])". Docstring config.py:477 nazywa to wprost: „Galaz domyslna to SINGLE, nie RICH".

**Dlaczego to wada:** Obie wersje wady sa juz nieaktualne — i klucz istnieje, i galaz domyslna zostala zmieniona. Czytelnik, ktory pojdzie „naprawic" to wedlug dokumentu, dopisze klucz, ktory tam juz jest, albo zacznie szukac bledu w dzialajacej funkcji. Gorzej dla odtwarzajacego bota od zera: dokument mowi mu, ze nieznana glebokosc ma wpadac w RICH, wiec odtworzy agenta, ktory kaze pisarzowi zapelniac 1075 slow tam, gdzie biezacy kod zamawia 650 — czyli odtworzy dokladnie usterke opisana w config.py:479-480.

**Plan naprawy:** Przeniesc oba akapity (rozdzial_artykul.md:919 i :1323) do rejestru wad naprawionych, z data i z nowymi wartosciami: klucz `THIN` = 420/300/560 (config.py:470), galaz domyslna `SINGLE` (config.py:483). Przy okazji poprawic odsylacz z rozdzial_artykul.md:826 — mowi „config.py:452-458", a tabela stoi teraz w config.py:455-471.

---

### 18. Dokument ostrzega przed sekcja [Install] w nia-agent.service, ktorej w pliku juz nie ma

**Powaga:** powazna   **Plik:** `dokumentacja-zrodla/rozdzial_dane.md:919`

**Dowod:** rozdzial_dane.md:919 (= JAK_ZBUDOWANY_JEST_BOT.md:5700): „**WADA — `nia-agent.service` ma sekcje `[Install]`.** Zawiera `WantedBy=multi-user.target` ... `systemctl enable nia-agent.service` ... sprawi, ze **platny, publikujacy przebieg wystartuje przy kazdym starcie systemu**". W pliku systemd/nia-agent.service nie ma ani `[Install]`, ani `WantedBy` — ostatnie wiersze (35-41) mowia odwrotnie: „# BEZ [Install]. Ta usluga ma jedno wejscie: nia-agent.timer. Sekcja [Install]\n# z WantedBy=multi-user.target sprawiala, ze `systemctl enable nia-agent` ... Bez tej sekcji usluga jest `static`: zegar ja odpala, `enable` nie ma czego wlaczyc. Tak samo jak nia-alarm i nia-artykul."

**Dlaczego to wada:** Ostrzezenie o nieistniejacym ryzyku wysyla operatora do edycji pliku jednostki na produkcji — czyli do dotykania rzeczy, ktora odpala platny przebieg z `--wyslij`. Osoba odtwarzajaca serwer z tego dokumentu moze tez zrozumiec opis jako opis stanu docelowego i DODAC `[Install]`, wprowadzajac wlasnie te awarie, przed ktora akapit ostrzega. Dokument sam nazywa te klase bledu w VIII.3: „Dodatek przychodzi poprawic system i psuje go po cichu".

**Plan naprawy:** Skreslic akapit `rozdzial_dane.md:919` i zastapic go zdaniem, ze wszystkie trzy uslugi sa `static`, z cytatem z komentarza systemd/nia-agent.service:35-41. Jesli wada ma zostac jako przestroga — przeniesc do rejestru naprawionych, z data usuniecia sekcji.

---

### 19. Dokument cytuje z jednostki systemd komentarz, ktory w pliku zostal skreslony jako nieprawdziwy, i opisuje jako WADE brak strazy czasu, ktora istnieje

**Powaga:** powazna   **Plik:** `dokumentacja-zrodla/rozdzial_dane.md:890`

**Dowod:** rozdzial_dane.md:890 (= dokument:5671): „**WADA.** ... nic nie pilnuje, zeby suma losowanych przerw (10-25 min po kazdej notce, piec notek dziennie) zmiescila sie pod nim. ... Kod nie wie, ile ma czasu." Kod wie: run.py:141 „def zostal_czas(na_co: str = \"\", potrzeba_s: float = 0.0) -> bool:" i run.py:190-193 „ przerwa = _s.losuj_odstep(co)\n if not zostal_czas(na_co, przerwa):\n return False\n _s.odczekaj(co, przerwa)". Wczesniejszy akapit rozdzial_dane.md:877 (= dokument:5658) cytuje jako „z komentarzy w pliku" zdanie „same przerwy miedzy dzialaniami to ~42 minuty, bo agent czeka po ludzku (10-25 min po notce)" — w systemd/nia-agent.service:20-23 stoi teraz cos przeciwnego: „Ten komentarz mowil wczesniej ... i to bylo NIEPRAWDA od czasu, gdy odstep miedzy notkami podniesiono do 45-90 minut (config.ODSTEPY)". Faktyczne wartosci: config.py:1230 „ \"notka\": (2700, 5400), # 45-90 min".

**Dlaczego to wada:** Dwie osobne pomylki w jednym miejscu. Cytat oznaczony jako doslowny („Z komentarzy w pliku") juz w pliku nie wystepuje i podaje odstep 10-25 min zamiast 45-90 — ktos odtwarzajacy bota wpisze do `ODSTEPY` liczby prawie czterokrotnie za male i dostanie profil publikujacy parami, czyli usterke opisana w config.py:1220-1225. A akapit „WADA ... Kod nie wie, ile ma czasu" wysyla do napisania od nowa `rytm`/`zostal_czas(potrzeba_s)`, ktore juz sa i ktorych sens jest wprost zapisany w run.py:178-184.

**Plan naprawy:** W rozdzial_dane.md:877 podmienic cytat na aktualna tresc komentarza systemd/nia-agent.service:19-26 i poprawic odstepy na 45-90 min za config.py:1230. Akapit :890 przeniesc do naprawionych, wskazujac `run.rytm` (run.py:168-194) i `zostal_czas(potrzeba_s)` (run.py:141-165) jako straz, oraz zdanie z jednostki „JEDYNA LICZBA W JEDNYM MIEJSCU: config.LIMIT_CZASU_PRZEBIEGU_S ma byc rowny tej wartosci" (sprawdzone: obie = 9000, config.py:1206).

---

### 20. Straz budzetu miesiecznego przed artykulem nie istnieje w dokumencie — a to krok, ktory potrafi zakonczyc caly przebieg przed skautem

**Powaga:** powazna   **Plik:** `dokumentacja-zrodla/rozdzial_artykul.md:3`

**Dowod:** Dokument (rozdzial_artykul.md:3): "Cala sciezka to czternascie krokow w `main()` (`agent-v2/run.py:645-1055`), z czego dziesiec ma nazwe etapu w krotce `STAGES`". Rozdzial "Rusztowanie wspolne dla wszystkich etapow" (rozdzial_artykul.md:48-175) wymienia: zamek, odmowe publikacji z kopii, `cached()`, `_prompt()`, `llm.call()` z `_preflight`, koszt i sufity tokenow — i konczy zdaniem (rozdzial_artykul.md:108): "Sufit przebiegu jest sprawdzany zawsze, takze przy `AGENT_V2_NO_LIMIT=1`; dzienny i miesieczny sa pomijane przy `NO_LIMIT`." Tymczasem run.py:753-768 ma osobna, wczesniejsza brame: "# NIE ZACZYNAJ TEGO, CZEGO NIE SKONCZYSZ [...] Skoro znamy koszt calego przebiegu / # (RUN_LIMIT_USD), umiemy o to zapytac zawczasu. / if not config.NO_LIMIT: / [...] _zostalo = config.MONTHLY_LIMIT_USD - db.spent_usd(conn, _m) / if _zostalo < config.RUN_LIMIT_USD: / print(f\" MIESIAC NA WYCZERPANIU [...] Nie zaczynam — lepiej nie napisac nic niz zaplacic za polowe.\") / return _done(conn, run_id, \"budzet\")". Fraz "MIESIAC NA WYCZERPANIU", "NIE ZACZYNAJ TEGO, CZEGO NIE SKONCZYSZ" ani slowa "budzet" jako etapu nie ma nigdzie w calym JAK_ZBUDOWANY_JEST_BOT.md (10 419 wierszy) — sprawdzone gerpem po calym pliku, nie tylko po Sekcji III.

**Dlaczego to wada:** To nie jest szczegol implementacyjny, tylko krok sterujacy: przy $37,50 wydanych w miesiacu agent NIE NAPISZE artykulu i wroci 0, mimo ze `_preflight` przepuscilby pierwsze wywolania. Kto odtwarza bota z dokumentu, dostanie agenta, ktory zaczyna artykul, placi za skauta, odsiew, dyskoverie i klasyfikacje, a potem pada na `BudgetExceeded` gdzies miedzy synteza a pisarzem — czyli dokladnie na scenariusz, ktory ten kod ma wykluczac. Osobno: `_done(conn, run_id, "budzet")` zapisuje do `runs.stage` wartosc SPOZA krotki STAGES, wiec kto czyta dokument ("dziesiec ma nazwe etapu w krotce STAGES") i buduje na tym raport z tabeli `runs`, nie przewidzi tej wartosci. Liczba "czternascie krokow" tez jest juz falszywa.

**Plan naprawy:** Dopisac w rozdzial_artykul.md podrozdzial "##### Straz budzetu miesiecznego przed artykulem" w sekcji "Rusztowanie wspolne" (po "Zamek i odmowa publikacji z kopii", ok. linii 53), z cytatem run.py:753-768, wzorem `MONTHLY_LIMIT_USD - db.spent_usd(conn, YYYY-MM) < RUN_LIMIT_USD`, informacja ze bramka jest pomijana przy `NO_LIMIT` i ze konczy przebieg przez `_done(conn, run_id, "budzet")`, czyli wpisuje do `runs.stage` wartosc spoza STAGES. Poprawic linie 3: "czternascie krokow" -> "pietnascie krokow" i zakres `run.py:645-1055` -> `run.py:703-1155`. Dopisac wiersz o wartosci `budzet` do tabeli "Co trafia do bazy i na dysk" (rozdzial_artykul.md ok. 1298).

---

### 21. WADA „regula roznorodnosci domen jest liczona i wyrzucana" jest nieaktualna — `recent_domains` trafia do promptu jako `ostatnie_domeny`

**Powaga:** powazna   **Plik:** `dokumentacja-zrodla/rozdzial_artykul.md:453`

**Dowod:** Dokument (rozdzial_artykul.md:453, w sklejce JAK_ZBUDOWANY_JEST_BOT.md:852): "**WADA — regula roznorodnosci domen jest liczona i wyrzucana.** [...] Sygnatura to `def discovery(conn, run_id, question, recent_domains)`. W calym ciele funkcji `recent_domains` **nie wystepuje ani razu**. Zapytanie SQL z `JOIN`-em po `articles`/`sources` wykonuje sie co przebieg, a wynik nigdy nie dociera do promptu." Powtorzone w zbiorczej liscie (rozdzial_artykul.md:1320): "4. **Regula roznorodnosci domen liczona i wyrzucana** — `recent_domains` jest nieuzywanym parametrem `discovery`." Kod (stages.py:1872-1884) mowi cos przeciwnego: "# DOMENY OSTATNICH ARTYKULOW. Baza liczyla je co przebieg / # (`db.recent_domains`), przekazywalismy je tu w parametrze — i nie / # czytala ich ani jedna linia. [...] To PREFERENCJA, nie bramka." i realnie: "ostatnie_domeny=(\", \".join( / d for d in (recent_domains or [])[:15] / if d and d.strip() == d and \" \" not in d / ) or \"(none yet - this is the first article of this account)\"),".

**Dlaczego to wada:** To jest falszywy alarm najgorszego rodzaju: wysyla wlasciciela do naprawiania czegos, co juz dziala, i to w miejscu, gdzie naprawa „na skroty" (twardy filtr hostow) jest wprost odradzana w komentarzu kodu, bo "potrafilby wyzerowac liste zrodel i wywalic przebieg PO oplaceniu researchu". Kto pojdzie za dokumentem, moze dolozyc bramke, ktora zabije przebieg po oplaconej dyskoverii. Osobno: dokument zostawia czytelnika w przekonaniu, ze `db.recent_domains` to martwe zapytanie SQL do usuniecia.

**Plan naprawy:** Zastapic WADE w rozdzial_artykul.md:453 opisem realnego zachowania: `recent` z `db.recent_domains(conn, config.DIVERSITY_LOOKBACK)` (run.py:797) idzie do `stages.discovery` i lezy w prompcie jako `{ostatnie_domeny}` — do 15 domen, odfiltrowane z wpisow z bialymi znakami, z zapasem "(none yet - this is the first article of this account)". Podkreslic, ze to PREFERENCJA w prompcie, nie bramka w kodzie, i zacytowac uzasadnienie ze stages.py:1877-1883. Usunac punkt 4 ze zbiorczej listy (rozdzial_artykul.md:1320) i przenumerowac.

---

### 22. Blok „Wejscie" dyskoverii nie przekazuje pola `ostatnie_domeny`, ktorego prompt wymaga — odtworzenie z dokumentu konczy sie KeyError na etapie 3

**Powaga:** powazna   **Plik:** `dokumentacja-zrodla/rozdzial_artykul.md:380`

**Dowod:** Dokument (rozdzial_artykul.md:378-388, w sklejce JAK_ZBUDOWANY_JEST_BOT.md:777-787) podaje wywolanie jako komplet: " prompt = _prompt( / \"dyskoveria.md\", / question=question, / max_results=config.DISCOVERY_MAX_RESULTS, / max_searches=config.DISCOVERY_MAX_SEARCHES, / min_primary=config.MIN_PRIMARY_SOURCES, / min_why=config.MIN_WHY_SOURCES, / blocked_hosts=\", \".join(list(config.BLOCKED_HOSTS) + martwe), / )" — bez `ostatnie_domeny`. Realny `prompts/dyskoveria.md` ma 41 wierszy i w wierszu 29 stoi " {ostatnie_domeny}". `_prompt` to goly `str.format` (dokument sam to podkresla, rozdzial_artykul.md:82: "To `str.format`, wiec **kazdy literalny nawias klamrowy w prompcie musi byc podwojony**"). Zalacznik promptow w tym samym dokumencie (JAK_ZBUDOWANY_JEST_BOT.md:7532-7534) tez jest stary: "**37 wierszy.** Pola wejsciowe: `blocked_hosts`, `max_results`, `max_searches`, `min_primary`, `min_why`, `question`" — bez `ostatnie_domeny`, i cala przytoczona tresc promptu nie ma punktu o domenach.

**Dlaczego to wada:** Dwa rozne, konkretne bledy przy odtwarzaniu. Kto sklada kod z dokumentu, a prompt bierze z repo, dostaje `KeyError: 'ostatnie_domeny'` w `_prompt` na etapie 3 — czyli JUZ PO oplaceniu skauta i odsiewu, dokladnie w miejscu, gdzie dokument uczy, ze research jest bezpowrotny. Kto sklada oba z dokumentu, dostaje bota bez reguly roznorodnosci domen i nie ma pojecia, ze czegos brakuje, bo dokument nigdzie nie wymienia tego pola (fraza "ostatnie_domeny" nie wystepuje w calym JAK_ZBUDOWANY_JEST_BOT.md ani razu).

**Plan naprawy:** W rozdzial_artykul.md:380-388 dopisac do bloku `_prompt` argument `ostatnie_domeny=...` dokladnie jak w stages.py:1884-1887, wraz z jednym zdaniem, ze puste wejscie daje literal "(none yet - this is the first article of this account)". Osobno zaktualizowac zalacznik promptow (dokumentacja-zrodla/zalacznik_prompty.md albo zalacznik_dysk.md — zaleznie od tego, ktory generuje JAK_ZBUDOWANY_JEST_BOT.md:7532): 37 -> 41 wierszy, dopisac `ostatnie_domeny` do listy pol wejsciowych i wkleic aktualna tresc prompts/dyskoveria.md razem z punktem 7 o domenach. Przesklejac.

---

### 23. WADA „THIN dostaje dlugosc RICH" jest nieaktualna — THIN ma wlasny wpis, a galaz domyslna to teraz SINGLE

**Powaga:** powazna   **Plik:** `dokumentacja-zrodla/rozdzial_artykul.md:919`

**Dowod:** Dokument (rozdzial_artykul.md:919, w sklejce JAK_ZBUDOWANY_JEST_BOT.md:1318): "**WADA — `THIN` dostaje dlugosc `RICH`.** `DLUGOSC_WG_GLEBOKOSCI` nie ma klucza `\"THIN\"`, a `dlugosc_dla` robi `.get(..., DLUGOSC_WG_GLEBOKOSCI[\"RICH\"])`. [...] Kod daje mu najdluzsza." Powtorzone w zbiorczej liscie (rozdzial_artykul.md:1323): "7. **`THIN` dostaje dlugosc `RICH`** — brak klucza w `DLUGOSC_WG_GLEBOKOSCI`". Kod (config.py:465-470) mowi wprost: "# Bez tego wpisu THIN wpadal w galaz domyslna, czyli RICH [...] / \"THIN\": {\"cel\": 420, \"min\": 300, \"max\": 560},", a domyslna galaz zmieniono (config.py:477-483): "Galaz domyslna to SINGLE, nie RICH. [...] / return DLUGOSC_WG_GLEBOKOSCI.get( / (glebokosc or \"\").upper(), DLUGOSC_WG_GLEBOKOSCI[\"SINGLE\"])". Blok kodu przytoczony w dokumencie (rozdzial_artykul.md:826-834) tez pokazuje slownik z dwoma kluczami zamiast trzech i podaje zly zakres linii: "`DLUGOSC_WG_GLEBOKOSCI` (`config.py:452-458`)" — realnie config.py:455-471.

**Dlaczego to wada:** Odtworzenie z tego dokumentu daje slownik bez THIN i fallback na RICH, czyli reprodukuje usterke, ktora komentarz w config.py:467-469 opisuje z nazwy jako powod istnienia calej tabeli ("artykul o symbolu otwartego sloiczka mial material na trzysta slow i wypelnil tysiac tym samym mechanizmem opisanym trzy razy"). A kto czyta liste wad jako liste zadan, idzie naprawiac cos, co jest naprawione — i najpewniej przywroci `DLUGOSC_WG_GLEBOKOSCI[\"RICH\"]` jako fallback, bo tak radzi dokument.

**Plan naprawy:** W rozdzial_artykul.md:826-834 wkleic aktualny slownik z config.py:455-471 (trzy klucze, THIN = cel 420 / 300-560) i poprawic odwolanie na `config.py:455-471`. Usunac WADE z linii 919 i punkt 7 ze zbiorczej listy (linia 1323); w ich miejsce dopisac jedno zdanie w opisie etapu 8, ze `dlugosc_dla` ma galaz domyslna SINGLE (config.py:483) i dlaczego — nieznana glebokosc znaczy "nie wiem, ile tu jest materialu". Zwrocic uwage, ze run.py:976 nadal robi `str(verdict.get(\"depth\") or \"RICH\").upper()`, wiec brak pola `depth` to wciaz RICH — to zostaje i warto to powiedziec wprost, bo inaczej wyglada na sprzecznosc.

---

### 24. Numery linii dla run.py i stages.py sa systematycznie rozjechane w calej mapie etapow i we wszystkich naglowkach etapow

**Powaga:** powazna   **Plik:** `dokumentacja-zrodla/rozdzial_artykul.md:20`

**Dowod:** Dokument (rozdzial_artykul.md:178): "**Funkcja:** `stages.scout` (`stages.py:2036`), wolana z `run.py:698`." Realnie `def scout(` jest w stages.py:2064, a wywolanie w run.py:773. Rozjazd dotyczy wszystkich etapow, ktorych funkcje leza w drugiej polowie stages.py, oraz KAZDEGO wywolania w run.py (main() przesunelo sie o ~75 wierszy): feasibility 1905->1940, pick_topic 1929->1964, discovery 1835->1852, fetch 1695->1712, classify 1569->1586, synthesis 1518->1535, fallback_card 1480->1497, warto_pisac 2429->2475, bibliotekarz 2288->2334, bank_fragmentow 2248->2294, `_ZAPRZECZENIE` 2414->2460, `MIN_FILAROW_POZA_PRZEKONANIEM` 2426->2472, `WYMAGANE_ZLAMANE_PRZEKONANIE` 2424->2471, `_precedens_ok` 2771->2842, `_to_pdf` 2610->2656, `_tekst_z_pdf` 2629->2675, `_dobierz_przegladarka` 1639->1656, `hosty_ktore_nigdy_nie_dzialaly` 1794->1811, `pytania_dla_skauta` 2605->2651. Wywolania w run.py: scout 698->773, feasibility 705-710->782-784, discovery 724-729->797-803, fetch 753->828, classify 798-802->875, synthesis 818-823->896, warto_pisac 855->928, write 900-903->978, review 935-937->1011, forma 986-987->1061, save 1024->1119, grafika 1030->1134. Zgadzaja sie tylko funkcje z gory stages.py (review:71, ocen_forme:90, poprzednie_teksty:111, _nazwa_zrodla:142, save:164, write:215, grafika:457) oraz gates.py (118, 324) i run.py:24-27/68/86-116.

**Dlaczego to wada:** Dokument sam sie reklamuje jako odtworzeniowy i kazde twierdzenie kotwiczy numerem linii — a te kotwice trafiaja teraz w srodek innej funkcji. Kto sprawdza `stages.py:2429` szukajac `warto_pisac`, laduje w `wez_z_banku_notek`; kto szuka `run.py:1030` szukajac grafiki, laduje w bloku recenzji. Przy 10 419 wierszach dokumentu i 3 043 wierszach stages.py to jest realna strata czasu przy kazdym sprawdzeniu, a ze czesc numerow (te z gory pliku) nadal sie zgadza, czytelnik nie ma podstaw podejrzewac reszty.

**Plan naprawy:** Wygenerowac numery linii maszynowo zamiast wpisywac recznie. Najmniejszy ruch: skrypt w dokumentacja-zrodla/ (obok sklej.py), ktory przed sklejeniem podmienia wzorce `stages.py:NNNN` / `run.py:NNNN` na wynik grepa po `^def <nazwa>` — albo, jesli to za duzo, zastapic numery linii samymi nazwami funkcji (`stages.scout`, bez `:2036`), bo nazwa jest stabilna, a numer nie. Doraznie: poprawic 14 naglowkow etapow (rozdzial_artykul.md:178, 297-299, 373, 461, 555, 627, 702, 813, 927, 992, 1108, 1180) i mape etapow (rozdzial_artykul.md:18-31) wedlug listy w dowodzie.

---

### 25. Par. 9 i 3.2 cytuja budzet["follow"] zamiast na_teraz["follow"], a par. 17 poz. 3 opisuje wade jako aktualna mimo etykiety NAPRAWIONE

**Powaga:** powazna   **Plik:** `dokumentacja-zrodla\rozdzial_dzien.md:1107`

**Dowod:** Dokument, par. 9: `if not budzet.get("follow"):` (linia 1098) i `for host in kandydaci[: budzet["follow"]]:` (linia 1107), plus zdanie "Blok `subskrybuj` jest identyczny, z `budzet["subskrypcje"]`" (linia 1119). Par. 3.2 cytuje `zostalo = {k: max(0, budzet[k] - juz.get(k, 0)) for k in ("notki", "komentarze", "lajki", "restacki")}` (linia 282) — cztery klucze. Kod: run.py:557 `if not na_teraz.get("follow"):`, run.py:562 `for host in kandydaci[: na_teraz["follow"]]:`, run.py:598/603 to samo dla subskrypcji, a run.py:285-287 ma szesc kluczy: `for k in ("notki", "komentarze", "lajki", "restacki", "follow", "subskrypcje")`. Par. 17 poz. 3 (linia 1806) nosi naglowek NAPRAWIONE, po czym twierdzi w czasie terazniejszym: "`budzet["follow"]` i `budzet["subskrypcje"]` sa brane w calosci w kazdym z trzech przebiegow, a `zostalo`/`z_dziennika_dzis` ich nie obejmuja. Realny wolumen ~ 3x konfiguracja".

**Dlaczego to wada:** Trzy zdania sa juz nieprawda (pelny budzet w kazdym przebiegu, brak w `zostalo`, 3x wolumen), a jedno nadal prawda (browser.z_dziennika_dzis, browser.py:107, liczy tylko {"komentarze", "lajki", "restacki"} — obserwacji i subskrypcji nikt dzis nie odejmuje). Czytelnik nie ma jak rozdzielic, ktora polowa obowiazuje, i albo zacznie "naprawiac" dzialajacy podzial przez przebiegi, albo uzna cala pozycje za zamknieta i przeoczy realnie brakujacy licznik dzienny. Odtworzenie par. 9 doslownie przywraca dokladnie ta wade, ktora ten punkt nazywa naprawiona: ~60-70 obserwacji miesiecznie zamiast 20-30 i ~27 subskrypcji zamiast 6-12, kazda idaca mailem do skrzynki wlasciciela.

**Plan naprawy:** Podmienic w wydruku par. 9 `budzet` na `na_teraz` (linie 1098, 1107, 1119), w par. 3.2 (linia 282) wpisac szesciokluczowa wersje `zostalo` z run.py:285-287, a par. 17 poz. 3 przepisac na to, co zostalo otwarte: podzial przez przebiegi dziala, ale z_dziennika_dzis nadal nie liczy rodzajow "obserwacja" i "subskrypcja", wiec ochrona przed powtorzeniem normy po restarcie dziala tylko dla notek, komentarzy, lajkow i restackow.

---

### 26. Zwloka przed pierwsza notka nadal spi do 40 minut bez pytania o zegar — wbrew docstringowi rytm

**Powaga:** powazna   **Plik:** `run.py:425`

**Dowod:** run.py:421-425: if wyslij: import random as _r ile = _r.uniform(*config.ZWLOKA_PRZED_NOTKAMI) print(f" (zwloka {ile / 60:.0f} min przed pierwsza notka)", flush=True) time.sleep(ile) Zadnego `zostal_czas` przed tym snem nie ma. config.py:1245: `ZWLOKA_PRZED_NOTKAMI = (0, 2400)` — do 40 minut. Docstring run.rytm (run.py:170-171) mowi: "Trzeci raz ta sama wada, tym razem zamknieta w jednym miejscu dla wszystkich blokow", a run.py:185-187: "Teraz przerwa jest najpierw losowana, potem sprawdzana wobec konca przebiegu, i dopiero wtedy odsypiana". Dokument (rozdzial_dzien.md:207) wymienia zwloke jako trzecia droge zuzycia odstepow, bez slowa o tym, ze ta jedna nie jest sprawdzana.

**Dlaczego to wada:** Zostal jeden nieopanowany sen i lezy w bloku, ktory ma pierwszenstwo. Sekwencja: zmiesci_sie("notka", ...) liczy obietnice na starcie przebiegu (run.py:335), potem blok notek zjada do 40 minut zegara, a dopiero potem pyta o czas — czyli obietnica jest wymierzona na budzecie, ktorego juz nie ma. Przy 40-minutowym losowaniu i 2h15 przebiegu to blisko 30% zegara wydane przed pierwszym dzialaniem. Kto czyta docstring rytm, wychodzi z przekonaniem, ze w run.py nie ma juz snu bez sprawdzenia — a jest, dwadziescia wierszy dalej.

**Plan naprawy:** Przepuscic zwloke ta sama sciezka co reszte: wylosowac `ile`, sprawdzic `zostal_czas("notki", ile)` i dopiero wtedy spac (albo skrocic zwloke do tego, co zostalo po odjeciu potrzeby na jedna notke). Rownolegle poprawic rozdzial_dzien.md:207, zeby nie sugerowal, ze wszystkie trzy drogi sa rownowazne wobec zegara.

---

### 27. rytm pyta o zegar PO oplaceniu wywolania modelu — gotowy tekst jest wyrzucany

**Powaga:** powazna   **Plik:** `run.py:487`

**Dowod:** W bloku komentarzy (run.py:475-490) kolejnosc jest taka: `strony = browser.read_pages([cel["url"]])`, potem `out = stages.comment_on(conn, run_id, strony[0])` — trzy warianty plus weryfikacja, wywolanie platne — i dopiero potem: if not rytm("komentarz", "komentarze", rytm_stanu): return To samo w odpowiedziach (stages.reply_to z web_search=True w run.py:381-386, rytm dopiero w :401), w notkach (generator stages.notki_dnia, rytm w :435) i w dyskusjach (stages.comment_on w :532, rytm w :539). Przed wprowadzeniem rytm jedynym wyjsciem bylo `zostal_czas` na gorze petli (np. run.py:472), czyli PRZED kosztem.

**Dlaczego to wada:** `zostal_czas` na gorze petli przepuszcza, dopoki zostala jakakolwiek sekunda (potrzeba_s=0). Pod koniec kazdego przebiegu wchodzi wiec jeszcze jedna iteracja, placi za komplet wywolan modelu, a nastepnie rytm odmawia 3-8-minutowej przerwy i blok wychodzi przez `return` — napisany i zweryfikowany komentarz nie jest nigdzie zapisywany ani wystawiany. To sie dzieje na ogonie kazdego dotknietego bloku, trzy razy dziennie. Dokument nie opisuje tej sciezki wcale (rozdzial IV zna tylko jedno wyjscie z petli), wiec nikt jej nie szuka.

**Plan naprawy:** Przesunac pytanie o rytm przed kosztowna czesc iteracji: policzyc przerwe raz na obrot i sprawdzic ja tuz po `if not zostal_czas(...)` na gorze petli, przed read_pages/comment_on/reply_to. Alternatywnie zawolac na gorze `zostal_czas(nazwa, config.CZAS_DZIALANIA_S + srodek_odstepu)`, zeby ostatnia iteracja w ogole nie startowala. Po zmianie dopisac ten warunek do wydrukow blokow w rozdziale IV.

---

### 28. Wszystkie numery linii run.py w rozdziale IV sa przesuniete o 35-58 wierszy — naglowki blokow wskazuja cudze bloki

**Powaga:** powazna   **Plik:** `dokumentacja-zrodla\rozdzial_dzien.md:1224`

**Dowod:** Naglowek "### 11. Blok 5 — restacki (`run.py:569`)" (rozdzial_dzien.md:1224) wskazuje na wiersz, ktory dzis brzmi: 569: for host in kandydaci[: na_teraz["follow"]]: czyli na srodek bloku OBSERWOWANIA (restacki zaczynaja sie w run.py:627). Podobnie "Blok 3c — obserwowanie (`run.py:494`)" (linia 1095) wskazuje na komentarz wewnatrz bloku komentarzy (obserwowanie: run.py:548), "Blok 1 — odpowiedzi (`run.py:307`)" (linia 445) trafia w `zostalo["notki"] = 0` w rozdzielniku budzetu (odpowiedzi: run.py:353), a "`run.py:645 main()`" (linia 17) trafia w pusty wiersz (main: run.py:703). Pelna lista przesuniec: main 645->703, SIGTERM 619->677, dzien 223->258, zmiesci_sie 162->197, ile_przebiegow_zostalo 197->232, blok() 298->344, kolejnosc 603->661, odpowiedzi 307->353, notki 365->413, komentarze 402->452, dyskusje 448->500, obserwowanie 494->548, subskrypcje 533->589, polubienia 564->622, restacki 569->627. Odsylacze do browser.py sa przesuniete rownomiernie o 9 (np. ile_dzis_wystawione 614->605, restackuj_w_kanale 2166->2157, main guard 2117->2108) po usunieciu wklejki z sprawdz_sesje/zaloguj.

**Dlaczego to wada:** Rozdzial deklaruje metode "cytat plus numer linii" i uzywa numerow w naglowkach sekcji, wiec czytelnik nawiguje wlasnie nimi. Trafiajac w srodek innego bloku, nie widzi bledu jako bledu — widzi kod i probuje go pogodzic z opisem. Przy blokach 3c/3d i 5, ktore roznia sie tylko nazwa funkcji browsera, to prowadzi wprost do pomylenia obserwowania z restackiem.

**Plan naprawy:** Przeliczyc wszystkie odsylacze run.py w rozdzial_dzien.md (+35 dla funkcji przed dzien(), +46..+58 dla wnetrza dzien()) i browser.py (-9). Docelowo: przestac zapisywac numery linii w recznym rozdziale albo dopisac do sklej.py kontrole, ktora sprawdza, czy pod `plik.py:N` stoi to, co naglowek obiecuje, i wypisuje ostrzezenie tak jak przy brakujacym module.

---

### 29. Dokument opisuje sekcje [Install] w nia-agent.service, ktorej w pliku nie ma — a odtworzenie jej z dokumentu wlacza platny, publikujacy przebieg przy kazdym bootcie

**Powaga:** powazna   **Plik:** `dokumentacja-zrodla\rozdzial_dane.md:919`

**Dowod:** Dokument (rozdzial_dane.md:919, w sklejce wiersz 5700): "**WADA — `nia-agent.service` ma sekcję `[Install]`.** Zawiera `WantedBy=multi-user.target`, mimo że jest to zadanie jednorazowe wyzwalane wyłącznie z zegara." — Plik systemd/nia-agent.service NIE MA zadnej sekcji [Install]; konczy sie na wierszu 40 komentarzem: "# BEZ [Install]. Ta usluga ma jedno wejscie: nia-agent.timer. Sekcja [Install] z WantedBy=multi-user.target sprawiala, ze `systemctl enable nia-agent` [...] uruchamialoby przebieg TAKZE przy kazdym starcie systemu". Dodatkowo tests/test_jednostki_systemd.py:52 egzekwuje ten stan: sprawdz("%s (oneshot) nie ma sekcji [Install]" % u.name, not ma_install).

**Dlaczego to wada:** To jest dokument ODTWORZENIOWY. Kto odtwarza jednostki systemd z tego opisu, dopisze `[Install] WantedBy=multi-user.target` (bo dokument mowi, ze tak wyglada plik) i tym samym sam zbuduje zagrozenie, ktore autor wlasnie usunal: `run.py --dzien --wyslij` ruszajacy przy kazdym starcie systemu, poza zegarem, publikujacy na zywo i wydajacy pieniadze. Testy repo od razu oblaja, ale reproducer nie bedzie wiedzial dlaczego. Ta sama nieaktualna przeslanka zywi instrukcje w wierszu 1080 ("**Nie** `enable nia-agent.service` — patrz WADA w sekcji 9") — wskazuje na WADE, ktora juz nie istnieje.

**Plan naprawy:** W rozdzial_dane.md:919 przepisac akapit na tryb NAPRAWIONE 2026-08-20: podac, ze plik nie ma [Install], zacytowac obecny komentarz z nia-agent.service:34-40 jako uzasadnienie, i dopisac, ze nieobecnosc [Install] we wszystkich uslugach `Type=oneshot` pilnuje tests/test_jednostki_systemd.py. Wiersz 1080 zostawic jako zalecenie operacyjne, ale odpiac od nieistniejacej juz WADY.

---

### 30. Tabela kontroli zdrowia w alarm.py wymienia szesc pozycji, kod ma siedem — brakuje tej, ktora dokument sam nazywa NAJWAZNIEJSZA

**Powaga:** powazna   **Plik:** `dokumentacja-zrodla\rozdzial_dane.md:999`

**Dowod:** Dokument w wierszu 986 mowi: "uruchamia siedem kontroli zdrowia (siódma dodana 2026-08-20)", ale tabela "Kontrole i progi" (wiersze 994-999) ma szesc wierszy i konczy sie na `powtorki()` — nie ma wiersza dla `kopia_subskrybentow()`. Dwa akapity nizej dokument sam sobie przeczy (wiersz 1026): "`alarm.sprawdz_wszystko()` ma sześć kontroli i żadna nie pyta „kiedy ostatnio robiono kopię listy"", a instrukcja weryfikacji (wiersz 1087) mowi: "alarm.py # sesja + sześć kontroli". Kod: alarm.py:345-354 wylicza SIEDEM par, ostatnia to ("kopia-subskrybentow", "BRAK KOPII LISTY SUBSKRYBENTOW", kopia_subskrybentow); prog siedzi w config.KOPIA_SUBSKRYBENTOW_CO_ILE_DNI = 14 (config.py:541), a docstring kontroli (alarm.py:303) mowi "NAJWAZNIEJSZA z tych kontroli".

**Dlaczego to wada:** Odtwarzajacy buduje warstwe alarmowa z tabeli, bo tabela jest jedynym miejscem z progami. Zbuduje szesc kontroli i pominie te jedna, ktora chroni jedyne aktywo nieodtwarzalne (lista subskrybentow) — czyli dokladnie tak, jak bylo przed 20 sierpnia. Czytajacy dokument bez kodu dostaje trzy sprzeczne liczby (siedem / szesc / szesc) i nie ma jak rozstrzygnac, ktora jest prawdziwa.

**Plan naprawy:** Dopisac do tabeli w rozdzial_dane.md siodmy wiersz: | `kopia_subskrybentow()` | wiek najnowszego pliku data/kopie/subskrybenci-*.csv | `KOPIA_SUBSKRYBENTOW_CO_ILE_DNI = 14` (brak katalogu lub brak plikow = alarm od razu) |. Poprawic wiersz 1026 na "siedem kontroli, a siodma pyta wprost..." i wiersz 1087 na "sesja + siedem kontroli".

---

### 31. Uzasadnienie TimeoutStartSec=9000 zacytowane z komentarza, ktory sam plik uslugi oznacza jako NIEPRAWDE — wraz z odstepami 10-25 min zamiast 45-90 min

**Powaga:** powazna   **Plik:** `dokumentacja-zrodla\rozdzial_dane.md:877`

**Dowod:** Dokument (877): "`TimeoutStartSec=9000` (2,5 h), bo „przebieg trwa okolo godziny: same przerwy miedzy dzialaniami to ~42 minuty, bo agent czeka po ludzku (10-25 min po notce)"". Plik systemd/nia-agent.service:21-27 mowi cos przeciwnego: "Ten komentarz mowil wczesniej „same przerwy to ~42 minuty, bo agent czeka 10-25 min po notce" i to bylo NIEPRAWDA od czasu, gdy odstep miedzy notkami podniesiono do 45-90 minut (config.ODSTEPY). [...] JEDYNA LICZBA W JEDNYM MIEJSCU: config.LIMIT_CZASU_PRZEBIEGU_S ma byc rowny tej wartosci". Realna wartosc: config.py:1230 `"notka": (2700, 5400), # 45-90 min`. Konkluzja dokumentu w wierszu 890 — "nic nie pilnuje, żeby suma losowanych przerw (10–25 min po każdej notce...) zmieściła się pod nim [...] Kod nie wie, ile ma czasu" — jest juz nieprawdziwa: run.py:168-193 `rytm()` losuje przerwe PRZED snem i oddaje ja do `zostal_czas(na_co, przerwa)` (run.py:141-165, parametr `potrzeba_s`), a budzet czasu stoi w config.py:1206 `LIMIT_CZASU_PRZEBIEGU_S = 9000` i jest porownywany z plikiem uslugi przez tests/test_jednostki_systemd.py:66.

**Dlaczego to wada:** Dwie szkody naraz. Po pierwsze, odtwarzajacy przepisze do config.ODSTEPY wartosci 10-25 min po notce — bo dokument podaje je jako fakt o dzialajacym systemie — i odtworzy rytm, ktory wlasciciel rozpoznal na profilu jako ksztalt przebiegu, a nie czlowieka. Po drugie, dokument gubi kontrakt "TimeoutStartSec == config.LIMIT_CZASU_PRZEBIEGU_S, pilnowany testem", ktory jest jedyna rzecza chroniaca przed rozjazdem, ktory zabil dwa przebiegi — i zamiast tego zostawia czytelnika z twierdzeniem, ze obrony nie ma zadnej.

**Plan naprawy:** W wierszu 877 zacytowac AKTUALNY komentarz z nia-agent.service (ten o 45-90 minutach i o `run.rytm`), a stary przypis pokazac jawnie jako wycofany. Wiersz 890 przepisac na NAPRAWIONE: opisac `run.rytm` + `zostal_czas(potrzeba_s)` + `LIMIT_CZASU_PRZEBIEGU_S`/`ZAPAS_CZASU_S` i test, ktory porownuje je z plikiem uslugi. Poprawic liczbe 10-25 min na 45-90 min z odsylaczem do config.py:1230.

---

### 32. Prog kontroli koszt() podany tylko dzienny — kod ma tez galaz miesieczna przy 75% MONTHLY_LIMIT_USD

**Powaga:** powazna   **Plik:** `dokumentacja-zrodla\rozdzial_dane.md:998`

**Dowod:** Dokument, wiersz 998: "| `koszt()` | `db.spent_usd(dziś)` | 90% × $5,00 = $4,50 |" — i tylko tyle; wiersz 617 powtarza to samo zawezenie: "`alarm.koszt()` bije przy `wydane > DAILY_LIMIT_USD * 0.9`, czyli przy $4,50". Kod alarm.py:268-277 ma druga galaz: "wydane_m = db.spent_usd(conn, dzis[:7]); if wydane_m > config.MONTHLY_LIMIT_USD * 0.75:" i liczy `zostalo_dni`/`zostalo_usd`; docstring (alarm.py:257) uzasadnia to wprost: "Prog miesieczny jest nizszy niz dzienny (75% wobec 90%), bo miesiac zostaje wyczerpany na wiele dni przed koncem i wtedy cisza jest dluga."

**Dlaczego to wada:** Tabela progow jest specyfikacja tej kontroli — odtwarzajacy zbuduje z niej wylacznie prog dzienny i straci ostrzezenie miesieczne. To akurat ta polowa, ktorej brak sam dokument piętnuje dwie sekcje wyzej ("WADA — limit miesięczny jest ustawiony poniżej realnego spalania [...] agent zamilkłby około 22. dnia miesiąca"): bez galezi miesiecznej pierwszym sygnalem wyczerpania budzetu jest BudgetExceeded w polowie przebiegu, np. po oplaceniu researchu i przed napisaniem artykulu. Dokument sam siebie unieważnia.

**Plan naprawy:** Rozbic wiersz 998 na dwa progi: "90% x DAILY_LIMIT_USD = $4,50 (doba) ORAZ 75% x MONTHLY_LIMIT_USD = $30,00 (miesiac, z wyliczeniem ile zostalo dni i dolarow)". W wierszu 617 dopisac, ze galaz miesieczna zostala dodana i dlaczego prog jest nizszy.

---

### 33. Cytowany `DLUGOSC_WG_GLEBOKOSCI` nie ma klucza THIN, a oparta na tym WADA jest juz nieprawdziwa

**Powaga:** powazna   **Plik:** `dokumentacja-zrodla/rozdzial_artykul.md:828`

**Dowod:** rozdzial_artykul.md:826-832: `DLUGOSC_WG_GLEBOKOSCI` (`config.py:452-458`): ```python DLUGOSC_WG_GLEBOKOSCI = { "RICH": {"cel": 1075, "min": 900, "max": 1250}, "SINGLE": {"cel": 650, "min": 480, "max": 820}, } ``` config.py:455-470 ma trzy klucze: "RICH": {"cel": 1075, "min": 900, "max": 1250}, "SINGLE": {"cel": 650, "min": 480, "max": 820}, "THIN": {"cel": 420, "min": 300, "max": 560}, Na tym cytacie stoja dwa twierdzenia, ktore juz nie sa prawda — rozdzial_artykul.md:919: "**WADA — `THIN` dostaje dlugosc `RICH`.** `DLUGOSC_WG_GLEBOKOSCI` nie ma klucza `\"THIN\"`, a `dlugosc_dla` robi `.get(..., DLUGOSC_WG_GLEBOKOSCI[\"RICH\"])`" oraz pozycja 7 na liscie wad, rozdzial_artykul.md:1323. Tymczasem config.py:485-486: `return DLUGOSC_WG_GLEBOKOSCI.get((glebokosc or "").upper(), DLUGOSC_WG_GLEBOKOSCI["SINGLE"])` — galaz domyslna to SINGLE, nie RICH.

**Dlaczego to wada:** To nie jest nieaktualne zdanie w komentarzu, tylko doslowny wycinek kodu podany jako obowiazujacy plus dwa miejsca, ktore nakazuja go naprawic. Odtwarzajacy przepisze slownik bez THIN i galaz domyslna RICH — czyli sam wprowadzi wade, ktora dokument kaze mu naprawiac. Skutek jest zmierzony i opisany w config.py:465-469: temat o materiale na 300 slow dostaje cel 1075 i wypelnia go tym samym mechanizmem opisanym trzy razy.

**Plan naprawy:** Uzupelnic blok w rozdzial_artykul.md:828-832 o klucz `THIN` i poprawic odsylacz na `config.py:455-470`. Skreslic WADE w rozdzial_artykul.md:919 i pozycje 7 w rozdzial_artykul.md:1323 albo przeniesc je do sekcji VIII.3 ("wady naprawione") z data 2026-08-23. Poprawic zdanie o galezi domyslnej na SINGLE.

---

### 34. WADA "regula roznorodnosci domen liczona i wyrzucana" zaprzecza sekcji VII i zalacznikowi A

**Powaga:** powazna   **Plik:** `dokumentacja-zrodla/rozdzial_artykul.md:453`

**Dowod:** rozdzial_artykul.md:453: "**WADA — regula roznorodnosci domen jest liczona i wyrzucana.** `run.py:725` robi `recent = db.recent_domains(...)` (...) W calym ciele funkcji `recent_domains` **nie wystepuje ani razu**", powtorzone jako pozycja 4 w rozdzial_artykul.md:1320 i jeszcze raz w rozdzial_dane.md:133. stages.py:1882-1885 (i ten sam kod stoi doslownie w sekcji VII pod <!--KOD:stages.discovery-->, kod.md:285): ostatnie_domeny=(", ".join( d for d in (recent_domains or [])[:15] if d and d.strip() == d and " " not in d ) or "(none yet - this is the first article of this account)"), prompts/dyskoveria.md:28-31, wydrukowany w calosci w ZALACZNIKU A, ma juz punkt 7: "These hosts already carried the sources of our recent articles: {ostatnie_domeny}". Wywolanie jest w run.py:800 i 803, nie 725.

**Dlaczego to wada:** Dokument w trzech miejscach nazywa dzialajaca funkcje martwa, a w dwoch innych (sekcja VII i zalacznik A) pokazuje jej dzialajacy kod i tresc promptu. Kto czyta rozdzial III jako zrodlo prawdy, albo usunie `ostatnie_domeny` jako rzekome rusztowanie, albo napisze "naprawe" czegos, co jest juz naprawione. Komentarz w stages.py:1869-1881 tlumaczy przy tym, ze to swiadoma PREFERENCJA, a nie bramka — czyli wiedza, ktora rozdzial III zaprzecza.

**Plan naprawy:** Skreslic WADE w rozdzial_artykul.md:453, pozycje 4 w rozdzial_artykul.md:1320 i akapit w rozdzial_dane.md:133; w ich miejsce opisac obecny mechanizm (preferencja, nie zakaz; 15 domen; sformulowanie zakazujace nawyku, a nie nakazujace pozycji) i poprawic numer linii wywolania z `run.py:725` na `run.py:800`.

---

### 35. WADA o sekcji [Install] w nia-agent.service opisuje ryzyko, ktorego juz nie ma

**Powaga:** powazna   **Plik:** `dokumentacja-zrodla/rozdzial_dane.md:919`

**Dowod:** rozdzial_dane.md:919: "**WADA — `nia-agent.service` ma sekcje `[Install]`.** Zawiera `WantedBy=multi-user.target` (...) `systemctl enable nia-agent.service` (...) sprawi, ze **platny, publikujacy przebieg wystartuje przy kazdym starcie systemu**". systemd/nia-agent.service nie ma juz zadnej sekcji `[Install]`; plik konczy sie komentarzem w wierszach 34-40: "# BEZ [Install]. Ta usluga ma jedno wejscie: nia-agent.timer. (...) Bez tej sekcji usluga jest `static`: zegar ja odpala, `enable` nie ma czego wlaczyc. Tak samo jak nia-alarm i nia-artykul."

**Dlaczego to wada:** To najbardziej operacyjna WADA w calym dokumencie — mowi wprost, ze jedno polecenie moze uruchomic platny, publikujacy przebieg przy kazdym boocie. Kto odtwarza wdrozenie z tego rozdzialu, albo doda `[Install]` wzorujac sie na opisie (bo dokument twierdzi, ze tam jest), albo pojdzie sprawdzic nieistniejacy problem na serwerze. Obie drogi kosztuja, a jedna z nich realnie wprowadza opisane zagrozenie.

**Plan naprawy:** Zamienic WADE na opis stanu docelowego: `nia-agent.service` jest `static`, `[Install]` swiadomie usuniete, uzasadnienie w komentarzu w wierszach 34-40 pliku jednostki. Jesli warto zachowac historie, przeniesc pozycje do sekcji VIII.3 "wady naprawione" z data 2026-08-23.

---

### 36. `stages.odczekaj` opisany pod zla linia, ze stara sygnatura i starym cialem

**Powaga:** powazna   **Plik:** `dokumentacja-zrodla/rozdzial_dzien.md:199`

**Dowod:** rozdzial_dzien.md:199-206: "- `stages.odczekaj(co)` (`stages.py:586`) — po odpowiedzi, notce, komentarzu, obserwacji i subskrypcji", a pod tym blok: dol, gora = config.ODSTEPY.get(co, config.ODSTEP_MIEDZY_DZIALANIAMI) ile = random.uniform(dol, gora) print(f" (przerwa {ile / 60:.1f} min przed kolejnym dzialaniem)", flush=True) time.sleep(ile) W stages.py pod linia 586 stoi dzis `def losuj_odstep(co: str = "") -> float:`, a `odczekaj` zaczyna sie w linii 602 i ma inna sygnature — `def odczekaj(co: str = "", ile: float | None = None) -> None:` — oraz inne cialo: `ile = losuj_odstep(co) if ile is None else float(ile)`. Docstring `losuj_odstep` (stages.py:587-595) tlumaczy podzial: "Rozdzielone, bo wywolujacy musi znac dlugosc przerwy ZANIM w nia wejdzie. Przebieg 28 zginal dokladnie na tym".

**Dlaczego to wada:** Odsylacz prowadzi do innej funkcji niz nazywa, a cytowane cialo nie pokazuje parametru `ile`, ktory jest calym sensem rozdzielenia. Odtwarzajacy napisze jedno `odczekaj`, ktore losuje i natychmiast zasypia — czyli usunie mechanizm, dzieki ktoremu `run.rytm` moze w ogole zapytac, czy przerwa sie zmiesci. To ta sama awaria, ktora zabila przebieg 28.

**Plan naprawy:** Rozbic ten punkt na dwa: `stages.losuj_odstep` (stages.py:586) i `stages.odczekaj(co, ile=None)` (stages.py:602), podac obecne ciala i wskazac, ze `run.rytm` uzywa ich parami. Obie funkcje sa juz w sekcji VII (kod.md:611 i 638) — najprosciej odeslac do znacznikow zamiast przepisywac.

---

### 37. Odsylacze `plik.py:linia` w rozdzialach analitycznych rozjechaly sie masowo — 32 z 50 wskazuja zle miejsce

**Powaga:** powazna   **Plik:** `dokumentacja-zrodla/rozdzial_artykul.md:20`

**Dowod:** Sprawdzilem maszynowo kazdy odsylacz w postaci "`nazwa` (`modul.py:NNN`)" w rozdzialach analitycznych i porownalem z `ast`: trafnych 18, blednych 32. Przyklady z tabeli etapow w rozdzial_artykul.md:20-27: `scout` (stages.py:2036) — naprawde 2064; `feasibility` (stages.py:1905) — 1940; `discovery` (stages.py:1835) — 1852; `fetch` (stages.py:1695) — 1712; `classify` (stages.py:1569) — 1586; `synthesis` (stages.py:1518) — 1535; `warto_pisac` (stages.py:2429) — 2475; `bibliotekarz` (stages.py:2288) — 2334. Dalej: `_precedens_ok` (stages.py:2771) — 2842; `pick_topic` (stages.py:1929) — 1964; `timeout_for` (config.py:1330) — 1369; `zmiesci_sie` (run.py:162) — 197; `wystaw_artykul` (browser.py:1495) — 1486. Stale tak samo: `EFFORT` (config.py:574-581) — naprawde 615; `ODSTEPY` (config.py:1178) — 1219; `RUCH_KONCOWY_MIX` (config.py:1418) — 1460; `_ZAPRZECZENIE` (stages.py:2414) — 2460.

**Dlaczego to wada:** Tabela etapow w rozdziale III to glowna mapa nawigacyjna dokumentu i osiem z osmiu jej odsylaczy prowadzi w zle miejsce, srednio 20-70 wierszy obok. Kto sprawdza dokument przy kodzie, laduje w srodku innej funkcji, uzna wpis za nieaktualny i przestanie ufac reszcie odsylaczy — takze tym 18, ktore sa poprawne. Rozjazd jest cichy: nic sie nie wywala, po prostu numer wskazuje nie to.

**Plan naprawy:** Odsylacze do funkcji i stalych sa wyprowadzalne z `ast` — dolozyc do `dokumentacja-zrodla/sklej.py` krok, ktory albo je generuje, albo (taniej) waliduje przy skladaniu: przejsc regexem po `rozdzial_*.md`, porownac kazde `nazwa (modul.py:NNN)` z rzeczywista linia definicji i zglosic rozbiezne przez `ostrzez()`. To ten sam mechanizm, ktorym `sklej.py` juz pilnuje `KOD_DOSLOWNIE` i spisu `MODULY`, wiec kod na to jest.

---

### 38. Naglowek gates.py mowi "Cztery bramki, ktore blokuja", a zadna nie blokuje — dokument cytuje tylko drugi akapit tego samego docstringa

**Powaga:** powazna   **Plik:** `gates.py:1`

**Dowod:** gates.py:1-5: "Cztery bramki, ktore blokuja. Reszta to notatki. / Wybor wlasciciela z 2026-08-15. Kazda z tych czterech ma udokumentowane trafienie na starym agencie; wszystko inne (styl, tytul, brief, dlugosc, mysliniki) niczego nie zlapalo, wiec jest zapisywane i NIE zatrzymuje artykulu." — a gates.py:464-472 to `def verdict(findings): """Artykul powstaje ZAWSZE...""" return "SAVED", None`. Dokument (rozdzial_bramki.md:1 / scalony 3611) otwiera rozdzial naglowkiem "### 1. Zasada nadrzedna: nic nie blokuje, wszystko zglasza", a w wierszu 270 (scalony 3880) pisze "Naglowek modulu wyjasnia, dlaczego podlogi porownuja z korpusem..." i cytuje wylacznie DRUGI akapit tego docstringa, pomijajac pierwsze zdanie. Potwierdza to tez spis rol w sklej.py: ("gates.py", "bramki jakosci; zadna nie blokuje").

**Dlaczego to wada:** Ktos czytajacy gates.py od gory dowiaduje sie, ze cztery bramki zatrzymuja artykul, i zaczyna szukac listy tych czterech — ktorej w module nie ma, bo jedyna funkcja werdyktu ignoruje wejscie. Dokument, ktory w tym samym rozdziale wypisuje kilkanascie drobniejszych WAD (martwa stala WYMAGANE_ZLAMANE_PRZEKONANIE, martwy sprawdz_fakty, kwadratowa praca w powtorzona_forma), akurat tej sprzecznosci nie odnotowuje i cytuje docstring w sposob, ktory ja ukrywa. To jest dokladnie ta klasa wady, ktora agent sciga u siebie: martwa obietnica czytana jak gwarancja.

**Plan naprawy:** Przepisac pierwszy akapit docstringa gates.py na zgodny z verdict() (np. "Bramki jakosci. Zadna nie blokuje artykulu — decyzja wlasciciela z 2026-08-15, patrz verdict().") i zachowac uzasadnienie ekonomiczne przy samej funkcji. W rozdzial_bramki.md:270 albo zacytowac caly naglowek, albo dopisac zdanie, ze naglowek modulu byl nieaktualny i zostal poprawiony.

---

### 39. "Siedem plikow w tests/platne/" — jest ich dziewiec, a poza tabela kosztow zostal najdrozszy test korpusu Federal Register

**Powaga:** powazna   **Plik:** `dokumentacja-zrodla/rozdzial_bramki.md:1034`

**Dowod:** Dokument (rozdzial_bramki.md:1034, scalony 4644): "Siedem plikow w `tests/platne/` robi prawdziwe wywolania API i **nie** jest lapanych przez `test_*.py` z katalogu wyzej", po czym tabela wymienia test_integracja, test_notki_ab, test_notki_szeroki_material, test_style, test_notki_z_banku, test_warto, test_bibliotekarz. W katalogu jest DZIEWIEC plikow test_*.py — brakuje `test_fedreg_pelna_sciezka.py` (wlasny docstring: "PLATNY: jedno wywolanie flasha na preambule. Wejscie bywa duze, wiec to jest najdrozsza czesc tego korpusu", wola `stages.kandydaci_z_fedreg(conn, run_id, d)`) i `test_slepa_ocena_notek.py` (wola `stages.note(conn, run_id, "CIEKAWOSTKA", dowod, ...)`, czyli pisarza notek plus platna `zweryfikuj`). Dodatkowo `porownanie_v1_v2.py` wola `llm.call` bezposrednio. Ta sama luka siedzi w tests/platne/PRZECZYTAJ.md, z ktorego tabela zostala przepisana.

**Dlaczego to wada:** Ta tabela nie jest inwentarzem, tylko lista ostrzegawcza "co kosztuje pieniadze" — powstala dokladnie po tym, jak ktos puscil wszystkie testy jedna petla i zaplacil. Kto zaufa jej kompletnosci, uruchomi test_fedreg_pelna_sciezka.py w przekonaniu, ze skoro nie ma go w tabeli kosztow, to jest darmowy — a on placi za jedno wywolanie modelu na kazdy gesty dokument z korpusu i nie ma podanego rzedu wielkosci, wiec nie da sie go nawet oszacowac przed uruchomieniem.

**Plan naprawy:** Dopisac dwa wiersze do tabeli w rozdzial_bramki.md:1036-1044 (test_fedreg_pelna_sciezka.py — pelna sciezka Federal Register, jedno wywolanie flasha na preambule, koszt rosnie z ILE_DOKUMENTOW; test_slepa_ocena_notek.py — te same fakty dwoma pisarzami plus weryfikacja), poprawic "Siedem" na "Dziewiec", wspomniec porownanie_v1_v2.py jako platny skrypt spoza konwencji test_*. Te same dwa wiersze dopisac do tests/platne/PRZECZYTAJ.md, bo to zrodlo bledu.

---

### 40. Tabela "Testy — wszystkie zestawy" pomija piec plikow testowych, a suma 945/5 jest juz nieprawdziwa

**Powaga:** powazna   **Plik:** `dokumentacja-zrodla/rozdzial_bramki.md:1011`

**Dowod:** Naglowek rozdzial_bramki.md:957 brzmi "### 8. Testy — wszystkie zestawy", tabela w wierszach 976-1010 ma 35 wierszy `| \`test_...\` |`, a wiersz 1011 (scalony 4621) podsumowuje: "| **razem** | **945 zdanych, 5 oblanych** | | |". W `agent-v2/tests/` jest dzis 40 plikow `test_*.py`. Nieobecne w tabeli: test_rytm_zegar.py (20 sie 22:31), test_jednostki_systemd.py (23 sie 05:43), test_cudze_dane.py (23 sie 06:18), test_obietnice_bez_pokrycia.py oraz test_dokumentacja_zywa.py (23 sie 06:48).

**Dlaczego to wada:** Rozdzial sam deklaruje kompletnosc ("wszystkie zestawy") i podaje sume jako liczbe do porownania po uruchomieniu katalogu. Odtwarzajacy zbuduje 35 plikow, puszcza katalog, dostanie inna liczbe asercji i inna liczbe oblanych — i nie bedzie mial jak rozstrzygnac, czy zepsul odtworzenie, czy dokument jest nieaktualny. Piec brakujacych testow pilnuje przy tym rzeczy, o ktorych rozdzial nie mowi nic: jednostek systemd, cudzych danych, obietnic bez pokrycia, zegara rytmu i zywotnosci samej dokumentacji.

**Plan naprawy:** Dopisac piec wierszy (plik, liczba wykonanych asercji, czego pilnuje, kolumna kontrdowodu) i przeliczyc sume przez jedno uruchomienie katalogu tests/. Trwalej: wygenerowac te tabele w sklej.py tak samo, jak generowany jest spis funkcji i stalych — recznie utrzymywana lista testow rozjedzie sie znowu przy nastepnym dopisanym pliku.

---

### 41. Zdanie otwierajace 0025 przypisane "bramce statystyk", ktora nie jest w stanie go zlapac — kod wskazuje FAKT_BEZ_POKRYCIA

**Powaga:** powazna   **Plik:** `dokumentacja-zrodla/rozdzial_bramki.md:496`

**Dowod:** Dokument (rozdzial_bramki.md:496, scalony 4106), w sekcji 4.9 ZAKAZANE_OTWARCIE: "0025 zaczyna sie od `\"Turn over almost any plastic container\"` — i to samo zdanie zglosila **niezaleznie** bramka statystyk, bo `\"almost any\"` bylo przesada nie do obrony". Komentarz w gates.py:72-74 mowi co innego: "0025 zaczyna sie od 'Turn over almost any plastic container' — i to samo zdanie zglosila niezaleznie bramka FAKT_BEZ_POKRYCIA, bo 'almost any' bylo przesada nie do obrony". Sprawdzone uruchomieniem: `gates.statystyki_bez_zrodla('Turn over almost any plastic container and you will find the chasing arrows.')` zwraca `[]`, bo gates.py:224 wymaga koniunkcji `NIBY_ZRODLO.search(zdanie) and DIGITS.search(zdanie)`, a w tym zdaniu nie ma zadnej cyfry.

**Dlaczego to wada:** To jest rozdzial o tym, ktora bramka co lapie, a tu wskazuje bramke, ktora tego zdania zlapac NIE MOZE — prawdziwym zrodlem uwagi jest FAKT_BEZ_POKRYCIA, czyli recenzja zdanie po zdaniu (run.py:1084), a wiec inny rodzaj kontroli: platny model, nie darmowa podloga deterministyczna. Odtwarzajacy STATYSTYKA_BEZ_ZRODLA wedlug tego zdania dorobi do niej wykrywanie przesadnych kwantyfikatorow ("almost any", "virtually every"), czego w kodzie nie ma i nie mialo byc, a przy okazji uzna, ze podlogi deterministyczne oceniaja retoryke bez liczb.

**Plan naprawy:** W rozdzial_bramki.md:496 zamienic "bramka statystyk" na "bramka FAKT_BEZ_POKRYCIA (recenzja zdanie po zdaniu)", zgodnie z komentarzem w gates.py:74, i dodac pol zdania, ze STATYSTYKA_BEZ_ZRODLA milczy na tym zdaniu z braku liczby — to jest wlasnie dowod na celowosc koniunkcji opisanej w 4.10.

---

### 42. `confidence: 0.0-1.0` to dokladnie zakazana samoocena — trzy inne prompty jej zabraniaja, a odsiew nadal ja zamawia i kod nadal nia sortuje

**Powaga:** powazna   **Plik:** `prompts/wykonalnosc.md:88`

**Dowod:** prompts/wykonalnosc.md:88 zamawia: `"feasible": true|false, "confidence": 0.0-1.0, "expected_primary_sources": <integer>`. Ten sam potok w prompts/skaut.md:405-408 mowi: "**Do not include scores.** Earlier versions of this brief asked for seven numbers between zero and one. Nothing ever read them, and self-assigned scores drift to the top of their range regardless of the thing being scored." prompts/warto_pisac.md:43-45: "Do not score. (...) Every such number comes back near full marks and tells nobody anything — we tried it, and every score was 1.0." prompts/bibliotekarz.md:38-40: "Do not score anything. Do not rank. (...) Numbers invented for those questions come back as a wall of high scores." A jednak stages.py:2035-2036 sortuje po `a.get("confidence", 0)` i `a.get("expected_primary_sources", 0)`, run.py:789 pokazuje to wlascicielowi jako `pewność={a.get('confidence')}`, a run.py:1108 wpisuje `float(verdict.get("confidence") or 0)` do uwag przy artykule.

**Dlaczego to wada:** To jest ta sama wada, ktora juz raz zabito w skaucie (siedem ocen 0-1, zawsze przy gornej granicy), tylko przeniesiona etap dalej i podpisana innym slowem. Samoocena "pewnosci" wystawiana na podstawie samego tytulu i pytania (patrz znalezisko 1) nie ma z czego sie roznicowac miedzy kandydatami. Wlasciciel czytajacy log widzi kolumne `pewność=` i uwage "pewność 0.95" w `.uwagi.md` i bierze je za pomiar, a to jest liczba, ktora model przypisal sam sobie. Ktos czytajacy prompty zobaczy trzy miejsca zakazujace ocen i jedno, ktore ich zada, i nie bedzie wiedzial, ktora zasada obowiazuje.

**Plan naprawy:** Albo wyrzucic `confidence` z prompts/wykonalnosc.md:88 i z krotki sortujacej w stages.py:2035 (a z run.py:789 i 1108 usunac slowo "pewność"), zastepujac je czyms sprawdzalnym — np. wymuszonym uporzadkowaniem ocen wzgledem siebie, jak w skaut.md:410-436. Albo, jesli ma zostac, dopisac w wykonalnosc.md jedno zdanie mowiace, DLACZEGO tu wyjatkowo wolno oceniac liczba, i objac to pole wykrywaczem z znaleziska 2 — inaczej za miesiac nikt nie odrozni wyjatku od przeoczenia.

---

### 43. Odwrotnosc tej samej wady: odsiew ma uporzadkowac oceny best-first (ranking odporny na wyrownanie), a `pick_topic` tej kolejnosci nie czyta

**Powaga:** powazna   **Plik:** `stages.py:2038`

**Dowod:** prompts/wykonalnosc.md:90-91 nakazuje: "Order the array best-first: RICH before SINGLE, and within each, most researchable first. THIN topics go last." stages.py:2038-2039 natychmiast to porzuca: `ranked = sorted((a for a in assessments if a.get("feasible")), key=kolejnosc, reverse=True)`, gdzie `kolejnosc` (2028-2036) sklada sie z osmiu wlasnych kluczy i ani jeden nie odwoluje sie do pozycji w tablicy. Efekt: kolejnosc modelu przezywa wylacznie jako uboczny skutek stabilnosci `sorted`, gdy wszystkie osiem kluczy sie zremisuje — nigdy jako odczytany sygnal.

**Dlaczego to wada:** To jest lustrzane odbicie problemu, ktory naprawiono w skaucie. Wymuszonego uporzadkowania wlasnej listy MODEL NIE MOZE WYROWNAC — musi kogos postawic przed kims — i wlasnie dlatego skaut.md:418 nazywa to jedynym odpornym sygnalem ("An absolute judgement can be equalised. A forced comparison cannot"). Tutaj mamy ten odporny sygnal zamowiony i wyrzucony, a jego miejsce w sortowaniu (klucze 6-8) zajmuja trzy pola bezwzgledne: `depth`, `confidence` i `expected_primary_sources`, czyli dokladnie te, ktore sie wyrownuja. Zamienilismy odporne na podatne i nikt tego nie odnotowal.

**Plan naprawy:** Albo dodac do krotki `kolejnosc` w stages.py:2028-2036 jawny klucz z pozycji oceny na liscie (np. `-assessments.index(a)`) i postawic go PRZED `confidence`/`expected_primary_sources`, albo usunac z wykonalnosc.md:90-91 nakaz porzadkowania, zeby nie placic za prace, ktorej nie czytamy. Pierwsze jest lepsze — to jedyny sygnal z tego etapu, ktorego model nie moze rozmyc.

---

### 44. `or "RICH"` w run.py omija zabezpieczenie, dla ktorego galaz domyslna w `dlugosc_dla` zmieniono na SINGLE

**Powaga:** powazna   **Plik:** `run.py:976`

**Dowod:** config.py:474-483, docstring `dlugosc_dla`: "Galaz domyslna to SINGLE, nie RICH. Nieznana glebokosc znaczy „nie wiem, ile tu jest materialu" — a na to uczciwa odpowiedzia jest forma srednia, nie najdluzsza. Domyslny RICH kazal pisarzowi zapelnic tysiac slow zawsze, gdy model oddal cokolwiek spoza slownika." A run.py:976 podstawia RICH jeszcze zanim ta funkcja zostanie wywolana: `glebokosc = str(verdict.get("depth") or "RICH").upper()`. To samo w stages.py:2034: `waga.get(str(a.get("depth", "RICH")).upper(), 1)`.

**Dlaczego to wada:** Zabezpieczenie dziala tylko w polowie przypadkow, ktore mialo pokryc. Gdy model odda `depth: "MEDIUM"` — wartosc spoza slownika — SINGLE lapie i artykul dostaje 650 slow, zgodnie z opisem. Ale gdy pole jest PUSTE albo w ogole go nie ma (a wlasnie tak wyglada wynik, gdy odsiew sie potknie), run.py podstawia RICH i pisarz dostaje cel 1075 slow z zakresem 900-1250. Czyli brak informacji o glebokosci daje NAJDLUZSZA forme — dokladnie to, przed czym config ostrzega dwa akapity wyzej, opisujac artykul o symbolu otwartego sloiczka. Kto czyta config.py, jest przekonany, ze ta pulapka jest zamknieta; kto czyta run.py, widzi, ze nie.

**Plan naprawy:** W run.py:976 zamienic na `glebokosc = str(verdict.get("depth") or "").upper()` i pozwolic `config.dlugosc_dla` obsluzyc pusty string swoja galezia domyslna (SINGLE). Analogicznie w stages.py:2034 zmienic `a.get("depth", "RICH")` na `a.get("depth", "SINGLE")`, zeby brak oceny nie wypychal tematu do gory kolejki.

---

### 45. Blok komentarzy placi za wybor celow, gdy przydzial komentarzy wynosi zero (jego dwaj sasiedzi maja te zapore)

**Powaga:** powazna   **Plik:** `run.py:468`

**Dowod:** run.py:452-469 — blok zaczyna sie od razu od roboty: `pula = [x for x in kanal.szukaj_nowych() + kanal.posty_z_kanalu() ...]`, potem `cele = stages.wybierz_cele(conn, run_id, unikalne)` (PLATNE wywolanie modelu, `llm.call("cele", ...)` w stages.py:688), a dopiero potem `for cel in cele[: na_teraz["komentarze"]]:`. Nie ma nigdzie `if not na_teraz["komentarze"]: return`. Dla porownania blizniaczy blok dyskusji ma ta zapore wprost: run.py:507-508 `if not na_teraz["komentarze"]:` / `return`, a blok notek run.py:413-416 `if not na_teraz["notki"]:` ... `return`. Zerowy przydzial jest zwyklym stanem, nie awaria: run.py:295-297 liczy `zostalo` odejmujac to, co juz dzis poszlo (`browser.z_dziennika_dzis`, browser.py:107 liczy komentarze przez caly dzien), przy `KOMENTARZE_DZIENNIE = (8, 12)` (config.py:1137) trzeci przebieg dnia zwykle zastaje zero; do tego run.py:320 `na_teraz["komentarze"] = zmiesci_sie("komentarz", ...)` potrafi zwrocic 0 (run.py:226 wypisuje wtedy „{ile} sie nie zmiesci, biore {mozliwe}"), a run.py:341-342 zeruje ten sam licznik poza oknem publikacji. `stages.wybierz_cele` nie ma tez zapory na pusta liste — przy `unikalne == []` buduje pusty `opis` i i tak wola model (stages.py:680-689).

**Dlaczego to wada:** Kazdy przebieg z wyczerpanym dziennym przydzialem komentarzy (a to zwykle trzeci przebieg doby) i kazdy przebieg, w ktorym filtry kanalu odsialy wszystko, placi za wywolanie „cele" i dodatkowo przeciaga przez przegladarke kilka zapytan wyszukiwarki Substacka (`kanal.szukaj_nowych`, kanal.py:214-239) oraz kanal czytelnika — po czym iteruje po pustym wycinku `cele[:0]` i nie robi NIC. Kogos, kto odtwarza bota z dokumentacji albo czyta kod, myli to podwojnie: widzi trzy bloki o tej samej budowie, z ktorych dwa pilnuja budzetu na wejsciu, i naturalnie zaklada, ze trzeci tez.

**Plan naprawy:** Na poczatku `def komentarze()` dopisac te sama zapore, co maja sasiedzi: `if not na_teraz["komentarze"]: return` (opcjonalnie z komunikatem jak w notkach). Dodatkowo w `stages.wybierz_cele` dac `if not posty: return []` przed `llm.call`, zeby pusta pula nigdy nie kosztowala.

---

### 46. Zwloka 0-40 min przed pierwsza notka jest odsypiana bez pytania, czy sie zmiesci — dokladnie ta wada, ktora naprawil `rytm`

**Powaga:** powazna   **Plik:** `run.py:425`

**Dowod:** run.py:421-425: `if wyslij:` / `ile = _r.uniform(*config.ZWLOKA_PRZED_NOTKAMI)` / `print(...)` / `time.sleep(ile)` — bez `zostal_czas`. `ZWLOKA_PRZED_NOTKAMI = (0, 2400) # 0-40 min` (config.py:1257). Tymczasem `rytm` opisuje te sama usterke jako naprawiona: run.py:178-181 „sen zaczynal sie BEZ pytania, czy sie zmiesci. `zostal_czas` mowilo tylko »czy zostala jakakolwiek sekunda«, wiec przepuszczalo dziewiecdziesieciominutowa przerwe przy dwudziestu minutach na zegarze", a `stages.losuj_odstep` (stages.py:586-594) istnieje po to, zeby „wywolujacy musial znac dlugosc przerwy ZANIM w nia wejdzie" — z przypisem o przebiegu 28 zabitym w srodku snu. Caly zegar przebiegu to `LIMIT_CZASU_PRZEBIEGU_S = 9000` minus `ZAPAS_CZASU_S = 900` (config.py:1218-1220), czyli 2h15, a na same notki przypada `UDZIAL_CZASU_NA_NOTKI = 0.60` (config.py:1268).

**Dlaczego to wada:** Ten sen zjada do 40 minut z 135 i nie jest z niczym skonfrontowany. Co gorsza, stoi PRZED `stages.notki_dnia`, czyli przed calym platnym pisaniem notek: agent moze przespac reszte zegara, a zaraz potem i tak zaplacic za notki, ktorych nie zdazy wystawic (patrz nastepne znalezisko). Jesli poprzedni blok `odpowiedzi` zabral duzo czasu, `zmiesci_sie` policzone na starcie przebiegu (run.py:318) jest juz nieaktualne, a ta zwloka nie ma zadnego zwiazku z zegarem. Czytelnik kodu widzi obok siebie `rytm`, ktore losuje-sprawdza-spi, i `time.sleep`, ktore tylko spi — i nie ma jak zgadnac, ze to niedopatrzenie, a nie decyzja.

**Plan naprawy:** Wylosowac zwloke, a przed `time.sleep` zapytac `if not zostal_czas("notki", ile + config.CZAS_DZIALANIA_S): return` — tak samo, jak robi to `rytm` (run.py:190-193). Alternatywnie przyciac zwloke do tego, co zostalo na zegarze po odjeciu czasu na pierwsza notke.

---

### 47. `notki_dnia` pisze i weryfikuje CALA paczke notek przed pierwszym sprawdzeniem zegara; niewystawiona notka przepada, choc istnieje bank na takie notki

**Powaga:** powazna   **Plik:** `run.py:426`

**Dowod:** run.py:426-429: `for n in stages.notki_dnia(conn, run_id, ile=na_teraz["notki"], od=juz.get("notki", 0)):` / `if not zostal_czas("notki"): return`. `notki_dnia` NIE jest generatorem — stages.py:1064-1071 deklaruje `-> list[dict[str, Any]]`, w petli robi `dzien.append(wynik)` (stages.py:1152) i konczy `return dzien` (stages.py:1153); w calym repozytorium nie ma ani jednego `yield`. Kazda notka to `stages.note` (model `"note": CLAUDE`, config.py:197) plus `zweryfikuj` z wyszukiwaniem (stages.py:918, model `factcheck`), a przy pustym zapasie jeszcze `znajdz_ciekawostki` z web_search (stages.py:1122, komentarz w stages.py:783 wycenia jedno takie szukanie na „$0,05 i 6-20 zapytan"). Odstep miedzy notkami to `"notka": (2700, 5400)` (config.py:1242), a `rytm` (run.py:435-436) potrafi po pierwszej notce zwrocic False i `return` — zostawiajac oplacona notke nr 2 w liscie, ktorej nikt juz nie odczyta. Bank, ktory mogl by ja przechowac, istnieje i jest przetestowany, ale nie wola go zadna linia produkcyjna: `dopisz_do_banku_notek`/`wez_z_banku_notek` (stages.py:2398, 2424) wystepuja poza definicjami wylacznie w tests/test_bank_notek.py.

**Dlaczego to wada:** Typowo `zmiesci_sie` przepuszcza dwie notki na przebieg, wiec placimy z gory za dwie, a po pierwszej losujemy przerwe 45-90 min — ktora przy koncowce zegara nie ma prawa sie zmiescic. Wtedy druga, w pelni oplacona notka (Opus + weryfikacja z wyszukiwaniem) jest po prostu wyrzucana: nie idzie na Substacka, nie trafia do banku notek, nie zostaje w bazie. To jest dokladnie klasa wady z zadania — platne wywolanie przed warunkiem, ktory je uniewaznia — i dla odtwarzajacego bota jest niewidoczna, bo z zewnatrz wyglada jak „przebieg wystawil jedna notke".

**Plan naprawy:** Dwie drogi, najlepiej obie: (a) przerobic `notki_dnia` na generator (`yield wynik` zamiast `dzien.append`), zeby kolejna notka byla pisana dopiero po tym, jak zegar i `rytm` przepuscza kolejne dzialanie; (b) w `run.py`, w miejscu gdzie `rytm` zwraca False, przed `return` wolac `stages.dopisz_do_banku_notek([...])` na niewykorzystanych kandydatach — bank juz istnieje i ma testy, brakuje tylko podlaczenia.

---

### 48. Kazda petla bloku wchodzi w platne dzialanie, majac na zegarze „jakakolwiek sekunde" — `potrzeba_s` jest podawane dopiero po zaplacie

**Powaga:** powazna   **Plik:** `run.py:382`

**Dowod:** Wszystkie strazniki na gorze petli wolaja `zostal_czas` BEZ drugiego argumentu: run.py:382 `if not zostal_czas("odpowiedzi")`, run.py:429 `zostal_czas("notki")`, run.py:470 `zostal_czas("komentarze")`, run.py:528 `zostal_czas("dyskusje")`. Sama funkcja mowi wprost, ze bez `potrzeba_s` odpowiada tylko na pytanie „czy zostalo cokolwiek": run.py:154-156 `zostalo = _KONIEC_CZASU - time.time()` / `if zostalo > potrzeba_s: return True` przy `potrzeba_s: float = 0.0` (run.py:141). Kolejnosc w bloku odpowiedzi: run.py:382 strażnik → run.py:384 `out = stages.reply_to(...)` (PLATNE, `for i in range(config.COMMENT_CANDIDATES)` = 3 wywolania, stages.py:357, `COMMENT_CANDIDATES = 3` w config.py:792) → dopiero run.py:401 `if not rytm("odpowiedz", "odpowiedzi", rytm_stanu): return`. Identycznie w komentarzach: run.py:481 `out = stages.comment_on(...)` przed run.py:487 `if not rytm("komentarz", "komentarze", rytm_stanu): return`, i w dyskusjach: run.py:530 przed run.py:539.

**Dlaczego to wada:** Parametr `potrzeba_s` zostal dodany wlasnie po to, by pytac „czy to sie zmiesci", ale trafil tylko do `rytm`, ktore stoi ZA platnym wywolaniem. Skutek: przy resztce zegara agent placi za komplet kandydatow (trzy warianty plus weryfikacja z wyszukiwaniem w `zweryfikuj`, stages.py:1477), po czym `rytm` losuje przerwe (`"odpowiedz": (120, 420)`, `"komentarz": (180, 480)`, config.py:1243-1244), stwierdza, ze sie nie miesci, i wychodzi z bloku — gotowy tekst laduje w koszu. Wada jest systemowa: dotyczy czterech blokow naraz, a dwa z nich (odpowiedzi, dyskusje) nie sa wcale przycinane przez `zmiesci_sie`, ktore laguje to czesciowo tylko dla notek i komentarzy (run.py:318-320).

**Plan naprawy:** W strażnikach na gorze petli podawac realne zapotrzebowanie, np. `zostal_czas("odpowiedzi", config.CZAS_DZIALANIA_S + sum(config.ODSTEPY["odpowiedz"]) / 2)` (analogicznie dla notek, komentarzy i dyskusji) — albo losowac przerwe przed platnym wywolaniem i przekazywac ja do `rytm`, zeby decyzja „nie zdaze" zapadala, zanim pojdzie zaplata.

---

### 49. W sciezce artykulu sesja jest sprawdzana dopiero przy publikacji — po calym oplaconym przebiegu — a jej brak zostawia wiersz przebiegu jako RUNNING

**Powaga:** powazna   **Plik:** `run.py:1136`

**Dowod:** run.py:1136-1140: `if args.wyslij:` / `import browser` / `print("\n-- publikacja --")` / `wynik = browser.wystaw_artykul(path, wyslij=True)`. Pierwsza rzecza, jaka robi `wystaw_artykul`, jest `wymagaj_sesji()` (browser.py:1491), ktore przy wygaslej sesji robi `raise SystemExit("Sesja Substacka wygasla...")` (browser.py:186-190); `podlacz_sie` (browser.py:344, 365) tez rzuca `SystemExit`. Tymczasem galaz artykulu lapie wylacznie `except Exception as exc:` (run.py:1146) — `SystemExit` nie jest podklasa `Exception`, wiec `db.finish_run(conn, run_id, "FAILED", ...)` sie nie wykona. Galaz `--dzien` zostala przed tym wprost zabezpieczona: run.py:738 `except BaseException as exc:` z komentarzem run.py:731-735 („Przerwany przebieg ma byc widoczny jako przerwany"). Tania kontrola istnieje i nic nie kosztuje — `browser.dni_do_wygasniecia()` (browser.py:154) czyta tylko plik sesji; rutyna dnia korzysta z niej na starcie (browser.py:621), sciezka artykulu nie.

**Dlaczego to wada:** Przy `--wyslij` i martwej sesji agent przechodzi caly lancuch (skaut, wykonalnosc, dyskoveria, pobieranie, klasyfikacja, synteza, pisarz, recenzja, forma, grafika — sufit `RUN_LIMIT_USD = 1.60`, config.py:380), zapisuje artykul na dysk i dopiero wtedy dowiaduje sie, ze nie ma jak go opublikowac. Nie ma tez sciezki „opublikuj zapisany artykul", wiec kolejna proba placi od nowa. Do tego proces konczy sie `SystemExit` poza `except Exception`, wiec wiersz w `runs` zostaje RUNNING i nie wypisuje sie `_summary` — czyli dokladnie ten stan, ktory `_sygnal_ma_zostawic_slad` (run.py:677-689) opisuje jako szkodliwy („wiersz wisial w bazie jako RUNNING az do kontroli zdrowia"). Kto odtwarza bota z dokumentacji, zobaczy w rutynie dnia zapore sesji na wejsciu i zalozy, ze artykul ma taka sama.

**Plan naprawy:** Zaraz po `odmow_publikacji_z_kopii(args.wyslij)` (run.py:723) dodac dla galezi artykulu kontrole `if args.wyslij: browser.wymagaj_sesji()` (albo tanie `dni_do_wygasniecia()` z jawnym komunikatem i `_done(conn, run_id, "sesja")`), zanim padnie pierwsze platne wywolanie. Niezaleznie od tego zamienic `except Exception` w run.py:1146 na `except BaseException` z ponownym `raise`, tak jak w galezi `--dzien`, zeby SystemExit z przegladarki tez zamykal przebieg jako FAILED.

---

### 50. Nieudane wywolanie ksieguje sie jako koszt ZERO, choc tokeny byly zuzyte i policzone przez dostawce — straz budzetu tego nie widzi

**Powaga:** powazna   **Plik:** `llm.py:482`

**Dowod:** Komentarz w llm.py:476-478 obiecuje: "Koszt nieudanego wywolania bywa nieznany. Zapisujemy \"nie wiadomo\" zamiast zgadywac kwote". Kod dwie linie nizej zapisuje NIE "nie wiadomo", tylko liczbe: `tokens_in=0, tokens_out=0, web_searches=0,` / `cost_usd=0.0, price_verified=0, ok=0,` (llm.py:481-482). A `db.spent_usd` — jedyne wejscie limitu dziennego i miesiecznego — sumuje slepo: "SELECT COALESCE(SUM(cost_usd), 0) AS total FROM calls WHERE at LIKE ?" (db.py:187), bez warunku na `ok` i bez spojrzenia na `price_verified`. Dla SQL-a 0.0 to nie "nie wiadomo", tylko "zero". Tokeny w chwili rzucenia wyjatku SA znane: w `_call_deepseek_responses` zmienna `usage = payload.get("usage", {})` powstaje w linii 265, a `raise Truncated(...)` leci w linii 287 — usage lezy w zasiegu i jest wyrzucany. Tak samo `_call_claude` rzuca Truncated w linii 177, majac `message.usage` pod reka, a `_call_deepseek` rzuca w linii 364, dwie linie przed `usage = payload.get("usage", {})` (linia 368). Dowod z produkcji: w data/agent-v2.db wszystkie 10 wierszy z ok=0 ma tokens_in=0, tokens_out=0, cost_usd=0.0, w tym id=148 'Truncated: odpowiedz ucieta na suficie 28764 tokenow dla etapu review' (28 764 tokenow wyjscia deepseek-v4-pro to co najmniej $0,057, poza szczytem) oraz piec wierszy discovery typu id=121 'DeepSeek wykonal 36 wyszukiwan i nie zwrocil tekstu' — przy sredniej udanej dyskoverii 152 740 tokenow wejscia i $0,0483 za sztuke.

**Dlaczego to wada:** Straz budzetu jest slepa dokladnie na najdrozszych awariach. `Truncated` znaczy "model wygenerowal PELNY sufit wyjscia" albo "zrobil 36 rund wyszukiwania, z ktorych kazda przeslala cala rozmowe od nowa" — to sa najdrozsze wywolania w calym potoku, a nie tanie. Dostawca je nalicza, my ksiegujemy zero. `_preflight` czyta `db.spent_usd` (llm.py:78-79) i przy DAILY_LIMIT_USD=5,00 puszcza dalej wywolania, na ktore realnie juz nie ma budzetu. Na dobie 2026-08-15 ($14,5271 zaksiegowane) ukryte jest co najmniej ~$0,3-0,6, i to jest podloga, nie sufit: im gorzej idzie, tym wieksza dziura, bo Truncated pojawia sie seriami (5 z 15 dyskoverii). Odtwarzajacy bota z dokumentacji przeczyta komentarz "zapisujemy nie wiadomo" i uzna, ze ksiegowosc jest szczera, podczas gdy jest zanizona. To jest ta sama klasa co cache_hit: pole z pozornie bezpieczna wartoscia domyslna, ktore wylacza kontrole.

**Plan naprawy:** Rozdzielic "koszt nieznany" od "koszt zero". Minimalnie: w `_call_claude`, `_call_deepseek` i `_call_deepseek_responses` przed `raise Truncated(...)` doczepic zuzycie do wyjatku (np. `exc.zuzycie = (tin, tout, searches)`), a w bloku except w `call` (llm.py:479-484) — gdy zuzycie jest znane — policzyc `_cost` normalnie i zapisac realny koszt przy ok=0. Gdy zuzycia naprawde nie ma (blad transportu przed odpowiedzia), zostawic 0.0, ale wtedy dopisac to wprost do `note`. Docelowo: kolumna `cost_usd` powinna dopuszczac NULL dla "nie wiadomo", a `spent_usd` liczyc osobno sume znana i liczbe wierszy o nieznanym koszcie, i wypisywac ostrzezenie, gdy tych drugich jest wiecej niz zero. Rownolegle poprawic komentarz llm.py:476-478, zeby nie obiecywal czegos, czego kod nie robi.

---

### 51. Licznik dzienny nie zna kluczy 'follow' i 'subskrypcje' — naprawa z 20 sierpnia opisana w run.py nigdy nie zadzialala

**Powaga:** powazna   **Plik:** `browser.py:107`

**Dowod:** run.py:288-297 opisuje naprawe: "OBSERWACJE I SUBSKRYPCJE TEZ, i to jest naprawa z 20 sierpnia. Bloki 3c i 3d braly `budzet[\"follow\"]` oraz `budzet[\"subskrypcje\"]` — czyli PELNY dzienny przydzial — w KAZDYM z trzech przebiegow. Realny wolumen wychodzil okolo trzykrotnosci konfiguracji: ~60-70 obserwacji miesiecznie zamiast 20-30 i ~27 subskrypcji zamiast 6-12", po czym: `zostalo = {k: max(0, budzet[k] - juz.get(k, 0)) for k in ("notki", "komentarze", "lajki", "restacki", "follow", "subskrypcje")}`. Ale `juz = browser.ile_dzis_wystawione()` (run.py:288), a ta funkcja zwraca `wynik = {"notki": 0, **z_dziennika_dzis()}` (browser.py:620), gdzie `z_dziennika_dzis` deklaruje TYLKO trzy klucze: `ile = {"komentarze": 0, "lajki": 0, "restacki": 0}` (browser.py:107) i mapuje tylko trzy rodzaje: `nazwa = {"komentarz": "komentarze", "polubienie": "lajki", "restack": "restacki"}` (browser.py:108-109). Ani "follow", ani "subskrypcje" tam nie ma, wiec `juz.get("follow", 0)` i `juz.get("subskrypcje", 0)` zwracaja 0 ZAWSZE. Rozjazd jest podwojny, bo do dziennika te dzialania ida pod jeszcze innymi nazwami: `_klik_na_profilu(handle, ("Follow", "Obserwuj"), "obserwacja", wyslij)` (browser.py:1117) i `..., "subskrypcja", ...` (browser.py:1122), zapisywane przez `zapisz_w_dzienniku(rodzaj, udane=wynik["zrobione"], komu=handle)` (browser.py:1096). Trzy slowniki, trzy rozne slowniki nazw.

**Dlaczego to wada:** Bloki 3c i 3d nie maja ZADNEGO innego hamulca — `obserwuj()` sprawdza wylacznie `if not na_teraz.get("follow"): return` (run.py:560), `subskrybuj()` wylacznie `if not na_teraz.get("subskrypcje"): return` (run.py:597). Skoro `zostalo` dla tych dwoch kluczy to za kazdym razem pelny dzienny budzet, to przy `na_teraz = round(v / zostalo_przebiegow)` (run.py:315-316) trzy przebiegi dnia biora kolejno B/3, B/2 i B — czyli ~1,83 dziennego przydzialu, a przy czterech posiedzeniach (`sesje_dnia` losuje 2, 3, 3 lub 4 — stages.py:565) jeszcze wiecej. Kazda nadmiarowa subskrypcja to poczta do skrzynki wlasciciela, czyli dokladnie ten skutek, ktory komentarz w run.py nazywa "nie pomylka kosmetyczna". Gorzej dla czytelnika dokumentacji i kodu: komentarz stwierdza, ze problem JEST naprawiony i podaje date, wiec nikt tam drugi raz nie zajrzy. Blad jest cichy, bo linia podsumowania (run.py:319-324) wypisuje tylko notki, komentarze i lajki — follow i subskrypcje nie pojawiaja sie w logu wcale.

**Plan naprawy:** W `z_dziennika_dzis` (browser.py:107-109) dopisac klucze i mapowanie: `ile = {"komentarze": 0, "lajki": 0, "restacki": 0, "follow": 0, "subskrypcje": 0}` oraz `nazwa["obserwacja"] = "follow"`, `nazwa["subskrypcja"] = "subskrypcje"` (nazwy rodzajow biora sie z browser.py:1117 i 1122 — nie zmieniac ich, bo dziennik na serwerze ma juz historyczne wpisy pod tymi etykietami). Dodatkowo dopisac follow i subskrypcje do linii podsumowania w run.py:319-324, zeby rozjazd byl widoczny w logu. Zabezpieczenie na przyszlosc: zamiast `juz.get(k, 0)` uzyc dostepu, ktory wybucha na nieznanym kluczu (albo jednorazowo sprawdzic `set(budzet) - set(juz)` i wypisac ostrzezenie) — cichy `.get` z zerem jest tu wlasnie tym, co ukrylo blad na trzy dni.

---

### 52. Gdy Substack nie odda JSON-a, licznik dzisiejszych notek po cichu zwraca zero — nie do odroznienia od prawdziwego zera

**Powaga:** powazna   **Plik:** `browser.py:626`

**Dowod:** `api_json` konczy sie: `try: return _json.loads(tekst)` / `except ValueError: return None` (browser.py:308-310) — bez ani jednego printa. `ile_dzis_wystawione` reaguje na to milczeniem: `profil = api_json(page, f"/api/v1/user/{PROFIL_HANDLE}/public_profile")` / `if not isinstance(profil, dict) or not profil.get("id"): return wynik` (browser.py:625-627), gdzie `wynik` to `{"notki": 0, ...}` z linii 620. Druga taka sciezka linijke nizej: `feed = api_json(page, f"/api/v1/reader/feed/profile/{profil['id']}") or {}` (browser.py:628) — `or {}` zamienia None na pusty slownik i petla nie wykonuje ani obrotu. Blok `except` PONIZEJ loguje (`print(f" (nie policzylem dzisiejszych: {type(exc).__name__})"` — browser.py:639), ale te dwie sciezki wychodza WCZESNIEJ i przez niego nie przechodza. Straznik sesji tego nie zatrzyma, bo `wymagaj_sesji` patrzy wylacznie na date wygasniecia ciasteczka w pliku (`dni = dni_do_wygasniecia()` — browser.py:179), a nie na to, czy Substack te sesje nadal honoruje. Sam plik dokumentuje, ze taka blokada jest realna: "Sprawdzone na serwerze: z centrum danych `fetch` z wnetrza strony wraca 403 ze strona wyzwania" (browser.py:284-285) i "[rozgrzewka] Cloudflare nie ustapil" (browser.py:270).

**Dlaczego to wada:** Docstring tej samej funkcji uzasadnia jej istnienie tak: "NOTKI liczymy u Substacka, bo rzeczywistosc jest lepszym zrodlem niz wlasna ksiegowosc: po restarcie albo przerwanym przebiegu ksiegowosc sie rozjezdza i agent wystawia dzienna norme drugi raz" (browser.py:608-610). Gdy Cloudflare pokaze wyzwanie albo sesja zostanie uniewazniona po stronie Substacka, funkcja mowi "dzis zero notek" zamiast "nie umiem zapytac" — i ochrona, dla ktorej powstala, znika bez sladu. run.py wypisuje wtedy `dzis juz: notki=0` (run.py:319), co wyglada identycznie jak uczciwy poczatek dnia. Skutek: caly dzienny przydzial notek idzie drugi raz, przy trzech przebiegach nawet trzy razy — czyli piec notek zamiast piecu na dobe staje sie pietnascie. Wystawianie normy w kolko to najostrzejszy sygnal automatu, jaki konto moze dac, a kara (cichy spadek zasiegu) jest dokladnie tego rodzaju, ktorego agent sam nigdy nie zauwazy.

**Plan naprawy:** Rozroznic "zero" od "nie wiem". W `api_json` zamienic nieme `return None` na `print` z pierwszymi ~80 znakami odpowiedzi (widac wtedy 'Just a moment' albo strone bledu) — funkcja nadal moze zwracac None. W `ile_dzis_wystawione` zamiast `return wynik` w linii 627 wypisac wyrazny komunikat i zwrocic sygnal nieznanej wartosci (np. `wynik["notki"] = None` albo osobne pole `wynik["policzone"] = False`); to samo dla `feed` w linii 628 — sprawdzic `isinstance(feed, dict)` zamiast maskowac `or {}`. W run.py, gdy licznik notek jest nieznany, przyjmowac zalozenie ostrozne (traktowac dzienna norme jako WYCZERPANA i nie nadawac), bo dublowanie notek jest nieodwracalne, a pominiecie jednego przebiegu nie kosztuje nic.

---

### 53. W sciezce artykulu SIGTERM i wygasla sesja Substacka omijaja `except Exception` — przebieg zostaje na zawsze w RUNNING, wbrew wlasnej dokumentacji

**Powaga:** powazna   **Plik:** `run.py:1146`

**Dowod:** Sciezka artykulu lapie tylko `except Exception as exc: db.finish_run(conn, run_id, "FAILED", stage, ...)` (run.py:1146-1147). Tymczasem handler sygnalu podnosi `raise KeyboardInterrupt(f"przerwany sygnalem {signal.Signals(numer).name}")` (run.py:692), a `KeyboardInterrupt` NIE jest podklasa `Exception`. Docstring tej samej funkcji obiecuje co innego: „Teraz sygnal podnosi wyjatek, wiec dziala ta sama sciezka co przy kazdej innej awarii: status FAILED i powod w notatce" (run.py:686-688). Galaz `--dzien` piec linii wyzej robi to poprawnie: `except BaseException as exc:` (run.py:738). Drugi, znacznie czestszy wyzwalacz: `browser.wystaw_artykul` wola `wymagaj_sesji()` (browser.py:1491) POZA swoim `try` (ktore zaczyna sie dopiero w browser.py:1501), a `wymagaj_sesji` przy wygaslej sesji robi `raise SystemExit(f"Sesja Substacka wygasła. ...")` (browser.py:187-190) — `SystemExit` tez jest BaseException. URUCHOMILEM TO: podstawilem `stages.scout` rzucajacy `KeyboardInterrupt("przerwany sygnalem SIGTERM")` i odpalilem `run.main()` z `sys.argv = ['run.py']`; wynik: `wyjatek wyszedl z main(): KeyboardInterrupt przerwany sygnalem SIGTERM` i `runs: [(1, 'RUNNING', 'start', None)]` — status RUNNING, etap 'start', notatka NULL. Test, ktory mial to pokrywac, sprawdza wylacznie galaz dzienna: tests/test_czas.py:94 ustawia `sys.argv = ['run.py', '--dzien']`, a potem twierdzi „przebieg zapisal sie po SIGTERM (nie zostal w RUNNING)" (test_czas.py:118). Wyzwalacz nie jest hipotetyczny: systemd/nia-artykul.service:14 ma `TimeoutStartSec=5400`, a sciezka artykulu nie ustawia w ogole `_KONIEC_CZASU` (jest ustawiany tylko w `dzien()`, run.py:262), wiec nie ma pojecia o tym suficie i nie potrafi skonczyc sama przed nim.

**Dlaczego to wada:** Przerwany przebieg artykulowy klamie o sobie w bazie przez ponad trzy godziny: `runs.status = 'RUNNING'`, `runs.stage = 'start'` (bo `finish_run` nigdy nie podmienil etapu), `runs.note = NULL`, `runs.cost_usd = 0` mimo realnie wydanych pieniedzy. `alarm.sprawdz_przebiegi_i_ostrzez` jawnie odsiewa takie wiersze (`WHERE status != 'RUNNING'`, alarm.py:145), wiec statystyka bledow milczy; jedyny sygnal to `alarm.zawieszone()` (alarm.py:203-219), ktory zamyka wiersz jako STALE dopiero po trzech godzinach i mowi tylko „przebieg wisial ponad trzy godziny" — bez slowa o prawdziwej przyczynie, ktora w przypadku wygaslej sesji zostala wypisana na stdout i przepadla. Przy wygaslej sesji dzieje sie to PO zaplaceniu za caly research, napisanie i grafike (run.py:1119-1134 wykonaly sie), czyli w najgorszym momencie. Odtwarzajacy bota czyta w JAK_ZBUDOWANY_JEST_BOT.md:1877-1882 wylacznie poprawna galaz `--dzien` z `except BaseException` i sekcje 1.4 („SIGTERM ma zostawić ślad"), wiec bedzie przekonany, ze obie sciezki sa zabezpieczone.

**Plan naprawy:** 1) Zamienic `except Exception as exc:` (run.py:1146) na `except BaseException as exc:` z `raise` na koncu bloku — dokladnie tym samym wzorcem, ktory juz dziala w galezi dziennej (run.py:738-743): najpierw `db.finish_run(..., "FAILED", stage, ...)`, potem `_summary`, potem `raise`. 2) W `browser.wystaw_artykul` przeniesc `wymagaj_sesji()` przed otwarcie Playwrighta ALBO zamienic w `wymagaj_sesji` `SystemExit` na zwykly `RuntimeError` (browser.py:181, 187) — `SystemExit` w bibliotece wolanej z petli przebiegu jest pulapka, a run.py i tak decyduje o kodzie wyjscia. 3) Rozszerzyc tests/test_czas.py o drugi podproces bez `--dzien`, z podstawionym `stages.scout`, i te sama asercje „zaden wiersz nie zostal w RUNNING" (test_czas.py:124).

---

### 54. Baza NIE WIE, czy artykul poszedl w swiat — publikacja jest odnotowana wylacznie w dwoch plikach, obu zapisywanych PO klikniecu Publish

**Powaga:** powazna   **Plik:** `stages.py:198`

**Dowod:** Wiersz artykulu powstaje ZANIM cokolwiek dotknie Substacka: `stages.save` robi `INSERT INTO articles (run_id, created_at, topic, title, body, evidence, status, blocked_by, notes)` i `conn.commit()` (stages.py:198-204), wolane z run.py:1119 — czyli 21 linii przed publikacja. Tabela nie ma ani jednej kolumny o publikacji: schemat to `status TEXT NOT NULL, -- SAVED / BLOCKED` i `blocked_by`, `notes` (db.py:63-65), a `gates.verdict` zwraca zawsze `return "SAVED", None` (gates.py:472). `browser.wystaw_artykul` NIE aktualizuje tego wiersza — po `publikuj.click()` (browser.py:1549) i `wynik["wyslane"] = potwierdz_artykul(page, artykul["tytul"])` (browser.py:1551) zapisuje tylko `zapisz_w_dzienniku("artykul", udane=wynik["wyslane"], tytul=...)` (browser.py:1552-1553) do pliku JSONL, ktory jawnie polyka wlasne bledy (`except Exception: pass`, browser.py:82-83), oraz — wylacznie gdy `if wynik["wyslane"]:` (browser.py:1556) — `stages.zapisz_do_promocji(adres, ...)` (browser.py:1569) do `data/promocja.json`. Caly ten fragment siedzi w `try`, ktorego handlerem jest `except Exception as exc: wynik["blad"] = ...` (browser.py:1573).

**Dlaczego to wada:** Odpowiedz na najwazniejsze pytanie brzmi: TAK, w obie strony. (A) ARTYKUL ZYJE, ZERO SLADU: miedzy `publikuj.click()` (browser.py:1549) a `zapisz_do_promocji` (browser.py:1569) jest ~15 s czekania plus dwa zapytania do API Substacka; SIGTERM (znalezisko wyzej) albo padniecie sesji przegladarki w tym oknie zostawia artykul opublikowany, a agent nie wie o tym NIGDZIE. To samo dzieje sie bez zadnej awarii, gdy `potwierdz_artykul` da falszywy negatyw — pyta `/api/v1/posts?limit=5` (browser.py:1479) tylko 15 sekund po klikniecu wysylki do wszystkich subskrybentow. Skutek jest wymierny, nie kosmetyczny: artykul nie trafia do `promocja.json`, wiec NIGDY nie dostanie swoich notek promujacych (`artykul_do_promocji` czyta wylacznie ten plik, stages.py:950-989), a `run.py` wypisze „NIE POSZEDŁ" (run.py:1145) o tekscie, ktory wlasnie poszedl mailem do wszystkich. (B) ODWROTNIE: gdy publikacja padnie (wygasla sesja, brak przycisku „Kontynuuj" — `raise RuntimeError("nie znalazłem przycisku przejścia do ustawień")`, browser.py:1525), wiersz w `articles` ma `status='SAVED'` — BIT W BIT TAKI SAM jak przy udanej publikacji. Nie ma zapytania SQL, ktore odroznia artykul opublikowany od lezacego w szufladzie, i nie ma zadnej sciezki, ktora by go dopublikowala: `wystaw_artykul` ma dokladnie jednego wolajacego (run.py:1140), a nastepny wtorkowy przebieg zaczyna od nowego scouta. Zaplacony i napisany artykul po prostu przepada na dysku.

**Plan naprawy:** 1) Dopisac do `articles` kolumny `published_at TEXT` i `published_url TEXT` przez istniejacy mechanizm `NOWE_KOLUMNY` w db.py:105-107 (dokladnie tak, jak dopisano `calls.cache_hit`). 2) `stages.save` ma zwracac takze `article_id` (`cur.lastrowid`), a run.py po `browser.wystaw_artykul` (run.py:1140) ma zrobic UPDATE tego wiersza na podstawie `wynik["wyslane"]` i adresu z `potwierdz_adres_artykulu`. 3) Zamknac okno „opublikowane, ale niezapisane": w `wystaw_artykul` zapisac znacznik proby ZARAZ PO `publikuj.click()` (browser.py:1549), przed `page.wait_for_timeout(15000)` — wtedy nawet SIGTERM w oknie potwierdzania zostawia w bazie „kliknieto publikacje, potwierdzenie nieznane", co jest informacja, a cisza nie jest. 4) Na starcie sciezki artykulu sprawdzic, czy w `articles` nie ma wiersza bez `published_at` z ostatnich N dni, i albo dokonczyc jego publikacje (`potwierdz_artykul` i tak broni przed dublem), albo glosno o nim powiedziec zamiast pisac kolejny.

---

### 55. Straz budzetu przed artykulem pyta TYLKO o sufit miesieczny — limit dzienny i sufit przebiegu nadal zabijaja artykul po oplaconym researchu

**Powaga:** powazna   **Plik:** `run.py:760`

**Dowod:** Komentarz zapowiada zamkniecie calej klasy: „NIE ZACZYNAJ TEGO, CZEGO NIE SKONCZYSZ (...) artykul mogl paść w dowolnym miejscu: po oplaconym researchu i przed napisaniem, albo po napisaniu i przed recenzja. Pieniadze wydane, artykulu nie ma. Skoro znamy koszt calego przebiegu (RUN_LIMIT_USD), umiemy o to zapytac zawczasu" (run.py:754-759). Kod pyta o jedna trzecia: `_m = _dt.now(_tz.utc).strftime("%Y-%m")` / `_zostalo = config.MONTHLY_LIMIT_USD - db.spent_usd(conn, _m)` / `if _zostalo < config.RUN_LIMIT_USD:` (run.py:763-765). Tymczasem `_preflight` egzekwuje TRZY sufity przed KAZDYM wywolaniem: sufit przebiegu `if float(row["s"]) >= config.RUN_LIMIT_USD: raise BudgetExceeded(...)` (llm.py:67-71), dzienny `if spent_today >= config.DAILY_LIMIT_USD:` (llm.py:80-83) i dopiero potem miesieczny (llm.py:84-87). Obie niesprawdzone granice sa realnie osiagalne — wlasna dokumentacja projektu: „19 sierpnia zabrakło 67 centów do limitu" dziennego przy `DAILY_LIMIT_USD = 5.00` (JAK_ZBUDOWANY_JEST_BOT.md:5625, config.py:367), a komentarz przy samym suficie przebiegu mowi „Przebieg 16 kosztował $1,92" przy `RUN_LIMIT_USD = 1.60` (config.py:372-373, 380). Gdy ktorykolwiek z nich trafi na etapie `write`, ratunek nie dziala: handler robi `config.MODEL_FOR["write"] = config.CLAUDE` i `draft = stages.write(conn, run_id, card, glebokosc)` (run.py:989-990), a to wywolanie wchodzi w `_preflight` i dostaje IDENTYCZNY `BudgetExceeded` — `llm.przejsciowy` klasyfikuje go jawnie jako trwaly: `if isinstance(exc, (BudgetExceeded, PreflightFailed, Truncated)): return False` (llm.py:395-396). Wyjatek leci do `except Exception` (run.py:1146) i przebieg konczy sie FAILED bez artykulu — mimo obietnicy piec ekranow wyzej: „Od tego miejsca artykuł MUSI powstać. Temat jest wybrany, research zrobiony i opłacony — żaden dalszy etap nie ma prawa zabić przebiegu" (run.py:889-890).

**Dlaczego to wada:** Dokladnie ten scenariusz, ktory straz miala wykluczyc, zostaje otwarty w dwoch z trzech przypadkow — i to w tych czestszych, bo sufit dzienny i sufit przebiegu sa o rzad wielkosci blizej niz miesieczny. Kosztuje to caly research (dokumentacja notuje przebiegi FAILED z $0,5898 i $0,3855 zaplaconymi bez artykulu, JAK_ZBUDOWANY_JEST_BOT.md:5614-5615) i nie zostawia nic poza wierszem FAILED. Wtorek to jedyny dzien artykulu (nia-artykul.timer:15), wiec przepada tygodniowy produkt. Odtwarzajacy bota z tego kodu przeczyta komentarz w run.py:754-759 i uzna, ze problem „zaplacone, artykulu nie ma" jest rozwiazany — nie jest. Dodatkowo `alarm.koszt()` opisuje ten sam mechanizm we wlasnym docstringu („Pierwszym sygnalem bylby wiec agent padajacy w polowie przebiegu — w najgorszym razie po oplaceniu researchu i przed napisaniem artykulu", alarm.py:251-254), ale jego prog dzienny to 90% i chodzi raz na dobe o 07:00 UTC (nia-alarm.timer:5), czyli po fakcie.

**Plan naprawy:** Rozszerzyc straz w run.py:760-769 o dwa brakujace pytania, przed `stage = "scout"`: (a) `db.spent_usd(conn, dzis)` wobec `config.DAILY_LIMIT_USD - config.RUN_LIMIT_USD`, (b) — to najprostsze i najwazniejsze — sprawdzic, czy `config.RUN_LIMIT_USD` w ogole pokrywa realny koszt pelnej sciezki (dokumentacja podaje $1,30-1,41 przy suficie $1,60, a przebieg 16 kosztowal $1,92), bo przy tak waskim marginesie sufit przebiegu jest de facto losowa bramka posrodku potoku. Niezaleznie od tego: w handlerze `write` (run.py:980-990) NIE ponawiac, gdy `isinstance(exc, (llm.BudgetExceeded, llm.PreflightFailed))` — jest juz gotowy predykat `llm.przejsciowy(exc)` (llm.py:381-405), ktory mowi dokladnie to, a ktorego ten handler nie uzywa; zamiast slepego powtorzenia wypisac jednoznaczny komunikat „research za $X oplacony, brak budzetu na pisarza".

---

### 56. Artykul tygodniowy i rutyna dnia dziela jeden zamek — kolizja kasuje caly tygodniowy artykul po cichu, z kodem wyjscia 0 i bez sladu w bazie

**Powaga:** powazna   **Plik:** `run.py:709`

**Dowod:** Oba zadania wolaja ten sam `zajmij_zamek()` na tym samym pliku `config.DATA_DIR / "agent.lock"` (run.py:96) i oba idą przez to samo `main()`. Przy zajetym zamku: `except JuzDziala as exc: print(f" {exc}", flush=True); return 0` (run.py:708-710) — komunikat idzie na stdout i przebieg konczy sie SUKCESEM. `zajmij_zamek` stoi PRZED `db.start_run` (run.py:707 wobec run.py:726), wiec w bazie nie powstaje ani jeden wiersz. Okno kolizji jest wyliczalne z jednostek: nia-agent.timer:35 `OnCalendar=*-*-* 11:20:00` + :39 `RandomizedDelaySec=1500` daje start 11:20-11:45, a nia-agent.service:28 `TimeoutStartSec=9000` i `LIMIT_CZASU_PRZEBIEGU_S = 9000` minus `ZAPAS_CZASU_S = 900` (config.py:1218-1220) daje samoograniczenie 2h15, czyli koniec okolo 14:00-14:15 — przy czym bloki `polubienia` i `restacki` w ogole nie patrza na zegar (przyznane w komentarzu run.py:1090-1093: „polubienia i restacki nie patrza na niego wcale"), wiec przebieg dzienny potrafi przeciagnac ponad ten czas. nia-artykul.timer:15-17 `OnCalendar=Tue *-*-* 14:00:00` + `RandomizedDelaySec=900` startuje artykul 14:00-14:15. Okna sie nakladaja. Kontrola ciszy tego nie zlapie: `alarm.cisza()` patrzy tylko na `SELECT MAX(started_at) FROM runs` (alarm.py:189) z progiem `CISZA_ALARMOWA_H = 26` (alarm.py:163), a przebiegi dzienne odpalaja sie trzy razy na dobe — wiec licznik ciszy jest zawsze swiezy.

**Dlaczego to wada:** Odpowiedz na pytanie „czy nastepny przebieg zrobi to samo drugi raz albo pominie" brzmi tu: POMINIE, na caly tydzien, i nikt sie nie dowie. Timer jest `Persistent=true` (nia-artykul.timer:16), ale zadanie sie ODBYLO i zwrocilo 0, wiec systemd nie ma czego powtarzac; nastepny strzal to za siedem dni. Nie powstaje wiersz w `runs`, wiec ani `alarm.sprawdz_przebiegi_i_ostrzez` (alarm.py:145), ani `alarm.zawieszone` (alarm.py:208), ani `alarm.cisza` (alarm.py:189) nie maja czego zobaczyc; jedynym sladem jest linia na stdout w journalctl, na ktora nikt nie patrzy. `return 0` jest sluszny dla rutyny dnia (nic nie przepada, kolejny przebieg jest za kilka godzin) i szkodliwy dla artykulu, ktory ma jedno okno w tygodniu — a kod nie rozroznia tych dwoch przypadkow, mimo ze `args.dzien` jest dostepny... ale dopiero PO `parse_args()`, ktore stoi ponizej zamka (run.py:711-720).

**Plan naprawy:** Przeniesc `zajmij_zamek()` ponizej `parse_args()` (i tak nic wczesniej nie dotyka danych — `odmow_publikacji_z_kopii` juz teraz wymaga `args`), a potem rozroznic zachowanie: dla `--dzien` zostawic `return 0`, dla sciezki artykulu zwrocic kod niezerowy ORAZ zapisac slad, ktory zobaczy alarm — najprosciej wiersz w `runs` ze statusem `SKIPPED` i notatka „zamek zajety przez inny przebieg" (`db.start_run` + `db.finish_run` nie wymagaja zamka). Niezaleznie: rozsunac zegary, zeby okno kolizji zniknelo — przesunac `nia-artykul.timer` na godzine, w ktorej zaden przebieg dzienny nie moze jeszcze trwac (np. Tue 15:30 UTC), albo obnizyc `RandomizedDelaySec` w nia-agent.timer. Dopisac do tests/test_jednostki_systemd.py sprawdzenie, ze najpozniejszy mozliwy koniec przebiegu dziennego (OnCalendar + RandomizedDelaySec + TimeoutStartSec) jest wczesniejszy niz najwczesniejszy mozliwy start przebiegu artykulowego.

---

### 57. Diagram wiesza gates.py pod stages.py, a bramki orkiestruje run.py

**Powaga:** drobna   **Plik:** `dokumentacja-zrodla\wstep.md:83`

**Dowod:** Dokument (wstep.md:82-85) rysuje `gates.py` i `db.py` jako dzieci `stages.py`: "run.py --+-> stages.py --+-> llm.py ... +-> gates.py ... +-> db.py". Kod: `run.py:22` "import gates" i to run.py je wola — run.py:1080 "findings = gates.deterministic_floors(", :1082 "findings.extend(gates.uwagi_z_formy(forma, draft[\"body\"]))", :1093 "status, blocked_by = gates.verdict(findings)", :1070 "gdzie = gates.pozycja_w_tekscie(moment, draft[\"body\"])". `stages.py` nie importuje `gates` na poziomie modulu (naglowek stages.py:17-19 to tylko `config`, `db`, `llm`); siega po nie odroczonym importem w dwoch miejscach i wylacznie po dwa wyrazenia regularne — stages.py:399-401 "import gates as _gates" ... "(_gates.FABRICATED_EXPERIENCE, \"zmyslone przezycie\"), (_gates.VAGUE_STUDY, \"nieistniejace badanie\")" oraz stages.py:1248-1252. Podobnie `db` jest importowany bezposrednio przez run.py:21.

**Dlaczego to wada:** Diagram sugeruje, ze etapy same sie oceniaja, czyli ze bramki nalezy wpiac wewnatrz `stages`. Odtwarzajacy zbuduje wtedy `write` wolajacy bramki u siebie i straci wlasnosc, na ktorej opiera sie cala sekcja V: bramki sa liczone RAZ, w rozdzielniku, na gotowym draftcie, i to rozdzielnik decyduje o statusie. To jest ta sama roznica, co miedzy "model rozstrzyga" a "kod rozstrzyga" z sekcji I.4.

**Plan naprawy:** Przesunac `gates.py` i `db.py` w diagramie na poziom `run.py` (rozdzielnik wola je bezposrednio), a od `stages.py` poprowadzic do `gates.py` cienka krawedz z dopiskiem "tylko dwa wzorce: FABRICATED_EXPERIENCE i VAGUE_STUDY" — zeby bylo widac, ze to zapozyczenie stalych, nie ocenianie.

---

### 58. kopia_subskrybentow.py: 125 wierszy w sekcji I, 135 w sekcji II tego samego dokumentu

**Powaga:** drobna   **Plik:** `dokumentacja-zrodla\wstep.md:61`

**Dowod:** Dokument (wstep.md:59-62): "**WADA — jedenascie plikow zamiast dziesieciu.** Najblizsze usunieciu: `style.py` (106 wierszy, wolany tylko z `stages.py`) i `kopia_subskrybentow.py` (125 wierszy, narzedzie reczne poza przebiegiem)." Sekcja II tego samego dokumentu, linia 396: "135 wierszy, 3 funkcji na poziomie modulu, 0 klas". `wc -l kopia_subskrybentow.py` = 135. Liczba dla `style.py` (106) jest zgodna, a teza "wolany tylko z stages.py" potwierdzona: jedyne odwolanie to stages.py:238-241.

**Dlaczego to wada:** Dwie rozne liczby dla tego samego pliku w jednym dokumencie, w odleglosci 335 wierszy, przy czym ta blizsza czytelnikowi jest nieaktualna. Sam wniosek ("scalenie ktoregokolwiek przywraca zgodnosc z mandatem") pozostaje prawdziwy, wiec to nie jest wada rozumowania — ale ktos, kto szuka roznicy miedzy swoja kopia a dokumentem, straci na tym czas.

**Plan naprawy:** Wpisac 135, albo — spojnie z naprawa liczby lacznej — wciagnac obie liczby w wstep.md z generatora, zeby pochodzily z tego samego zrodla co sekcja II.

---

### 59. Wada 9 (skaut nie trafia w kryteria artykulowe) nie odnotowuje, ze prompt przepisano wlasnie pod to — 103 wiersze zmian po dacie listy

**Powaga:** drobna   **Plik:** `dokumentacja-zrodla/wady.md:21`

**Dowod:** wady.md:21 (= dokument:7217): „| 9 | skaut nie trafia w kryteria artykulowe | ... ostatni przebieg dal **0 z 10** | mamy miernik, nie mamy generatora |". Po commicie, ktorym spisano liste, `prompts/skaut.md` urosl o 103 wiersze w commicie 6cca96f „Skaut szukal w zlym miejscu — stad zero tematow artykulowych z dziesieciu". Prompt ma teraz sekcje „### Where these live, and how to find them" z dziewiecioma dziedzinami do przeszukania i twardy limit: „**At least half your list must be `SYSTEM_UNDER_TEST`, and at least three of them must carry two or more precedents each.**" Prog w kodzie nie zmienil sie: config.py:533 `PRECEDENSOW_NA_ARTYKUL = 2`, config.py:551 `ZASIEGI_ARTYKULOWE = ("AN_INDUSTRY", "A_COUNTRY")`.

**Dlaczego to wada:** Zdanie „mamy miernik, nie mamy generatora" opisuje stan sprzed przepisania promptu. Czytelnik dokumentu — ktory w zalaczniku A widzi juz NOWY skaut.md, bo zalaczniki sa generowane z plikow — dostaje sprzecznosc: lista wad mowi, ze generatora nie ma, a zalacznik pokazuje generator z lista dziedzin i twardym limitem. Ryzyko jest realne, bo naturalna „naprawa" wedlug listy to obnizenie `PRECEDENSOW_NA_ARTYKUL` z 2 na 1, co skasowalo by jedyne kryterium odrozniajace artykul od notki (stages.py:2173).

**Plan naprawy:** Przepisac pozycje 9 na stan faktyczny: prog niezmieniony, prompt przepisany 21 sierpnia (commit 6cca96f) wlasnie pod ten wynik, POMIAR PO ZMIANIE NIE ZOSTAL WYKONANY. Wada zostaje otwarta, ale jako „czeka na pomiar", nie jako „nie ma generatora" — i z jawnym zakazem ruszania `PRECEDENSOW_NA_ARTYKUL` przed pomiarem.

---

### 60. Wada 8 wymienia trzy kopie `.przed-*` w data/, ktorych tam nie ma

**Powaga:** drobna   **Plik:** `dokumentacja-zrodla/wady.md:20`

**Dowod:** wady.md:20 (= dokument:7216): „| 8 | dwie zerowe bazy i trzy kopie `.przed-*` w `data/` | smieci; jedna zerowa baze stworzylo zle zapytanie diagnostyczne | brak |". `find data -name "*.przed-*"` nie zwraca nic; `find data -name "*.db"` zwraca dokladnie trzy pliki: `data/agent-v2.db` (372736 B), `data/agent.db` (0 B), `data/zasiew-produkcji.db` (0 B). Czesc o dwoch zerowych bazach jest wiec prawdziwa, czesc o trzech kopiach `.przed-*` — nie.

**Dlaczego to wada:** Polowa pozycji opisuje smieci, ktorych nie ma. Ktos sprzatajacy katalog wedlug listy bedzie szukal plikow `.przed-*`, nie znajdzie ich i albo uzna liste za nieaktualna w calosci (a wtedy przeoczy dwie zerowe bazy, ktore SA), albo zacznie szukac ich gdzie indziej — na serwerze, w kopiach zapasowych. Przy pozycji, ktorej koszt sam dokument ocenia na „brak", to czysta strata czasu w najgorszym miejscu: na koncu listy, gdzie czytelnik i tak jest juz zmeczony.

**Plan naprawy:** Skrocic pozycje 8 do stanu faktycznego: dwie zerowe bazy — `data/agent.db` i `data/zasiew-produkcji.db`, obie 0 B — i wskazac, ze zywa baza to `data/agent-v2.db`. Czesc o kopiach `.przed-*` usunac albo przeniesc do naprawionych, jesli zostaly skasowane swiadomie.

---

### 61. Numeracja etapow w dokumencie (1-14) klóci sie z numeracja w docstringach stages.py (1-9) i nikt tego nie zaznacza

**Powaga:** drobna   **Plik:** `dokumentacja-zrodla/rozdzial_artykul.md:811`

**Dowod:** Dokument tytuluje sekcje (rozdzial_artykul.md:811) "#### Etap 8 — pisarz", (925) "#### Etap 9 — recenzja", (1106) "#### Etap 12 — zapis". Kod trzyma wlasna, starsza numeracje: stages.py:219 "\"\"\"Etap 7 — artykul (Claude). To jest produkt.", stages.py:74 "\"\"\"Etap 8 — recenzja: rozliczenie kazdego zdania (Claude).\"\"\"", stages.py:169 "\"\"\"Etap 9 — zapis. Artykul do szuflady: baza + plik .md.\"\"\"". Docstringi stages.py numeruja 1-9 (scout=1 ... write=7, review=8, save=9), czyli ze schematu sprzed dolozenia `warto_pisac` i `ocen_forme`. Dokument nigdzie tej rozbieznosci nie odnotowuje, mimo ze ma cala zbiorcza liste wad tej klasy (np. punkt 20 o "czterech bramkach" w naglowku gates.py).

**Dlaczego to wada:** „Etap 8" znaczy dwie rozne rzeczy zaleznie od tego, gdzie sie patrzy: w dokumencie to pisarz, w kodzie to recenzent. Kto czyta dokument i grepuje kod po "Etap 8", zeby znalezc pisarza, trafia na `review`. Trzy najwazniejsze etapy potoku (write, review, save) maja w kodzie numery o jeden do trzech nizsze niz w dokumencie. To dokladnie ta klasa bledu, ktora dokument sam tropi w innych miejscach — martwy/nieaktualny napis, ktory czyta sie jak informacje.

**Plan naprawy:** Dopisac w rozdzial_artykul.md, w "Mapie etapow" (po linii 31), jedno zdanie ostrzegawcze: docstringi w stages.py nosza starsza numeracje 1-9 sprzed dolozenia `warto_pisac` i `ocen_forme`, wiec "Etap 7" w stages.py:219 to pisarz (tu etap 8), "Etap 8" w stages.py:74 to recenzja (tu etap 9), "Etap 9" w stages.py:169 to zapis (tu etap 12). Alternatywnie dopisac ten rozjazd jako pozycje do zbiorczej listy wad. Numeracji w kodzie NIE ruszac w ramach tej zmiany dokumentacyjnej — to osobna decyzja.

---

### 62. Cytat z `llm._call_claude` w rozdziale o EFFORT nie odpowiada juz kodowi — brakuje galezi, ktora ostrzega o martwym wpisie

**Powaga:** drobna   **Plik:** `dokumentacja-zrodla/rozdzial_artykul.md:40`

**Dowod:** Dokument (rozdzial_artykul.md:38-41, w sklejce JAK_ZBUDOWANY_JEST_BOT.md:467-470) podaje jako doslowny kod: " # `effort` istnieje na Opusie 5, Sonnecie 5 i Fable 5. / if purpose in config.EFFORT and model in (config.CLAUDE, config.SONNET, config.FABLE): / kwargs[\"output_config\"] = {\"effort\": config.EFFORT[purpose]}" i konczy WADE zdaniem: "Efekt: `EFFORT[\"review\"] = \"high\"` nie ma zadnego wplywu na cokolwiek, a plik sugeruje, ze ma." Kod (llm.py:154-167) jest teraz dwupoziomowy: "if purpose in config.EFFORT: / if model in (config.CLAUDE, config.SONNET, config.FABLE): / kwargs[\"output_config\"] = ... / elif purpose not in _EFFORT_BEZ_SKUTKU: / [...] _EFFORT_BEZ_SKUTKU.add(purpose) / print(f\" [effort] {purpose}={config.EFFORT[purpose]} NIE MA SKUTKU [...]\")", z licznikiem `_EFFORT_BEZ_SKUTKU` (llm.py:90-91) i uzasadnieniem w config.py:612-614: "Wpisy dla etapow deepseekowych ZOSTAJA [...] Zeby jednak nie byly cicha ozdoba, `llm.call` mowi RAZ NA PROCES, ktory wpis nie zadzialal i dlaczego."

**Dlaczego to wada:** Samo twierdzenie (EFFORT dziala tylko dla `write`) jest nadal prawdziwe, wiec to nie jest falszywy alarm — ale konkluzja "plik sugeruje, ze ma" juz nie obowiazuje: kod krzyczy o tym raz na proces. Kto odtwarza `_call_claude` z tego cytatu, zgubi ostrzezenie i wroci do stanu, w ktorym martwy wpis jest niewidoczny; kto uruchomi bota i zobaczy w logu linie "[effort] scout=medium NIE MA SKUTKU", nie znajdzie jej zrodla w dokumencie.

**Plan naprawy:** W rozdzial_artykul.md:38-41 podmienic cytat na aktualne llm.py:154-167 (z galezia `elif` i `_EFFORT_BEZ_SKUTKU`). Do WADY dopisac zdanie: wpis dalej nie dociera do API, ale od 2026-08-21 `llm.call` wypisuje o tym komunikat raz na proces, wiec martwa stala nie jest juz cicha — z odwolaniem do uzasadnienia w config.py:606-614. Punkt 1 zbiorczej listy wad (rozdzial_artykul.md:1317) doprecyzowac tak samo.

---

### 63. zmiesci_sie zaklada o jedna przerwe mniej niz dzialan, ale rytm_stanu jest wspolny dla calego przebiegu

**Powaga:** drobna   **Plik:** `run.py:219`

**Dowod:** run.py:218-221: # PRZERW JEST O JEDNA MNIEJ NIZ DZIALAN. Przy dwoch notkach czekamy raz, nie # dwa — pierwsza wersja liczyla przerwe po kazdej i wychodzilo o polowe za malo. def potrzeba(n: int) -> float: return n * config.CZAS_DZIALANIA_S + max(0, n - 1) * odstep Dla komentarzy to zalozenie nie zachodzi. Bloki obserwowania i subskrypcji chodza PRZED komentarzami (run.py:662-663) i po udanym kliknieciu ustawiaja ten sam klucz: run.py:584 `rytm_stanu["komentarz"] = True` oraz run.py:617 to samo. Blok komentarzy pyta `rytm("komentarz", "komentarze", rytm_stanu)` (run.py:487), wiec `stan.get("komentarz")` jest juz prawda i PIERWSZY komentarz tez odsypia przerwe. Przerw jest n, nie n-1, a `zmiesci_sie("komentarz", ...)` (run.py:337) policzyl n-1.

**Dlaczego to wada:** Rachunek czasu zaniza potrzebe o jedna przerwe komentarzowa (ODSTEPY["komentarz"] = 180-480 s, srednio 5,5 min) za kazdym razem, gdy w przebiegu poszla obserwacja albo subskrypcja — czyli w wiekszosci przebiegow, bo te bloki stoja wczesniej wlasnie po to, zeby zawsze dochodzily. Skutek jest ten sam, przed ktorym zmiesci_sie ma bronic: obietnica minimalnie za duza, wiec ostatni komentarz w bloku wypada przez rytm juz po oplaceniu modelu (patrz znalezisko o rytm po wywolaniu). Sam komentarz w kodzie mowi cos, co dla tej sciezki nie jest prawda.

**Plan naprawy:** Albo policzyc przerwy uczciwie — przekazac do zmiesci_sie informacje, czy dany rodzaj juz w tym przebiegu wystapil, i wtedy uzyc `n * odstep` zamiast `(n-1) * odstep` — albo rozdzielic klucze rytmu: obserwacja i subskrypcja nie sa komentarzami i moglyby miec wlasny klucz z wlasnymi widelkami. Przy okazji poprawic komentarz w run.py:218-219, zeby nie obiecywal n-1 bezwarunkowo.

---

### 64. Sekcja VI cytuje _cost w wersji SPRZED poprawki, sekcja VII te sama funkcje po poprawce — dokument zawiera dwie sprzeczne wersje jednej funkcji finansowej

**Powaga:** drobna   **Plik:** `dokumentacja-zrodla\rozdzial_dane.md:447`

**Dowod:** Blok w rozdzial_dane.md:439-462 (sklejka 5220-5243) pokazuje: "price = {\"in\": stawka[\"in\"], \"out\": stawka[\"out\"], \"verified\": config.PRICING[model][\"verified\"]}" — bez klucza `cache`. Sekcja VII, blok generowany z ast (`<!--KOD:llm._cost-->`, sklejka 6038+) pokazuje wersje z kodu: "price = {\"in\": stawka[\"in\"], \"out\": stawka[\"out\"], \"cache\": stawka[\"cache\"], \"verified\": ...}". Prawda jest ta druga: llm.py:110-112. Adnotacja "NAPRAWIONY 2026-08-20" stoi DOPIERO POD blokiem (wiersz 464).

**Dlaczego to wada:** Kto kopiuje kod z sekcji VI (a ona jest bezposrednio zatytulowana "Jak liczony jest koszt"), odtwarza blad, ktory wycenia trafienia w cache stawka wejsciowa — trzydziestokrotnie za drogo u pro ($0,66 zamiast $0,022) — przy `price_verified` nadal ustawionym na 1. To jest zapis finansowy liczony stawka, ktorej nie ma na fakturze. Dokument daje dwie rozne odpowiedzi na to samo pytanie w dwoch miejscach, a marker "stan sprzed poprawki" jest pod blokiem, nie nad nim.

**Plan naprawy:** Przeniesc adnotacje NAD blok kodu (jedno zdanie: "PONIZSZY BLOK TO STAN SPRZED POPRAWKI — aktualna wersja w sekcji VII") albo, prosciej, wstawic w sekcji VI wersje aktualna z llm.py i zostawic stara jako wyraznie oznaczony diff.

---

### 65. Odsylacz do config.py:588 przy MAX_TOKENS wskazuje na inna stala

**Powaga:** drobna   **Plik:** `dokumentacja-zrodla\rozdzial_dane.md:558`

**Dowod:** Dokument: "Czytający wersję pierwszą (linia 588) widzi `\"restack\": 3000`." W config.py wiersz 588 to komentarz do zapasu na myslenie ("# 28 tys., nie 16. Zmierzone na realnych przebiegach..."), a stala tam konczaca sie to THINKING_HEADROOM_TOKENS = 28000 (config.py:595). Pierwszy slownik MAX_TOKENS zaczyna sie w config.py:629, wpis `"restack": 3000` jest ponizej; drugi (ten z doliczonym zapasem) w config.py:1348.

**Dlaczego to wada:** Cala pointa tej WADY brzmi „ta sama nazwa w dwoch miejscach, ktorych czytelnik nie zestawi" — i dokument wysyla go pod zly numer w pliku majacym 1600 wierszy, wiec czytelnik trafia na stala o zupelnie innym znaczeniu i albo uzna WADE za zmyslona, albo zacznie szukac na oslep.

**Plan naprawy:** Zamienic "(linia 588)" na odsylacz do obecnych pozycji: pierwszy `MAX_TOKENS` config.py:629, drugi (nadpisujacy, z `+ THINKING_HEADROOM_TOKENS`) config.py:1348. Odleglosc "~700 linii" jest zgodna (719) i moze zostac.

---

### 66. Sciezka artykulowa nie ma wlasnego zegara: nia-artykul.service tnie po 5400 s, a agent na tej galezi nie ustawia konca czasu w ogole

**Powaga:** drobna   **Plik:** `dokumentacja-zrodla\rozdzial_dane.md:903`

**Dowod:** Dokument, wiersz 903: "**`nia-artykul.service`** różni się od dziennego jednym: `ExecStart=… run.py --wyslij` (bez `--dzien`), `TimeoutStartSec=5400` (1,5 h)" — i nic wiecej. Kod: run.py:276-278 ustawia `global _KONIEC_CZASU` WYLACZNIE wewnatrz funkcji `dzien()`; na sciezce artykulowej `_KONIEC_CZASU` zostaje None, a run.py:152-153 mowi "if _KONIEC_CZASU is None: return True", czyli `zostal_czas` zawsze przepuszcza. Do tego config.py:1206 `LIMIT_CZASU_PRZEBIEGU_S = 9000` z komentarzem "Musi zgadzac sie z `TimeoutStartSec` w pliku uslugi — to jedyne miejsce, gdzie ta sama liczba stoi dwa razy, i pilnuje tego test", podczas gdy tests/test_jednostki_systemd.py:62-68 sprawdza WYLACZNIE nia-agent.service; drugi, rozny limit (5400) w nia-artykul.service:14 nie jest porownywany z niczym.

**Dlaczego to wada:** Czytajacy config.py wychodzi z przekonaniem, ze limit czasu przebiegu to jedna liczba pilnowana testem. Sa dwie: 9000 dla dnia i 5400 dla artykulu, przy czym ta druga nie odpowiada zadnej stalej i przebieg artykulowy nie zna jej wcale — obrona opisana w nia-agent.service ("`run.rytm` nie zaczyna przerwy, ktora sie nie miesci") na tej galezi nie dziala, bo nie ma z czym porownywac. Jesli sciezka artykulowa kiedykolwiek przekroczy 1,5 h, dostanie SIGTERM w dowolnym momencie — po oplaceniu dyskoverii i pisarza, a przed zapisem — czyli dokladnie ten scenariusz, ktory dokument opisuje jako strate $0,5898 na przebiegu 12.

**Plan naprawy:** W dokumencie: dopisac przy wierszu 903, ze artykul ma wlasny, KROTSZY sufit i ze agent go nie widzi. W kodzie (osobno, do decyzji wlasciciela): albo ustawiac `_KONIEC_CZASU` takze na sciezce artykulowej z osobnej stalej (np. LIMIT_CZASU_ARTYKULU_S = 5400), albo rozszerzyc tests/test_jednostki_systemd.py o sprawdzenie kazdego `TimeoutStartSec` wobec odpowiadajacej mu stalej — dzis komentarz config.py:1206 obiecuje wiecej, niz test daje.

---

### 67. ZALACZNIK A: cztery pliki referencyjne stoja pod naglowkiem "A.1. Prompty robocze", a A.2 juz nie ma

**Powaga:** drobna   **Plik:** `dokumentacja-zrodla/sklej.py:255`

**Dowod:** sklej.py:255 wpisuje na sztywno jedyny podnaglowek zalacznika: "### A.1. Prompty robocze", po czym `gen_zalacznik_prompty` leci petla po `sorted(PROMPTY_KAT.glob("*.md"))` bez zadnego podzialu. W efekcie pod "A.1. Prompty robocze" laduja rowniez `prompts/OSWIADCZENIE_AI.md`, `prompts/ROZWOJ_KONTA.md`, `prompts/SKAD_BRAC.md` (zalacznik_prompty.md:1894) i `prompts/ZASADY_NOTEK_I_KOMENTARZY.md`, a numeracja "A.1" nie ma juz swojego "A.2" — `grep -n "### A" JAK_ZBUDOWANY_JEST_BOT.md` zwraca dokladnie jedna linie (7572). Zaden z tych czterech plikow nie jest promptem roboczym: `grep` po `_prompt(` w calym agencie nie znajduje dla nich wywolania. `OSWIADCZENIE_AI.md` jest czytany bezposrednio przez browser.py:1300, a `SKAD_BRAC.md` to notatka dla czlowieka, cytowana wylacznie z `START_TUTAJ.md:225` ("jest tam lista plikow i linii. Kopiuj stamtad").

**Dlaczego to wada:** Naglowek zapowiada prompty robocze, a lista zawiera cztery pliki, ktorych zaden model nigdy nie dostaje jako promptu etapu. Odtwarzajacy bota zobaczy 25 "promptow roboczych", zacznie szukac dla nich etapow i wywolan `_prompt`, a dla czterech ich nie znajdzie — albo, gorzej, dolozy je do jakiegos etapu "bo byly w zalaczniku". Osierocone "A.1" bez "A.2" dodatkowo sugeruje, ze fragment dokumentu zaginal.

**Plan naprawy:** W `gen_zalacznik_prompty` rozdzielic petle na dwie listy wedlug tego, czy plik ma wywolanie `stages._prompt("nazwa")` w zrodlach (albo prosciej: wedlug jawnego zbioru nazw referencyjnych), i wypisac je pod "### A.1. Prompty robocze" oraz "### A.2. Material referencyjny (nie sa podawane modelowi jako prompt etapu)". Przy kazdym pliku z A.2 dopisac jednym zdaniem, kto go czyta — np. `OSWIADCZENIE_AI.md` -> browser.py:1300, `SKAD_BRAC.md` -> notatka operacyjna, bez czytelnika w kodzie.

---

### 68. "Szesc plikow pilnuje produkcji" — wyliczenie tuz obok podaje siedem nazw, a w kodzie jest ich osiem

**Powaga:** drobna   **Plik:** `dokumentacja-zrodla/rozdzial_bramki.md:1017`

**Dowod:** Dokument (rozdzial_bramki.md:1017, scalony 4627): "Szesc plikow pilnuje, ze test niczego nie ruszyl w produkcji:", a wyliczenie kilka wierszy nizej podaje SIEDEM nazw: "`test_bramki_jakosci`, `test_forma`, `test_komentarze`, `test_obserwacje`, `test_piec`, `test_pomiar`, `test_forma_artykulu`". `grep -l PILNOWANE agent-v2/tests/*.py` daje OSIEM plikow — do tych siedmiu dochodzi `tests/test_wstrzykniecie.py` (ma zarowno `def odcisk`, jak i liste `PILNOWANE`).

**Dlaczego to wada:** Liczba w zdaniu przeczy wlasnemu wyliczeniu polozonemu dwa wiersze nizej, a oba przecza kodowi. To jest lista bezpieczenstwa — mowi, ktore testy udowadniaja, ze nie tknely produkcyjnej bazy i plikow danych. Jej niedoszacowanie sugeruje, ze test_wstrzykniecie takiej ochrony nie ma, wiec przy odtwarzaniu latwo go pominac przy dodawaniu odciskow i przy okazji zgubic zabezpieczenie, ktore juz istnieje.

**Plan naprawy:** Poprawic "Szesc plikow" na "Osiem plikow" i dopisac `test_wstrzykniecie` do wyliczenia w rozdzial_bramki.md:1017-1031.

---

### 69. "Piec plikow nie ma kontrdowodu" — po dwukropku sa cztery i cztery sa w tabeli

**Powaga:** drobna   **Plik:** `dokumentacja-zrodla/rozdzial_bramki.md:1165`

**Dowod:** Dokument (rozdzial_bramki.md:1165, scalony 4775): "Piec plikow nie ma kontrdowodu w zadnej z odmian: `test_czas`, `test_generatory`, `test_indeks_kandydatow`, `test_pobieranie` (oraz w formie nieklasycznej `test_martwe_sygnaly` i `test_bibliotekarz_bramka` — maja rozrozniacz, ale nie odtwarzaja starego kodu)." Po dwukropku sa CZTERY nazwy; nawias jest jawnie osobna kategoria. Kolumna "kontrdowod" w tabeli 976-1010 ma dokladnie cztery wpisy "nie" (te same cztery pliki) i dwa wpisy "czesciowo" (te dwa z nawiasu).

**Dlaczego to wada:** Sekcja 9 to zasada metodologiczna, ktora rozdzial stawia jako najwazniejszy wniosek z calego systemu testow, a jej podsumowanie nie zgadza sie ani z wlasna lista, ani z wlasna tabela. Czytelnik szuka piatego pliku bez kontrdowodu i albo go nie znajdzie, albo bledenie zaliczy do tej grupy jeden z dwoch "czesciowo" — czyli uzna, ze test_martwe_sygnaly nie rozroznia wersji, choc rozdzial dwa zdania wczesniej mowi, ze rozroznia.

**Plan naprawy:** Zamienic "Piec plikow" na "Cztery pliki" w rozdzial_bramki.md:1165.

---

### 70. Odnosnik "stages.scout (linia 2068)" wskazuje komentarz o pytaniach czytelnikow; prog 5 stoi w wierszu 2096

**Powaga:** drobna   **Plik:** `dokumentacja-zrodla/rozdzial_bramki.md:152`

**Dowod:** Dokument (rozdzial_bramki.md:152, scalony 3762), tabela "trzy rozne progi na to samo pytanie": "| `stages.scout` (linia 2068) | `len(wiara.split()) >= 5` → `ma_przekonanie` |". W stages.py wiersz 2068 to ` # wiedzial, czego unikac, i nic o tym, czego ludzie faktycznie chca.`, a szukane przypisanie stoi w wierszu 2096: ` t["ma_przekonanie"] = len(wiara.split()) >= 5`.

**Dlaczego to wada:** To jedyny odnosnik z numerem wiersza w calej tabeli trzech progow, ktora jest osia calej WADY o niespojnych progach (4 / 4 / 5). Kto pojdzie pod wskazany wiersz, zobaczy komentarz o pytaniach czytelnikow i uzna, ze dokument opisuje kod, ktorego nie ma — a wtedy podwazy cala tabele, mimo ze sam prog 5 i pozostale dwa progi sa opisane poprawnie.

**Plan naprawy:** Poprawic 2068 na 2096 albo — bezpieczniej, bo stages.py przesuwa sie przy kazdej zmianie — usunac numer wiersza i zostawic sama nazwe funkcji, tak jak w pozostalych dwoch wierszach tej tabeli.

---

### 71. Sekcja 6 opisuje leniwa weryfikacje notek jako zywa oszczednosc, a kandydat na notke jest dzis jeden

**Powaga:** drobna   **Plik:** `dokumentacja-zrodla/rozdzial_bramki.md:831`

**Dowod:** Dokument (rozdzial_bramki.md:812) "**Weryfikacja jest leniwa.** Kandydaci sa sortowani (najpierw ci, ktorzy nie powtarzaja otwarcia poprzednich notek), a petla konczy sie na pierwszym, ktory przejdzie" i (rozdzial_bramki.md:831) "Powod: *„Przy pieciu notkach dziennie po trzech kandydatow to roznica miedzy pietnastoma sprawdzeniami a szescioma."*". Tymczasem config.py:704 `NOTE_CANDIDATES = 1`, a stages.py:860 `for i in range(config.NOTE_CANDIDATES):` — petla notki produkuje dokladnie jednego kandydata, wiec ani `candidates.sort(key=powtarza_otwarcie)` (stages.py:905), ani `break` po pierwszym przejsciu niczego juz nie oszczedzaja. Cytowany "powod" to nieaktualny komentarz ze stages.py:895-898. Dla komentarzy mechanizm nadal zyje: config.py:792 `COMMENT_CANDIDATES = 3`.

**Dlaczego to wada:** Ten sam dokument podaje dwie sprzeczne liczby kandydatow na notke: sekcja 6 mowi "po trzech kandydatow", a tabela testow w wierszu 995 mowi "`test_jeden_wariant` | 14 | `NOTE_CANDIDATES == 1` **plus warunek konieczny**". Odtwarzajacy pojdzie za opisem mechanizmu, ustawi trzech kandydatow i cofnie zmiane, ktora config.py nazywa najwieksza pojedyncza oszczednoscia w systemie (28 USD miesiecznie) — a przy okazji potroi liczbe platnych weryfikacji notek.

**Plan naprawy:** W rozdzial_bramki.md:812-831 dopisac, ze dla notek NOTE_CANDIDATES == 1, wiec lenistwo i sortowanie po otwarciu sa dzis martwe (zostaje mechanizm i pokretlo), a zywy przyklad to komentarze przy COMMENT_CANDIDATES = 3; cytat oznaczyc jako nieaktualny komentarz w stages.py:895-898 i zglosic go osobno do poprawki w kodzie.

---

### 72. Prompt skauta podaje modelowi dokladny prog, ktorego kod uzywa — `ile_precedensow` jest wypelnianiem limitu, nie pomiarem

**Powaga:** drobna   **Plik:** `prompts/skaut.md:290`

**Dowod:** prompts/skaut.md:290-292: "**At least half your list must be `SYSTEM_UNDER_TEST`, and at least three of them must carry two or more precedents each. Keep at least two `BROKEN_BELIEF` as well**" — i dalej linia 386: "**Fewer than two, and the subject is a note.**" config.py:533: `PRECEDENSOW_NA_ARTYKUL = 2`, a stages.py:2173-2174 rozstrzyga nim artykul kontra notke: `t["na_artykul"] = (t["ile_precedensow"] >= config.PRECEDENSOW_NA_ARTYKUL and t["duzy_zasieg"])`. `na_artykul` jest DRUGIM kluczem sortowania w `pick_topic` (stages.py:2030).

**Dlaczego to wada:** Prompt mowi modelowi wprost, ile precedensow trzeba miec i ile tematow ma je miec — czyli sam podaje wartosc, do ktorej pole ma sie wyrownac. To ta sama mechanika, ktora dala "kazdy temat po trzy znane teksty" i "kazdy po szesc watkow", tylko tym razem liczbe podal prompt zamiast wygody modelu. Wynik: `ile_precedensow` bedzie sie kleil do 2 (albo 0), `na_artykul` stanie sie kwota wypelniona na zamowienie, a nie zmierzona wlasciwoscia tematu — i kryterium, ktore mialo oddzielic artykul od notki, przestanie cokolwiek rozroznaic. Wykrywacz z stages.py:2259 to zglosi jako `ile_precedensow=2` u wszystkich, ale dopiero po fakcie i po zaplaceniu za przebieg.

**Plan naprawy:** Wyjac liczbe z prompta: zamiast "at least three of them must carry two or more precedents each" napisac, ze precedensy maja byc wymienione wszystkie, ktore model naprawde pamieta, i ze pusta lista jest uczciwa odpowiedzia (to juz jest w skaut.md:386-391, wystarczy nie przebijac tego kwota). Prog `PRECEDENSOW_NA_ARTYKUL` zostawic po stronie kodu i nie ujawniac go w promptcie — tak samo jak `NASYCENIE_OD_ILU` nie jest podany skautowi.

---

### 73. Jedyny licznik komentarzy, lajkow i restackow moze paść bez jednej linii w logu — dwa puste `except` pod rzad

**Powaga:** drobna   **Plik:** `browser.py:82`

**Dowod:** Zapis: `except Exception:` / `pass` (browser.py:82-83), bez printa, bez licznika bledow. Odczyt: `except Exception:` / `pass # licznik nie moze zatrzymac przebiegu` (browser.py:130-131) — tez bez printa, i zwraca CZESCIOWO policzony slownik `ile`, a nie sygnal awarii. Tymczasem docstring `z_dziennika_dzis` mowi, ze to jedyne zrodlo tych liczb: "Notki potrafimy policzyc u zrodla, komentarzy i polubien NIE. Kanal profilu zwraca wylacznie notki (...) Bez niego kazdy przebieg widzial zero i bral pelny dzienny budzet od nowa" (browser.py:89-96). Sam plik dziennika nie jest w repo (`ls agent-v2/data/` nie pokazuje `dziennik.jsonl`), wiec na maszynie deweloperskiej ten licznik JUZ czyta zera — i nic o tym nie mowi.

**Dlaczego to wada:** Decyzja "dziennik nie moze wywalic agenta" (browser.py:70-71) jest sluszna, ale polkniecie bez logu to co innego niz niewywalanie. Katalog `data/` tylko do odczytu na serwerze, pelny dysk albo jeden blad przy `mkdir` sprawia, ze zapisy przepadaja, `z_dziennika_dzis` zwraca same zera, a run.py wypisuje `komentarze=0 lajki=0` (run.py:319-321) — nie do odroznienia od czystego dnia. Agent bierze wtedy pelny dzienny budzet komentarzy i polubien w KAZDYM z trzech przebiegow. Przy odczycie jest jeszcze gorzej: `except` w linii 130 lapie blad w SRODKU petli po liniach (browser.py:113-129), wiec zwrocony slownik moze byc policzony do polowy pliku — czyli licznik nie tyle znika, co klamie liczba, ktora wyglada wiarygodnie.

**Plan naprawy:** Zostawic zachowanie (nie przerywac przebiegu), ale dodac slad: w `zapisz_w_dzienniku` zamienic `pass` na `print(f" [dziennik] NIE ZAPISALEM {rodzaj}: {type(exc).__name__}: {exc}", flush=True)` (przechwytujac wyjatek jako `exc`). W `z_dziennika_dzis` (browser.py:130) tak samo wypisac blad i dodatkowo zaznaczyc, ze wynik jest niepelny — najprosciej zwrocic dodatkowy klucz `"policzone": False`, ktory run.py potraktuje jak wyczerpany budzet, zamiast jak zero.

---

### 74. Polubienie zapisuje sie jako udane od razu po klikniecu — jedyne dzialanie w pliku bez potwierdzenia

**Powaga:** drobna   **Plik:** `browser.py:1040`

**Dowod:** W `polub_w_kanale`: `kandydat.click(timeout=8000)` / `wynik["polubione"] += 1` / `print(f" polubione {wynik['polubione']}/{ile}", flush=True)` / `zapisz_w_dzienniku("polubienie", udane=True)` (browser.py:1036-1040). Nic miedzy klikiem a zapisem nie sprawdza, czy Substack polubienie przyjal. Reszta pliku robi to inaczej i to w tej samej funkcji sasiedniej: `_klik_na_profilu` po kliknieciu czeka i sprawdza stan przycisku — `# Po kliknieciu napis zmienia sie na stan przeciwny.` / `wynik["zrobione"] = k.count() == 0 or not k.is_visible()` / `zapisz_w_dzienniku(rodzaj, udane=wynik["zrobione"], komu=handle)` (browser.py:1094-1096). Komentarz i notka ida jeszcze dalej i pytaja API (browser.py:2089-2090, 1765). Zasade nazywa wprost komentarz przy komentarzach: "Klikniecie przycisku nie jest dowodem, ze komentarz zostal przyjety, a agent bez czlowieka nie ma komu tego sprawdzic" (browser.py:2085-2086).

**Dlaczego to wada:** Ten wpis jest jedynym zrodlem liczby lajkow dnia (`nazwa = {..., "polubienie": "lajki", ...}` — browser.py:108), a `z_dziennika_dzis` liczy wylacznie wpisy z `udane` prawdziwym (browser.py:125-126). Klik, ktory Playwright wykonal, a Substack odrzucil (wyczerpany limit po ich stronie, wygasla sesja w polowie przebiegu, przycisk przerysowany przez React miedzy `is_visible` a `click`), zostaje policzony jako polubienie, ktorego nie ma. Skutki sa dwa i oba ciche: dzienny budzet lajkow zjada sie na dzialaniach, ktore nie doszly do skutku, a `alarm.przeglad` — jedyne narzedzie mierzace, co z dzialan wynika (browser.py:646-660) — porownuje realne reakcje z zawyzona liczba wystawionych sygnalow i wyprowadza falszywy wniosek o skutecznosci. Sprawdzenie jest tanie i wzor stoi 50 linii nizej w tym samym pliku, wiec to nie jest kompromis, tylko przeoczenie.

**Plan naprawy:** Po `kandydat.click(...)` odczekac ~1,5 s i sprawdzic stan przycisku tak jak `_klik_na_profilu`: przycisk "Like" po polubieniu zmienia dostepna nazwe/stan, wiec wystarczy `udane = kandydat.get_attribute("aria-pressed") == "true"` albo — analogicznie do wzoru z linii 1094 — sprawdzic, czy lokator o nazwie "Like" w tym miejscu przestal byc widoczny. `wynik["polubione"] += 1` i `zapisz_w_dzienniku("polubienie", udane=...)` przeniesc ZA to sprawdzenie i przekazac realny wynik zamiast stalej True.

---
