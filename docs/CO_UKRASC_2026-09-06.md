# Co da się wziąć z cudzych projektów — przegląd z 6 września 2026

Przejrzane pod jednym kątem: **czy to sprawi, że nasz bot zadziała lepiej albo
taniej.** Pomysły „ładne, ale u nas bez skutku" pomijam i mówię dlaczego.

Metoda: nie czytałem README, tylko **wyciągnąłem końcówki API z kodu** i
porównałem z tym, czego używamy.

## 1. NAJWAŻNIEJSZE: notki da się publikować przez API, nie klikaniem

Źródło: [adelaidasofia/substack-mcp](https://github.com/adelaidasofia/substack-mcp),
plik `publish_from_queue.py`.

```
POST https://substack.com/api/v1/comment/feed/
{
  "bodyJson": <dokument ProseMirror>,
  "tabId": "for-you",
  "surface": "feed",
  "replyMinimumRole": "everyone",
  "attachmentIds": []
}
```

**To jest ta sama końcówka, z której my już CZYTAMY** (`/api/v1/comment/feed`
jest w `browser.py`). Nikt nie próbował do niej pisać.

Dziś `wystaw_notke` robi `page.goto` i `kand.click` — klika w przeglądarce.

### Co to by naprawiło, zmierzone

- **10 publikacji w 14 dni z powodem „Substack nie potwierdził, że wyszło"** —
  przy publikacji przez API odpowiedź HTTP jest potwierdzeniem albo błędem,
  bez zgadywania z DOM-u.
- Kruchość przeglądarki: każda zmiana układu strony u Substacka może zepsuć
  klikanie. Końcówka API jest stabilniejsza (choć nieoficjalna).
- Czas przebiegu — patrz punkt 2.

### Czego to NIE naprawia i jakie niesie ryzyko

Publikowanie to jedyna rzecz w tym systemie, która **musi** działać. Ścieżka
przez przeglądarkę jest sprawdzona setkami wpisów. Nowa końcówka jest
nieoficjalna i może zniknąć bez ostrzeżenia.

**Dlatego nie proponuję zamiany, tylko kolejność:** najpierw użyć API tam,
gdzie przeglądarka NIE POTWIERDZIŁA — jako drugie sprawdzenie zamiast zapisu
„nieudane". To zamyka te 10 przypadków bez ruszania działającej ścieżki.

Do tego trzeba jeszcze zamiany tekstu na `bodyJson` (ProseMirror). W tamtym
repozytorium jest na to osobny plik `prosemirror.py`.

## 2. Kolejka zamiast spania w przebiegu

Ten sam projekt publikuje **z kolejki** (`publish_from_queue.py`), a nie
w trakcie jednego długiego przebiegu.

My śpimy **40–60 minut między notkami wewnątrz przebiegu**. Skutek zmierzony:
przebieg 152 trwał ponad dwie godziny, a bloki są ucinane na czasie
**11 razy w 14 dni** (6× dyskusje, 3× komentarze, 2× notki).

Gdyby notka trafiała do kolejki, a krótkie zadanie z zegara ją publikowało,
żaden przebieg nie musiałby trwać dwóch godzin i nic nie byłoby ucinane.

**To jest zmiana architektury, nie poprawka.** Zapisuję jako pomysł wart
rozważenia, nie jako rzecz do zrobienia dziś.

## 3. Osiem końcówek, których nie używamy

Z [ma2za/python-substack](https://github.com/ma2za/python-substack)
(`substack/api.py`) — publikowanie ARTYKUŁÓW przez API:

    /drafts                          tworzenie i lista szkiców
    /drafts/{id}                     edycja
    /drafts/{id}/prepublish          sprawdzenie przed publikacją
    /drafts/{id}/publish             publikacja
    /drafts/{id}/scheduled_release   PUBLIKACJA ZAPLANOWANA
    /image                           wysłanie obrazka
    /publication/users               lista subskrybentów
    /user/profile/self               własny profil

`scheduled_release` jest ciekawe przy artykule tygodniowym: dziś artykuł
powstaje i publikuje się w tym samym przebiegu, więc godzina publikacji zależy
od tego, kiedy skończy pisać.

## 4. Czego NIE warto brać

- **MCP jako warstwa** (`arthurcolle`, `alxgntv`, `aboyalejandro`) — to jest
  sposób na sterowanie botem z cudzego klienta. My mamy własny zegar i nie
  potrzebujemy człowieka w pętli; doktryna mówi wprost o zerze zatwierdzeń.
- **„Autonomiczny przebieg" z `Gaurav-UwU/daily-substack`** — sygnały →
  research → tekst → ocena → publikacja. Mamy to samo, tylko rozbudowane
  (bank pomysłów, kąty, bramki formy, sprawdzanie faktów).
- **Generatory obrazków** (`adelaidasofia` ma pięć: DALL-E, Midjourney, Canva,
  nano-banana, Pillow) — mamy własną ścieżkę graficzną i nie jest wąskim
  gardłem.
- **Obserwowalność przez Opik** (`aboyalejandro`) — mamy własny dziennik
  i tabelę `calls` z kosztem na wywołanie, czyli więcej, niż daje typowe
  śledzenie przebiegów.

## 5. Co bym zrobił, w kolejności

1. **Potwierdzanie publikacji notki przez API** gdy przeglądarka milczy.
   Zamyka 10 zmierzonych przypadków, nie rusza działającej ścieżki.
2. **Publikowanie notki przez API jako ścieżka główna** — dopiero gdy punkt 1
   pokaże, że końcówka odpowiada tak, jak zakładamy.
3. **Kolejka zamiast spania** — największa zmiana i największy zysk na czasie
   przebiegu, ale to przebudowa, nie poprawka.
