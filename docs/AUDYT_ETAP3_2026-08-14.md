# Audyt Etapu 3 — 2026-08-14

> **AKTUALIZACJA — ETAP 3 DOMKNIĘTY.** Content 20 osiągnął
> `PENDING_APPROVAL`, score `1.0`, dziewięć z dziewięciu ewaluacji `PASS`,
> `writer_attempts: 2`. Koszt tego przebiegu `0.907595 USD`. Nadal nic nie
> zostało opublikowane. Sekcja „Czego nadal nie ma" była aktualna w chwili
> pisania; domknięcie opisuje sekcja na końcu dokumentu.

Podstawa: jeden dzień pracy wyłącznie na produkcji, **26 naprawionych usterek**,
koszt **~30 USD**. `published_at` i `external_url` pozostają `NULL` wszędzie.

Audyt nie jest listą przypadków. Wszystkie 22 usterki układają się w **cztery
wzorce**, a każdy z nich ma konkretne, sprawdzalne zalecenie.

---

## Wzorzec 1 — „element nieważny" zaimplementowane jako „całość zepsuta"

**Osiem z 22 usterek.** Najkosztowniejszy wzorzec dnia.

| Bramka | Co odrzucała | Co powinna była zrobić |
|---|---|---|
| Klasyfikacja źródła | cały korpus, gdy jedno z sześciu źródeł miało nieznaną klasę | nie dopuścić tego jednego |
| Deduplikacja | cały korpus przy powtórzonym pobraniu | zwinąć duplikat (to jej nazwa) |
| Świeżość | cały korpus, gdy żadne źródło nie miało daty | odróżnić „stare" od „nieznane" |
| Rozmiar karty | całą kartę przy siódmej liczbie do zacytowania | przyciąć pole kosmetyczne |
| Odcisk reviewera | całą recenzję przy jednym skróconym hashu | przyjąć skrót wartości, którą sami podaliśmy |
| Nowość (dispatcher) | temat jako duplikat własnego draftu | wykluczyć własny dorobek tematu |
| Nowość (pipeline) | to samo, w drugim miejscu | jw. |
| Dzierżawa | całą pracę po przekroczeniu 60 s | termin dłuższy niż praca |

**Zalecenie 1.** Każda bramka musi rozróżniać *odrzucenie elementu* od
*odrzucenia zbioru*. Reguła operacyjna: jeśli po odrzuceniu wadliwego elementu
pozostała część nadal spełnia próg, przebieg ma trwać, a wada ma zostać
zapisana jako `finding`, nie jako `reason`. Odwrotność jest dopuszczalna tylko
tam, gdzie element jest **konstytutywny** — jak brak lineage dla twierdzenia,
które writer ma zacytować.

---

## Wzorzec 2 — budżety dobrane do mniejszej pracy, niż system dziś wykonuje

**Pięć usterek.** Każda ujawniła się dopiero, gdy coś urosło.

- `max_tokens=1500` dla generatora tematów — myślenie zjadło budżet, JSON się urwał
- `timeout=60 s` zamrożony w intencie syntezy — wystarczał przy 3 źródłach, nie przy 6
- `lease=60 s` dla artykułu — krótszy niż writer i reviewer razem
- `max_output=8192` dla reviewera — za mało, gdy artykuł urósł z 470 do 1244 słów
- cap A1 z enuma `{0.3, 0.5, 0.6, 1.0}` — zabijał wywołanie kosztujące 1.17

**Zalecenie 2.** Każdy limit czasu, tokenów i kosztu musi mieć zapisane
**w komentarzu przy definicji**, jakiej wielkości pracy dotyczy i co go
przekracza. Dwa sprzężenia, które dziś nie były nigdzie zapisane i kosztowały
najwięcej:

- **długość artykułu ↔ budżet wyjścia reviewera** — reviewer rozlicza każdy
  segment osobno, więc jego wyjście rośnie liniowo z tekstem;
- **liczba źródeł w korpusie ↔ czas syntezy** — sześć źródeł nie mieści się
  w terminie dobranym do trzech.

Podniesienie `max_words` bez sprawdzenia reviewera złamie go ponownie.

---

## Wzorzec 3 — jedna reguła w dwóch miejscach, rozjechana

**Cztery usterki**, w tym jedna wprowadzona w trakcie audytowanej sesji.

- werdykt reviewera (`REWRITE_ONCE`) kontra agregat C2 (`BLOCK`) — na tych samych dowodach
- nowość w dispatcherze kontra nowość w pipeline — dwie różne definicje wykluczenia
- tożsamość segmentu w parserze kontra w quality gate — tolerancja dodana tylko w jednym
- ścieżka REVIEW-ONLY bramkowana na decyzji reviewera, inline na agregacie

**Zalecenie 3.** Projekt ma już wzorzec rozwiązania: `classification_contract_error`
został świadomie wyciągnięty do jednego predykatu (ADR-148). Tę samą operację
należy wykonać dla **tożsamości segmentu** i dla **nowości redakcyjnej**.
Dopóki reguła istnieje w dwóch egzemplarzach, rozjazd jest kwestią czasu, a nie
uwagi programisty — dowodem jest to, że popełniłem go dziś sam, znając wzorzec.

---

## Wzorzec 4 — brak tolerancji na potknięcie (POZYCJA NUMER JEDEN)

To jest bezpośrednia odpowiedź na wymaganie „agent ma się nie zacinać".

```
input_sha256  UNIQUE   → jedna karta  = jeden job content
job_id        UNIQUE   → jeden job    = jedna zgoda
zgoda jednorazowa      → po awarii nie ma czym wznowić
nowość                 → drugiej karty dla tematu nie będzie
```

Każde ogniwo osobno jest rozsądne. Razem znaczą, że **dowolna awaria techniczna
kasuje opłacony research bezpowrotnie**. Dziś zdarzyło się to sześć razy:
wielokropek w odcisku, utrata dzierżawy, urwana recenzja, niesklasyfikowane
źródło, duplikat pobrania, rozjazd tożsamości.

**Zalecenie 4a (projekt gotowy).** Dopuścić kolejną zgodę dla tej samej,
niedokończonej pracy, gdy poprzednia próba zakończyła się **awarią techniczną**,
a nie werdyktem redakcyjnym. Rozróżnienie już istnieje w kodzie i jest
sprawdzone: warunek `claim_coverage_complete` odróżnia „reviewer orzekł" od
„reviewer nie odpowiedział". Wymaga migracji na `content_provider_approvals`
(dziś `job_id UNIQUE`).

**Zalecenie 4b.** Wiązanie lineage ma **odfiltrować niepowiązane twierdzenia
przed zapisem czegokolwiek**, przenumerować i dopiero zapisać — zamiast
odrzucać całą kartę. Inwariant „każde twierdzenie w karcie PROCEED ma lineage"
zostaje nietknięty, a karta z pięcioma dobrymi twierdzeniami przeżywa szóste
niecytowalne. To jedyna pozycja, przy której **nie wolno** rozluźnić bramki —
tylko przenieść filtrowanie przed zapis.

---

## Co zostało naprawione i zweryfikowane na żywo

| Element | Dowód |
|---|---|
| Generowanie tematu | 3 udane przebiegi, `SETTLED` |
| Discovery A1 | 10 kandydatów zamiast 6 |
| Pobrania | **10/10** i 9/10 — nie „połowa 403" |
| Korpus | 6 źródeł / 57 739 znaków zamiast 3 |
| Karta `PROCEED` | 4 kolejne (23–26) |
| Writer | 1 183–1 244 słowa zamiast 470 |
| Reviewer | `SUCCESS` po skróceniu odcisku |
| Rezerwacja | rozlicza się do `SETTLED`, nie wisi |

## Czego nadal nie ma

- **żaden artykuł nie osiągnął `PENDING_APPROVAL`**
- **pętla rewrite nie została potwierdzona na produkcji** — `writer_attempts: 2`
  nie wystąpiło ani razu; ADR-149 jest potwierdzony wyłącznie testami
- Etapy 5–7 (publikacja, notki, komentarze, statystyki) nie istnieją

## Ocena jakości artykułu

Draft z karty 23 (*„The Button Is Not an Input"*, 470 słów) był **dobry**:
otwarcie konkretne i sprawdzalne, mechanizm nazwany wcześnie, kontrargument
z realną wagą, ograniczenia powiedziane raz własnym głosem, zamknięcie
oddające czytelnikowi narzędzie zamiast streszczenia. Draft z karty 26
(1 183 słowa) trzymał ten sam poziom przy pełnej długości substackowej.

Warstwa rzemiosła dopisana do promptu writera **działa i jest widoczna
w tekście**. Styl dociera do modelu (5 fragmentów korpusu, profil pozytywny
i negatywny) — to zostało zweryfikowane osobno, więc płaskość wcześniejszych
tekstów nie wynikała z braku wytycznych.

## Koszt

`20.22 USD` w ciągu dnia, `11.90 USD` w tej sesji. Zmarnowane bezpowrotnie:
`0.14` na zgadywanie `max_tokens` przy istniejącym dowodzie w bazie,
`~0.78` na korpus tematu 96, oraz sześć kart spalonych przez awarie
techniczne. Trzy pozycje pozostają w `NEEDS_RECONCILIATION`.


---

# Domknięcie etapu 3

## Co przeszło

Pełny łańcuch, jeden przebieg, bez ręcznej interwencji:

`temat → discovery → pobranie → karta PROCEED → writer → reviewer →
przepisanie → decyzja polityki → PENDING_APPROVAL`

| Fakt | Wartość |
|---|---|
| Content | `20`, karta badawcza `35` |
| Status | `PENDING_APPROVAL`, score `1.0` |
| Ewaluacje | 9/9 `PASS` na próbie 2 |
| Pętla rewrite | **potwierdzona na produkcji** (`writer_attempts: 2`, 18 ewaluacji) |
| Koszt | `0.907595 USD` |
| Publikacja | `published_at` i `external_url` `NULL` w **całej** bazie |
| Baza | `integrity_check: ok`, 0 naruszeń FK |

Tym samym znika pozycja „pętla rewrite nie została potwierdzona na produkcji"
z sekcji „Czego nadal nie ma".

## Cztery usterki domykające, wszystkie w bramce spójności

Ostatnie cztery przebiegi blokowała **ta sama warstwa** — nie reviewer.
Reviewer wydawał werdykty wewnętrznie spójne; odrzucała je nasza lokalna
kontrola. Liczba blokowanych segmentów w kolejnych przebiegach:
**12 → 8 → 2 → 0**.

1. **`BLOCK` na klasie nie-faktycznej.** Model używał `BLOCK` w znaczeniu
   „to nie jest fakt", z uzasadnieniami „Transition", „Framing title". To
   sprzeczność, nie werdykt: proza nie twierdzi niczego sprawdzalnego, więc nie
   ma czego nie poprzeć. Normalizowane w parserze. Prompt nie wystarczył —
   trzy próby.
2. **Definicja prozy jako listy siedmiu słów.** Zdanie uchodziło za prozę
   tylko wtedy, gdy zaczynało się od „now/first/next/instead…" **i nie
   zawierało żadnego czasownika**. Praktycznie żadne prawdziwe zdanie tego nie
   spełnia.
3. **Test „czy jest cyfra".** Fałszywe trafienia na `The 9 percent is the part
   that gets quoted` i na akapicie o granicach dowodów cytującym własny rok.
   **Usunięty** — podłoga liczbowa pyta o rzecz właściwą: czy ta liczba jest
   w korpusie.
4. **Test „czy jest nazwa instytucji".** Fałszywe trafienie na
   `the Highway Capacity Manual` — dokument obecny w korpusie przez cały czas.
   **Nie usunięty**, bo empirycznie sprawdzone, że nic innego go nie zastąpi:
   po jego wyłączeniu zdanie *„The Federal Railroad Administration requires a
   backup battery"* zaklasyfikowane jako proza przeszło całą bramkę. Zamiast
   tego uczyniony świadomym korpusu.

## Wzorzec 3 wystąpił jeszcze cztery razy

Wzorzec „jedna reguła w dwóch miejscach" opisany wyżej powtórzył się tego
samego dnia **cztery razy**, w tym **dwa razy z winy poprawek pisanych już po
napisaniu tego audytu**: pozycje 3 i 4 powyżej to duplikaty kontroli
istniejących sto linii niżej w tym samym pliku, tyle że gorzej postawione.

Wniosek mocniejszy niż pierwotny: **zanim dołożysz kontrolę, sprawdź, czy
odpowiedzi na to pytanie nie udziela już coś, co ma dostęp do korpusu.**
Kontrola porównująca z rzeczywistym materiałem zawsze bije heurystykę
porównującą z alfabetem.

## Budżet wyjścia reviewera — naprawa jest zapasem, nie lekarstwem

Content 18 padł na `REVIEWER_RESPONSE_NOT_JSON` przy `output_tokens: 8192`,
czyli dokładnie na suficie. Przyczyną **nie była długość artykułu**: content 17
i 18 mają po ~1 115 słów, ale 48 zdań wobec 64. Reviewer wypisuje jeden wpis na
zdanie, więc styl krótkich zdań kosztuje budżet.

Naprawa: trzy z siedmiu pól nie niosły informacji, której reviewer nie
dostał — `segment_fingerprint` (bo `segment_id` **kończy się** tymi 16 znakami),
`contains_external_fact` (wymuszone przez klasyfikację) i puste `evidence_ids`
dla klas nie-faktycznych. Wpis prozy stracił 43%, faktyczny ~24%.

**To nadal jest zapas.** Wyjście rośnie liniowo z liczbą zdań, sufit `8192`
jest stały i **kwalifikowany** — podniesienie wymaga nowej płatnej
kwalifikacji. Trwałe rozwiązanie: podział rozliczenia na dwa wywołania
z całym artykułem w obu, `document_review` tylko w pierwszym. Koszt ~0.15 USD
za artykuł.

## Otwarte, uporządkowane po wartości

1. **Zużyta karta po awarii technicznej** (niezmieniony punkt 4a). Każde
   nieudane podejście do treści bezpowrotnie kasuje opłaconą kartę, bo
   `content_frozen_inputs.input_sha256` jest globalnie `UNIQUE`, a hasz nie
   zawiera `job_id`. Podniosło to koszt iteracji z ~0.9 do ~2.1 USD i zrobiło
   to **dwa razy dzisiaj**. Projekt: kolumna `superseded_at` + częściowy indeks
   unikalny obejmujący wyłącznie żywe wiersze — niezmiennik „jedno kanoniczne
   wejście na jedną pracę w toku" zostaje, powtórka po porażce staje się
   legalna.
2. **Podział recenzji na dwa wywołania** — patrz wyżej.
3. **Bramka powtórek myli się w obie strony.** Sprawdzone empirycznie na
   przykładzie właściciela: przemianowany tytuł z **tą samą** tezą
   *przechodzi* (teza 0.63 przy progu 0.70, bo pytanie doklejone do tezy
   rozcieńcza podobieństwo), a **inne miasto** z tą samą tezą *blokuje*
   (tytuł 0.74 przy progu 0.72 — nazwa własna liczona jak każde inne słowo).
   Brakuje też furtki na nowe informacje: powrót do tezy, bo pojawiły się nowe
   dane, jest blokowany bezwarunkowo.
4. **Dług prowenancyjny.** Prompt reviewera zmieniał się dziś kilkakrotnie
   w obrębie tej samej wersji `production_article_reviewer_opus_v3`. Semantyka
   oceny się nie zmieniła, ale przed pierwszą publikacją wersję należy podbić.

## Czego ten audyt nie stwierdza

Nie stwierdza, że etap 3 jest niezawodny. Stwierdza, że **przeszedł raz, do
końca, bez ustępstw wobec bramki jakości**. Jeden przebieg to nie jest
statystyka. Trzy rzeczy z listy otwartych wrócą przy dostatecznie odmiennym
artykule i żadna z nich nie została jeszcze zamknięta.
