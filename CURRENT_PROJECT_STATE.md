# CURRENT_PROJECT_STATE — Nothing Is Accidental Agent

> **BIEŻĄCY SNAPSHOT 2026-08-09 — PRE-C5 QUESTION SEMANTIC BOUNDARY CONTRACT = `PRE-C5 QUESTION SEMANTIC BOUNDARY CONTRACT — REPAIR CANDIDATE COMPLETE — AWAITING RE-REVIEW`; ETAP 3 = `IN PROGRESS` (ADR-123).** Niezależny review poprzedniego kandydata wydał `REJECT — MAJOR`, ponieważ ochrona `NON_FACTUAL_PROSE` używała `text.endswith("?")` i przepuszczała znacznik pytania poprzedzający dalszą interpunkcję. Jedyna fala naprawcza zastąpiła tę końcówkową bramkę prostym kontraktem obecności `?` albo `？` w dowolnym miejscu segmentu; nie dodano parsera, blacklisty, whitelisty ani analizy semantycznej. Reviewer nadal odpowiada za `EVIDENCE_GROUNDED_FACT` lub `ARGUMENT_OR_INFERENCE`, a `SEMANTIC REVIEWER TRUST BOUNDARY` pozostaje bez zmian. Dowód implementera: question module `220/220`, claim accounting `38/38`, PRE-C5 `328/328`, affected `399/399`, full suite i exact collect `2322/2322`, exact duplicates `0`; lokalny sweep `216` kombinacji ma `0` leaks; `compileall app tests scripts` i `git diff --check` PASS. Produkcja pozostaje byte-identical na `0020_topic_generation_lifecycle` (SHA-256 `91f593923f3ca36cd8d2c816219e551b0ff231bc3149d6d584b025465f656a1f`, `700416 B`, integrity `ok`, FK `0`, WAL `0 B`, SHM `32768 B`, journal brak), korpus stylu również byte-identical (`0b05cefa6701e6447c44810b686828a83c19ca7ffb29066778a13c24207acb1d`, `57561 B`). Koszt `0.000000 USD`; zero sieci/API/SDK/browsera/publikacji i tylko temp DB do zapisów. Bez stage/commita/pushu/PR/merge; PRE-C5 QUALITY GATE i C5 pozostają `NOT STARTED`.

> **BIEŻĄCY SNAPSHOT 2026-08-09 — PRE-C5 CLAIM ACCOUNTING & COST-CAP = `PRE-C5 CLAIM ACCOUNTING & COST-CAP CANDIDATE COMPLETE — AWAITING INDEPENDENT REVIEW`; ETAP 3 = `IN PROGRESS` (ADR-122).** Ten blok jest nadrzędny wobec niższych snapshotów. PRE5-RR-01 zostało zamknięte na poziomie kandydata implementera przez obowiązkowe claim accounting każdego deterministycznego segmentu ARTICLE: dokładnie jeden audytowalny wynik niezależnego review, jawna klasyfikacja, reason/outcome, evidence dla faktu tylko z frozen Research Package, pełne pokrycie oraz trwały ledger także przy PASS. Brak/awaria reviewera, brakujący/duplikowany/nadmiarowy rekord, nieznana lub niespójna klasyfikacja i fakt ukryty jako inferencja/non-factual kończą się blokadą; nie istnieje produkcyjny default reviewer ani heurystyczna ścieżka PASS. Paid CONTENT z kosztem ponad rezerwację w jednym commicie zapisuje jedno usage i koszt, ustawia attempt `NEEDS_RECONCILIATION`, content/job `NEEDS_VERIFICATION`, run `STOPPED`; reaper później niczego nie dopisuje. Nie powstała nowa migracja ponad `0026`. Walidacja: nowy moduł `38/38`, PRE-C5 `108/108`, zakres `436/436`, full `2102/2102`; koszt `0.000000 USD`. Produkcyjna `data/agent.db` nadal byte-identical na `0020` (SHA `91f593923f3ca36cd8d2c816219e551b0ff231bc3149d6d584b025465f656a1f`, `700416 B`, WAL `0 B`, SHM `32768 B`, journal brak), raw style corpus nadal SHA `0b05cefa6701e6447c44810b686828a83c19ca7ffb29066778a13c24207acb1d`, `57561 B`. Zero sieci/API/SDK/browsera/publikacji, zapisów do produkcji i operacji Git. To nie jest niezależny approval: PRE-C5 QUALITY GATE i C5 pozostają `NOT STARTED`, live readiness nie jest ogłoszone.

> **BIEŻĄCY SNAPSHOT 2026-08-09 — Etap 3 / PRE-C5 TECHNICAL GATES = `PRE-C5 TECHNICAL GATES REPAIR CANDIDATE COMPLETE — AWAITING RE-REVIEW`; ETAP 3 = `IN PROGRESS` (ADR-121).** Niezależny review kandydata PRE-C5 wydał `REJECT — MAJOR`; ta jedyna dozwolona fala naprawcza zamyka wyłącznie cztery autorytatywne findings oraz otwarty gate paid CONTENT z pierwotnego zakresu. PRE5-RV-01: klasyfikacja twierdzeń (factual/opinion/stylistic) i wymóg ugruntowania każdego faktu zewnętrznego w zamrożonym Research Package, plus obowiązkowy niezależny reviewer dla ARTICLE (brak = fail-closed `CONTENT_INDEPENDENT_REVIEW_UNAVAILABLE`). PRE5-RV-02: `source_admission_policy_v1` działa w rzeczywistym runtime E3 i jako nieomijalna podłoga w transakcji finalizacji. PRE5-RV-03: `syndication_of` kanonizowane jak każdy URL, łańcuchy zwijane, nieparsowalna deklaracja fail-closed. PRE5-RV-04: trwała bariera in-flight — `external_effect_started_at` stemplowane PRZED wywołaniem, recovery normalizuje attempt do `NEEDS_RECONCILIATION` i terminalizuje `NEEDS_VERIFICATION` zamiast drugiego calla. Paid CONTENT: nowy `CONTROLLED_PROVIDER_PIPELINE` z jawną autoryzacją, realną rezerwacją i wyłącznie fake callerem; ochrona offline nietknięta. Migracja `0026_controlled_provider_content` była konieczna, bo trzy triggery SQL pinowały `execution_mode`/`provider_enabled`; rozszerza dokładnie te predykaty, bez nowych tabel. Runtime wymaga `0026`; produkcyjna `data/agent.db` pozostaje byte-identical na `0020_topic_generation_lifecycle` (SHA-256 `91f593923f3ca36cd8d2c816219e551b0ff231bc3149d6d584b025465f656a1f`, `700416 B`, integrity `ok`, FK `0`, WAL `0 B`, SHM `32768 B`, journal brak) i NIE została zmigrowana. Dowód: repair regression `31/31`, dotknięty zakres `420/420`, pełna suita `2064/2064`, collect `2064`, unique (exact) `2064`, duplikaty (exact) `0`, `compileall` i `git diff --check` PASS, koszt `0.000000 USD`; zero sieci/API/SDK/browsera/publikacji. Prywatny korpus stylu nietknięty (SHA `0b05cefa…`, `57561 B`). Nie implementowano `claude-opus-5`/`claude-sonnet-5`/`claude-fable-5`, realnego composition rootu Anthropic, cen, model IDs ani dynamicznego routingu — to osobna fala po APPROVE. Brak stage/commita/pushu/PR/merge. PRE-C5 QUALITY GATE i C5 pozostają `NOT STARTED`; gotowość C5 i live readiness NIE są ogłoszone; następny krok to dokładnie jeden niezależny re-review.

> **HISTORYCZNY SNAPSHOT 2026-08-08 — Etap 3 / PRE-C5 TECHNICAL GATES = `PRE-C5 TECHNICAL GATES CANDIDATE COMPLETE — AWAITING INDEPENDENT REVIEW`; ETAP 3 = `IN PROGRESS` (ADR-120).** C1/C2 = `CLOSED`; C3 = `C3 FULLY CLOSED — DOCUMENTATION MERGED AND VERIFIED`; C4 = `C4 FULLY CLOSED — DOCUMENTATION MERGED AND VERIFIED` i NIE było otwierane; C5 oraz PRE-C5 QUALITY GATE pozostają `NOT STARTED`. Jedna skonsolidowana fala techniczna zamknęła sześć potwierdzonych wymagań przed C5: (1) niezależna ocena draftu zastąpiła self-report writera — nowy `app/content/quality_gate.py`, a `FakeDraft`/`ProviderDraftPayload` straciły fail-open defaults; (2) deterministyczny `source_admission_policy_v1` w `app/research/source_admission.py` (niezależność właściciela domeny, dedup syndykacji, klasy PRIMARY/SUPPORTING/ORIENTATION z twardym capem Wikipedii, freshness tylko dla twierdzeń wrażliwych na czas, pokrycie każdego claimu, nieznana klasyfikacja = fail-closed); (3) real-compatible E3 zapisuje ten sam authoritative `evidence_source_lineage` co offline E2-A, więc `PROCEED` przechodzi `prepare_content_job`; (4) po zwróceniu usage każda ścieżka terminalizuje jawnie zamiast wyjątku; (5) typowany `heartbeat_content_execution` przedłuża lease CONTENT przez istniejący generation fence; (6) ARTICLE dostaje 3–5 krótkich przykładów stylu z trwałymi example IDs, hashami fragmentów i fingerprintem zestawu. Migracja `0025_evidence_research_content_lineage` rozszerza wyłącznie predykat wyłączności istniejącego triggera; kod wymaga `0025`, a produkcyjna `data/agent.db` pozostaje byte-identical na `0020_topic_generation_lifecycle` (SHA-256 `91f593923f3ca36cd8d2c816219e551b0ff231bc3149d6d584b025465f656a1f`, `700416 B`, 20 migracji, integrity `ok`, FK `0`, WAL `0 B`, SHM `32768 B`, journal brak, pięć flag fail-closed bez zmian) i NIE została zmigrowana. Dowód: dotknięty zakres `389/389`, pełna suita `2033/2033`, collect `2033`, 0 dup/skip/xfail/errors, `compileall` i `git diff --check` PASS, koszt `0.000000 USD`; zero realnego API/SDK/sieci/browsera/publikacji/controlled-live i zero migracji produkcji. Prywatny korpus stylu nietknięty (SHA `0b05cefa6701e6447c44810b686828a83c19ca7ffb29066778a13c24207acb1d`, `57561 B`); do promptu trafia `3,83 %` korpusu. Osiem wcześniejszych P2 pozostaje otwartych, a P2-1/P2-2/P2-3 nadal są gate'ami przed C5. Brak niezależnego review, stage, commita, pushu, PR i merge; PRE-C5 QUALITY GATE NIE jest zamknięty, a gotowość C5 NIE jest ogłoszona.

> **HISTORYCZNY SNAPSHOT 2026-07-24 — Etap 3 / WAVE C4 = `C4 CODE MERGED — POST-MERGE VERIFIED — AWAITING DOCUMENTATION SYNC MERGE`; ETAP 3 = `IN PROGRESS`.** C1/C2 = `CLOSED — MERGED AND POST-MERGE VERIFIED`; C3 = `C3 FULLY CLOSED — DOCUMENTATION MERGED AND VERIFIED`; kod C4 jest zmergowany i zweryfikowany post-merge, a formalne zamknięcie C4 czeka wyłącznie na niezależny review i merge tego dokumentacyjnego PR; C5 i PRE-C5 QUALITY GATE = `NOT STARTED`. Rozdzielony łańcuch dowodu C4: implementacja offline, provider-independent decision boundary w `PolicyEngine` → testy implementera → pełny niezależny review `APPROVE WITH P2` (0 MAJOR / 0 MINOR) i osobny review integralności PR `APPROVE WITH P2` → commit implementacji `6a97620048d1099b9c1f0da29ec343ae12a54559` (`feat: add autonomous content decision layer`) → PR #26 → merge commit `7eb93ba93b131d0a9a3c33e7d8495500afaa721f` (rodzice `b6abd60583b371b4501551735f4d67dffd7f2944` poprzedni `main` i `6a97620048d1099b9c1f0da29ec343ae12a54559` head C4) → synchronizacja `main` wyłącznie ff-only. Post-merge na `main`: C4 `23/23`, C2+C3 `48/48`, pełna suita `1994/1994`, collected/exact case-sensitive unique `1994`, duplikaty `0`, zero failures/skipped/xfail/errors, `compileall` i `git diff --check` PASS; koszt `0.000000 USD`. Kod na `main` wymaga schema `0024_autonomous_content_decision`; produkcyjna `data/agent.db` pozostaje byte-identical na `0020_topic_generation_lifecycle` (SHA-256 `91f593923f3ca36cd8d2c816219e551b0ff231bc3149d6d584b025465f656a1f`, `700416 B`, 20 migracji, integrity `ok`, FK `0`, WAL `0 B`, SHM `32768 B`, journal brak), nie została zmigrowana, a runtime pozostaje wobec niej fail-closed; tabela `autonomous_decisions` jest w produkcji nieobecna. Nie wykonano realnego API/SDK, prawdziwego artykułu, controlled-live, browsera, publikacji ani migracji produkcji. Osiem P2 pozostaje otwartych (paid usage/terminalizacja kosztu >0; CONTENT heartbeat/lease; evaluations zależne od self-report; sentinel migracji 0023; obsługa SQLite URI w helperze migracyjnym; flake heartbeat pod obciążeniem; provider/model/pricing/availability `UNVERIFIED`; Notes profile `PROVISIONAL`), a P2-1/P2-2/P2-3 pozostają wymaganymi gate'ami przed C5. Sześć nieblokujących ustaleń review C4 (trzy z pełnego review, trzy z review integralności) pozostaje zapisami historycznymi/review, nie nowymi blockerami. `C4 FULLY CLOSED` NIE jest jeszcze ogłoszony.

> **HISTORYCZNY SNAPSHOT 2026-07-24 — Etap 3 / WAVE C4 (kandydat przed review/merge; zastąpiony przez blok wyżej) = `C4 CANDIDATE COMPLETE — AWAITING INDEPENDENT REVIEW`; ETAP 3 = `IN PROGRESS` (ADR-119).** Autorytatywny stan wejściowy właściciela: C1/C2 zamknięte; C3 = `C3 FULLY CLOSED — DOCUMENTATION MERGED AND VERIFIED`; C5 i PRE-C5 QUALITY GATE = `NOT STARTED`. C4 jest offline i provider-independent: `PolicyEngine` odczytuje aktualny trwały account mode/autonomy/policy, dokładny current draft+lineage, komplet dziewięciu evaluations i wersjonowane progi; wynik wiąże input/decision fingerprint, reason, score, actor i fence w append-only `autonomous_decisions`, a jedna transakcja finalizuje human-required `PENDING_APPROVAL`, autonomous `APPROVED|REJECTED`, drift `NEEDS_VERIFICATION` albo technical `FAILED`. LEVEL_1 nie auto-approve; LEVEL_3 jest przetestowany tylko offline. Kod wymaga `0024_autonomous_content_decision`; produkcja pozostaje byte-identical na `0020` (SHA `91f593923f3ca36cd8d2c816219e551b0ff231bc3149d6d584b025465f656a1f`, `700416 B`, 20 migracji, integrity `ok`, FK `0`, WAL `0 B`, SHM `32768 B`, journal brak). Dowód: C4 `23/23`, full/collect/unique `1994/1994`, 0 dup/skip/xfail/errors, koszt `0.000000 USD`; zero realnego API/SDK/sieci/browsera/publikacji/controlled-live i zero migracji produkcji. Osiem P2 pozostaje otwartych, P2-1/P2-2/P2-3 nadal gate'ami C5. Brak niezależnego review, stage, commita, pushu, PR i merge; C4 nie jest zamknięte.

> **HISTORYCZNY SNAPSHOT 2026-07-24 — Etap 3 / WAVE C3 przed formalnym doc-merge; zastąpiony przez bieżący C4/ADR-119 wyżej.** Status był wtedy `C3 CODE MERGED — POST-MERGE VERIFIED — AWAITING DOCUMENTATION SYNC MERGE`, a checkpoint wynosił `1971/1971`. Autorytatywny stan właściciela dla C4 ustanowił później `C3 FULLY CLOSED — DOCUMENTATION MERGED AND VERIFIED`.

> **HISTORYCZNY SNAPSHOT 2026-07-23 — Etap 3 / WAVE C3 (kandydat przed review/merge; zastąpiony przez blok wyżej):** C1 i C2 = `CLOSED — MERGED AND POST-MERGE VERIFIED`; C3 = `C3 CANDIDATE COMPLETE — AWAITING INDEPENDENT REVIEW` (ADR-118); C4–C5 = `NOT STARTED`; Etap 3 = `IN PROGRESS`. Kandydat C3 istnieje wyłącznie jako niezacommitowane zmiany robocze na bazowym `main`/`origin/main` `54c761d0d6c1b7b7402d89e9c24db169b694f00a`. Dodaje provider-ready kontrakt i adapter z fake SDK/caller, deterministic prompt, typed result/error, rozdzielony routing oraz migrację temp-only `0023_provider_ready_writer`; wspierany production composition root nadal odmawia. Walidacja implementera: C3 `26/26`, regresja `463/463`, full/collect/exact unique `1971/1971`, duplikaty/skipped/xfail/errors `0`, compile/diff PASS. Brak niezależnego review, stage/commita/PR/merge, produkcyjnej migracji, realnego API, controlled-live, prawdziwego artykułu, approval UI, browsera i publikacji. Produkcyjna `data/agent.db` pozostaje na `0020_topic_generation_lifecycle`, a runtime wymagający `0023` jest wobec niej fail-closed; koszt C3 `0.000000 USD`.

> **BIEŻĄCY SNAPSHOT 2026-07-23 — Etap 3:** C1 = `CLOSED — MERGED AND POST-MERGE VERIFIED`; C2 = `CLOSED — MERGED AND POST-MERGE VERIFIED` (ADR-116/117); C3–C5 = `NOT STARTED`; cały Etap 3 pozostaje `IN PROGRESS`. Commit C2 `86e284e997607b54107cbb42deb35410a38bd0e4` został zmergowany przez PR #22 jako merge commit `458a86e9d86c9f09fd1f8ec17d3e8134aa44155a` po pełnym review i whitespace re-review `APPROVE WITH MINOR/P2`. Post-merge: C2 `22/22`, zatwierdzona regresja `384/384`, pełna suita `1945/1945`, collected/exact case-sensitive unique `1945`, duplikaty `0`, zero failures/skipped/xfail/errors, `compileall` i `git diff --check` PASS. Kod na `main` wymaga schema `0022_offline_content_pipeline`; produkcyjna `data/agent.db` pozostaje na `0020_topic_generation_lifecycle`, nie została migrowana, a runtime pozostaje wobec niej fail-closed. C2 jest wyłącznie offline/fake i kończy w `PENDING_APPROVAL`; brak realnego API/SDK/callera, prawdziwego artykułu, controlled-live, approval UI, browsera i publikacji; koszt `0.000000 USD`.

> **HISTORYCZNY SNAPSHOT 2026-07-23 — ETAP 3 / WAVE C1 = `C1 CLOSED — MERGED AND POST-MERGE VERIFIED` (ADR-114/115); ETAP 3 = `IN PROGRESS`; C2 = `NOT STARTED`.**
>
> - **Assurance C1:** implementacja trwałego fundamentu została wykonana, a testy implementera zaliczone. Pierwszy niezależny review zakończył się `REJECT — MAJOR`; wykonano dokładnie jedną falę naprawczą, po której niezależny re-review wydał `APPROVE WITH MINOR/P2`.
> - **Git i weryfikacja:** zatwierdzony commit `9197500e5bd881c4c9e9c135451bfa63b443f560` (`feat: add durable content foundation for stage 3`) został zmergowany przez PR #20 `Stage 3 C1: durable content foundation`. Merge commit to `8b5a2f7d835f0c04bec1f914e1cc255906991566`. Post-merge na `main`: foundation+migracja `69/69`, pełna suita `1923/1923`, collected i exact case-sensitive unique node IDs `1923`, dokładne duplikaty `0`, zero skipped/xfail, `compileall` i `git diff --check` = PASS.
> - **Zakres formalny:** C1 jest formalnie zamknięte; Etap 3 pozostaje `IN PROGRESS`. C2 nie zostało rozpoczęte. Planner, writer, Article Brief generation, generowanie artykułu/Note, audyty, rewrite, controlled-live contentu i publikacja nadal nie istnieją.
> - **Produkcja:** migracja `0021_durable_content_foundation` istnieje w `main`, lecz produkcyjna `data/agent.db` pozostaje na `0020_topic_generation_lifecycle` i nie była migrowana. Runtime aktualnego `main` pozostaje fail-closed wobec produkcyjnego schematu `0020` do czasu osobno autoryzowanej migracji. Nie powstał realny artykuł i nie wykonano controlled-live contentu ani publikacji.
> - **Nieblokujące P2:** produkcja wymaga osobno autoryzowanej migracji `0020→0021`; legacy `content_items.DRAFT` bez C1 identity pozostaje compatibility row; casefold `hidden/HIDDEN` nie jest duplikatem pytest ani utratą pokrycia.

> **HISTORYCZNY STAN FORMALNY 2026-07-23 — zastąpiony przez bieżący snapshot C4 wyżej — ETAP 2 = `CLOSED` (ADR-113).** Zmergowany durable `TOPIC_GENERATION` został zweryfikowany jednym kontrolowanym live na `main` `a4e314ff1d9a9ac6bd24fdcdf159ca3e24356916`, a niezależny post-live review wydał `APPROVE WITH MINOR/P2 — ETAP 2 MAY BE FORMALLY CLOSED`. L1 pozostaje `ACTIVE`; LEVEL_3 = `NOT CONFIRMED`; publikacja = `NOT VERIFIED`. Właściciel później rozpoczął Etap 3; jego C1 i C2 są zamknięte, C3–C5 pozostają `NOT STARTED`, a cały Etap 3 pozostaje `IN PROGRESS`. Każde kolejne live lub publikacja nadal wymaga osobnej zgody. P2 dotyczące proceduralnych sidecarów WAL/SHM, minimalnego JSON raportu oraz historycznego legacy ledgeru poza tym runem pozostają w backlogu i nie blokują zamknięcia.

> **HISTORYCZNY CHECKPOINT LIVE PRZED FORMALNYM ZAMKNIĘCIEM — 2026-07-23, ADR-112.** Na zmergowanym `main` `a4e314ff1d9a9ac6bd24fdcdf159ca3e24356916` właściciel autoryzował dokładnie jeden job dla `nothing_is_accidental`: `claude-sonnet-5`, `candidate_count=2`, `max_tokens=1500`, timeout `60 s`, `max_retries=0`, `max_web_searches=0`, cap `0.024303 USD` i approval ważny 13:59.723. Job `topic-generation-037eb2d3db158a70791e30064ad95403`, request `…:topics:1` i run `4cf8c448-5358-43c6-9d47-e5daf6d0f040` wykonały dokładnie jeden HTTP 200. Usage: 219 input / 1269 output / 0 cache / 0 search; koszt `0.013128 USD`. Attempt #1 = `SETTLED`, job = `DONE`, run = `SUCCESS`, approval = consumed, dwa generated topics, wybrany topic `21`, reconciliation = false. Pięć flag wróciło do dokładnego fail-closed snapshotu; browser, Fetch, publikacja, maintenance i retry nie zostały uruchomione. Po operacji DB: schema `0020_topic_generation_lifecycle`, 20 migracji, SHA-256 `91f593923f3ca36cd8d2c816219e551b0ff231bc3149d6d584b025465f656a1f`, `700416 B`, integrity `ok`, FK `0`; counts `jobs=12`, `runs=21`, `topics=21`, `provider_attempts=8`, `model_usage=26`, `topic_generation_approvals=1`, `generated_topics=2`. Pozostały nieusuwane sidecary: pusty WAL `0 B` i SHM `32768 B`; quiescence `PASS`, locków brak. W tym checkpointcie Etap 2 pozostawał `IN PROGRESS`; stan został zastąpiony przez formalną decyzję ADR-113. Pojedynczy sukces nadal nie potwierdza LEVEL_3.

> **HISTORYCZNY SNAPSHOT PRZED MERGE PR #18 I PRZED LIVE — 2026-07-22, ADR-111.** Publiczny `controlled-live-topic-generation` miał status `CANDIDATE COMPLETE — READY FOR INDEPENDENT REVIEW`; collect/full wynosił `1854/1854`, a controlled-live nie był jeszcze wykonany. Ten stan został zastąpiony przez merge PR #18 i wynik ADR-112 wyżej.

> **HISTORYCZNY SNAPSHOT PRZED MERGE PR #17 — F1-BLOCK-01 `TOPIC_GENERATION` SETTLED recovery (ADR-110).** Kandydat domknął settled recovery i osiągnął `1821/1821`; wzmianki o kandydackiej 0020 oraz produkcji 0019 opisują chwilę przed merge i zostały zastąpione przez bieżący blok wyżej. Zachowano je jako historię, nie aktualny stan.

> **AKTUALIZACJA FORMALNA 2026-07-19 — PRODUCTION SCHEMA MIGRATION ORCHESTRATOR = `CLOSED — APPROVED WITH MINOR/P2`; production migration readiness = `VERIFIED IN MERGED CODE`; owner authorization for real production migration = `NOT GRANTED`; real production migration = `NOT EXECUTED`; Etap 2 = `IN PROGRESS — E1 CLOSED, E2-A CLOSED, E2-B CLOSED, E2-C CLOSED, ORCHESTRATOR CLOSED`; controlled-live = `NOT READY`; następna operacja techniczna = `NOT STARTED` (ADR-109).** Implementer dostarczył kandydata ADR-108 (`CANDIDATE COMPLETE`); niezależny review wydał `APPROVE WITH MINOR/P2` (0 CRITICAL / 0 MAJOR); PR #11 został zmergowany jako merge commit `7faf62e5f71c838c20e00e61121ea052f4bf9348` (rodzice `889567507a82097f1e4f5d62a2d2bc1f74d8e936` i zatwierdzony head `9a10bf5fc364534ee08d4329753cb1a537e10a76`); post-merge checkpoint na `main` potwierdził collect `1630`, exact-once `1630` unikalnych node ID / 0 duplikatów, pełną suitę `1630/1630` z zero skipped, orchestrator `58/58`, targeted `224/224`, dodatkową regresję `218/218`, runtime schema QA `30/30`, harnessy E2-B/E2-C `13/13+13/13`, acceptance orchestratora `63/63` oraz zielone `compileall`/`git diff --check`. Formalną decyzję o zamknięciu fali podjął właściciel (nie implementer). Pierwszy przebieg pełnej suity dał `1628/1630`: dwa nieproduktowe failures dotyczyły wyłącznie reason code sondy quiescence (`PROCESSES_PRESENT` zamiast `DB_HANDLES_PRESENT`) przy równolegle działających procesach acceptance; flow zatrzymał się fail-closed, testy w izolacji przeszły `2/2`, czysty rerun dał `1630/1630`, defektu zmergowanego kodu nie zidentyfikowano — klasyfikacja: operational test interference / procedural lesson (nie uruchamiać acceptance z sondą procesów równolegle z testami LA-02). Zaakceptowane P2 (nienaprawiane w tej fali): `F-PR11-01` (błędna statystyka diffu implementera; prawidłowo 28 plików `+2182/−37`), `F-PR11-02` (nieaktualna liczba testów w README), `F-PR11-03` (semantyka `--expected-from-version` przy resume — wartość oznacza początek drabiny, nie bieżący szczebel; barierą pozostają aktualny SHA/size, ledger, nowy snapshot, jawne potwierdzenie i resume requirements w raporcie); obserwacje `OBS-PR11-01`/`OBS-PR11-02`/`OBS-PR11-03` zachowane. Produkcja pozostaje byte-identical na `0014`/14 migracji, SHA `9906AFBFB580BE8F576A6449B0930C41ED964FED814D99C947D1C28C5B060836`, `364544 B`, integrity `ok`, FK `0`, sidecary `0`; runtime kodu wymaga `0018`. Każda rzeczywista migracja nadal wymaga aktualnego SHA i rozmiaru, nowego snapshotu poza repo, braku sidecarów, jawnej flagi `--confirm-production-schema-migration-0014-to-0018` i osobnej decyzji właściciela; drabina `0014→0018` nie jest jedną atomową transakcją, spójne szczeble pośrednie `0015–0017` wymagają jawnego owner-approved resume, finalna awaria może wymagać oceny operatora (`MIGRATION_RESULT_NEEDS_OPERATOR_ASSESSMENT`), a snapshot nigdy nie jest automatycznie przywracany. Zero realnego Fetch/DNS/socketów/HTTP/API/providera/browsera/publikacji i kosztu (`0.000000 USD`); realny Fetch i staged A1/A2/B pozostają niewykonane. Zamknięcie orchestratora nie zamyka Etapu 2, nie autoryzuje rzeczywistej migracji produkcji i nie czyni controlled-live gotowym.

> **HISTORYCZNY SNAPSHOT IMPLEMENTERA PRZED NIEZALEŻNYM REVIEW — 2026-07-19, ADR-108 (zastąpiony przez ADR-109 wyżej): PRODUCTION SCHEMA MIGRATION ORCHESTRATOR = `CANDIDATE COMPLETE — AWAITING INDEPENDENT REVIEW`.** Dostępny jest jeden osobny, jawny CLI dla dokładnego `0014→0018`; wymaga absolutnego kanonicznego path, SHA-256, rozmiaru, stałych wersji kontraktu, zewnętrznego nowego snapshotu i flagi owner confirmation. Odrzuca niespójny ledger/schema, integrity/FK, aliasy, aktywność oraz każdy WAL/SHM/journal; po snapshotcie ponownie odczytuje tożsamość/SHA/size/ledger i robi ostatni gate bezpośrednio przed `mode=rw`. Resume `0015–0017` wymaga nowej zgody, aktualnego fingerprintu i nowego snapshotu; nie ma auto-retry. `0018` = `ALREADY_AT_TARGET` bez mutacji. Pełna walidacja: `1630/1630`, exact-once `1630`, partycje `390+398+412+430`, orchestrator `58/58`, QA `30/30`, harnessy `13/13+13/13`. Branch kandydata: `dev/stage2-production-migration-orchestrator`. Produkcja NIE była migrowana i pozostaje byte-identical: `0014`, 14 migracji, SHA `9906AFBFB580BE8F576A6449B0930C41ED964FED814D99C947D1C28C5B060836`, `364544 B`, integrity `ok`, FK `0`, sidecary `0`; koszt `0.000000 USD`, zero skutków zewnętrznych. Etap 2 pozostaje `IN PROGRESS`, controlled-live `NOT READY`.

> **AKTUALIZACJA FORMALNA 2026-07-19 — WAVE E2-C = `CLOSED — APPROVED WITH MINOR/P2`; Etap 2 = `IN PROGRESS — E1 CLOSED, E2-A CLOSED, E2-B CLOSED, E2-C CLOSED`; controlled-live = `NOT READY`; następna operacja techniczna = `NOT STARTED` (ADR-107).** Implementer dostarczył kandydata ADR-106; niezależny review wydał `APPROVE WITH MINOR/P2`; PR #9 został zmergowany jako `ff323746c35f733507a7b0a30837ebf645020b2b` (rodzice `cf3d083bc66387b2fa35f1dce4435ad0eb527b21` i zatwierdzony head `c508646be011a51fe973e42c197d23ddbca7fcd6`); post-merge checkpoint potwierdził `1572/1572`, zero skipped, exact-once `378+389+394+411`, targeted `478/478`, basic `101/101`, harnessy E2-C/E2-B `13/13+13/13` oraz zielone `compileall`/`git diff --check`. Właściciel formalnie zamknął falę. `E2B-F-01`, `E2B-F-02` i `E2B-F-03` = `TECHNICALLY CLOSED IN MERGED E2-C`. Prawdziwa granica: realny transport jest chroniony w granicach wspieranego runtime i composition roots; nie jest to ochrona przed autorem dowolnego kodu Python. Otwarte i nienaprawiane: `E2B-F-04` (`MINOR/P2`), `E2B-F-05` (`P2`), `E2B-OBS-02` (informational), `PR8-DOC-P2-01` (`P2`), `F-DOC-01` (`P2`, reporting accuracy), `F-01-RESIDUAL` (`P2`, poza wspieranym runtime). Runtime wymaga `0018`; produkcja pozostaje byte-identical na `0014`/14 migracji, SHA `9906AFBFB580BE8F576A6449B0930C41ED964FED814D99C947D1C28C5B060836`, `364544 B`, integrity `ok`, FK `0`, sidecary `0`. Zero realnego Fetch/DNS/socketu/HTTP/API/providera/browsera/publikacji, migracji produkcji i kosztu. Zamknięcie E2-C nie zamyka Etapu 2 ani nie autoryzuje controlled-live.

> **HISTORYCZNY SNAPSHOT IMPLEMENTERA PRZED NIEZALEŻNYM REVIEW — 2026-07-19, ADR-106 (zastąpiony przez ADR-107 wyżej): WAVE E2-C = `CANDIDATE COMPLETE — AWAITING INDEPENDENT REVIEW`.** Wspierany flow `controlled_fetch_v1` konsumuje dokładny approval L1 atomowo i tworzy `RESERVED`, po czym storage ponownie sprawdza trwały fence/intent/approval/attempt i wydaje wygasającą capability potrzebną composition rootowi. Publiczny konstruktor realnego transportu zniknął; prywatny transport wymaga pieczęci factory, a factory capability — to bariera wspieranego runtime, nie próba ochrony przed autorem dowolnego kodu. Globalny gate to strict boolean YAML `controlled_fetch_policy.real_transport_enabled=false`; ENV `REAL_CONTROLLED_FETCH_ENABLED` nie jest źródłem konfiguracji ani approvalem requestu. Resolver tworzy immutable `BoundHttpTarget` dopiero po sprawdzeniu wszystkich adresów; wybrany numeryczny IP trafia bezpośrednio do transportu, a Host/SNI zachowują nazwę zatwierdzonego URL. Initial target jest resolvowany raz, redirect otrzymuje nowe pełne wiązanie, niespójność/empty/mixed-unsafe kończy się fail-closed. Lifecycle i recovery pozostają bez zmian: attempt #1, `RESERVED→REQUEST_STARTED→SUCCEEDED|FAILED|NEEDS_VERIFICATION`, zero retry i attemptu #2. Nowa migracja nie była potrzebna; runtime nadal wymaga `0018`, produkcja pozostaje `0014`/14 migracji, SHA `9906AFBFB580BE8F576A6449B0930C41ED964FED814D99C947D1C28C5B060836`, `364544 B`, integrity `ok`, FK `0`, sidecary `0`. Walidacja implementera: pełna suita `1572/1572`; exact-once `378+389+394+411=1572`; E2-C harness `13/13`; E2-B harness `13/13`; `compileall` i `git diff --check` zielone. Zero realnego Fetch/DNS/socketu/HTTP/API/providera/browsera/publikacji i kosztu.

> **AKTUALIZACJA FORMALNA 2026-07-19 — ETAP 2 / WAVE E2-B = `CLOSED — APPROVED WITH MINOR/P2`; cały Etap 2 = `IN PROGRESS — E1 CLOSED, E2-A CLOSED, E2-B CLOSED` (ADR-105).** Podstawą jest zatwierdzony niezależny review `APPROVE WITH MINOR/P2`, merge PR #7 i zielony post-merge checkpoint; formalną decyzję o zamknięciu podjął właściciel (nie implementer). PR #7 zmergowany jako merge commit `03aa9724dbe5cbcda86515c62f238e93e3d6b4ae` (rodzice `1765e664f9e078f189ae41d4e13685d1561c57b9` i zatwierdzony head `0fcc2acd6808504a348e054fd23bf0ff9970056e`). Post-merge checkpoint na `main`: collect/full **1551/1551**, zero skipped, exact-once `1551` unikalnych node ID (case-sensitive; wcześniejsza obserwacja `1550` była artefaktem case-insensitive PowerShell dla parametrów `hidden`/`HIDDEN`, nie realną kolizją), harness kontrprób `13/13`, `compileall app scripts` i `git diff --check` zielone, pełny subprocess acceptance PASS. Runtime kodu wymaga dokładnie `0018`; produkcja pozostaje na `0014` (SHA-256 `9906AFBFB580BE8F576A6449B0930C41ED964FED814D99C947D1C28C5B060836`, `364544 B`, integrity_check `ok`, foreign_key_check `0`, bez WAL/SHM/journal; otwierana wyłącznie `mode=ro&immutable=1`); `0015`–`0018` niezastosowane. Rzeczywista sieć/DNS/HTTP/API/provider/browser/publikacja NIE użyte; koszt `0.000000 USD`; żaden realny staged A1/A2/B nie wykonany; nie istnieje jeszcze realna Research Card z nowego staged pipeline. **Prawdziwa granica po zamknięciu:** realny adapter (`ControlledHttpFetch`/`RealControlledHttpTransport`) istnieje w kodzie, ale **realny transport pozostaje nieosiągalny przez wspierane composition roots i runtime flow przy `REAL_CONTROLLED_FETCH_ENABLED=False`**; controlled-live = `NOT READY`; browser i publikacja pozostają nieautoryzowane; Etap 2 nie jest zamknięty. Findings review są jawne i nieblokujące dla zamknięcia E2-B: `E2B-F-01` (bezpośrednia ręczna konstrukcja realnego transportu poza wspieranym composition root jest możliwa w dowolnym własnym kodzie Python — dokumentacja NIE twierdzi absolutnie, że klasy nie da się skonstruować; blokuje controlled-live), `E2B-F-02` (TOCTOU DNS między walidacją a połączeniem — blokuje controlled-live), `E2B-F-03` (`REAL_CONTROLLED_FETCH_ENABLED=False` to globalny deny-all; docelowo aktywacja przez runtime'ową zgodę, nie edycję kodu na każdy request — blokuje controlled-live), `E2B-F-04` (ogólny enqueue może utrwalić strukturalnie niepoprawny payload, lecz approval L1 odrzuca go przed attemptem i transportem — `MINOR/P2 defense-in-depth`), `E2B-F-05` (harness wymaga `PYTHONIOENCODING=utf-8` na Windows — `P2 QA ergonomics`); `E2B-OBS-02` (wspólna allowlista portów 80/443 dla obu schematów — informational/non-blocking). `E2-A-P2-03` dla `controlled_fetch_v1` pozostaje `CLOSED` (opcja A, trigger `jobs_controlled_fetch_payload_frozen`); dla pozostałych payloadów `OPEN — BLOCKS FUTURE EXTERNAL EXECUTION` (poza zakresem). Kolejna fala techniczna (controlled-live) = `NOT STARTED` i wymaga osobnej decyzji właściciela. Ten wpis jest wyłącznie dokumentacyjny — bez zmian kodu, testów, migracji, konfiguracji i produkcyjnej bazy.

> **HISTORYCZNY SNAPSHOT PRZED NIEZALEŻNYM REVIEW — 2026-07-19, ADR-104 (zastąpiony przez formalne zamknięcie E2-B, ADR-105 wyżej):** ETAP 2 / WAVE E2-B = `CANDIDATE COMPLETE — AWAITING INDEPENDENT REVIEW`. Zbudowano najmniejszy trwały, kontrolowany flow rzeczywistego FetchPort — bez wykonania jakiegokolwiek realnego pobrania (cała implementacja i testy offline: fake transport, fake resolver, fake SDK, zero socketów; safety kernel blokuje sieć). Nowe: realny adapter `ControlledHttpFetch` (`app/ports/controlled_fetch.py`) na istniejącym `FetchPort` — tylko http/https, wstrzykiwalny transport, skończony timeout, twardy limit bajtów/przekierowań, allowlista content-type (`text/html`/`text/plain`), bez JS/browsera/cookies/credentials/sekretów/proxy-z-ENV, typowany wynik/błąd; deterministyczna lokalna polityka adresów z fake resolverem (schemat, userinfo, localhost/loopback, private, link-local, multicast, unspecified, reserved, port, redirect poza polityką); wersjonowany, samopodpisany `controlled_fetch_intent_v1` (`app/research/controlled_fetch_intent.py`) niosący wyłącznie dane jednego pobrania — nie może wybrać modelu/providera/kosztu/browsera/publikacji; jednorazowa zgoda **L1** (`controlled_fetch_approvals`) wiążąca job/account/URL/fingerprint/limity, wygasająca, konsumowana atomowo raz, nieprzenaszalna, nieodwracalna po restarcie; zamknięty lifecycle requestu (`controlled_fetch_attempts`: `RESERVED→REQUEST_STARTED→SUCCEEDED|FAILED|NEEDS_VERIFICATION`) z lease/fencing i recovery bez automatycznego ponowienia po stanie niejednoznacznym; migracja addytywna `0018_controlled_fetch_lifecycle` (runtime wymaga dokładnie `0018`, jawny krok `0017→0018`). **`E2-A-P2-03` ponownie ocenione dla `controlled_fetch_v1` = ZAMKNIĘTE (opcja A)**: trigger SQL `jobs_controlled_fetch_payload_frozen` czyni payload kontraktu fetchu niemutowalnym (payload zatwierdzony = payload wykonywany), a approval/attempt wiążą i rewalidują `intent_fingerprint` w transakcji; bariera wąska (tylko `controlled_fetch_v1`), `E2-A-P2-03` pozostaje `OPEN` dla pozostałych payloadów; `E2-A-P2-01`/`E2-A-P2-02` poza zakresem. Composition gate: `REAL_CONTROLLED_FETCH_ENABLED = False` — realny transport nieautoryzowany; adapter buduje się wyłącznie przez `controlled_fetch_v1` po wszystkich bramkach (offline/legacy/dry-run/paid/maintenance/reaper/default branch go nie konstruują). Dowód: **1551/1551 passed**, zero skipped, exact-once `1551` unikalnych node ID, zero duplikatów/failures; nowe testy E2-B `50` (adapter/polityka/intent/gate) + `27` (acceptance/negatywne/lease/recovery/migracja); własny harness kontrprób `scripts/harness/e2b_refutation_harness.py` = **13/13 inwariantów obronionych**; `compileall app scripts` i `git diff --check` zielone. Produkcja **byte-identical przed i po**: schema `0014`, 14 migracji, SHA-256 `9906AFBFB580BE8F576A6449B0930C41ED964FED814D99C947D1C28C5B060836`, `364544 B`, integrity `ok`, FK `0`, bez WAL/SHM/journal; `0015`–`0018` niezastosowane. Rzeczywista sieć/DNS/HTTP/API/provider/browser/publikacja NIE użyte; koszt fali `0.000000 USD`. E2-B NIE jest zamknięte; brak APPROVE/merge; controlled-live fetch może być rozważony dopiero po review i merge E2-B, osobnej fali domykającej okno TOCTOU DNS realnego transportu, migracji produkcji do `0018` oraz osobnej jednorazowej zgodzie właściciela na dokładny job/URL/limity/termin.

> **AKTUALIZACJA FORMALNA 2026-07-18 — ETAP 2 / WAVE E2-A = `CLOSED — APPROVED WITH MINOR/P2`; cały Etap 2 = `IN PROGRESS — E1 CLOSED, E2-A CLOSED, E2-B NOT STARTED` (ADR-103).** Podstawą jest zatwierdzony niezależny review `APPROVE WITH MINOR/P2`, merge PR #5 i post-merge checkpoint; offline evidence integration spine jest zbudowany i zweryfikowany. PR #5 zmergowany jako merge commit `404d2d306bbfa24fc08f2f5db68931e7441f040a` (rodzice `07fda5e68a61c7b9ff68e4388b2689acdca55818` i zatwierdzony head `61a509bd9c0a457ac78bb8893438664017a14063`). Post-merge suite: **1474/1474 passed**, zero skipped, exact-once `357 + 361 + 369 + 387 = 1474` unikalnych node ID. Pełny subprocess acceptance: CLI → enqueue → Worker → Dispatcher → staged A1/A2/B → `FakeFetch` → evidence → lokalny verifier → Research Card → atomowa terminalizacja → reopen. Trwały wynik z bazy: job `DONE`; run `DRY_RUN`, koszt 0; research_run `COMPLETE`, flow staged; topic `USED`; 1 Research Card; 3/3 source candidates `VERIFIED`; 3 retrievals; 3 excerpts; pełne lineage account/topic/run/candidate/source/card; drugi Worker `IDLE`; provider_attempts `0`, model_usage `0`, reservations `0`, settlements `0`, koszt `0.000000 USD`. Kod ma migracje `0001`–`0017`, a runtime wymaga dokładnie `0017`; produkcja pozostaje nietknięta na `0014` (SHA-256 `9906AFBFB580BE8F576A6449B0930C41ED964FED814D99C947D1C28C5B060836`, `364544 B`, integrity_check `ok`, foreign_key_check `0`, bez WAL/SHM/journal; otwierana wyłącznie `mode=ro&immutable=1`); `0015`, `0016` i `0017` nie zostały zastosowane. Trzy findings review przyjęte jako P2: **`E2-A-P2-01`** (QA harness `scripts/qa/e2a_lineage_disproof.py` nie aktywuje sam safety kernela, przy niepełnym ENV zatrzymuje się fail-closed przed sondami — `OPEN P2 / BACKLOG`); **`E2-A-P2-02`** (strukturalnie niepoprawny payload zmieniony poza wspieranym flow po przypięciu runu może zakończyć się `NEEDS_VERIFICATION` zamiast `FAILED`; bez kosztu, bez fałszywego sukcesu, wymaga operatora — `ACCEPTED P2`); **`E2-A-P2-03`** (`jobs.payload_json` nie ma triggera blokującego UPDATE po enqueue; aktualne flow ponownie waliduje payload i odrzuca niespójny stan — `OPEN P2 — FUTURE PAID/LIVE GATE`, MUST REASSESS przed paid staged recovery, realnym staged providerem, controlled-live i działaniem zewnętrznym zależnym od trwałego intentu). Real Fetch nie istnieje, realny staged provider nie istnieje, controlled-live nie jest autoryzowane, browser i publikacja nie są autoryzowane; E2-B `NOT STARTED`; cały Etap 2 nie jest zamknięty, a live nie jest gotowe. Koszt E2-A i tego zamknięcia dokumentacyjnego: `0.000000 USD`.

> **HISTORYCZNY SNAPSHOT PRZED NIEZALEŻNYM REVIEW — 2026-07-18, ADR-102 (zastąpiony przez formalne zamknięcie E2-A, ADR-103 wyżej):** ETAP 2 / E2-A = `CANDIDATE COMPLETE — AWAITING INDEPENDENT REVIEW`. Jawny CLI `enqueue-offline-evidence-research` utrwala wersjonowany `offline_evidence_intent_v1`; prawdziwy Worker i Dispatcher uruchamiają wyłącznie nowy, zero-cost `offline_evidence_v1` STAGED flow A1→`FakeFetch`→E1 retrieval→A2 proposal→lokalny verifier→excerpt/`VERIFIED`→B→atomowa karta/run/research_run/topic/job. Migracja addytywna `0017_evidence_pipeline_lineage` dodaje append-only candidate→retrieval→excerpt→card-source lineage z triggerami same-account/same-run także przy FK OFF; runtime wymaga dokładnie `0017`, a produkcja pozostaje nietknięta na `0014` (SHA `9906AFBFB580BE8F576A6449B0930C41ED964FED814D99C947D1C28C5B060836`, 364544 B). Restarty po retrievalu i po excerptach są bezpiecznie wznawiane wyłącznie dla dokładnego offline intentu bez external effect, provider attempt, usage, rezerwacji i kosztu. Legacy single, `durable_provider_v2`, provider lifecycle i progi jakości pozostają bez zmian. Live, realny Fetch/HTTP, provider, browser i publikacja są `NOT AUTHORIZED`. E2 nie jest zamknięte.

> **AKTUALIZACJA FORMALNA 2026-07-18 — ETAP 2 / WAVE E1 = `CLOSED — APPROVED WITH MINOR/P2`; cały Etap 2 = `IN PROGRESS — E1 CLOSED, E2 NOT STARTED` (ADR-101).** Właściciel przyjął niezależny re-review `APPROVE WITH MINOR/P2` po pierwszym `REJECT` i jedynej fali naprawczej B01–B04. PR #3 został zmergowany do `main` jako `42762a76d8c151cdb13d07fa384d32c9bfef0231` (rodzice: `17af8dee9c86068d5e338d2e1dd6b1a9004209b8`, zatwierdzony head `f42790b4cbfdc9a2ede4ae02443e4973c14203a5`). Post-merge checkpoint na `main`: collect/full **1454/1454**, zero skipped, exact-once 1454 unikalne node ID bez luk i duplikatów, rzeczywiste partycje `352+355+366+381`, evidence `79/79` i migration `44/44`, QA evidence/schema/recovery/lineage `35/35`, `21/21`, `4/4`, `10/10`, `compileall app scripts` i `git diff --check` zielone. Zamknięty zakres to izolowany fundament evidence: `0016`, retrievals/excerpts, canonical text i jego offsety, verifier, NUL/hash/account/idempotency floor, ukryty HTML, fake/disabled Fetch i jawny gate/migrator. **Nie obejmuje** integracji z pipeline'em, zmiany `verification_status`, globalnego triggera `VERIFIED`, pełnego run/source/topic/claim lineage, realnego Fetch/HTTP/API/providera, E2, controlled-live, browsera, publikacji ani kosztu. Produkcja jest niezmieniona: `0014`, 14 migracji, SHA `9906AFBFB580BE8F576A6449B0930C41ED964FED814D99C947D1C28C5B060836`, `364544 B`, integrity `ok`, FK `0`, bez sidecarów; `0015`/`0016` niezastosowane. `E1-RR-P2-01` pozostaje nieblokującym backlogiem; niższe noty kandydackie są historyczne.

> **HISTORYCZNY SNAPSHOT 2026-07-18 — ETAP 2 / WAVE E1 = `REPAIR CANDIDATE — AWAITING INDEPENDENT RE-REVIEW` (ADR-100).** Pierwszy kandydat E1 (ADR-099, PR #3) **NIE przeszedł niezależnego review — wynik `REJECT` z czterema findings B01–B04**; ta nota zastępuje wcześniejsze twierdzenie, że podłoga SQLite 0016 blokowała te przypadki. Naprawa (jedna fala, jeden commit na tym samym branchu PR #3): **(B01)** schema wymusza brak NUL i storage class TEXT w `canonical_text`/`excerpt_text`/`claim_text` (`instr(CAST(x AS BLOB), x'00')=0` + `typeof='text'`), więc historyczny exploit „`length()` zatrzymuje się na NUL" nie desynchronizuje już `substr()` SQLite od slicingu Pythona; **(B02)** hashe są autorytatywne: triggery 0016 wiążą `canonical_sha256`/`claim_sha256` z realnie utrwalonym tekstem przez deterministyczną funkcję `evidence_sha256_hex` rejestrowaną na każdym kontrolowanym połączeniu (writer bez funkcji w ogóle nie INSERT-uje — fail-closed; z prawdziwą funkcją nie utrwali fałszywego hasha); publiczna ścieżka zapisu retrievalu to wyłącznie `record_evidence_retrieval(FetchedDocument)` — wywołujący NIE może przekazać żadnego gotowego hasha, a `raw_sha256`/`extracted_sha256` są JAWNIE metadanymi audytowymi recordera (ich źródła nie są utrwalane; SQLite pilnuje tylko formatu, nie pochodzenia); logiczna idempotencja excerptu opiera się o unikalność (retrieval, claim_text, zakres) — kontrpróba reviewera `logical_duplicate_alt_claim_hash` jest trwale zablokowana i odtworzona w regresji; **(B03)** obie tabele evidence mają `account_id NOT NULL REFERENCES accounts(id)`, trigger lineage wymusza ten sam account excerptu i retrievalu także przy `PRAGMA foreign_keys=OFF`, a całe API repozytorium (zapis/get/list) wymaga jawnego `account_id` — globalny odczyt nie istnieje; **(B04)** deterministyczna ekstrakcja HTML pomija całe statycznie ukryte poddrzewa (`hidden`, `aria-hidden="true"` case/whitespace-insensitive, inline `display:none`/`visibility:hidden`/`content-visibility:hidden`), bez silnika CSS/zewnętrznych arkuszy/JS, fail-closed przy niedomkniętym ukrytym elemencie. Granica zaufania opisana uczciwie: podłoga NIE broni przed autorem zmieniającym schemat, usuwającym triggery lub rejestrującym fałszywą funkcję hash. Migracja `0016` poprawiona in-place (nie było `0017`; `0016` nigdzie nie była zastosowana). P2 z review (progi span 10–600, guard truncation 100, kategoria `Cf`, timezone, `raw_size_bytes`, transakcje po failed insert) celowo NIE ruszone — backlog. Dowód: collect/full **1454/1454** (1408 + 46 regresji naprawy), exact-once partycje `366+372+406+310`, QA evidence floor **35/35** (rozszerzony), schema gate `21/21`, recovery `4/4`, lineage `10/10`. **Produkcja bez zmian: schema `0014`, 14 migracji, SHA `9906AFBFB580BE8F576A6449B0930C41ED964FED814D99C947D1C28C5B060836`, `364544 B`, bez sidecarów; `0015` i `0016` NIE zostały zastosowane i wymagają osobnej autoryzacji.** Pipeline/worker/dispatcher/ledger/settlement/`research_min_sources`/legacy/prompty — nietknięte; integracja evidence z pipeline'em oraz realny Fetch adapter = następne fale (E2+). Etap 2 pozostaje OTWARTY; kolejny live = `NOT AUTHORIZED`; browser/publikacja = `BLOCKED`; koszt fali `0.000000 USD`.

> **HISTORYCZNY SNAPSHOT PRZED NIEZALEŻNYM REVIEW — 2026-07-18, ADR-099 (zastąpiony: review wydał `REJECT`, naprawa = ADR-100 wyżej):** ETAP 2 / WAVE E1 = `CANDIDATE COMPLETE — AWAITING INDEPENDENT REVIEW` (ADR-099). Pierwsza fala Etapu 2 zbudowała IZOLOWANY lokalny fundament evidence, bez integracji z pipeline'em researchu i bez zmiany semantyki istniejącego `verification_status`. Nowe: migracja `0016_evidence_foundation` (`evidence_retrievals` + `evidence_excerpts`; NULL-proof CHECK, trigger exact-substring przeciw utrwalonemu `canonical_text` z guardem truncation 100 znaków, span ∈ [10,600], zakaz spacji brzegowych, pełny append-only — surowy SQLite bez FK nie zapisze niespójnego evidence); dokładnie jedna kanonizacja `canonicalize_text` (offsety excerptów adresują WYŁĄCZNIE utrwalony `canonical_text`; pierwotna ekstrakcja tylko fingerprintowana); deterministyczny weryfikator + `record_verified_evidence_excerpt` jako jedyna aplikacyjna droga zapisu; `FetchPort`/`FakeFetch`/`DisabledFetch` i deterministyczna ekstrakcja HTML→tekst bez JS (realny adapter sieciowy celowo NIE istnieje — osobna fala i osobna zgoda); jawna drabina `migrate_0015_to_0016` + `scripts/migrate_schema_0016.py` (`--confirm-0015-to-0016`); runtime `SqliteStorage.open()` wymaga teraz DOKŁADNIE `0016` (`0015` = szczebel pośredni, odmowa `TOO_OLD`). Dowód: collect/full **1408/1408**, exact-once `339+342+356+371`, QA evidence floor `21/21` (nowy), schema gate `21/21` (rozszerzony), recovery `4/4`, lineage `10/10`. **Produkcja bez zmian: schema `0014`, 14 migracji, SHA `9906AFBFB580BE8F576A6449B0930C41ED964FED814D99C947D1C28C5B060836`, `364544 B`, bez sidecarów; `0015` i `0016` NIE zostały zastosowane i wymagają osobnej autoryzacji.** Pipeline/worker/dispatcher/ledger/settlement/`research_min_sources`/legacy — nietknięte. Kolejny live = `NOT AUTHORIZED`; browser/publikacja = `BLOCKED`; koszt fali `0.000000 USD`.

> **AKTUALIZACJA POST-MERGE 2026-07-18 — PR #1 = `MERGED`; CHECKPOINT TESTOWY = `CANDIDATE` (ADR-098).** PR #1 został zmergowany do `main` jako `548cc65cad70eaef631fafff7c350845984d18e6`; zatwierdzony HEAD `4a63e18863cef74dc135d96a858614c4f8da212b` jest jego ancestorem. Pierwszy pełny suite na `main` dał `1330 passed, 1 failed`, ponieważ subprocess test przekazywał historyczny branch `dev/first-successful-research-card`; produkcyjny branch gate poprawnie odmówił. Checkpoint `fix/post-merge-branch-sensitive-test` pobiera branch i HEAD z kontrolowanego repo testowego, a celowy mismatch nadal zatrzymuje worker. Dowód po poprawce: **1331/1331**, exact-once `320+322+339+350`, schema-gate QA `17/17`, recovery QA `4/4`, lineage QA `10/10`. **Produkcja nadal ma schema `0014`, 14 migracji, SHA `9906AFBFB580BE8F576A6449B0930C41ED964FED814D99C947D1C28C5B060836`, `364544 B`, bez sidecarów; `0015` nie została zastosowana i wymaga osobnej autoryzacji.** Historyczny branch jest technicznie zakończony; dalsza praca odbywa się na nowych branchach z `main`. Etap 2 = `NOT STARTED`; kolejny live = `NOT AUTHORIZED`.

> **HISTORYCZNY SNAPSHOT PRZED MERGE — 2026-07-18, ADR-096:** PR #1 miał wtedy status `CANDIDATE FOR INDEPENDENT RE-REVIEW`. Niezależny finalny review całego PR wydał `REJECT — MAJOR`: PR1-MAJ-001 odtworzył dead-end `SETTLED` po crashu przed terminalizacją; PR1-MAJ-002/003 dotyczyły chronionego katalogu pisarskiego i zerwanej aktywnej referencji; PR1-P2-004 — lokalnych ścieżek. Naprawa dodała migrację kodu `0015_settled_execution_recovery`: append-only `EXECUTION_RECOVERY` domyka wyłącznie lifecycle wykonawczy, nie zmienia `SETTLED`, kosztu ani jedynego `model_usage`, nie tworzy attemptu #2 i nie wywołuje providera. Sukces wymaga istniejącej, wyłącznej Research Card ze spójnym lineage; brak karty kończy wykonanie jako `FAILED`; stany niespójne pozostają fail-closed w `NEEDS_VERIFICATION`. Dowód: **1311/1311**, partycje exact-once `314+319+333+345`, 23 testy naprawy i niezależny QA 4/4. Katalog `instrukcja dla pisania artykulow/` przywrócono dokładnie z `origin/main`; decyzją właściciela historia prywatnego brancha nie została przepisana. Produkcja pozostała na schema `0014`/14 migracji. Ten snapshot został zastąpiony przez merge i ADR-098; Etap 2 nadal `NOT STARTED`, kolejny live `NOT AUTHORIZED`, browser/publikacja `BLOCKED`.

> **AKTUALIZACJA 2026-07-18 — `POSITIVE CONTROLLED-LIVE = INDEPENDENTLY CONFIRMED`; `ETAP 2 POSITIVE-LIVE GATE = FORMALLY ACCEPTED` (ADR-095).** Rozdzielenie dowodu jest jawne: implementer najpierw zadeklarował techniczny sukces i wykazał 1288/1288 oraz exact-once `306+312+328+342`; niezależny końcowy review wykonał własne wąskie testy `223/223`, potwierdził bajtową identyczność kodu/testów z zaakceptowanym baseline'em, zero CRITICAL, zero MAJOR i zero nowych MINOR, po czym wydał `APPROVE`; właściciel dopiero na tej podstawie formalnie przyjął bramkę. WAVE OUTPUT-SIZE CONTRACT pozostaje `CLOSED — APPROVED WITH MINOR/P2`. Etap 2 = `NOT STARTED`; kolejny realny request = `NOT AUTHORIZED`; browser i publikacja = `BLOCKED`; gate `False`; pięć flag fail-closed. Trwały wynik pozostaje: koszt `0.063278 USD`, ledger miesiąca `1.012590 USD`, job `DONE`, run `SUCCESS`, research_run `COMPLETE`, attempt `SETTLED`, Research Card `id=3`, redakcyjne `REJECT/WEAK_SOURCES`. Sześć P2 ADR-094 pozostaje nieblokującym backlogiem. Formalne przyjęcie bramki nie uruchamia Etapu 2 ani żadnej akcji zewnętrznej.

> **AKTUALIZACJA 2026-07-18 04:48 UTC — WAVE OUTPUT-SIZE CONTRACT = `CLOSED — APPROVED WITH MINOR/P2`; pierwszy pozytywny controlled-live Research Card.** Właściciel przyjął niezależny review i sześć nieblokujących P2 (założenie profilu tokenowego English/ASCII, brak jawnego limitu URL w promptcie, rozjazd limity słów↔znaki, możliwy wzrost po neutralizacji injection, nieaktualna liczba testów w README, rozróżnienie diagnostyczne `thinking_tokens=None` vs `0`) oraz autoryzował dokładnie jedną próbę `claude-sonnet-5`, prompt v3, `max_tokens=6000`, 1 web search, `max_attempts=1`, SDK retry `0`, cap `0.20 USD`. Operation `positive-live-output-size-20260718-09fe2f3684f14919`, job `real-research-b153efbd48d44e0e6388ec98e5e7afb0`, request `…:research:1` i run `bd0dd102-2526-4b2d-8c04-6b96ed9f8ef6` wykonały dokładnie jeden provider call: HTTP 200, `stop_reason=end_turn`, raw payload 4928/16000 znaków, poprawny JSON/schema/limity pól i Research Card `id=3`. Usage: 16834 input, 1961 output, `thinking_tokens=51`, 1 web search; koszt `0.063278 USD`, miesiąc `1.012590 USD`. Job `DONE`, run `SUCCESS`, research_run `COMPLETE`, attempt `SETTLED`; karta otrzymała redakcyjne `REJECT/WEAK_SOURCES`, co nie zmienia technicznego sukcesu kontraktu. Zero retry/attemptu #2/fallbacku/repair/verification/reconciliation; gate przywrócony do `False` przed analizą, flags fail-closed, marker/lease/rezerwacja brak, staging pusty. DB `9906AFBFB580BE8F576A6449B0930C41ED964FED814D99C947D1C28C5B060836`, `364544 B`, bez sidecarów. Zgoda została zużyta i nie autoryzuje kolejnego live. Szczegóły: ADR-094.

> **HISTORYCZNY SNAPSHOT PRZED NIEZALEŻNYM REVIEW — 2026-07-18; zastąpiony przez ADR-094 i ADR-095.** WAVE OUTPUT-SIZE CONTRACT miała wtedy status `CANDIDATE COMPLETE — AWAITING INDEPENDENT REVIEW`. Root cause zmiennego rozmiaru odpowiedzi jest ustalony z inwersji diagnostyk trzech realnych prób. Fala wprowadziła jawny kontrakt rozmiaru, prompt v3, typowany `ResearchCardSizeContractError`, pomiar `thinking_tokens` oraz profil 6000/1 search/cap 0.20 USD. Kandydacki dowód implementera: 1288/1288 i exact-once `306+312+328+342`; zero sieci/API/browsera/publikacji/kosztu. Ten akapit zachowuje stan sprzed review, nie jest bieżącą blokadą.

> **NOTA HISTORYCZNA:** trzy aktualizacje controlled-live z 2026-07-17 poniżej zachowują chronologię wcześniejszych prób. Ich zawarte w treści określenie „bieżący stan” dotyczy wyłącznie chwili danego zapisu i zostało zastąpione przez aktualizacje z 2026-07-18 oraz ADR-094/095.

> **AKTUALIZACJA CONTROLLED-LIVE 20:46 UTC — bieżący stan nadrzędny wobec starszych liczb poniżej:** po niezależnym `APPROVE` naprawy kontraktu prompt/schema właściciel dopuścił jeden nowy request z `max_tokens=3000`, jednym web search, `max_attempts=1`, SDK retry `0` i capem `0.127500 USD`. Operation `positive-live-contract-20260717-ee093a1d54cd4111`, job `real-research-85151c312b180759cd2387c5458f1248`, request `…:research:1` i run/research_run `08aa35eb-a87c-4ec0-bf3c-b2d608165e85` wykonały dokładnie jeden provider call/attempt. Anthropic zwrócił HTTP 200 i `stop_reason=max_tokens`; parser zakończył typowanym `ResearchTruncatedError` przed schema validation, więc poprawione kontrakty `supports_claim` i `citable_numbers` nie zostały ocenione live. Usage: 16381 input, 3155 output, 1 web search; koszt `0.074312 USD`, attempt `SETTLED`, job/run/research_run `FAILED`, brak Research Card, retry/attemptu #2/reconciliation, aktywnej lease i rezerwacji. Miesięczny koszt wynosi `0.949312 USD`. DB SHA `3FAD98FD8C5E1A8DCA995A97A7F2AA244C2861FBD230F96398EE3E8DC53C91C8`, `352256 B`, bez WAL/SHM/journal. Gate i flags są fail-closed, marker nie istnieje. Etap 1 pozostaje `CLOSED`, Etap 2 nierozpoczęty; wykorzystana decyzja nie autoryzuje kolejnego live.

> **AKTUALIZACJA CONTROLLED-LIVE 19:44 UTC — bieżący stan nadrzędny wobec starszych liczb poniżej:** osobna decyzja właściciela dopuściła jeden nowy durable request z `max_tokens=3000`, jednym web search, `max_attempts=1`, SDK retry `0` i capem `0.127500 USD`. Operation `positive-live-3000-20260717-9dcb59eef3674138`, job `real-research-e33abc717c655c7c7b6abeccd43554f3`, request `…:research:1` i run/research_run `65841541-10c9-4aa6-aee8-8fe1161d8f85` wykonały dokładnie jeden provider call/attempt. Anthropic zwrócił HTTP 200 i `stop_reason=end_turn`; odpowiedź nie była ucięta, lecz fail-closed schema validation odrzuciła `sources[0].supports_claim` jako wartość inną niż `string_or_null`. Usage: 19945 input, 2727 output, 1 web search; koszt `0.077160 USD`, attempt `SETTLED`, job/run/research_run `FAILED`, brak Research Card, retry/attemptu #2/reconciliation, aktywnej lease i rezerwacji. Miesięczny koszt wynosi `0.875000 USD`. DB SHA `540CF8F2EF020984F48DE97D8CDF31F1EC545F3262EE135B94B3783A521248AA`, `348160 B`, bez WAL/SHM/journal. Gate i flags są fail-closed, marker nie istnieje. Etap 1 pozostaje `CLOSED`, Etap 2 nierozpoczęty; wykorzystana decyzja nie autoryzuje kolejnego live.

> **AKTUALIZACJA CONTROLLED-LIVE 19:18 UTC — bieżący stan nadrzędny wobec starszych liczb poniżej:** właściciel wydał późniejszą, jawną decyzję L1 dla jednego nowego joba i jednej tymczasowej zmiany gate. Job `real-research-9f244684711acf4f82a07da8d4a139ea`, request `…:research:1` i run/research_run `8bcf15e4-c4e9-48ed-95f7-64f0b93fcee5` wykonały dokładnie jeden provider call/attempt. Anthropic zwrócił HTTP 200 i `stop_reason=max_tokens`; parser zakończył typowanym `ResearchTruncatedError`, bez Research Card. Usage: 16704 input, 1667 output, 1 web search; koszt `0.060078 USD`, attempt `SETTLED`, job/run/research_run `FAILED`, zero retry/attemptu #2/reconciliation, brak aktywnej lease i rezerwacji. Miesięczny koszt wynosi teraz `0.797840 USD`. DB SHA `10C5C9A6071BE3C46EA9EDB627EF31D26A27FDE10AD36334D1AC8A9F92EC10AB`, `344064 B`, bez WAL/SHM/journal. Gate i flags są fail-closed, marker nie istnieje. Etap 1 pozostaje `CLOSED`, Etap 2 nierozpoczęty; wykorzystana decyzja nie autoryzuje kolejnego live.

> **HISTORYCZNY SKONSOLIDOWANY STATUS Z 2026-07-17 — zastąpiony aktualizacjami i ADR-095 powyżej.**
> Data weryfikacji: **2026-07-17**. **STATUS ETAPU 1: `CLOSED` — formalna decyzja właściciela z 2026-07-17, podstawa: niezależny re-review NIA-P2-RV-01…05 = `APPROVE WITH MINOR/P2` (zero CRITICAL, zero MAJOR, pięć findings technicznie zamkniętych). WAVE 0A/0B/1A = `CLOSED — APPROVED WITH P2`; pierwsza WAVE LA-01 = `REJECTED — MAJOR`; WAVE LA-01-R1 i LA-02 = `APPROVED WITH MINOR/P2 — CHECKPOINTED`; LA-03 = `APPROVE WITH MINOR/P2`; pierwszy pakiet P2 po LA-03 = `REJECTED — MAJOR`; naprawa NIA-P2-RV-01…05 = `APPROVE WITH MINOR/P2`.** Root causes `PROCESSES_PRESENT` i self-handle `DB_HANDLES_PRESENT` są zamknięte bez wyłączenia zabezpieczeń. Produkcja ma schema `0014`, 14 migracji i fail-closed flags. Job `real-research-09fd6a30e07e63e96699ca002dbaead4` wykonał dokładnie attempt #1/request #1: `REQUEST_STARTED`, jedno usage, `SETTLED` za `0.053182 USD`, zero retry/attemptu #2. HTTP 200 zakończył się `ResearchParseError`, więc job/run/research_run są terminalnie `FAILED`, Research Card nie powstała. Brak Research Card z tego controlled-live NIE blokuje zamknięcia Etapu 1. Miesięczny koszt to `0.737762 USD`; gate `False`, marker nie istnieje, lease/rezerwacja brak. Bieżąca regresja to **1235/1235**, exact-once `294+299+311+331`. **Zamknięcie Etapu 1 nie autoryzuje kolejnego live: nowy realny request wymaga oddzielnej, jawnej decyzji właściciela i nowego dozwolonego joba; terminalny job nie może być retry'owany**; worker/browser/public actions = BLOCKED, Etap 2 nierozpoczęty.

## Formalne zamknięcie Etapu 1 — decyzja właściciela 2026-07-17

Właściciel formalnie zamknął **Etap 1 jako `CLOSED`** na podstawie niezależnego re-review NIA-P2-RV-01…05 z werdyktem **`APPROVE WITH MINOR/P2`**. Review potwierdził: zero CRITICAL, zero MAJOR, wszystkie pięć findings technicznie zamknięte, pełny suite `1235/1235`, exact-once `294+299+311+331` (`1235` node ID), produkcyjna DB nietknięta co do SHA/rozmiaru/mtime, zero sieci/API/browsera/publikacji/kosztu. Brak Research Card z ostatniego controlled-live nie jest bramką zamknięcia. Nowy realny request nadal wymaga oddzielnej, jawnej decyzji właściciela; to zamknięcie nie jest zgodą na kolejny live. Formalny zapis: `docs/DECISIONS.md` ADR-088.

Dwa MINOR/P2 z re-review są **nieblokujące** i przechodzą do backlogu Etapu 2 (nie naprawiane teraz, bez otwierania nowej fali):
- **`RV-R2-P2-1`** — prose zaczynająca się od `N` lub `I` może otrzymać etykietę diagnostyczną `json_syntax` zamiast `prose_outside_json`; odrzucenie pozostaje fail-closed tym samym `ResearchParseError`.
- **`RV-R2-P2-2`** — kompletny błędny JSON z trailing comma może otrzymać etykietę `incomplete_json` zamiast `json_syntax`; bez wpływu na lifecycle, `truncation` nadal wymaga `stop_reason=max_tokens`.

Pozostałe obserwacje re-review pozostają `BACKLOG — NON-BLOCKING` (m.in. liberalny sanitizer gołego stringu bez etykiety/kształtu klucza/env; legacy parsery gather/synthesize/extract poza reachable durable single path; `datetime.now()` w display budżetu `_run_resume`, gdzie realny resume i tak jest odmawiany przed mutacją). Nie otwierają nowej fali.

## Naprawa NIA-P2-RV-01…05 po `REJECT — MAJOR` — 2026-07-17

Niezależny review pierwszego pakietu P2 wydał `REJECT — MAJOR`. Zamknięta fala naprawia wyłącznie pięć findings: score overflow bez usage/settlement, niesanitizowany raw diagnostic, mieszany zegar enqueue/wrappera, bare fence i błędne klasyfikacje wielu/root values oraz aktywne sprzeczności tego dokumentu. Forensics historycznego requestu pozostaje bez zmiany: trwały błąd to `Expecting property name enclosed in double quotes: line 29 column 6 (char 4376)`, ale brak raw/stop reason/diagnostic uniemożliwia uczciwe dopisanie bardziej szczegółowej przyczyny.

Reachable durable single parser przyjmuje teraz dokładnie jeden obiekt JSON albo jeden kompletny zewnętrzny fence rozpoczynający się literalnie od `json`. Drugi `raw_decode` klasyfikuje object/array/scalar jako `multiple_json_values`, root scalar/array jako schema, a prose jako `prose_outside_json`. Score jest walidowany przez `Decimal` przed konwersją; 400-cyfrowy integer, ogromny exponent, Infinity, -Infinity, NaN, tekst i wartość poza zakresem kończą się `ResearchSchemaError` po jednym callu, usage i settlement. Diagnostic używa tego samego rekurencyjnego sanitizera co operator report i jest utrwalany temp→file fsync→replace→directory fsync; jego cztery failpointy nie zmieniają terminalnego lifecycle. Nie ma repair requestu, retry ani attemptu #2.

Raport controlled-live ma teraz append-preserving identity `<session_id>--<attempt-1>-<UTC timestamp>-<nonce>.json`; provisional/final tego samego invocation nadal używają atomowego replace/fsync, ale dwa invocation tej samej operation identity nie nadpisują historii. Marker niesie `report_key`, a recovery tworzy osobny raport i wskazuje właściwy poprzedni klucz. `run_controlled_live_once` wymaga jawnego `frozen_quiescence`; usunięto ukryty default probe po otwarciu głównego storage. Composition root nadal wykonuje canonical probe przed `SqliteStorage.open`.

Enqueue otrzymuje jawny `now=clock.now()` i ten sam czas jest używany przez fake controlled-live; granice przed/równo/po `earliest_run_at` są testowane osobno. Dowód: 28-przypadkowa macierz parsera, 10 trwałych failure classes, boundary success, pięć klas sekretów, cztery failpointy diagnostyki, historia raportów i frozen quiescence. Collect/full `1235/1235`, exact-once `294+299+311+331`, wszystkie cztery partycje zielone. Zero sieci/API/provider calla/browsera/publikacji/kosztu; produkcyjny job i DB nie zostały zmienione. Status naprawy: `APPROVE WITH MINOR/P2` (niezależny re-review); na tej podstawie właściciel formalnie zamknął **Etap 1 = `CLOSED`** (patrz sekcja „Formalne zamknięcie Etapu 1” wyżej i ADR-088).

## WAVE LA-03 — pre-storage probe i pierwszy realny provider request — 2026-07-17

Potwierdzony `DB_HANDLES_PRESENT` był observer effect composition rootu: `app.main` otwierał `SqliteStorage`, a następnie `CreateFileW(..., dwShareMode=0)` poprawnie wykrywał to własne połączenie. LA-03 przenosi wspólny canonical quiescence/DB handle check przed główne otwarcie storage, zamraża wynik i po open wykonuje trwałą rewalidację DB SHA/schema/job identity/claimability/pricing/intent/flags. Marker O_EXCL jest tworzony dopiero po tej rewalidacji; drugi recheck wiąże marker i plan przed otwarciem flag. Foreign read-only/writable DB, WAL/SHM, drift DB i drugi wrapper nadal STOP.

Dowód: 1181/1181 offline, exact-once cover 1181, pełny subprocess fake i standalone temp. Produkcyjny standalone przed i po wykonaniu zwrócił PASS. Jedyna autoryzowana komenda doszła do providera dokładnie raz; durable attempt ma `request_started_at=2026-07-17 10:38:47.228489`, `SETTLED` i koszt `0.053182 USD`. Usage: 13306 input, 1657 output, jeden web search. Provider zwrócił HTTP 200, lecz odpowiedź nie była poprawnym JSON-em; typowany failure zakończył job/run/research_run jako `FAILED` bez reconciliation i bez Research Card. Raport `99f52dd3889688440ef8dc8f26f5e318.json` potwierdza jeden attempt/usage, restoration/reopen, marker clear i fail-closed. Post-run DB SHA to `5BEA9E26597E6A628EF875A7F5115465E94CB600B38213A67794EE94232C6D10`.

## Finalny preflight realnego pricing profile — 2026-07-17

Właściciel zatwierdził lokalny profil `anthropic-sonnet-5-intro-2026-07` dla `claude-sonnet-5`, wersję `sonnet-5-intro-pricing-valid-through-2026-08-31`, pięć cen, `max_tokens=1500`, jeden web search i cap `0.12 USD`. Lokalny, niecommitowany `config/pricing_profiles.yaml` przechodzi resolver: wszystkie ceny są `Decimal`, pricing fingerprint to `1b98c7c9656c5b7791ac4f8eb189d538386c31f52b760920a3f2d89f78bb4062`, projected cost `0.070000 USD`, pessimistic cost `0.105000 USD`. Profil jest `READY`; API, SDK i sieć nie zostały użyte.

Job `real-research-09fd6a30e07e63e96699ca002dbaead4` został osobno autoryzowany i enqueue’owany, a post-enqueue SHA zamrożono jako `5FF5DBA3FA57A2DFBB8B638DD7E6CC9E84825A96C6080AA17F8A05B188D97B78`. Historyczna pierwsza komenda zatrzymała się przed providerem na `PROCESSES_PRESENT`; jej wynik zachowują ADR-082 i logi dokumentacyjne. Deterministyczna ścieżka raportu `runtime/controlled_live_reports/99f52dd3889688440ef8dc8f26f5e318.json` zawiera obecnie nowszy wynik LA-03 dla tej samej operation identity: jeden `SETTLED` request, usage `0.053182 USD`, terminalny parse failure, marker cleared i pełny profil fail-closed.

## WAVE LA-02 — ancestry quiescence i diagnostyka preflightu — 2026-07-17

Potwierdzony root cause pierwszej próby to `PROCESSES_PRESENT` na checku nr 6 (`QUIESCENCE_PROJECT_PROCESSES`). Stary wyjątek launchera obejmował tylko bezpośredni parent i tylko `powershell.exe`/`pwsh.exe`; nie obejmował `cmd.exe`, `bash.exe`, innych shelli ani dalszych przodków. Każdy taki proces z tekstem `-m app.main controlled-live-once` otrzymywał `APP_ROLE_OPERATOR_CLI` i blokował własne uruchomienie. Adapter controlled-live dodatkowo odrzucał `process_diagnostics`, a `_safe_error` zastępował wewnętrzny kod ogólnym `PREFLIGHT_FAILED`.

LA-02 wprowadza wąski kontrakt zweryfikowanego ancestry: jeden snapshot, dokładna relacja PID/PPID, pełne executable/command line/creation time, zgodny entrypoint i monotoniczna kolejność czasów. Typ shella sam nie jest dowodem. Wykluczane są tylko current PID i potwierdzeni przodkowie bieżącego entrypointu oraz osobno zweryfikowany helper z nonce. Niezależny operator z identycznym command line, drugi controlled-live, worker, maintenance, scheduler CLI, niezarejestrowany potomek, holder DB, task, PID reuse i niepełna identity nadal blokują fail-closed.

Trwały raport zachowuje zewnętrzny `PREFLIGHT_FAILED` oraz wewnętrzny `PROCESSES_PRESENT`, nazwę i kolejność inwariantu, blocking PIDs i deterministyczną sanitizowaną identity każdego procesu wraz z `belongs_to_probe_ancestry` i fingerprintem. Defense-in-depth fallback sanitizera z open P2 LA-01-R1 został domknięty rekurencyjnie. Nowe `python -m app.main controlled-live-quiescence-check [--db-path ...]` używa tego samego canonical probe'a, fingerprintuje DB/WAL/SHM przed i po, zwraca `PASS/STOP` i nie otwiera SQLite/storage, nie tworzy markera/providera i nie wymaga gate'u.

Dowód offline: **1174/1174 passed/collected**, exact-once partycje `284+284+298+308`, 21 nowych testów LA-02 i jedna nowa regresja controlled-live ancestry oraz istniejące LA-01-R1/QP-01 zielone. Testy obejmują PowerShell/pwsh/cmd/bash, ancestry wielopoziomowe, drugi entrant, realny proces workera, PID reuse, creation mismatch, redakcję sekretu i pełny subprocess standalone. Produkcyjna DB/WAL/SHM nie została przez LA-02 otwarta do zapisu; schema pozostaje `0014`, job `QUEUED/attempts=0`, attempts/usage=0, flags fail-closed, gate `False`, koszt `0.000000 USD`. Niezależny review wydał **`APPROVE WITH MINOR/P2`**; root cause jest `CLOSED`. P2-2 pozostaje jawną obserwacją operacyjną: obce terminale/edytory/shelle z pełnym tekstem komendy mogą spowodować false STOP i muszą być zamknięte przed standalone checkiem z tego samego launchera.
> Architektura: `MASTER_ARCHITECTURE.md` · Kolejność prac: `IMPLEMENTATION_ROADMAP.md`.
> Aktualizować przy każdej zmianie stanu modułu; statusy tylko z zestawu: `NOT_STARTED / SKELETON / PARTIAL / WORKING / VERIFIED / BLOCKED / DEPRECATED`. `VERIFIED` wyłącznie dla kodu URUCHOMIONEGO i przetestowanego.

## WAVE LA-01-R1 — pełna naprawa po `REJECTED — MAJOR` — 2026-07-17

Niezależny review odrzucił pierwszą WAVE LA-01. Findings P1-01…P1-06 obejmowały obejście zatwierdzonego cennika, fałszywy sukces bez trwałego raportu, brak ownership joba/workera, wyciek surowego wyjątku, zafałszowanie `REQUEST_STARTED` w recovery i brak gotowego composition root. Findings P2-01…P2-04 obejmowały brak fsync markera, częściowy profil flag, okno bare workera bez fencing oraz niespójną aktywną dokumentację.

LA-01-R1 zamyka technicznie te przypadki. Frozen intent wiąże ID, wersję, model, walutę, jednostki, wszystkie ceny, fingerprint, limity, cap i obie projekcje; wrapper i dispatcher ponownie porównują go z aktualnie zatwierdzonym profilem. Realny wrapper wymaga wcześniej utrwalonego joba z kanonicznego enqueue; tworzenie joba przez wrapper jest dostępne tylko za podwójną bramką test/fake. Enqueue zapisuje, a wrapper niezależnie wyprowadza przez ten sam deterministyczny helper identyczny `session_id`, operation key, expected job/request/attempt oraz execution fence; test kompozycyjny uruchamia fake worker przez normalny wariant `allow_job_creation=false`. Raport jest sanitizowany i fsyncowany przed usunięciem markera; brak raportu, błąd unlink/fsync, brak usage/settlement albo obcy wynik wykluczają `COMPLETED`. Recovery nie ponawia providera i zachowuje możliwy unknown outcome po `REQUEST_STARTED`.

Niezależny review wydał `APPROVE WITH MINOR/P2`: LA-01-R1, pricing contract, controlled-live wrapper i max_tokens są zatwierdzone, a P1-01…P1-06 zamknięte. Jedyny open P2 brzmi: fallback `sanitize_report_payload` powinien rekurencyjnie wywołać `sanitize_report_payload(str(value))`; ścieżka jest obecnie nieosiągalna i finding nie blokuje checkpointu ani controlled live acceptance.

## Historyczna WAVE LA-01 — odrzucona przez niezależny review

WAVE LA-01 zaimplementowała jeden spójny kontrakt umożliwiający w przyszłości dokładnie jeden controlled live acceptance, naprawiając trzy potwierdzone blokery preflightu:

- **LA-01-A (autorytatywny cennik):** nowy `app/core/pricing.py` + `config/pricing_profiles.yaml`(`.example`) z wersjonowanymi profilami (`profile_id`, `version`, `model`, ceny, waluta, jednostka, `status: example|approved`, `approved_by`). Realny enqueue (`scripts/run_capped_research.py --real`) wymaga `--pricing-profile <id>` wskazującego profil **zatwierdzony, wersjonowany i zgodny modelem**; profil `example`/bez wersji/niepełny/niedodatni/mismatch modelu blokuje fail-closed. Durable job utrwala `pricing_profile_id` + `version` obok istniejącego `pricing_fingerprint`; zmiana pliku po enqueue nie zmienia zapisanego kontraktu. **`.env`/`.env.example` NIE są już autorytatywnym źródłem realnego kosztu.**
- **LA-01-C (konfigurowalny max_tokens):** `--max-tokens` w kanonicznym CLI z zamkniętym kontraktem (dodatni int, w `[256, 8192]`, bez float), utrwalany w durable job, przekazywany do provider requestu i użyty w projekcji kosztu — inwariant CLI == persisted == provider == projekcja == raport. Domyślnie `3000`; controlled acceptance może zamrozić niższą wartość jawnie.
- **LA-01-B (operatorski wrapper):** `app/operations/controlled_live.py` + `app.main controlled-live-once`. Pełny preflight (branch/HEAD/schema/baseline, brak procesów/tasków/lease/rezerwacji, dokładnie jeden claimable job albo atomowe utworzenie, `max_attempts=1`, `max_retries=0`, brak fallbacku, `browser=false`, zatwierdzony profil, cost≤cap), zamrożony plan, atomowe otwarcie minimalnego profilu (`kill_switch` zdejmowany OSTATNI), dokładnie jeden `worker --once`, BEZWARUNKOWE przywrócenie fail-closed (`kill_switch` ustawiany PIERWSZY), reopen+potwierdzenie, trwały raport bez sekretów. Crash pomiędzy zmianami flag jest wykrywalny: filesystem marker `runtime/controlled_live_session.json` (O_EXCL) wymusza fail-closed przy następnym starcie; `operational-report` raportuje niedomkniętą sesję read-only. **Jedynym kodem otwierającym flagi paid/worker jest ten wrapper** — zawsze przywraca fail-closed. Nowa atomowa operacja `StoragePort.apply_security_flag_profile` ustawia pełny profil w jednej transakcji (bez migracji schematu).

`REAL_CONTROLLED_LIVE_ENABLED=false`: `controlled-live-once` odmawia realnego wykonania (nie zmienia produkcyjnych flag, nie otwiera profilu, nie uruchamia workera, nie woła API). Ten opis dokumentuje pierwszą, później odrzuconą implementację i nie jest bieżącym werdyktem. Bieżący dowód LA-01-R1 to **1151/1151 offline**; produkcyjna `data/agent.db` pozostaje niezmieniona (schema 0014, baseline `630E34…`).

## Skonsolidowany pakiet końcowy Etapu 1 — 2026-07-16

Niezależny post-migration review wydał **`APPROVE WITH MINOR/P2`**, zatwierdził QP-01, zweryfikował produkcyjną bazę jako schema `0014` oraz nowy baseline. (Stan historyczny na 2026-07-16: Etap 1 nie był wtedy `CLOSED`; pozostawał **`OPEN / BLOCKED PENDING CONTROLLED LIVE ACCEPTANCE`**. Etap 1 został formalnie zamknięty 2026-07-17 — patrz nagłówek dokumentu i ADR-088.) Migracyjny collect i pełny suite to **1079/1079 offline**, z exact-once coverage 1079 node ID w czterech partycjach `259 + 264 + 277 + 279` (przed LA-01). Systemowy launcher istnieje jako generowana konfiguracja dwóch zadań Windows Task Scheduler (`worker --once --offline-only` co minutę oraz `maintain --once --stale-after-seconds 300` co pięć minut), z `IgnoreNew`, jawnym interpreterem i katalogiem roboczym oraz lokalnymi logami `runtime/`. Rejestracja zadań nie została wykonana i wymaga osobnej zgody właściciela dla każdego zadania.

`attempts` jest zwiększane atomowo podczas claimu, `max_attempts` ogranicza claim i recovery, a bezpieczny enqueue otrzymał typowane `worker_policy.default_max_attempts`. Durable paid flow nadal ma twarde `max_attempts=1`. Nie dodano globalnego timeoutu dispatchu: provider timeout, lease 60 s, heartbeat 20 s, bounded join guarda i jawny poll interval mają odrębne znaczenia; zabijanie synchronicznego wątku podczas zapisu SQLite byłoby niebezpieczne. Globalny worker timeout pozostaje descope/P2.

Read-only `operational-report` otwiera SQLite w `mode=ro` i `query_only`, nie uruchamia migracji, claimu, workera ani SDK. Raportuje joby, lease, `NEEDS_VERIFICATION`, `NEEDS_RECONCILIATION`, rezerwacje i pięć flag. Brak/uszkodzenie danych to `UNKNOWN/BLOCKED`, nie zero. Schemat 0014 nie utrwala czasu cyklu maintenance, dlatego pole jest jawnie `UNKNOWN`.

### Skonsolidowana poprawka procedury migracji — 2026-07-16

Jedyny profil fail-closed został przeniesiony do `app.core.security_flags.SECURITY_FLAG_DEFAULTS` i jest wspólny dla storage oraz migracji: `kill_switch=true`, `safe_mode=true`, `worker_enabled=false`, `paid_actions_enabled=false`, `browser_actions_enabled=false`. Jedyny opakowany executor produkcyjny to `run_stage1_in_place_migration`, dostępny przez istniejący `scripts/prepare_stage1_db_migration.py execute-in-place` wyłącznie z jawnym przełącznikiem potwierdzającym.

Executor nie otwiera produkcyjnej SQLite przed zakończeniem gate'ów branch/HEAD, starego baseline'u, pełnego quiesce, zweryfikowanego backupu DB/WAL/SHM, rehearsal oraz bezpośredniego rechecku driftu. WAL nieobecny albo 0 B i obecny SHM są legalne; nonzero WAL, journal, proces/uchwyt/task lub drift DB/WAL/SHM blokują. Dowolny błąd po otwarciu produkcji wymusza pełny restore DB/WAL/SHM; reverse SQL nie istnieje. Ten opis przygotowania jest historyczny; późniejsza kontrolowana próba zakończyła się sukcesem opisanym poniżej.

### Druga zatwierdzona próba migracji — odrzucona przed mutacją

Na HEAD `ddc3c63190eb82bca171174dc7ee70c2d0a1ec15` właściciel udzielił jednorazowej zgody na uruchomienie wyłącznie zacommitowanego `execute-in-place`. Repository gate, stary baseline, brak journala, runtime i zadań przeszły. Executor zakończył się fail-closed na pierwszym `_require_quiescence`: `processes=(17196, 34228)`, `handles=()`, `tasks=()`. Oba zgłoszone procesy były już nieobecne podczas kontroli po zakończeniu polecenia; nie przypisano im tożsamości po fakcie.

Wynik formalny: **`MIGRATION REJECTED BEFORE MUTATION`**. Workspace nie powstał, więc nie utworzono backupu, rehearsal, manifestu ani raportu executora; produkcyjna SQLite nie została otwarta. DB/WAL/SHM zachowały dokładnie stary SHA/size/mtime, schemat pozostaje `0009`, system flags nie zostały zainicjalizowane, a rollback nie był potrzebny. Nie wykonano automatycznej drugiej próby. Każde kolejne uruchomienie wymaga nowej zgody właściciela.

### QP-01 — lokalna poprawka self-detection quiesce probe

Po ponownej odmowie zidentyfikowano dokładny false positive: potomny PowerShell uruchomiony przez `_default_quiesce_probe` zawierał ścieżkę repozytorium we własnej command line i został uznany za obcy proces projektu. Zatwierdzona poprawka zastępuje blokowanie po samej ścieżce dwustopniowym kontraktem: PowerShell zwraca pełny snapshot, a Python rejestruje i weryfikuje current PID, parent PID oraz dokładny helper PID przez relację parent/child, executable, creation time i jednorazowy nonce.

Wykluczenie dotyczy wyłącznie bieżącego procesu, jawnego launchera oraz dokładnie zarejestrowanej tożsamości helpera; nie dziedziczy się na potomków. Worker, maintenance i operator CLI nadal blokują przez zamknięte reason codes, podobnie jak uchwyt DB/WAL/SHM. Sama obecność project root w pełnej command line jest raportowanym `PROJECT_ROOT_COMMAND_LINE_ONLY`, ale bez roli aplikacyjnej lub niepełnej tożsamości nie blokuje. Stan niejednoznaczny kończy się fail-closed.

Dowód offline: 13 nowych kontrprób Windows/temp DB, collect i pełny suite **1079/1079**, partycje **259 + 264 + 277 + 279 = 1079** z exact-once coverage, `compileall` i `git diff --check` zielone. W jedynej późniejszej, jawnie zatwierdzonej próbie QP-01 nie powtórzył się na żadnym z trzech gate'ów quiesce.

### Historyczny zapis kontrolowanej migracji — baseline 0014 przed live

Jedyna zatwierdzona próba po QP-01 została uruchomiona przez ten sam `scripts/prepare_stage1_db_migration.py execute-in-place`, interpreter Python 3.12, CWD repozytorium i łańcuch PowerShell → Python → helper PowerShell. Gate initial, after-backup i pre-mutation miały po zero blocking processes, handles i tasks; helper był za każdym razem `PROBE_HELPER / PROBE_REGISTERED_HELPER_IDENTITY`.

Backup starego zestawu został zweryfikowany bez driftu, rehearsal zastosował dokładnie `0010`–`0014`, a produkcja przeszła post-verification. Wynik: 14 migracji, 35 triggerów, 13 legacy proofs, koszt `0.684580` USD bez zmiany, 0 jobs, 0 provider attempts, 0 reconciliation events, historyczne tabele bez zmiany oraz profil `kill_switch=true`, `safe_mode=true`, worker/paid/browser `false`. Executor sprawdził `integrity_check=ok` i pusty `foreign_key_check` przed raportem sukcesu.

Historyczny wymagany wtedy baseline głównego DB: SHA-256 `630E3411F2FDFBD232F593DC7E7F3B0DF3EB8125274365815CDBDBC2A3C036A6`, 335872 B, mtime UTC `2026-07-16T19:42:25.5377560Z`. WAL: 0 B, SHA pustego pliku, mtime `2026-07-16T19:42:25.5417557Z`. SHM: SHA `FD4C9FDA9CD3F9AE7C962B0DDF37232294D55580E1AA165AA06129B8549389EB`, 32768 B, mtime `2026-07-16T19:42:25.5507558Z`. Na tym historycznym etapie live API, worker, browser, paid actions i Windows Tasks nie zostały uruchomione. Bieżący post-live baseline jest podany w sekcji „Liczby kontrolne”.

### Kryterium formalnego zamknięcia Etapu 1 — SPEŁNIONE

1. **Wykonane technicznie:** trwała kolejka, claim, lease, fencing, heartbeat, restart, recovery, maintenance, reaper, scheduling policy, runtime flags, dry-run worker, durable provider boundary, usage/settlement, reconciliation operatorskie, kontrolowany launcher systemowy, minimalny raport operacyjny oraz produkcyjna migracja `0009→0014` z nowym baseline'em i kanonicznymi flagami. ✅
2. **Wykonane raz w LA-03:** dokładny kontrakt testu, twardy cap, `max_retries=0`, dokładnie jeden job/provider request i osobna zgoda właściciela. Request został rozliczony, lecz zakończył się `ResearchParseError` bez Research Card. ✅ Ta autoryzacja jest zużyta; kolejny request wymaga nowej, oddzielnej zgody właściciela i nowego joba.
3. **Spełnione przy zamknięciu:** wykonano jeden kontrolowany live durable single flow (attempt/request rozliczony); niezależny re-review naprawy NIA-P2-RV-01…05 wydał `APPROVE WITH MINOR/P2`; brak otwartego MAJOR/CRITICAL; formalna decyzja właściciela z 2026-07-17 (ADR-088). ✅ Pozytywna Research Card NIE była wymaganym kryterium.
4. **Niewymagane:** browser, publikacja, FetchPort, evidence excerpts, content pipeline, panel FastAPI, autonomia, interakcje, analytics, Etap 2+ oraz backlog P2 bez osiągalnego naruszenia (w tym nieblokujące `RV-R2-P2-1`/`RV-R2-P2-2`).

Wszystkie bieżące blockery Etapu 1 zostały zamknięte: niezależny re-review naprawy NIA-P2-RV-01…05 (`APPROVE WITH MINOR/P2`), brak otwartego MAJOR/CRITICAL oraz formalna decyzja właściciela. Produkcyjna baza ma zweryfikowany schemat `0014`, post-live baseline `5BEA9E…C6D10` i pięć flag fail-closed. QP-01 jest zatwierdzone; zadania systemowe nie zostały zarejestrowane. Dokładnie jeden controlled-live request wykonano i rozliczono; nie utworzył Research Card, co nie było bramką zamknięcia.

Historyczny baseline bezpośrednio po migracji miał główny SHA-256 `630E3411F2FDFBD232F593DC7E7F3B0DF3EB8125274365815CDBDBC2A3C036A6`, 335872 B, mtime UTC `2026-07-16T19:42:25.5377560Z`; WAL 0 B i SHM 32768 B. Został zastąpiony przez post-enqueue `5FF5…97B78`, a następnie przez bieżący post-live `5BEA9E…C6D10`. `CAEDDA05…EFB` jest historycznym baseline'em schematu 0009 i backupu.

## Formalne zamknięcie WAVE 1A — 2026-07-16

Implementer zadeklarował `CANDIDATE COMPLETE — AWAITING INDEPENDENT REVIEW` po `W1A-R4-01`. Niezależny finalny re-review odtworzył jego 1036/1036 testów i cztery partycje exact-once, potwierdził `compileall` oraz `git diff --check`, a następnie wykonał własne kontrpróby: 149/149 przez prawdziwy `Worker.run_once`, 36/36 dla SQLite floor i 30/30 dla recovery/reaper/crash-window. Reviewer nie znalazł osiągalnego MAJOR ani CRITICAL i wydał werdykt `APPROVE WITH MINOR/P2` z rekomendacją zamknięcia WAVE. Na tej podstawie właściciel formalnie zamknął WAVE 1A jako **`CLOSED — APPROVED WITH P2`**. P2-1 i P2-2 pozostają świadomymi, nieblokującymi ograniczeniami opisanymi w ADR-068. Zamknięcie WAVE 1A **nie** oznaczało zamknięcia Etapu 1 ani gotowości live API: Etap 1 pozostawał wtedy `BLOCKED`, live API `ZABRONIONE`, Etap 2 nierozpoczęty. (Stan historyczny na 2026-07-16; Etap 1 został formalnie zamknięty 2026-07-17 — ADR-088.)

## Historia implementacji i review — WAVE 1A (2026-07-15–2026-07-16)

Ten wpis zastępuje wcześniejsze statusy checkpointu WAVE 0B w tym dokumencie. WAVE 0B jest **`CLOSED — APPROVED WITH P2`** po commicie `c25e1254044d89c7703a6614e9ee831eb226e87c`; historyczny stan kandydacki WAVE 1A został później zastąpiony formalnym **`CLOSED — APPROVED WITH P2`**.

**WAVE 1A — naprawa po `REJECTED — MAJOR` (2026-07-15).** Niezależny audyt odrzucił pierwszą próbę WAVE 1A (W1A-RR-01…06, W1A-NEW-01/02). Naprawiono wszystko w jednej fali, bez migracji 0015 (migracja `0014_provider_attempt_reconciliation` poprawiona in place): pełny kontrakt stanów `provider_attempts` z przyciętymi polami audytu i enumem `reconciliation_resolution`; terminalne `RECONCILED_SETTLED`/`RECONCILED_RELEASED` są niezmienne i nieusuwalne; nowa **append-only** tabela `reconciliation_events` jest jedynym źródłem historii operatora (`UNRESOLVED_OBSERVATION` → `FOLLOW_UP` → `FINAL_RESOLUTION`, monotoniczny numer sekwencji, idempotency key). Istniejący `model_usage` jest akceptowany wyłącznie po pełnej weryfikacji tożsamości (provider, model, task z formalnego zbioru, run_id, konto, real/legacy, fingerprint, koszt) — nie na podstawie samego kosztu. `RESULT_ALREADY_FINALIZED` wymaga wyłącznego trwałego dowodu Research Card przez `UNIQUE research_runs(research_card_id)`. Zlikwidowano dead-end: `MANUAL_REVIEW_REMAINS_REQUIRED` jest dozwolone **tylko** z `CHARGE_UNKNOWN` (obserwacja, nigdy terminalna); `CHARGED_KNOWN`/`NOT_CHARGED` muszą wybrać `EXECUTION_FAILED` albo `RESULT_ALREADY_FINALIZED`, więc żaden terminalny attempt nie zostaje z jobem w `NEEDS_VERIFICATION`. Po każdej zmianie kanonu ledgera zachowana jest niezmienna spójność `SUM(model_usage) = runs.cost_usd = research_runs.total_cost_usd` (Decimal). `CHARGED_KNOWN` wymaga kosztu `> 0` (koszt 0 → `NOT_CHARGED`). CLI: `reconcile-attempt` czyta trwały stan i zwraca **version token**; `--confirm` wymaga tokenu i fail-closed przy stale (kontrolowane exit codes). `list-reconciliations` i `reconcile-attempt` nie ładują SDK, dispatchera ani workera; `model_usage` pozostaje jedynym kanonem kosztu. Stan po tej historycznej fali (przed `W1A-VERIFY-01/02`): 955 testów offline, a po następnym audycie **982**; 14 migracji, brak zmiany `data/agent.db`; Etap 1 `BLOCKED`, live API `ZABRONIONE`. Historyczne wartości **894/13** i `READY FOR CHECKPOINT` to zapis checkpointu WAVE 0B i są historyczne.

**WAVE 1A — poprawka `W1A-VERIFY-01` (2026-07-15, autoryzowana przez właściciela).** Niezależna weryfikacja wykazała jeden niedeterministyczny test (`test_resolver_interleaves_with_recovery_and_reaper_without_reviving_attempt`, ~50% flaky). **Root cause:** reaper `reap_orphaned_stale_runs` ustawia osierocony stale run na `STOPPED`, gdy job pozostaje `NEEDS_VERIFICATION` (guard nie blokuje, bo `NEEDS_VERIFICATION ∉ {QUEUED,LEASED,RUNNING}`), a resolver `EXECUTION_FAILED` akceptował tylko `run_status ∈ {RUNNING, FAILED}` → gdy reaper wygrywał wyścig, resolver odmawiał („Execution failure requires a non-success single lifecycle"; fail-closed i bezpieczny, ale suite flaky). **Naprawa (minimalna, kompletna):** `EXECUTION_FAILED` akceptuje teraz `run_status ∈ {RUNNING, STOPPED, FAILED}` przez jeden wspólny `_EXECUTION_FAILED_RUN_STATUSES` używany zarówno w warunku, jak i w CAS `UPDATE` (nie mogą się rozjechać — to była pierwotna wada), z `COALESCE(error, finished_at)` zachowującym historię reaper/maintenance; atomowo doprowadza `STOPPED → FAILED` (attempt→`RECONCILED_SETTLED`/`RECONCILED_RELEASED`, job/run/research_run→`FAILED`, rozliczona/zwolniona rezerwacja, zgodny `model_usage`+cache, append-only `FINAL_RESOLUTION`) — bez wskrzeszania runu, `RUNNING`, requeue, retry, attemptu #2, providera ani `DONE`. Semantyka `RESULT_ALREADY_FINALIZED` i kontrakt finansowy **bez zmian**. Dodano 7 deterministycznych testów (wyścig, reopen po STOPPED, foreign account, conflicting research_run, RESULT_ALREADY_FINALIZED na STOPPED). Stabilność: flaky node **30/30**, plik **10/10**; pełny suite **955/955** offline, 4 partycje exact-once (227+233+248+247), 20/20 niezależnych kontrprób BLOCKED, `data/agent.db` niezmieniona. WAVE 1A pozostaje `CANDIDATE — AWAITING INDEPENDENT REVIEW`. Szczegóły: `docs/DECISIONS.md` ADR-064.

**WAVE 1A — poprawka `W1A-VERIFY-02` (2026-07-15, po drugim niezależnym review = `REJECTED — MAJOR`, autoryzowana przez właściciela).** Reviewer wykazał na tymczasowej bazie **fail-open pełnego lineage**: przy `jobs.account_id`/`research_runs.account_id` = konto właściciela, `runs.account_id` = konto obce i `runs.workflow` = `ANALYTICS` resolver akceptował reconciliation i terminalizował attempt/job/run/research_run. **Root cause:** `_reconciliation_state_row` nie czytał `runs.account_id`/`runs.workflow`/`jobs.kind`/`jobs.workflow`, a walidacja sprawdzała tylko `research_runs` account/topic — brak weryfikacji pełnej relacji `provider_attempt → job → run → research_run → account → workflow → topic → durable intent`. **Wcześniejsze 955/955 nie obejmowało tego przypadku.** **Naprawa (defense-in-depth):** (1) warstwa aplikacji — nowy `_reconciliation_require_consistent_lineage` waliduje CAŁY lineage przed jakąkolwiek mutacją (job kind/workflow=RESEARCH; `runs.account_id == jobs.account_id == research_runs.account_id == intent.account_id`; `runs.workflow == RESEARCH`; `research_runs.topic_id == jobs.topic_id == intent.topic_id`; `flow == single`; fingerprint intentu) i zwraca zweryfikowany intent; (2) **version token → v2** rozszerzony o wszystkie pola lineage (run/job/research account, workflow, kind, run_id, topic, flow, fingerprint) — każda zmiana między preview a confirm = `ReconciliationPreviewStaleError`; (3) warstwa SQLite — trigger `provider_attempts_reconcile_requires_consistent_lineage` (migracja `0014` in-place) blokuje terminalizację przy niespójnym lineage, również przez `json_extract` (payload↔job). Każda niespójność = fail-closed, **zero mutacji** (attempt/job/run/research_run/rezerwacja/usage/event), bez retry/attemptu #2/providera. Trwałe testy w repo: `tests/test_reconciliation_lineage.py` (17 negatywnych rozjazdów + raw-trigger + pozytywne) i `scripts/qa/reconciliation_lineage_disproof.py` (uruchamialny self-disproof z exit code). Licznik **955 → 980**; pełny suite 980/980, 4 partycje exact-once (236+240+254+250), concurrency 30/30, lineage counter-proofs 10/10, `data/agent.db` niezmieniona. WAVE 1A pozostaje `CANDIDATE — AWAITING INDEPENDENT REVIEW`. Szczegóły: `docs/DECISIONS.md` ADR-065.

**WAVE 1A — pełny audyt software-assurance working tree (2026-07-16, zlecony przez właściciela, autoryzowana jedna fala napraw).** Pełny read-only audit HEAD + wszystkich niecommitowanych zmian (request identity, lineage, version token, lease/fencing, budget/usage/settlement, migracje/SQLite, provider boundary, CLI/subprocessy, recovery/reaper, test safety, dokumentacja) potwierdził zamknięcie wszystkich wcześniejszych findingów (W1A-RR-01…06, W1A-NEW-01/02, W1A-VERIFY-01/02) i **nie wykrył żadnego MAJOR/CRITICAL**. Wykryto i naprawiono trzy MINOR: **W1A-AUD-01** — `list-reconciliations` bez kontrolowanych exit codes dla błędów konfiguracji/storage (traceback; teraz symetrycznie 3/6 jak `reconcile-attempt`); **W1A-AUD-02** — martwe, nigdy nieustawiane pole `version_token` w `ProviderAttemptReconciliationResult` (usunięte; token zwraca wyłącznie preview); **W1A-AUD-03** — anotacja `actual_cost_usd: float | None` w porcie/implementacji niezgodna z faktycznym kontraktem (dokładność Decimal przez `str`; teraz `float | str | None`). Udokumentowano **W1A-AUD-04 (P2, report-only, styczne do otwartego P2-19):** attempt `RESERVED`/`REQUEST_STARTED` po twardym crashu i wygaśnięciu lease pozostaje na jobie `NEEDS_VERIFICATION` niewidoczny dla `list-reconciliations` i nierozwiązywalny przez resolver (kontrakt „Only NEEDS_RECONCILIATION"), a jego rezerwacja bezterminowo pomniejsza budżet (kierunek konserwatywny, zero fail-open; preview pozostaje oknem odczytu) — rozszerzenie kontraktu resolvera wymaga decyzji właściciela. Dodane trwałe testy: kontrolowane błędy CLI + granica stuck-attempt. Kontrpróby audytora (temp DB, świeże procesy): replay z innymi parametrami po terminalu, granice half-quantum (0.0000004/0.0000005/0.0000015, ROUND_HALF_UP), ekstremalne koszty CLI (`NaN`/`Infinity`/`-Infinity`/`1e400`) — wszystko fail-closed, zero mutacji. Licznik **980 → 982**; pełny suite 982/982, 4 partycje exact-once (237+241+254+250), `data/agent.db` byte-identical. WAVE 1A pozostaje `CANDIDATE — AWAITING INDEPENDENT REVIEW`.

**WAVE 1A — fala naprawcza po TRZECIM niezależnym review = `REJECTED — MAJOR` (2026-07-16, autoryzowana przez właściciela).** Reviewer potwierdził audytowy W1A-AUD-04 jako **MAJOR BLOCKING** (trwałe `RESERVED`/`REQUEST_STARTED` po crashu: niewidoczne dla `list-reconciliations`, nierozstrzygalne przez resolver, bezterminowo blokujące budżet; test utrwalał stuck state jako sukces) oraz wykazał **W1A-SQLITE-01** (surowa terminalizacja attemptu bez pełnego lifecycle i bez `FINAL_RESOLUTION` eventu) i **W1A-SQLITE-02** (po `RECONCILED_SETTLED` surowy SQLite mógł zmienić/usunąć kanoniczny `model_usage` przy nietkniętych cache'ach); dodatkowo niepełne W1A-AUD-01 (błędy query-time), stale baseline'y 980 w części źródeł (W1A-DOC-01) i ciek katalogów tymczasowych QA (W1A-QA-01). **Naprawa (jedna fala):** (1) `release_or_requeue_expired_leases` w tej samej transakcji **eskaluje** każdy dead-fence attempt: `RESERVED → NEEDS_RECONCILIATION` (`LEASE_EXPIRED_BEFORE_REQUEST_STARTED`) i `REQUEST_STARTED → NEEDS_RECONCILIATION` (`LEASE_EXPIRED_AFTER_REQUEST_STARTED`), z append-only eventem **`AUTO_ESCALATION`**, idempotentnie, bez dotykania żywego lease/terminali, bez retry/attemptu #2/providera; licznik `escalated_reconciliation_count` w recovery i wydrukach CLI; attempt, który nigdy nie osiągnął `REQUEST_STARTED`, może być rozstrzygnięty **wyłącznie `NOT_CHARGED`** (aplikacja + macierz stanów 0014: nigdy `RECONCILED_SETTLED` bez startu requestu); (2) **W1A-SQLITE-01:** resolver przepisany na kolejność `walidacja → lifecycle → usage/cache → FINAL_RESOLUTION → attempt na końcu`, a migracja `0014` (in-place, bez 0015) wymusza przy `NEEDS_RECONCILIATION → RECONCILED_*` istnienie w tej samej transakcji: dokładnie zgodnego eventu `FINAL_RESOLUTION` (request/status/resolution/operator/note), terminalnego spójnego lifecycle `job/run/research_run` ze zwolnioną rezerwacją oraz cache'ów równych kanonowi (tolerancja pół kwantu); eventy mogą powstawać tylko przy żywym `NEEDS_RECONCILIATION` (nigdy po terminalu); (3) **W1A-SQLITE-02:** kanoniczny `model_usage` rekoncyliowanego attemptu jest niemutowalny i nieusuwalny, a `runs.cost_usd`/`research_runs.total_cost_usd` zamrożone po terminalu; (4) pełna kontrolowana obsługa błędów `list-reconciliations` (config→3; open/query/format/close→6) + `RuntimeError`/guarded close w `reconcile-attempt`; (5) sweep baseline'ów w dokumentach; (6) QA script: prefiksowane katalogi, cleanup w `finally`, twarda kontrola pozostałości w exit code + test. **+25 trwałych testów** (macierz eskalacji H1–H20 z wyścigiem dwóch maintenance, reopen, stale-token, budżetem przed/po i rollbackiem failpointów eskalacji; macierz raw-SQLite: partial lifecycle, brak/niezgodny event, rozjechany cache, mutacja/kasowanie/duplikat kanonu, zamrożone cache; ciek QA). Licznik **982 → 1007**; pełny suite 1007/1007; 4 partycje exact-once 4/4 exit 0; concurrency 33 nodes × 30 = 30/30; pliki reconciliation/lineage/recovery/migration 10/10; QA 10/10 bez pozostałości; niezależne kontrpróby (wyścig resolver↔eskalacja, heartbeat↔recovery, partial lifecycle, porzucona transakcja eskalacji po reopen, budżet przed/po) 5/5 BLOCKED/OK; `data/agent.db` byte-identical. WAVE 1A pozostaje **`CANDIDATE — AWAITING INDEPENDENT REVIEW`**; Etap 1 `BLOCKED`; live API `ZABRONIONE`. Szczegóły: ADR-066.

**WAVE 1A — systemowa naprawa `W1A-R4-01` po CZWARTYM niezależnym review = `REJECTED — MAJOR` (2026-07-16, autoryzowana przez właściciela).** Reviewer odtworzył przez prawdziwy `Worker.run_once` stan `job=FAILED` + `attempt=REQUEST_STARTED` po lokalnym `sqlite3.OperationalError`: attempt był niewidoczny w kolejce L1, nierozstrzygalny i nadal blokował budżet. Root cause leżał w workerowym fallbacku, który wywoływał terminalizację lifecycle bez sprawdzenia provider attemptu. Naprawa centralizuje decyzję w `StoragePort.fail_or_escalate_job_research_execution` (jedna transakcja `BEGIN IMMEDIATE`, jawny outcome): brak aktywnego attemptu zachowuje normalne `FAILED`; `RESERVED` i `REQUEST_STARTED` przechodzą odpowiednio z powodami `UNEXPECTED_EXECUTION_FAILURE_BEFORE_REQUEST_STARTED` / `...AFTER_REQUEST_STARTED` do `NEEDS_RECONCILIATION`, z jednym append-only `AUTO_ESCALATION`, jobem `NEEDS_VERIFICATION` i zachowaną rezerwacją; istniejące `NEEDS_RECONCILIATION` jest idempotentne. Ten sam kontrakt obejmuje unexpected exception, `sqlite3.Error`, `OSError`, `RuntimeError`, błąd mark-reconciliation, kontrolowaną niepewność i błąd heartbeat po granicy. Zero retry/attemptu #2/provider calla. Triggery 0014 blokują terminalne job/run/research_run przy `RESERVED`/`REQUEST_STARTED`, lecz pozwalają legalnemu resolverowi przy `NEEDS_RECONCILIATION`. P2-1 pozostaje fail-closed: normalization nie zmienia niespójnego payloadu/fingerprintu, tylko może zachować rezerwację i wystawić attempt do kolejki; resolver nadal odmawia po fingerprint mismatch. P2-2 jest opisane precyzyjnie: **StoragePort wykonuje resolver atomowo w jednej transakcji; SQLite wymusza spójny trwały stan końcowy; SQLite nie udowadnia pochodzenia wszystkich danych wobec arbitralnego uprzywilejowanego autora wielu tabel.** +29 testów, **1007 → 1036**, pełny suite 1036/1036, partycje 248+253+267+268, concurrency 38×30, pliki krytyczne 10×, QA 10×, E2E Worker 10 świeżych procesów, 0 USD i zero działań zewnętrznych. Status: **`WAVE 1A — CANDIDATE COMPLETE — AWAITING INDEPENDENT REVIEW`**; WAVE pozostaje otwarta, Etap 1 `BLOCKED`, live API `ZABRONIONE`. Szczegóły: ADR-067.

## Liczby kontrolne (zweryfikowane)

- Bieżący checkpoint po merge PR #11 / Production Schema Migration Orchestrator: **1630 passed / collected**, zero skipped; exact-once `1630` unikalnych node ID (case-sensitive), zero duplikatów; orchestrator `58/58`, targeted `224/224`, dodatkowa regresja `218/218`, runtime schema QA `30/30`, harnessy E2-B/E2-C `13/13+13/13`, acceptance orchestratora `63/63`; `compileall app scripts` i `git diff --check`: **PASS / EXIT 0**. Pierwszy przebieg pełnej suity dał `1628/1630` przez nieproduktową interferencję sond procesów (reason code quiescence przy równoległych procesach acceptance; izolacja `2/2`, czysty rerun `1630/1630`; operational test interference / procedural lesson, nie defekt runtime). Poprzednie checkpointy po merge PR #9 / WAVE E2-C (**1572 passed**, targeted `478/478`, basic `101/101`, harnessy `13/13+13/13`), PR #7 / WAVE E2-B (**1551 passed**, harness `13/13`), PR #5 / WAVE E2-A (**1474 passed**, partycje `357 + 361 + 369 + 387`) i PR #3 / WAVE E1 (**1454 passed**, partycje `352 + 355 + 366 + 381`) pozostają zapisami historycznymi; szczegóły w `docs/BUILD_LOG.md`, ADR-101, ADR-103, ADR-105, ADR-107 i ADR-109.
- Realny koszt projektu w ledgerze miesiąca: **1,012590 USD**; jedyna próba objęta bramką positive-live kosztowała **0,063278 USD**. Formalne zamknięcie dokumentacyjne kosztuje `0,000000 USD`.
- Positive controlled-live: dokładnie jeden request/attempt/usage; HTTP 200 i `end_turn`; job `DONE`, run `SUCCESS`, research_run `COMPLETE`, attempt `SETTLED`, Research Card `id=3`; redakcyjne `REJECT/WEAK_SOURCES`; zero retry i attemptu #2. Wynik jest niezależnie potwierdzony.
- **Bieżący produkcyjny baseline:** `data/agent.db` ma schema `0020_topic_generation_lifecycle`, 20 migracji i SHA-256 `91f593923f3ca36cd8d2c816219e551b0ff231bc3149d6d584b025465f656a1f`, `700416 B`; WAL `0 B`, SHM `32768 B`, journal nie istnieje. Kod na `main` zawiera migracje do `0024_autonomous_content_decision` (runtime wymaga `0024`), lecz produkcja nie została zmigrowana, a runtime pozostaje wobec niej fail-closed. Starsze schema/SHA są wyłącznie historyczne.
- Publikacje na Substacku: **0** (fizycznie zablokowane przez `DisabledBrowser`).

## Tabela stanu modułów

| Moduł | Status | % | Co działa | Co nie działa / znane błędy | Testy | Ostatnia weryfikacja | Następny krok |
|---|---|---|---|---|---|---|---|
| Konfiguracja (`app/core/config.py`) | VERIFIED | 90 | .env+YAML, zero ścieżek absolutnych, fallback na *.example | kill-switch czytany raz przy starcie (runtime — Etap 1) | pośrednie (fixtures) | 2026-07-12 | bez zmian do Etapu 1 |
| Modele domenowe (`app/models.py`) | VERIFIED | 94 | modele zbudowanej części oraz zamknięty `JobExecutionContext(job_id, lease_owner, run_id, Clock, kind, workflow)` tworzony po atomowej inicjalizacji | paid/browser execution context nie jest jeszcze projektowany | tak | 2026-07-13 | osobne granice paid/browser |
| Storage SQLite: kod C4 24 migracje; produkcja 20 | C4 SCHEMA MERGED / PROD VERIFIED 0020 | 99 | runtime wymaga `0024`; nowy append-only decision ledger, SQL contract, exact terminal uniqueness, atomowa apply boundary i jawny migrator `0023→0024`; fresh/upgrade/idempotent/raw SQLite/concurrency zielone | zmiany C4 zmergowane (PR #26, merge commit `7eb93ba…`) i zweryfikowane post-merge, dokumentacyjny sync czeka na niezależny review/merge; produkcyjna drabina `0020→0024` = `NOT EXECUTED`, owner authorization = `NOT GRANTED`; runtime fail-closed na produkcyjnym 0020 | C4 `23/23`; full/collect/unique `1994/1994`; compile/diff PASS | 2026-07-24 | review i merge dokumentacyjnego PR C4; bez migracji produkcji |
| Evidence foundation (Etap 2 / WAVE E1) | CLOSED — APPROVED WITH MINOR/P2 | 100 | `evidence_retrievals`, `evidence_excerpts`, canonical text i jego offsety, verifier, NUL/hash/account/idempotency floor, ukryty HTML, `FetchPort` z fake/disabled Fetch, ekstrakcja bez JS oraz migracja/gate `0016` są w `main` po PR #3 | poza zakresem E1: integracja z pipeline, zmiana `verification_status`, globalny trigger `VERIFIED`, pełny lineage, real Fetch/HTTP/API/provider i zapis do produkcyjnej DB | 79 passed evidence; 44 passed migrations; pełny checkout: 1454 passed, zero skipped; QA/env/compile/diff: PASS | 2026-07-18 | E2-A CLOSED (integracja offline zbudowana i zweryfikowana); E2-B CLOSED; `E1-RR-P2-01` pozostaje nieblokującym backlogiem |
| Evidence integration spine (Etap 2 / WAVE E2-A) | CLOSED — APPROVED WITH MINOR/P2 | 100 | offline `enqueue-offline-evidence-research` → job `offline_evidence_v1` → Worker/Dispatcher → STAGED A1/A2/B → `FakeFetch` → E1 retrieval/verifier → excerpt/`VERIFIED` → Research Card → atomowa terminalizacja; migracja `0017` z append-only candidate/retrieval/excerpt/card-source lineage; lease-fenced restart/recovery bez external effect | poza zakresem: real Fetch/HTTP, realny staged provider, controlled-live, browser, publikacja, migracja produkcji; trzy P2 (`E2-A-P2-01`/`-02`/`-03`) | 16 passed E2-A acceptance; 4 passed E2-A lineage; pełny checkout po PR #5: 1474 passed, zero skipped; QA 35/24/4/10/4; koszt `0.000000 USD` | 2026-07-19 | E2-B i E2-C CLOSED; live/real Fetch/provider NOT AUTHORIZED (controlled-live NOT READY) |
| Controlled Fetch live-readiness boundary (Etap 2 / WAVE E2-C) | CLOSED — APPROVED WITH MINOR/P2 | 100 | storage-issued capability po atomowym zużyciu L1, sealed factory w granicach wspieranego runtime, strict boolean YAML domyślnie `false`, immutable `BoundHttpTarget`, przypięty IP z Host/SNI i pełne ponowne wiązanie redirectu | produkcja nadal `0014`; controlled-live, realny Fetch/DNS/HTTP, realny staged provider, browser i publikacja nieautoryzowane; otwarte P2 z ADR-107 | pełny checkpoint po PR #9: 1572/1572; targeted 478/478; basic 101/101; harnessy 13/13+13/13; koszt `0.000000 USD` | 2026-07-19 | następna operacja techniczna `NOT STARTED`; wymaga odrębnej zgody |
| Policy Engine | PARTIAL | 70 | dotychczasowe budget/runtime gates oraz C4: trwały autonomy level, AccountMode, article/note approval policy, wersjonowane progi, complete evaluations i hard-failure override | brak pełnych limitów publikacji/interakcji, cooldownów, automatycznego SAFE MODE i panelu; paid/browser celowo BLOCKED | policy/worker + C4 `23/23` | 2026-07-24 | review i merge dokumentacyjnego PR C4; pozostała część Etapu 4 później |
| UsageTracker (koszty) | VERIFIED | 97 | `record_job` sprawdza fence, zapisuje usage z request_id i rozlicza dokładnie aktywny attempt; parse error z usage nie gubi kosztu | unknown outcome pozostaje `NEEDS_RECONCILIATION`; eksport CSV wymaga audytu KEEP/DEPRECATE/REMOVE przed Etapem 8 | tak + ledger/parse/race + CSV failpoint | 2026-07-16 | dalszy zakres wyłącznie po decyzji właściciela; Etap 1 `CLOSED` (ADR-088) |
| ModelRouter | VERIFIED | 90 | zadanie→model z .env | scripts omijają router (P2-8) | tak | 2026-07-12 | P2-8 przy Etapie 0/1 |
| FakeLLMClient / FakeResearchClient | VERIFIED | 100 | deterministyczne dry_run/testy, scenariusze brzegowe | — | tak | 2026-07-12 | — |
| AnthropicLLMClient (tematy) | WORKING | 88 | odpowiedź→Usage→parse, typowane błędy; SDK ma `max_retries=0`, derived request identity i exact `Idempotency-Key` | realny adapter nie jest dostępny z `app.main`; WAVE 0A formalnie zamknięta jako `APPROVED WITH P2` | fake SDK + workflow SQLite + WAVE 0A | 2026-07-14 | backlog P2; osobny zakres Etapu 1 |
| AnthropicResearchClient (3 generacje metod) | VERIFIED | 99 | produkcyjny klient wymaga `DurableProviderAttemptContext`, derived request identity, snapshotu requestu i pełnej asercji lifecycle; positive controlled-live potwierdził HTTP 200/`end_turn`, usage, settlement i Research Card `id=3` przy `max_retries=0` | P2-19 `timeout-billed-unrecorded`; sześć P2 ADR-094 pozostaje nieblokującym backlogiem | offline gate/ledger + 1288 baseline + positive durable live + niezależny review 223/223 | 2026-07-18 | kolejny live `NOT AUTHORIZED`; Etap 2 jest `IN PROGRESS — E1 CLOSED, E2-A CLOSED, E2-B CLOSED, E2-C CLOSED` |
| Estymator kosztów | VERIFIED | 85 | conservative+expected z 2 realnych obserwacji, margines ≥50% | dwie kalibracje (legacy z cennika vs staged stałe) — P2-1; kalibracja n=2 | tak | 2026-07-12 | ujednolicić przy Etapie 2 |
| Workflow tematów + dedup | VERIFIED | 90 | pełny przepływ dry_run, dedup lokalny, progi, SUCCESS-fix | realny run tematów nigdy nie wykonany | tak | 2026-07-12 | realny run po Etap 0 zad. 6 |
| Research staged A1/A2/B + resume | VERIFIED | 99 | realny A1 + 4×A2 + B; failure zachował SOURCES_COMPLETE, repair naprawił audit, resume dał COMPLETE/SUCCESS/USED; typed error ma identyczny bezpieczny format w run/research_run/stage/candidate audit bez SDK body i sekretów; finalizacja B używa trwałego force i CAS, a wyłącznie `finalize_staged_research_with_card` może zapisać staged COMPLETE/SUCCESS/B SUCCESS/USED z kosztem z `model_usage`; legacy finalizery i `finish_run` nie mogą oznaczyć staged sukcesu | karta #2 jakościowo REJECT; `research_runs.error` zachował historyczny parse-error (P2-20) | 454 offline + Task 9 live + repair + resume | 2026-07-13 | niezależne review; scheduler/jobs/workery osobnym zadaniem |
| Research legacy (single, two-stage) | WORKING | 100 | działa, 24 testy | NIEZALECANY (ADR-016→020); do DEPRECATED po sukcesie staged live | tak | 2026-07-12 | Etap 2 zad. 6 |
| Walidacja + injection guard | VERIFIED | 90 | deterministyczna bramka, min_verified_sources, neutralizacja injection | wzorce EN-only (P2-7) | tak | 2026-07-12 | rozszerzenie przy Etapie 2 |
| Diagnostyka odpowiedzi | VERIFIED | 99 | raw+stop_reason per etap; wspólny rekurencyjny sanitizer; atomowy temp/fsync/replace/directory-fsync; best-effort po terminalizacji | staged per-stage path nadal przechowuje najnowszą próbę danego rozróżnionego etapu (historyczne P2-13, poza tą falą) | sanitizer + 4 failpointy + durable lifecycle | 2026-07-17 | niezależny review naprawy |
| CLI `app/main.py` + runner | VERIFIED | 99 | canonical pre-storage probe poprzedza główne `SqliteStorage.open`; post-open durable revalidation, marker, flags, worker, raport i recovery tworzą jeden root; realny LA-03 przeszedł do dokładnie jednego requestu | browser CLI i realne resume nie istnieją; bieżący job jest terminalny i nie może być ponowiony | CLI + worker + restart acceptance + 46 LA-01 + 25 LA-02/LA-03 tests + real result | 2026-07-17 | niezależny review NIA-P2-RV-01…05; nowy live tylko po nowej zgodzie |
| `scripts/run_capped_research.py` | VERIFIED | 94 | pre-flight deleguje do `PolicyEngine.check_run_budget`; estymata obejmuje `1+max_retries`; estimate-only bez klienta; resume uwzględnia zapisany usage | `--max-sources 0` znaczy „wszyscy" (P2-15) | black-box CLI + budget delegation | 2026-07-12 | bez zmian w Task 6 |
| Porty: Storage/Notification | VERIFIED / WORKING | 99/70 | Storage rozdziela manualne mutacje od worker-only fenced API; success terminalizuje job atomowo, a post-terminal notification jest diagnostyką best-effort | brak trwałego timestampu cyklu maintenance, dlatego raport pokazuje UNKNOWN | queue + worker + maintenance + restart acceptance + report | 2026-07-16 | niezależny review |
| Porty: SecretStore/FileStore | SKELETON | 20 | kod adapterów istnieje | **martwy kod — zero wywołań** (config używa os.getenv wprost) | brak | 2026-07-12 | podpiąć w Etapie 8 (lub usunąć decyzją) |
| Porty: Browser/Scheduler | SKELETON / VERIFIED | 10/95 | DisabledBrowser blokuje każdą akcję; worker, reaper, maintenance, polityka okien i minimalna konfiguracja Windows Task Scheduler są zweryfikowane offline | browser/public worker BLOCKED; zadania systemowe nie są zarejestrowane | worker + maintenance + scheduling + launcher XML/command tests | 2026-07-16 | niezależny review; rejestracja tylko za osobną zgodą |
| Tabele bez kodu (interactions, target_items, approvals, metrics_daily, screenshots) | SKELETON | 5 | schemat od migracji 0001; `content_items` ma osobny, zmergowany fundament C1 | brak runtime dla wymienionych tabel | — | 2026-07-23 | Etapy 4–7 |
| PRE-C5 question semantic boundary | REPAIR CANDIDATE COMPLETE — AWAITING RE-REVIEW | 100 naprawa; 0 niezależny re-review | każdy segment ARTICLE z `?` albo `？` ma exactly-one accounting; `NON_FACTUAL_PROSE` zawsze BLOCK niezależnie od pozycji markera; factual question przechodzi tylko jako grounded fact z in-package evidence; non-factual ma honest-inference route | realny semantic reviewer nie istnieje; błędny `ARGUMENT_OR_INFERENCE/false` pozostaje jawną trust boundary; C5/PRE-C5 QUALITY GATE NOT STARTED | module `220/220`; PRE-C5 `328/328`; affected `399/399`; full/collect `2322/2322` | 2026-08-09 | dokładnie jeden niezależny re-review; potem osobna fala provider/reviewer tylko po decyzji właściciela |
| PRE-C5 ARTICLE claim accounting / paid CONTENT cost cap | CANDIDATE COMPLETE — AWAITING INDEPENDENT REVIEW | 100 implementacja; 0 niezależny review | dokładnie jeden audytowalny rekord na każdy deterministyczny segment ARTICLE; fakt→frozen evidence; inferencja bez nowego faktu; audit trwały także przy PASS; over-cap atomowo daje usage+cost+reconciliation+NV/STOPPED | brak produkcyjnego reviewera/composition rootu; PRE-C5 QUALITY GATE i C5 NOT STARTED; malformed `syndication_of` P2 poza zakresem | new `38/38`; PRE-C5 `108/108`; affected `436/436`; full `2102/2102` | 2026-08-09 | dokładnie jeden niezależny review; bez C5/live przed APPROVE i osobną zgodą |
| Task queue storage (jobs/flags/lease/rezerwacja) | VERIFIED | 99 | `0009`, atomowy claim/idempotency/recovery/rezerwacje oraz trwałe `earliest_run_at` i kontrolowany `schedule_reason`; claim pobiera czas po write locku, więc może przejąć job, który stał się eligible podczas oczekiwania | reaper wymaga jawnego progu i recovery jobów; przypięty run po expiry wymaga reconciliation, nie resume | `test_jobs_queue` + worker/maintenance/scheduling Barrier/reopen | 2026-07-14 | scheduler service osobno |
| Worker loop / scheduler runtime | VERIFIED | 97 | istniejący worker/maintenance plus minimalny launcher Task Scheduler; `--offline-only` blokuje durable-real niezależnie od flag; controlled single positive-live jest niezależnie potwierdzony | kolejny paid request `NOT AUTHORIZED`; browser/public BLOCKED; zadania systemowe NIEZAREJESTROWANE; globalny dispatch timeout = descope/P2; realne resume NOT IMPLEMENTED | runtime + maintenance + scheduling + launcher/contraprobes + positive-live review | 2026-07-18 | Etap 2 jest `IN PROGRESS — E1 CLOSED, E2-A CLOSED, E2-B CLOSED, E2-C CLOSED`; osobna zgoda na każde działanie zewnętrzne |
| Approved pricing + controlled-live wrapper | VERIFIED | 99 | pełny profil `Decimal`, complete fingerprint, persisted projections, dispatcher recheck; session/job/request/attempt/token fencing; report-before-clear, wspólny sanitizer, recovery bez retry, prawdziwy reopen, pełny profil flag i jawny clock enqueue; positive-live zakończył `DONE/SUCCESS/COMPLETE` i kartą `id=3` | sześć P2 ADR-094 pozostaje nieblokującym backlogiem; kolejny request `NOT AUTHORIZED` | pricing + controlled-live + execution intent + 1288 baseline + niezależny review 223/223 | 2026-07-18 | bramka positive-live formalnie przyjęta; Etap 2 jest `IN PROGRESS — E1 CLOSED, E2-A CLOSED, E2-B CLOSED, E2-C CLOSED` |
| Content foundation C1 / pipeline C2–C5 | C1/C2 CLOSED; C3 FULLY CLOSED; C4 CODE MERGED — POST-MERGE VERIFIED — AWAITING DOC SYNC MERGE; ETAP 3 IN_PROGRESS | C1–C3: 100; C4 impl: 100 | C4 rozstrzyga current draft po C3 do human-required/approve/reject/stale/technical, z trwałym snapshotem, policy threshold, audit, fence i atomowością; brak publikacji | C4 zmergowane (PR #26, merge commit `7eb93ba…`) i zweryfikowane post-merge, dokumentacyjny sync czeka na review/merge; C5/PRE-C5 NOT STARTED; provider/model/pricing/availability `UNVERIFIED`; Notes `PROVISIONAL`; brak realnego rootu, approval UI, controlled-live, publikacji i produkcyjnej migracji | C4 `23/23`; full/collect/unique `1994/1994`; 0 dup/skip/xfail/errors | 2026-07-24 | review i merge dokumentacyjnego PR C4; potem osobno PRE-C5 gate |
| Approval/autonomy + panel FastAPI | NOT_STARTED | 0 | — | — | — | — | Etap 4 |
| Publishing (Playwright/Substack) | NOT_STARTED | 0 | — | — | — | — | Etap 5 |
| Interakcje (komentarze/odpowiedzi) | NOT_STARTED | 0 | dokumentacyjny podział: publiczne Notes, K1–K8, odpowiedzi/restacki w Etapie 6 | brak kodu i akcji publicznych | — | 2026-07-13 | Etap 6 |
| Analytics + strategy engine | NOT_STARTED | 0 | dokumentacyjne definicje oddzielnych metryk i `is_estimated` | brak kolektora, atrybucji, eksperymentów i strategy engine | — | 2026-07-13 | Etap 7 |
| Backend API / frontend | NOT_STARTED | 0 | — | (panel = Etap 4; poza nim brak frontendu w planie MVP) | — | — | Etap 4 |

## Aktualne blokery

**HISTORYCZNY STAN PO E2-C: E2-C = `CLOSED — APPROVED WITH MINOR/P2`; Etap 2 = `IN PROGRESS`; controlled-live = `NOT READY`; następna operacja techniczna = `NOT STARTED`.** `E2B-F-01`/`F-02`/`F-03` są `TECHNICALLY CLOSED IN MERGED E2-C` po review, merge PR #9 i post-merge checkpointcie. Pozostałe blokery pierwszego realnego Fetch to kontrolowany snapshot i migracja produkcji `0014→0018`, osobna formalna decyzja właściciela, jawne globalne ustawienie YAML oraz nowy jednorazowy approval L1 dokładnego joba. Realny Fetch/HTTP/DNS, realny staged provider, browser i publikacja pozostają nieautoryzowane. Otwarte i nienaprawiane: `E2B-F-04`, `E2B-F-05`, `E2B-OBS-02`, `PR8-DOC-P2-01`, `F-DOC-01`, `F-01-RESIDUAL`, P2 E2-A dla innych flow i historyczny `E1-RR-P2-01`. Ewentualna następna operacja może być rozważona wyłącznie oddzielnie: snapshot, migracja `0014→0018`, jedna minimalna realna próba bez auto-retry i pełny audyt.

**HISTORYCZNY STAN PO E2-B:** PR #7 = `MERGED`; WAVE E2-B = `CLOSED — APPROVED WITH MINOR/P2`; WAVE E2-A = `CLOSED`; WAVE E1 = `CLOSED`; ETAP 1 = `CLOSED`; ETAP 2 = `IN PROGRESS — E1 CLOSED, E2-A CLOSED, E2-B CLOSED`; controlled-live = `NOT READY`; kolejna fala techniczna była wtedy `NOT STARTED`. Merge PR #7 `03aa9724dbe5cbcda86515c62f238e93e3d6b4ae` (rodzice `1765e664f9e078f189ae41d4e13685d1561c57b9` i zatwierdzony head `0fcc2acd6808504a348e054fd23bf0ff9970056e`) dołączył controlled real fetch foundation (offline, bez realnego pobrania); post-merge checkpoint to 1551 passed, zero skipped, harness kontrprób 13/13. Wcześniejszy merge PR #5 `404d2d306bbfa24fc08f2f5db68931e7441f040a` (rodzice `07fda5e68a61c7b9ff68e4388b2689acdca55818` i zatwierdzony head `61a509bd9c0a457ac78bb8893438664017a14063`) dołączył offline evidence integration spine; post-merge checkpoint to 1474 passed, zero skipped. Wcześniejszy merge PR #3 `42762a76...` (rodzice `17af8dee...` i `f42790b4...`) zamknął WAVE E1 (1454 passed). Kod ma migracje do `0018`, a runtime wymaga dokładnie `0018`; produkcyjna baza celowo pozostaje niezmieniona na schema `0014`, 14 migracjach i SHA `9906AFBFB580BE8F576A6449B0930C41ED964FED814D99C947D1C28C5B060836`, `364544 B`, integrity `ok`, FK `0`, bez sidecarów; `0015`–`0018` niezastosowane. Findings E2-B były wtedy jawne i blokowały controlled-live; szczegóły w ADR-105.

## Ostatnie ważne decyzje

- **ADR-107:** właściciel formalnie zamknął WAVE E2-C jako `CLOSED — APPROVED WITH MINOR/P2` po kandydacie ADR-106, niezależnym review, merge PR #9 (`ff323746c35f733507a7b0a30837ebf645020b2b`) i checkpointcie `1572/1572`; `E2B-F-01`/`F-02`/`F-03` = `TECHNICALLY CLOSED IN MERGED E2-C`, otwarte P2 pozostają bez naprawy. Etap 2 nadal `IN PROGRESS`, controlled-live `NOT READY`, następna operacja `NOT STARTED`.
- **ADR-106:** historyczna decyzja implementera dla kandydata E2-C: storage-issued capability po atomowym zużyciu L1, prywatny sealed transport factory, strict globalny boolean YAML i immutable host binding używany bezpośrednio przez transport. Brak nowej migracji; dowód offline `1572/1572`, exact-once `378+389+394+411`, harnessy `13/13+13/13`; produkcja `0014` byte-identical. Status kandydacki został zastąpiony przez formalne zamknięcie ADR-107.
- **ADR-105:** właściciel formalnie zamknął Etap 2 / WAVE E2-B jako `CLOSED — APPROVED WITH MINOR/P2` na podstawie niezależnego review `APPROVE WITH MINOR/P2`, merge PR #7 (`03aa9724dbe5cbcda86515c62f238e93e3d6b4ae`, rodzice `1765e664f9e078f189ae41d4e13685d1561c57b9` i zatwierdzony head `0fcc2acd6808504a348e054fd23bf0ff9970056e`) i post-merge checkpointu 1551/1551 (harness kontrprób 13/13); cały Etap 2 pozostaje `IN PROGRESS — E1 CLOSED, E2-A CLOSED, E2-B CLOSED`, controlled-live = `NOT READY`, kolejna fala techniczna = `NOT STARTED`. `E2-A-P2-03` dla `controlled_fetch_v1` = `CLOSED` (opcja A); findings `E2B-F-01…F-03` blokują wyłącznie controlled-live (nie zamknięcie fundamentu), `E2B-F-04`/`E2B-F-05` = nieblokujące P2. Realny transport pozostaje nieosiągalny przez wspierane composition roots i runtime flow przy `REAL_CONTROLLED_FETCH_ENABLED=False`; produkcja na `0014` (runtime kodu wymaga `0018`), live/browser/publikacja nieautoryzowane.
- **ADR-103:** właściciel formalnie zamknął Etap 2 / WAVE E2-A jako `CLOSED — APPROVED WITH MINOR/P2` na podstawie niezależnego review `APPROVE WITH MINOR/P2`, merge PR #5 (`404d2d30...`) i post-merge checkpointu 1474/1474; cały Etap 2 pozostaje `IN PROGRESS — E1 CLOSED, E2-A CLOSED, E2-B NOT STARTED`. Przyjęto trzy P2 (`E2-A-P2-01` OPEN/BACKLOG, `E2-A-P2-02` ACCEPTED, `E2-A-P2-03` OPEN — FUTURE PAID/LIVE GATE) bez naprawy; produkcja pozostaje na `0014` (runtime wymaga `0017`), a live/real Fetch/provider/browser/publikacja są nieautoryzowane.
- **ADR-101:** właściciel formalnie zamknął Etap 2 / WAVE E1 jako `CLOSED — APPROVED WITH MINOR/P2`; Etap 2 pozostaje `IN PROGRESS — E1 CLOSED, E2 NOT STARTED`; merge PR #3 nie uruchomił migracji ani operacji produkcyjnych, a P2 partycjonera jest jawnym backlogiem bez wpływu na akceptację E1.
- **ADR-098:** PR #1 został zmergowany do `main` jako `548cc65cad70eaef631fafff7c350845984d18e6`. Osobny post-merge checkpoint usuwa wyłącznie testową zależność od starej nazwy brancha, zachowując produkcyjny branch gate; historyczny branch jest technicznie zakończony, a dalsza praca powstaje z `main`. Produkcja nadal `0014`, Etap 2 `NOT STARTED`, live `NOT AUTHORIZED`.
- **ADR-096:** owner przyjął `REJECT — MAJOR` finalnego review PR #1 i zlecił naprawę PR1-MAJ-001/003 oraz P2-004. PR1-MAJ-002 został zdegradowany do cleanupu końcowego drzewa: bez przepisywania historii prywatnego brancha; katalog pisarski ma być identyczny z `origin/main`. Implementacja `0015` jest kandydatem do niezależnego re-review, nie zgodą na merge, produkcyjną migrację ani Etap 2.
- **ADR-095:** niezależny finalny review (`223/223`, zero CRITICAL/MAJOR/nowych MINOR, bajtowa identyczność kodu/testów z zaakceptowanym 1288/1288) wydał `APPROVE`; właściciel formalnie przyjął ETAP 2 POSITIVE-LIVE GATE. Nie rozpoczął Etapu 2 i nie autoryzował kolejnego live, browsera ani publikacji.
- **ADR-088:** właściciel formalnie zamknął **Etap 1 = `CLOSED`** (2026-07-17) na podstawie niezależnego re-review naprawy NIA-P2-RV-01…05 = `APPROVE WITH MINOR/P2` (zero MAJOR/CRITICAL); brak Research Card nie był bramką, `RV-R2-P2-1`/`RV-R2-P2-2` → nieblokujący backlog Etapu 2, a kolejny realny request wymaga nowej, oddzielnej zgody właściciela i nowego joba.
- **ADR-087:** niezależny review pierwszego pakietu P2 wydał `REJECT — MAJOR`; właściciel zlecił wyłącznie NIA-P2-RV-01…05. Naprawa miała wtedy status `CANDIDATE COMPLETE — AWAITING INDEPENDENT REVIEW` i nie zamykała jeszcze Etapu 1 (zamknięcie nastąpiło w ADR-088).
- **ADR-086:** review LA-03 zatwierdził lifecycle pierwszego requestu i zdefiniował pierwotny pakiet parser/report/frozen-prestorage; jego pierwszy review został później odrzucony i naprawiony przez ADR-087.
- **ADR-085:** LA-03 zamknęła self-handle `DB_HANDLES_PRESENT` przez pre-storage probe i doprowadziła do dokładnie jednego rozliczonego provider requestu bez Research Card.
- **ADR-084:** niezależny review zatwierdził LA-02 jako `APPROVE WITH MINOR/P2`; root cause `PROCESSES_PRESENT` jest `CLOSED`, P2-2 false STOP pozostaje `OPEN OBSERVATION / DOCUMENTED`, a checkpoint nie autoryzuje drugiej próby ani providera.
- **ADR-022 (ACCEPTED / EXECUTED 2026-07-13):** właściciel zatwierdził dokładnie jeden świeży staged run z capem 0,55 USD. A1/A2 odniosły sukces, B zakończyło się `max_tokens`/parse-error; koszt 0,170050 USD; bez retry/resume.
- **2026-07-13 (jawna zgoda właściciela):** po osobnym repairze zatwierdzono dokładnie jeden resume wyłącznie B z absolutnym capem 0,20 USD i `max_retries=0`; wynik `end_turn`, karta #2, łączny koszt 0,183964 USD.
- **ADR-021:** repo GitHub PRIVATE, main stabilny + branche dev.
- **ADR-020:** research staged A1/A2/B; **ADR-019:** trwałość etapów; **ADR-017/018:** cel = pełna autonomia operacyjna + anonimowa marka redakcyjna bez proaktywnego ujawniania AI (NO_REPLY, zero impersonacji).
- **ADR-029:** retry błędów Anthropic jest dozwolone wyłącznie dla jawnie typowanych timeout/SDK-network/429/500/502/503/504; 400/401/403/404/422, unknown, parse, truncation i validation są terminalne dla próby.
- **ADR-037 (ACCEPTED / zweryfikowane offline):** jobs+lease+idempotency+active-topic lock+global reservation oraz `system_flags`; BROWSER po lease expiry → NEEDS_VERIFICATION.
- **ADR-038 (ACCEPTED / zweryfikowane offline):** jeden worker odczytuje runtime flags bez cache, wykonuje tylko LOCAL noop i RESEARCH `dry_run=true`, wiąże `job.run_id` przez CAS; paid/browser pozostają BLOCKED. Jednorazowy reaper dodaje ADR-040, lecz worker nie uruchamia go automatycznie.
- **ADR-039 (ACCEPTED / zweryfikowane offline):** CAS sprawdza pełną relację RESEARCH job→run→research_run (account/topic/workflow/flow). Po expiry RESEARCH z `run_id` trafia fail-closed do NEEDS_VERIFICATION z zachowaną rezerwacją; worker nie uruchamia go od początku ani nie implementuje resume.
- **ADR-040 (ACCEPTED / zweryfikowane offline):** jawny reaper po recovery atomowo zatrzymuje wyłącznie stale `RUNNING` bez executable joba; `QUEUED/LEASED/RUNNING` job blokuje stop, `NEEDS_VERIFICATION` pozostaje trwały, a cykliczny scheduler nie istnieje.
- **ADR-041 (ACCEPTED / zweryfikowane offline):** podczas synchronicznego dispatchu worker uruchamia daemon periodic heartbeat guard z osobnym połączeniem SQLite. Daemon jest tylko osłoną procesu; worker zawsze podejmuje stop event, `wake`, bounded join i kontrolę `is_alive()`. Zwykle wątek kończy się i jest dołączony, lecz timeout może zostawić go żywego do odblokowania zależności — bez prawa workera do `DONE`; po odblokowaniu guard widzi stop event przed kolejnym heartbeat. `lost_lease` i `failure` są in-memory, a trwałe rozstrzygnięcie należy do SQLite oraz recovery/reconciliation. Guard nie wznawia wygasłego lease ani nie retry’uje dispatchu, a utrata lease ma pierwszeństwo przed błędem dispatchu i blokuje `DONE`. Zweryfikowane wyłącznie dla LOCAL/RESEARCH `dry_run`; paid/live API = NOT VERIFIED, browser/public = BLOCKED.
- **ADR-043 (ACCEPTED / zweryfikowane offline):** przed utworzeniem nowego joba `SchedulingPolicy` deterministycznie wybiera lokalne okno z konfiguracji IANA/DST, zapisuje UTC `earliest_run_at` i zamknięty `schedule_reason`; claim egzekwuje eligibility. W chwili tej decyzji final restart acceptance była NOT_STARTED; późniejsze ADR-044/045 doprowadziły ją do candidate awaiting review. Usługa systemowa nadal NOT_STARTED; paid/browser/public = BLOCKED.
- **ADR-046 (ACCEPTED / candidate po testach, awaiting independent review):** lifecycle pobiera UTC dopiero po `BEGIN IMMEDIATE`; heartbeat nie wskrzesza lease, który wygasł podczas oczekiwania. `COSTS.csv` jest odtwarzalnym eksportem po commicie SQLite, więc jego błąd jest ostrzeżeniem. Nieoczekiwany błąd jobowego pipeline’u po inicjalizacji atomowo terminalizuje job/run/research_run.
- **ADR-047 (ACCEPTED / candidate po testach, awaiting independent review):** `finalize_job_research_execution` atomowo terminalizuje `jobs=DONE` razem z kartą, źródłami i lifecycle researchu. Dispatcher zwraca typowany `TerminalizationMode`; tylko `WORKER_MUST_COMPLETE` używa generic completion.
- **ADR-048 (ACCEPTED / candidate po testach, awaiting independent review):** `DispatchResult` ma wymagany, zweryfikowany runtime `TerminalizationMode`; Worker obsługuje wyczerpująco trzy mode i propaguje naruszenie kontraktu bez canonical write. Inserty finalizacji wymagają `rowcount == 1` i rollback nie maskuje primary error.
- **ADR-050 (ACCEPTED / EXECUTED):** nowy baseline SQLite `CAEDDA05B4E9BCA70346031F5812D5EA38C4A7390D1E52E22FDFA12AF4EBFEFB` pochodzi z kontrolowanego logicznego odtworzenia wyłącznie klasy A/B; brak bitowego snapshotu poprzedniego pliku pozostaje udokumentowany.
- **ADR-051 (ACCEPTED / EXECUTED, historyczne uzasadnienie):** WAVE 0A została formalnie zamknięta jako `APPROVED WITH P2`; P0-01, P1-01 i P1-02 są zamknięte. Bieżące blockery Etapu 1 są wyłącznie zamkniętą listą ADR-070.
- **Korekta ADR-031 po końcowym review F4:** COMPLETE nie omija kontraktu `StagedFinalizationContext`; terminalny no-op odrzuca sprzeczny fresh/force/resume mode oraz niezgodny CAS bez mutacji. `finalize_research_success` i `mark_research_run_complete` są legacy-only i odrzucają `staged` we wszystkich stanach, także COMPLETE i FAILED.
- **ADR-032–036 (ACCEPTED, wyłącznie dokumentacyjnie):** modularny system redakcyjny, prawo do `SKIP`, izolacja NIA/build logu, rozdzielenie follows i subscribers oraz Notes dry-run w Etapie 3/publiczne operacje w Etapie 6. Pełny snapshot Fable jest w `docs/research/FABLE_GROWTH_EDITORIAL_REPORT.md`; wszystkie elementy wdrożeniowe pozostają PLANNED/NOT_STARTED.
- **2026-07-12 (ten audyt):** konsolidacja dokumentacji do 3 dokumentów źródła prawdy; stare plany w `docs/archive/superseded_plans/` (ADR-023).

## Etapy

### Aktualizacja nadrzędna — final restart acceptance Etapu 1 (2026-07-14)

Ten wpis zastępuje niżej starsze wzmianki „końcowa akceptacja restartu = NOT_STARTED” oraz historyczne liczby 26/42/53 acceptance i 667/683/695 testów. ADR-044 atomizuje inicjalizację, ADR-045 zamyka old-owner research fencing, ADR-046 usuwa trzy P1 czasu/CSV/unexpected failure, ADR-047 przenosi sukces joba do transakcji workflow, a ADR-048 zamyka kontrakt wyniku dispatchu. `tests/test_stage1_restart_acceptance.py` ma 58 scenariuszy, w tym final heartbeat failpoint, brak generic `complete_job`/`fail_job`, atomic failure, malformed terminalization, success/failure transaction failpoints, crash po commicie, macierz expiry przed recovery, claim po write locku i błąd katalogu CSV. Pełny suite: 700 passed; realny koszt 0 USD. WAVE 0A została formalnie zamknięta jako `APPROVED WITH P2`; formalne zamknięcie Etapu 1 nie zostało ogłoszone.

- **Aktywny stan roadmapy:** Etap 1 = `CLOSED`; Etap 2 = `CLOSED`; Etap 3 = `IN PROGRESS`; C1/C2/C3/C4 zachowują wcześniejsze statusy, a najnowsza fala to `PRE-C5 QUESTION SEMANTIC BOUNDARY CONTRACT — REPAIR CANDIDATE COMPLETE — AWAITING RE-REVIEW`. C5 i formalny PRE-C5 QUALITY GATE pozostają `NOT STARTED`; L1 = `ACTIVE`, LEVEL_3 = `NOT CONFIRMED`, publikacja = `NOT VERIFIED`.
- **Ostatni ukończony krok techniczny:** jedyna fala naprawcza po `REJECT — MAJOR` usunęła końcówkowe `text.endswith("?")` i wyłącza prose shortcut przy `?` albo `？` w dowolnym miejscu segmentu, bez parsera semantycznego. Wynik: question module `220/220`, PRE-C5 `328/328`, affected `399/399`, full/collect `2322/2322`, produkcja i styl byte-identical, koszt `0.000000 USD`.
- **Następne trzy zadania:**
1. Dokładnie jeden niezależny re-review naprawionego kandydata question semantic boundary; implementer nie wykonuje review własnego wyniku.
  2. Ewentualna realna fala semantic provider/reviewer wyłącznie po wyniku review i nowej decyzji właściciela; bez regexowej protezy dla trust boundary.
  3. C5 pozostaje `NOT STARTED`; każde realne API, controlled-live i publikacja wymaga osobnej zgody.
- **Zachowane granice:** source admission, E3 lineage, paid CONTENT lifecycle, leases, storage, migracje 0025/0026, style examples, PSL, `[hidden]/[HIDDEN]`, Notes, provider/model/pricing/routing, C4, produkcja i publikacja nie zostały otwarte.
- **PRE-C5 QUALITY GATE — `NOT STARTED`:** bieżąca fala zmienia wyłącznie kontrakt pytań w claim accounting; nie ogłasza ukończenia ośmiopunktowej bramki jakości ani gotowości C5.

## Znane długi techniczne (poza blokerami; numeracja z audytu 12.07)

| # | Dług | Plan |
|---|---|---|
| P1-3/P1-4 | ZAMKNIĘTE po pełnym review: retry worst-case, re-check, obowiązkowy cap realnego pipeline, stały cap resume i centralne D/M | Task 5; 257 testów offline |
| P1-7 | kill-switch nie działa w runtime (snapshot z .env) | Etap 1 (system_flags) |
| P2-1 | dwie kalibracje estymatora (rozjazd przy zmianie cen w .env) | Etap 2 |
| P2-2 | koszt w 3 miejscach (model_usage=kanon; runs.cost_usd i research_runs.total_cost_usd=cache) | `runs.cost_usd` synchronizowany w Etapie 0 zad. 2; total_cost_usd pozostaje cache |
| P2-4 | budżet dzienny wg dnia UTC (właściciel w Europe/Warsaw) | decyzja: zostaje UTC — udokumentowane |
| P2-5 | migracje bez transakcji (częściowa awaria = zakleszczenie) | nowe migracje od 0006 z BEGIN/COMMIT |
| P2-6 | RESEARCH_LOG.md tylko przy sukcesie (nieudane realne runy nielogowane automatycznie) | Etap 2 |
| P2-7 | injection guard EN-only, URL-e nieskanowane | Etap 2 (przy fetch) |
| P2-8 | scripts używają settings.model_quality zamiast ModelRouter | Etap 0/1 |
| P2-9 | ZAMKNIĘTE: ADR-001/002/003/005/006 miały status PROPOSED mimo wdrożenia | Task 7: wszystkie zweryfikowane i oznaczone ACCEPTED |
| P2-10/P2-11 | migracja przy każdym otwarciu bazy; runtime pisze do docs/ | Etap 8 |
| P2-12 | brak prompt cachingu (N×A2 dzieli system prompt) | po sukcesie live |
| P2-13/P2-14 | diagnostyka nadpisuje poprzednią próbę; stage log nie mierzy czasu | świadomie odłożone |
| P2-15 | `--max-sources 0` znaczy „wszyscy" | Etap 0 przy zad. 1 |
| P2-16 | ujemne `research_source_candidates.attempts` spełnia `attempts < cap` i może przyznać dodatkowe claimy po ręcznym uszkodzeniu rekordu; normalny kod tworzy wyłącznie 0/1+ | dodać `attempts >= 0` do claimu, `Field(ge=0)`, test i ewentualny CHECK constraint |
| P2-17 | dwa równoległe świeże procesy mogą przejść guard przed utworzeniem któregokolwiek runu i oba rozpocząć research tego samego tematu | Etap 1: trwały claim/lease lub constraint aktywnego researchu per topic |
| P2-18 | idempotentny no-op porównuje koszt przez dokładne `float == float`; binarnie różne reprezentacje tej samej kwoty mogą dać fałszywą odmowę, ale nie nadpiszą danych | docelowo najmniejsza jednostka, `Decimal` albo tolerancja zgodna z kanonem `model_usage`; bez zmiany w Task 4 |
| zamknięty | AnthropicLLMClient bez księgowania kosztu przy parse-error, bez testów | Task 6: typowane błędy, fence parser, ledger workflow; 286 testów offline |
| nowy | EnvSecretStore/LocalFileStore = martwy kod | Etap 8 (podpiąć) albo usunąć decyzją |
| znany | lokalne `.venv`: open-interpreter wymaga anthropic<0.38 przy zainstalowanym 0.116 (ostrzeżenie pip, poza projektem) | obserwować |
| P2-19 | `timeout-billed-unrecorded`: provider może zbilować timeout bez zwrócenia usage | niskie retry/capy, worst-case i re-check; rekonsyliacja billingowa poza Task 5 |
| P2-20 | po udanym resume `research_runs=COMPLETE`, ale pole `research_runs.error` zachowuje historyczny parse-error pierwszego B; historia jest też w `research_stage_results`, więc bieżące pole może mylić odczytujących | niezależne review; rozstrzygnąć, czy finalizacja ma czyścić bieżący error, bez mutacji w tym zadaniu |

## WAVE 0B.1 — zapis historyczny, zastąpiony przez WAVE 0B.2 (2026-07-14)

**Status historyczny, zastąpiony przez WAVE 0B.2.** Etap 1 nadal jest `BLOCKED` i nie został zamknięty. WAVE 0B.1 przygotowała globalny `operation_key` i 0011; WAVE 0B.2 domknęła centralną bramkę klienta, pełny intent oraz ledger 0012. Nie jest to akceptacja WAVE 0B.

`0010_provider_attempts` wraz z `0011_provider_attempt_invariants` wiążą nowy realny request z `(job_id, stage, attempt_no)` oraz deterministycznym `request_id=job_id:stage:attempt_no`. Stany są ograniczone do `RESERVED → REQUEST_STARTED → SETTLED/NEEDS_RECONCILIATION` albo `RESERVED → RELEASED`; liczba próby i rezerwacja są dodatnie. Nowy realny `model_usage` wymaga request_id istniejącego attemptu; historyczne nieudowadnialne wiersze są jawnie oznaczone `is_legacy_usage=1`.

Świeży `scripts/run_capped_research.py --real --operation-key …` nadal wykonuje tylko pre-flight i durable enqueue, nie tworzy klienta ani nie woła providera. `enqueue_job_result()` atomowo rozstrzyga `JOB_ENQUEUED` albo `JOB_ALREADY_EXISTS`; ten sam globalny klucz z semantycznie innym kanonicznym payloadem kończy się `OPERATION_KEY_CONFLICT`. Budżet liczy porównania jako `Decimal` (6 miejsc); test obejmuje granicę `0.10 + 0.20` i realny wyścig dwóch konekcji SQLite.

Historyczny wynik WAVE 0B.1 (**741 passed**) został zastąpiony przez wynik WAVE 0B.2: **752 passed**. Nie uruchomiono API, sieci, browsera, publikacji, kosztu ani migracji `data/agent.db`; baseline SHA pozostaje `CAEDDA05B4E9BCA70346031F5812D5EA38C4A7390D1E52E22FDFA12AF4EBFEFB`.

## WAVE 0B.2 — zapis historyczny, zastąpiony przez WAVE 0B.3 (2026-07-14)

**Status historyczny: wynik 752 testów został zastąpiony przez WAVE 0B.3.** Etap 1 pozostaje `BLOCKED`. Produkcyjny klient Anthropic wymaga `DurableProviderAttemptContext` i potwierdzenia aktywnego `REQUEST_STARTED` tuż przed callerem/SDK; adapter offline jest jawnie oddzielony.

Addytywna migracja `0012_provider_ledger_hardening` waliduje przed kopiowaniem deterministiczny request_id oraz sprzeczną historię usage, a następnie wymusza relację request→attempt→job.run_id→usage.run_id. Udowodnione historyczne usage nie jest legacy; legacy oznacza wyłącznie brak historycznego request_id i ma immutable proof migracji. Runtime nie może zadeklarować legacy, a attempt #2 po `RESERVED`, `REQUEST_STARTED` albo `NEEDS_RECONCILIATION` jest blokowany atomowo.

Payload durable zawiera kanoniczny snapshot modelu, timeoutu, tokenów, cennika/fingerprintu, pipeline/prompt contract, capu i limitu requestów. Worker używa snapshotu po zmianie ENV. Kwoty są `ROUND_HALF_UP` do 6 miejsc; dodatnia sub-kwantowa wartość dająca zero jest typowo odrzucana. WAVE 1A (durable real A1/A2/B, real resume) i operator reconciliation pozostają poza zakresem.

## WAVE 0B.3 — historyczna derived request identity i świeży lease (2026-07-14)

**Status historyczny, zastąpiony przez końcową falę WAVE 0B.** `expected_request_id` jest wyłącznie dokładnym `f"{job_id}:{stage}:{attempt_no}"`: context, confirmation i `Idempotency-Key` muszą być równe tej wartości przed callerem/SDK. Nie ma normalizacji whitespace/case, stage nie może zawierać separatora, a nieprawidłowa tożsamość kończy się typed error bez callera.

`checked_at` pozostało śladem diagnostycznym i nie bierze udziału w autoryzacji. `assert_durable_provider_attempt_active()` pobiera bieżący czas z injected execution clock po `BEGIN IMMEDIATE`; odrzuca lease po expiry, takeover, zmianę run/fence i `NEEDS_RECONCILIATION`, a widzi odnowienie lease. Historyczna regresja offline: **770 passed**, koszt 0 USD, bez API/sieci/browsera/publikacji i bez zmiany baselineu bazy.

## WAVE 0B — domykająca fala po niezależnym review (2026-07-15)

**[Zapis historyczny — WAVE 0B, 2026-07-15; zastąpiony przez ADR-088: WAVE 0B = `CLOSED — APPROVED WITH P2`, a Etap 1 = `CLOSED` 2026-07-17.]** **Status wówczas: `WAVE 0B CANDIDATE — AWAITING INDEPENDENT REVIEW`; Etap 1 nadal `BLOCKED`; live API = `ZABRONIONE`.** WAVE nie była wtedy zamknięta formalnie i nie odblokowywała paid workera, browsera, publikacji ani realnego resume.

- Kernel testowy jest aktywowany przez `sitecustomize.py` wyłącznie dla pytest albo dziedziczonego `NIA_TEST_MODE`. Przed collection, setup/call/teardown i w subprocessach czyści także lowercase `anthropic_api_key` oraz wszystkie proxy, blokuje `socket`/DNS, konstrukcję realnego `Anthropic`/`AsyncAnthropic`, `sqlite3.connect` i `sqlite3.dbapi2.connect` dla kanonicznie rozpoznanego `data/agent.db`. Chroni Windows drive-case, slash/backslash, lokalne URI `file:` i fail-closed odrzuca URI z nielokalnym authority; tymczasowe SQLite pozostaje dozwolone.
- Jedynym paid kontraktem pozostaje `durable_provider_v2` z `durable_research_intent_v2`. `0013_provider_attempt_usage_integrity` jest trzynastą i ostatnią obecną migracją; attempt runtime zapisuje SHA-256 kanonicznego `execution_intent`. Snapshot obejmuje canonical prompt-input (`question`, `niche`, `required_depth`, `guidance`), stage i wszystkie parametry requestu. Finalna asercja przed fake/SDK callerem ponownie kanonizuje `jobs.payload_json` i sprawdza pełne `job→run→research_run→attempt`; rozbieżność nie wykonuje callera, usage ani settlementu i przechodzi do `NEEDS_RECONCILIATION`. v1 fail-closed.
- `scripts/run_capped_research.py --resume … --real` odmawia przed `load_settings`, SQLite, `ensure_account`, polityką, klientem, usage trackerem i log writerem. Fake/offline resume pozostaje obsługiwane.
- Weryfikacja historyczna przed W0B-REV-06: collect **861**; cztery rozłączne grupy (204 + 217 + 212 + 228) dały **861 passed** offline; `data/agent.db` przed/po miała SHA-256 `CAEDDA05B4E9BCA70346031F5812D5EA38C4A7390D1E52E22FDFA12AF4EBFEFB`.

### W0B-REV-06 — spójność `max_tokens` i settlementu (2026-07-15)

**[Zapis historyczny — WAVE 0B / W0B-REV-06, 2026-07-15; zastąpiony przez ADR-088: Etap 1 = `CLOSED` 2026-07-17.]** **Status wówczas: `WAVE 0B CANDIDATE — AWAITING INDEPENDENT RE-REVIEW`; Etap 1 = `BLOCKED`; live API = `ZABRONIONE`.** `max_tokens` jest wspieranym, dodatnim parametrem trwałego `durable_research_intent_v2`, objętym fingerprintem. Dispatcher przekazuje go jednocześnie do realnego klienta i do `run_research_pipeline`; pipeline używa go dla `estimate_worst_case_search_call_usd`, policy check i dokładnie tej samej rezerwacji provider attempt.

Settlement canonicalizuje rezerwację i actual cost do sześciu miejsc USD (`ROUND_HALF_UP`). Przy `actual_cost <= reserved_amount` attempt przechodzi do `SETTLED`. Przy `actual_cost > reserved_amount` jedna transakcja zachowuje `model_usage` i cache kosztu runu, ustawia attempt na `NEEDS_RECONCILIATION` z `PROVIDER_ATTEMPT_COST_EXCEEDS_RESERVATION` (bez cichego SUCCESS i bez attempt #2), a pipeline przekazuje typowany wynik do ścieżki verification. Operator reconciliation nadal nie istnieje.

Weryfikacja po W0B-REV-06 była **historyczna**: collect **873** i partycje **206 + 218 + 226 + 223 = 873 passed**. Nie jest to bieżąca liczba po W0B-REV-10.

### W0B-REV-09/10 — kronika i jeden kontrakt kwoty (2026-07-15)

Obowiązkowa kronika została uzupełniona, a wszystkie aktywne źródła wskazują ten sam stan. W0B-REV-10 przenosi estymator, `UsageTracker`, projekcje pipeline, rezerwacje, comparison actual/reserved i cache kosztu na wspólny kontrakt `Decimal(str(value)) → quantize(0.000001, ROUND_HALF_UP)`. Kwoty są sumowane jako Decimal przed pojedynczą granicą kwantyzacji; nie ma aktywnego Pythonowego `round(..., 6)` dla pieniędzy.

Weryfikacja po W0B-REV-09/10 jest **historyczna**: **887 collected/passed**; dokładny podział to **211 + 222 + 229 + 225 = 887**. Runner nadal używa pełnego SHA-256 UTF-8 node ID jako big-endian integer modulo 4 i potwierdza exact-once coverage, brak BOM, brak duplikatów, brak pominięć i brak nadmiarowych node IDs. Chroniona baza nie była otwierana do zapisu.

### W0B-RR-01 / W0B-CLEAN-01 — domknięcie agregacji i decyzji finansowych (2026-07-15)

Niezależny re-review znalazł pozostałą lukę: publicznie skwantyzowany koszt jednego źródła był mnożony w staged estimate, a policy, część storage/pipeline i komunikat CLI jeszcze wykonywały decyzje pieniężne na `float`. Naprawa nie zmienia lifecycle, intentu, `max_tokens`, request identity, schematu ani migracji. Estymator przechowuje raw komponenty jako `Decimal` do pojedynczej granicy publicznej, a policy, ledgerowe sumy, rezerwacje i CLI canonicalizują przez wspólny helper przed porównaniem lub wyświetleniem. Trzy składniki po `0.0000005` dają więc jeden wynik `0.000002`, nie trzy osobne roundingi.

Usunięto też dwa martwe konstruktory `AnthropicResearchClient` z prywatnych helperów resume w `scripts/run_capped_research.py`; real resume nadal kończy się fail-closed przed klientem, a dispatcher jest jedynym rootem realnego klienta. Końcowa walidacja offline: **894 collected/passed**, partycje **213 + 224 + 231 + 226 = 894**, pełny SHA-256 UTF-8 node ID modulo 4, exact-once, bez BOM, duplikatów, pominięć i nadmiarowych node IDs. Testy użyły wyłącznie fake callerów i tymczasowych SQLite. `WAVE 0B` miał wtedy status `APPROVED WITH P2 — READY FOR CHECKPOINT`; Etap 1 `BLOCKED`, live API `ZABRONIONE`. (Zapis historyczny z 2026-07-15; WAVE 0B jest później `CLOSED — APPROVED WITH P2`, a Etap 1 = `CLOSED` 2026-07-17 — ADR-088.)

## Ponowna próba migracji po clean quiesce — odrzucona (2026-07-16)

Na branchu `dev/first-successful-research-card` i wymaganym HEAD `ddc3c63190eb82bca171174dc7ee70c2d0a1ec15` potwierdzono przed uruchomieniem brak procesów projektu, uchwytów DB/WAL/SHM, zadań Windows projektu, runtime i journala oraz zgodność starego baseline'u. Jedna faktyczna próba zatwierdzonego executora została przerwana przez jego pierwszy gate quiesce: `processes=(15404,), handles=(), tasks=()`.

PID `15404` był potomnym `powershell.exe` utworzonym przez sam domyślny probe executora. Jego command line zawierał literalną ścieżkę repozytorium, więc filtr procesów dopasował własny proces potomny. Finding lokalny `QP-01`: obecny filtr quiesce jest samodopasowujący na Windows. Nie zmieniono kodu i nie wykonano kolejnej próby.

Wynik formalny: **`MIGRATION REJECTED — QUIESCE PROCESS IDENTIFIED`**. Workspace, backup, rehearsal i nowy baseline nie powstały; produkcyjna SQLite nie została otwarta. DB/WAL/SHM zachowały wówczas dokładnie stary fingerprint, produkcja była wtedy na schemacie `0009`, live API było zabronione, a Etap 1 był wtedy `OPEN / BLOCKED PENDING CONTROLLED LIVE ACCEPTANCE` (stan historyczny na 2026-07-16; migracja `0009→0014` została później wykonana, a Etap 1 formalnie zamknięty 2026-07-17 — ADR-088).
