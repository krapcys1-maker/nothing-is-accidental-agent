# scripts/

Skrypty pomocnicze uruchamiane ręcznie (dev/ops). Nie zawierają logiki biznesowej — to cienkie nakładki na `app/`.

- `run_topics.py` — uruchamia przepływ tematów (walking skeleton, dry_run).

Przyszłe: `init_db.py`, `seed_accounts.py`, `export_costs.py`.
