# WEEK_29 — 2026-07-13 → 2026-07-19

- **Co zbudowano:** domknięto Etap 1, fundament evidence E1, offline integration E2-A i controlled real fetch foundation E2-B; E2-C dostarczył capability-gated construction, aktywację YAML i immutable host binding, przeszedł niezależny review, merge PR #9 i post-merge checkpoint, po czym właściciel formalnie zamknął falę jako `CLOSED — APPROVED WITH MINOR/P2` (ADR-107). Następnie powstał jeden Production Schema Migration Orchestrator `0014→0018` z snapshotem, file-state revalidation, sidecar STOP i recovery (ADR-108), który przeszedł niezależny review `APPROVE WITH MINOR/P2`, merge PR #11 (`7faf62e5f71c838c20e00e61121ea052f4bf9348`) i zielony post-merge checkpoint `1630/1630`, po czym właściciel formalnie zamknął falę jako `CLOSED — APPROVED WITH MINOR/P2` (ADR-109). Cały Etap 2 pozostaje `IN PROGRESS`. Szczegóły: `docs/BUILD_LOG.md`.
- **Co działa:** trwały paid-provider lifecycle z exact-once, lokalne evidence i lineage, offline staged Research Card, jednorazowy approval controlled fetch, lifecycle attemptu, bariera adresu bez DNS TOCTOU oraz kontrolowany migrator schema z resume `0015–0017` i `ALREADY_AT_TARGET`. Bieżący kandydacki checkpoint: `1630/1630`, exact-once `390+398+412+430`, harness `13/13+13/13`.
- **Co nie działa / nie jest gotowe:** rzeczywista migracja produkcji `NOT EXECUTED` — readiness jest `VERIFIED IN MERGED CODE`, ale owner authorization = `NOT GRANTED`; controlled-live Fetch pozostaje `NOT READY`; produkcja jest na `0014`, runtime kodu wymaga `0018`; nie udzielono zgody na pierwszy realny Fetch. `E2B-F-01`/`F-02`/`F-03` są technicznie zamknięte w zmergowanym E2-C; nowe zaakceptowane P2 fali orchestratora (`F-PR11-01`/`F-PR11-02`/`F-PR11-03`) oraz pozostałe otwarte P2 i obserwacje są w `docs/ERRORS_AND_FAILURES.md`. Pierwszy post-merge przebieg suity dał `1628/1630` przez interferencję sond procesów z równoległym acceptance (czysty rerun `1630/1630`; procedural lesson, nie defekt runtime).
- **Największy błąd tygodnia:** utożsamienie wcześniejszej walidacji hostname z adresem rzeczywistego połączenia. Wniosek: wynik policy musi być wejściem transportu, nie tylko wcześniejszą obserwacją.
- **Najważniejsza decyzja:** ADR-108 — snapshot nie zastępuje ponownego sprawdzenia tożsamości pliku bezpośrednio przed writable open, a resume jest nową owner-approved operacją, nie automatycznym retry.
- **Koszty:** nowa praca E2-C `0.000000 USD`; rzeczywisty ledger tygodnia/miesiąca po osobno autoryzowanych wcześniejszych requestach: `1.012590 USD`. Koszt/artykuł i koszt/subskrybent: brak danych publikacyjnych.
- **Czas człowieka:** nieagregowany w metrykę minut; decyzje i interwencje są opisane w `docs/HUMAN_INTERVENTIONS.md`.
- **Liczba interwencji człowieka:** nieagregowana ilościowo; obejmowała osobne autoryzacje/review/stop oraz formalne zamknięcia fal. Brak podstaw do uczciwego rozbicia liczbowego.
- **Wyniki publikacji:** brak nowej publikacji; metryki Substack nie były częścią prac.
- **Najlepsze screenshoty:** `SCREENSHOT REQUIRED` dla orchestratora; screenshotu nie wykonano z powodu zakazu browsera i ryzyka ujawnienia lokalnych ścieżek/fingerprintów.
- **Czego nauczył się agent:** poprawny backup chroni poprzedni stan, ale dopiero ponowny gate chroni aktualność decyzji o zapisie; sidecar jest dowodem do zatrzymania, nie śmieciem do usunięcia.
- **Co zmienimy w kolejnym tygodniu:** następna operacja techniczna = `NOT STARTED`; będzie nią rzeczywista migracja `data/agent.db` `0014→0018` i wymaga osobnej, jeszcze nieudzielonej zgody właściciela (aktualny SHA/size, nowy snapshot, brak sidecarów, jawna flaga confirmation). Pierwszy realny Fetch pozostaje osobną, nieudzieloną zgodą.
